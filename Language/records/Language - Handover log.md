# Language - Handover log

Kept for a fresh context after an auto-compaction: what is running, where things are, what comes next. Updated at every step; the log (project story) stays the record. Newest entry first.

## 22 September 2026, later - L82 third version, awaiting the second premise check
- **Now**: after Arm B (done, recorded, pushed; owner's note at tests/L85), the next reversible pick under decision L10 is running: PLAN L86 (tests/, DRAFT, hash 104dba892bebdf6c; mocks in tests/L86 Mocks; tools/L86_strip.py) for the driver's THIRD version `run_check_3.py` (new file beside run_check_2.py) that prints, under every JUMP and NO CONNECTION, the general lines it examined and which condition failed (D1 to D4). A fresh Opus premise checker (background agent) is checking it; report to scratchpad/premise_check_l86/PREMISE CHECK - L86.md. On FREEZE: record the hash in the log (entry L86), build run_check_3.py from the frozen D-spec (copy run_check_2.py, add the two blocks after the `Effect's direct lines` / `Expected's direct lines` listed_lines calls and before the abduction line / the `It would take a line nobody wrote` line; parse general clauses from ledger_text; unify head with effect; ask each goal), have a fresh Opus reviewer check it against D1 to D4 with one fix pass, run it on the four mocks, the 32 Arm B ledgers and the 76 Arm A ledgers (E1 to E6; L86_strip.py for E3, E4), then four prose-reader calls (E7, Mimo, keys in scratchpad/keys/api.env, ask_model_2.py), mark, second marker, results (L87), log, Status, push. The prior state (Arm B) is in the entry below this one.
- **Changed this step** (committed bef3dc7, main fast-forwarded): tools/L82_run.sh 47774a3437d81efd (absolute paths, fresh OUT_DIR required, VALID gate on the validation file's second line, stop if no ledger validates, return codes reported, consequences timed, COUNTS.txt per ledger, reader and prose reader in eight lanes with FAILED/SKIPPED lists, deeper blinding: world names to world_k in headings and in the `[TOLD in world x]` prefixes of line texts, bin quotations cut from the GAUGE line, the key records the map); tools/ask_model.py 0a7f66febbc3b908 (shared lock dir via ASK_MODEL_LOCKDIR, gap plus a tenth, no retry on 400/401/403/404/413/422; timeout 2400 s). The two orphaned pilot calls on T11-B were stopped at 14:44 UTC unreturned (P4 says so).
- **Premise checker agent**: the "Premise check of plan L82 v2" agent (task abe049b3b1e069c22); continue it with SendMessage so it keeps its reading of the files; if it is gone, start a fresh Opus agent at extra effort with the same brief (read the plan whole; Part 1 every premise at its hash; Part 2 every expectation on six questions; Part 3 the runner line by line; close with FREEZE or DO NOT FREEZE YET and a numbered list).

## 22 September 2026, evening - the API agents and Arm B
- **Now**: L82 second version (hash b82f7447452b72a0) with sixteen passages is DRAFTED and being premise-checked by a fresh Opus agent (scratchpad/premise_check_l82/). On FREEZE: record the hash in the log, then run `sh Language/tools/L82_run.sh "Language/results/L82 Corpus - sixteen reported arguments" "Language/results/L82 Arm B outputs" "Language/tests/L82 Arm B corpus brief/PAIRS.txt"` with the keys exported (the runner uses one lane per provider; the plan says four lanes: split TEXTS into four lists per provider if the runner is not amended first). Then mark B1 to B13, open the maps (passphrase scratchpad/keys/l82_map_passphrase.txt; `openssl enc -d -aes-256-cbc -pbkdf2 -in MAP.enc -pass file:...`), second marker (Opus), results, log, push.
- **L81 reviewed** (results/L81 Review): the calibration now has a second version (authority/), with a prose reader role, a hedged pair, a narrator's-own passage and three rewordings added to the corpus (Sonnet writer resumed; files to scratchpad/l82_texts2, then into the corpus folder as B11..B16 with the map sealed). Runner drafted: tools/L82_run.sh (translate, drivers, consequences, sameness, blind reader, prose reader, manifest). Plan L82 gets a second version with these before the premise check.
- **Earlier**: L82 Arm B plan DRAFTED (tests/, hash 0b28c0395364afa4), awaiting (a) the pilot's Mimo and T11-B results to fill premise P4, (b) the L81 critic (Opus, background), (c) a premise check by a fresh Opus agent; then freeze (hash in the log), then run: `translate_via_api.py` for Atria and Mimo on B01..B10 (corpus in results/"L82 Corpus - ten reported arguments"; the map is sealed in MAP.enc, passphrase at scratchpad/keys/l82_map_passphrase.txt; do NOT open before both markings), both drivers, consequences_2, sameness_2 within pairs and across translators, the reader (Atria, one call per report, `READER BRIEF for the API reader.md` + report appended, shuffled names), then mark, second marker (Opus), results, log, push.
- **Pilot state**: Atria on T10-D valid (6 lines, the guide's own example text, so not a test of translating); Atria T11-B response was produced by an orphaned call (rebuild it with `translate_via_api.py --rebuild atria T11B <out> <response>`); Mimo on both running in the background (scratchpad/l82_pilot/pilot_mimo.log).
- **Owner's instructions this stretch** (decision L11, verbatim in Decisions): use two API models for the outside-agent roles, Atria (30 requests per minute) and Mimo (100 per minute); think as high as possible, skills as needed; work autonomously until tokens run out; at most 5 Opus subagents at a time, never Fable subagents; keep goals calibrated to file 11 (Semantics revision 1) and to the goal, reasoning with prose; keep asking when a vaguely incorrect translation is acceptable; the owner reviews at the end; push often, "work lost is more costly than a cluttered repo"; keep this handover log.
- **Agents rule** (owner, later in the same stretch): Opus 5 at extra effort for complex analysis; Sonnet 5 for testing language candidates; at most 5 Opus at a time; never Fable.
- **Keys**: NOT in the repository. They sit in the scratchpad at `scratchpad/keys/api.env` (variables ATRIA_API_KEY, MIMO_API_KEY). If the scratchpad is gone, the owner has them.
- **Atria**: works. OpenAI-shaped. `POST https://api.atria-asi.ai/v1/chat/completions`, header `Authorization: Bearer $ATRIA_API_KEY`, model `Atria-Dawn-Preview`; returns `reasoning_content` beside `content`. 30 RPM.
- **Mimo**: works at the Singapore token-plan endpoint only: `POST https://token-plan-sgp.xiaomimimo.com/v1/chat/completions`, Bearer, model `mimo-v2.6-pro`; the China and Amsterdam endpoints reject the key. 100 RPM. Returns `reasoning_content`.
- **The caller**: `Language/tools/ask_model.py PROVIDER --system F --user F --out DIR --tag T`; keys from the environment (`. scratchpad/keys/api.env; export ATRIA_API_KEY MIMO_API_KEY`); writes response, reasoning, request and a receipt with the provider's response id per call; rate-limited per provider; retries.
- **State of the project**: Arm A done (L79 results, eight of eight on two markings; new versions run_check_2.py, consequences_2.py, sameness_2.py, L80 built beside the old). Next is Arm B: ten new short texts nobody in the loop wrote, two translators (Atria, Mimo), old and new rig, a reader on shuffled reports, a plan with quoted premises and a mock suite covering every fixed string (Lesson L12), frozen by a second agent (Opus), marked twice.
- **Done**: L81 calibration note (authority/), being tested by an Opus critic; the Arm B corpus brief (tests/L82 Arm B corpus brief/), texts to be written by a Sonnet agent, map sealed by hash.
- **Before Arm B**: a calibration note against file 11 and the "reasoning with prose" goal, and the rule for when a vaguely incorrect translation is acceptable (L64 section 6 has the layer table; make the rule explicit for Arm B's marking).
- **Where things are**: plan L79 and its checks in tests/; outputs in results/"L79 Arm A outputs"; the API caller script, once written, in Language/tools/ (reads keys from the env, never from a file in the repo).

## Traps
- Committing a key. `git diff --cached | grep -i "atr_\|tp-"` before every commit.
- Letting this file replace the log. It is a pointer; the entries in the project story are the record.
