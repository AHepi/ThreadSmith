% Written by OpenAI Codex under frozen plan 45.
% N03-A, new comparison side; actual-ledger standing is recorded in the sibling JSON.
line(p).
kind(pear, pear) :- line(p).
line(y).
holds(has_yellow_skin(pear)) :- line(y).
line(r).
holds(has_red_colouring(pear)) :- line(r).
