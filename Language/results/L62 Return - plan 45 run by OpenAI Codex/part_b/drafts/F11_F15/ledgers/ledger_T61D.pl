% Written by OpenAI Codex, under plan 45, from the other model's corpus.
line(1). line(2). line(3). line(4). line(5). line(6). line(7).
holds(stated_happening_exists) :- line(1).
holds(stated_movement_exists) :- line(2).
holds(unassessed_remainder_exists) :- line(3).
holds(retains_separately(translator, stated_happening)) :- line(4).
holds(retains_separately(translator, stated_movement)) :- line(5).
holds(retains_separately(translator, unassessed_remainder)) :- line(6).
denied(claims_missing_physical_check_ran(translator)) :- line(7).

