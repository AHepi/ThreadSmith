% Written by OpenAI Codex, under plan 45, from the other model's corpus.
% N22-A contains no SO THAT or claim_plan/3 line.
line(1). line(2).
depends(kept_cool(pear), setting(dial)) :- line(1).
changes(turn(dial), setting(dial)) :- line(2).

