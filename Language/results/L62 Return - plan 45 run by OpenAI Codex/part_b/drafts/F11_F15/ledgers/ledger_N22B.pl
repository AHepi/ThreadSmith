% Written by OpenAI Codex, under plan 45, from the other model's corpus.
% N22-B contains a specific change relation but no SO THAT or claim_plan/3 line.
line(1). line(2).
depends(kept_cool(pear), set_to_cool(dial)) :- line(1).
changes(turn(dial), set_to_cool(dial)) :- line(2).
