# Frozen plan 45 execution return

Written by: OpenAI Codex

This is the execution evidence and draft result for frozen plan 45. Start with:

- `DRAFT 45 Test results - the 38 and 39 audit package: ledgers compared, findings sorted.md` for the result and the hard-to-vary audit;
- `EVIDENCE_LIMITS.md` for runner and interpretation limits;
- `part_b/PART_B_COVERAGE.md` and `part_b/PART_B_PREREGISTERED_RUN_MATRIX.md` for the exact 48-record inventory and governing run roles;
- `RETURN_VALIDATION.json` and `BUNDLE_MANIFEST.json` for structural validation and file hashes.

## Result in brief

Part A found mostly shared content on six disclosed, non-independent translation pairs, not unique or blind reproducibility. T02-B has one archived-only commitment, and the frozen T02-D Result/Fact prediction did not appear. Every nonempty pair also differs materially in standing.

Part B sorts the findings as follows:

- pile 1: F01, F02, F03, F10 and F12;
- pile 2: F04 (conditional on promised scope), F09 (bundle failure; exact layer open) and F15;
- pile 3: F05, F06, F08, F11, F13, F14, plus F07/N20 only by the separately logged patch-7 give-up;
- no pile on this evidence: F07/N19, because the patched rig already separates the unsupported offered route from the independently supported endpoint.

The hard-to-vary review changed or narrowed several frozen predictions instead of fitting the fixtures to them. In particular, F04 and F09 carry explicit `held-if` qualifications; F05's predicted mechanism, F07/N19's predicted label, F08-B's predicted warning and F12's predicted runtime answer were not observed. No language or rig patch was built after sorting.

## Evidence inventory

- Part A: six new five-section translations, 12 JSON/Prolog ledger pairs, 12 isolated native runs, six exact sameness outputs and hand decisions.
- Part B: all 48 prepared IDs accounted for, 50 isolated native runs and 149 labelled direct-query runs.
- Logs: byte-identical copies of the two supplied historical whole logs, every new per-invocation raw log, and two reconstructed whole-rig concatenations with exact segment offsets and hashes.
- Runtime: s(CASP) 1.1.4 on SWI-Prolog 9.0.4, with executable and installation provenance recorded under `runtime/`.
- Integrity: the supplied ZIP and its 194 extracted files were byte-checked; the source tree, frozen plan, authority files and rigs were not modified.

Native headlines such as `NO FAULT FOUND`, `Fine`, `JUMP`, or wrapper exit zero are not treated as successful negative semantic results when a bearing solver query errored. Clean direct queries are supplemental evidence and never become source commitments or native-driver verdicts.

Receipt paths preserve the original execution workspace for provenance. Re-running those commands requires the original source bundle and the recorded runtime; the return ZIP contains the authored adapters, outputs, receipts and hashes, not a second copy of the unchanged source bundle.
