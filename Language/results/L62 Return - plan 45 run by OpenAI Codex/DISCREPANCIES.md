# Supplied-package discrepancies and resolutions

Written by: OpenAI Codex

| Supplied statement or tension | Observed package state | Resolution in this return |
| --- | --- | --- |
| The results/58 handoff says to read `evidence/native_smoke_result.json` and use `tools/resume_smoke.py`. | Neither path exists in the supplied ZIP. | Ran the top-level installation/smoke procedure directly and preserved reports, raw logs, exits and runtime identity under `runtime/`. |
| The requested manifest is described by reference to `results/58/BUNDLE_MANIFEST.json`. | No such manifest is present in the supplied ZIP. | Created a new return manifest with relative paths and SHA-256 values; did not fabricate the missing historical file. |
| The frozen plan names “the sameness test from rig 1” but the historical rig entry points are single-ledger checkers. | The top-level handoff supplies `tools/sameness.py` as the now-provided mechanical comparison. | Ran that exact supplied program, preserved all six outputs, and added human wording/meaning decisions separately. |
| Plan 45 says Claude translates. | The top-level handoff says the new executing agent is the translator and must use its own name. | Every authored artifact names OpenAI Codex. No output is labelled as Claude-authored. |
| The old export uses TOLD fixture worlds; the handoff fixes actual-ledger standing for fresh translations. | Both conventions are present and semantically active. | Kept the archived bytes unchanged, used CLAIMED/GIVEN on the new side, and reported standing differences separately from shared content. |
| The prepared Part B manifest expands unsuffixed families to 48 exact IDs. | Its own handoff says expansion is preparation, not an instruction to run all members. | Authored coverage for all 48, then froze a prospective execution matrix distinguishing adjudicative runs, controls, diagnostics and no-runs. |
| The frozen plan's N17 prediction says LETS against a stated tendency. | N17-A says “assisted” and supplies no tendency; N17-B states an opening. | Used HELPS with no tendency for N17-A and recorded the exact prediction miss. No LETS/tendency line was inserted. |
| The frozen plan's N22 prediction assumes a SO THAT check. | N22 supplies a dependency and a change but no SO THAT source assertion. | Did not convert the assessment question into a source line; ran labelled direct relevance queries instead. |
| The top-level work order says to keep whole raw logs, while shared append logs obscure per-run boundaries. | Historical logs already contain earlier material. | Copied the two whole supplied logs unchanged and placed each new run in its own raw log. |
| Historical drivers print normal prose after child s(CASP) predicate-existence errors. | Smoke and later raw logs exhibit this behavior while wrappers exit zero. | Preserved the prose and raw errors, and treated the bearing errored queries as untested rather than negative answers. |
