# L72 return — Astra Ultra

Written by Astra Ultra. This archive contains the eight independent translations, eight JSON/Prolog ledger pairs, their unchanged checker reports and raw query logs, unchanged consequences outputs and raw query logs, the gauge, the requested draft, execution receipts, and a SHA-256 manifest. Source material copied for inspection and replay retains its original authorship and exact bytes. Machine output is kept verbatim, including empty stderr files; Astra Ultra authorship and execution provenance are recorded in its enclosing receipts rather than inserted into raw evidence.

Start with `DRAFT L72 - what the run showed.md` and `GAUGE_Astra_Ultra.md`. Each text’s full five-section translation is under `translations/`. The executable pair is under `rigs/rig 1 - arguments/`. Its stdout, stderr and raw logs are under `runs/Astra_Ultra/` in the directory matching its ID with the hyphen removed. Initial translations and pairs are frozen and unchanged; no corrections were necessary in the read-only conformance review. The translation sequence receipt records each freeze in the required order.

The smoke run passed using the existing SWI-Prolog 9.0.4 and s(CASP) 1.1.4 installation. The output matches the bundled expected report after excluding the timing-bearing GAUGE line. All sixteen text commands exited zero with no driver stderr or recorded query timeouts. Expected missing-predicate messages are preserved in raw logs. These execution facts are not endorsements of the text commitments or proof that every language feature was queried.

For replay, use a machine where the supplied driver’s `/home/claude/sCASP/scasp` exists. From `rigs/rig 1 - arguments/`, run `python3 patched/run_check.py ledger_T05B.pl /new/path/checker.raw_log.txt` and save stdout and stderr to fresh files. From the archive root run `python3 tools/consequences.py 'rigs/rig 1 - arguments/ledger_T05B.pl' /new/path/consequences.raw_log.txt`, again saving output to fresh files. Substitute the other IDs. Use fresh paths because both programs append to raw logs; never replay over the evidence in this archive. The receipt files retain the exact original commands and paths.

The original runtime scripts are included in their working relative locations, without edits. The source inputs are under `source_inputs/`. The source manifest’s text hashes, exact sentence reconstruction and unchanged corpus questions were verified. Every original member of the input ZIP remained byte-identical in the working copy; the new ledger pairs were additional files. No repository was opened, cloned, read or written, and nothing was pushed.

`BUNDLE_MANIFEST.json` hashes every other archived file, with byte sizes and a file count. The manifest excludes itself, as in the provided example; a self-hash would be circular. `Astra_Ultra_validation.json` records checks completed before packaging. The ZIP is the sole return deliverable.
