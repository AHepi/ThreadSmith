# Reruns by the orchestrator, 21 September 2026

Eight of the worker's Part B ledgers (its files, unchanged, copied here beside the outputs) run again on this machine's copy of the patched rig-1 driver, s(CASP) 1.1.4 on SWI-Prolog 9.0.4, to check the worker's reports before writing file 45's results. Each `report_<case>.txt` is the driver's printed report; each `raw_<case>.txt` is the raw log of that one run. Compare with the worker's `part_b/runs/.../rig1_patched/` in the return folder L62. Timings differ; nothing else was found to.
