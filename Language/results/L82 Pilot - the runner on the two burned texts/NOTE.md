# L82 Pilot - the runner on the two burned texts (B98 = T10-D, B99 = T11-B), 22 September 2026

Run at the frozen runner (59084b21d61230d1), caller (edited during the run: bdd97f6b4e0ed5ba at its start, 6736ac6793471387 by its reader step) and translator (284be80d1703b60f), started 15:15:18 UTC, from the scratchpad (path `.../scratchpad/l82_pilot2/out`). Not a test of translating (T10-D is the guide's first example) but of the pipeline. What it showed (**seen**):
- Atria valid at the first validation attempt on both texts; Mimo valid on B98 (7 lines, 535 s). Mimo's call on B99 did not return within the 2400 s timeout; its second HTTP attempt was stopped by the orchestrator at 16:01 so the pilot could go on with three ledgers (the earlier pilot, at the 24000-token cap, had returned empty content on this text after 651 s with finish reason `length`).
- Steps 2 to 4: drivers 7 s, consequences 14 s, sameness and COUNTS as expected, no error file non-empty.
- Step 5: six reader calls in eight lanes, 1108 s (the slowest call about 18 minutes); every report blinded (world names mapped, bin quotations cut, nothing still present); no failure, no skip.
- Step 6: three prose-reader calls, 725 s; no failure, no skip. The prose reader labelled every line of the report, OUTCOMES rows and gauge notes included, not only the findings; the marker reads only the findings proper.
- Step 7: the manifest hashed nothing, because the runner skips any directory whose path contains "scratch" and the pilot's output path did (`.../scratchpad/...`). The real run's path (`Language/results/L82 Arm B outputs`) does not; the runner is frozen and was not changed; the run's manifest is checked after the run.
- The start guard: it flags the tool shell that launched the runner when that shell's command line names the runner and the launching subshell has exited (neither ancestor nor same group); the run was therefore started through a launcher script whose command line does not name the runner (`scratchpad/launch_l82.sh`). Recorded in the log.
