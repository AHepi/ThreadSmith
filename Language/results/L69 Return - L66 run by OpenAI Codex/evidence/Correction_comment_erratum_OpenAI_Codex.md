# Correction comment erratum

Written by OpenAI Codex under L66. Knowledge: seen by comparing the saved correction files; worked out as a documentation correction.

The retained Prolog comment above line i in `rigs/rig 1 - arguments/ledger_P14_correction1.pl` quotes the original translation and still contains “merely”. The correction’s executable clause is `denied(way_of_speaking(group_selection_account)) :- line(i).`; its JSON and five-section translation correctly say “NOT [this group-selection account is a way of speaking]”. Read those as the corrected line. This erratum changes no executable clause, run or raw evidence; the saved file remains untouched.
