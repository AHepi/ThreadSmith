Let me work through this cross-examination carefully.

The question: does the HV skill add anything when handed to a reader auditing a theory, vs. should it be abandoned?

Design summary:
- 3 API models (atria, mimo, deepseek) × 4 conditions (ST, NT, SO, NO) × 2 docs (seeded, clean) × 3 reps = 72 reader reports.
- Opus 5.5 subagent: 2 conditions (S, N) × 2 docs × 3 reps = 12 reports. Total 84.
- Marking by Atria and Mimo, thinking on, each marks all 84 reports → 168 marking calls.
- Blinding via anonymous ids in MAP.json.

Now find flaws. Let me enumerate systematically.

**1. The reader task prompt names the error kinds — the task prompt tells every reader, including no-skill readers, exactly what kinds of defects to look for, including the skill's target kinds ("conditions or clauses that do no work", "anything that shields the theory from criticism").** This is stated in the plan as deliberate ("The task names the HV kinds and the GEN kinds alike, so a reader without the skill is not left to guess that idle parts or shields count"). This is a confound: it means the no-skill arm is partly taught the skill's targets by name, so the measured effect is "skill + task hint" vs "task hint", not "skill" vs "no skill". If the skill's value is partly in telling you what to look for, the task prompt has already given that away. Also it makes the no-skill condition not a control for the skill. Why it matters: it biases toward the null — it reduces the skill's apparent advantage, so a null result cannot distinguish "skill adds nothing" from "the task prompt already supplied the skill's key content". Smallest fix: strip the HV-specific kind names from the task prompt for all conditions — describe defects generically ("statements that are false, proofs that do not prove what they claim, claims wider than their support, contradictions") and drop "conditions or clauses that do no work" and "shields from criticism" — or run a second task-prompt variant. Actually the smallest change: remove the two HV-kind phrases from `reader task.md`.

Wait — but careful: the GEN errors are also named ("claims wider than support" is a GEN error kind — "a biconditional wider than its proof"). Hmm. The task names "claims wider than their support" which is the D3/GEN kind. If we strip everything, readers won't know what to look for at all. The cleanest minimal fix: keep the generic defect taxonomy (false statements, failed proofs, unsupported-width claims, contradictions) which is standard review language, and remove the two skill-specific categories ("conditions or clauses that do no work", "shields the theory from criticism"), which are the HV skill's signature targets. That makes the comparison "skill vs no skill" fair: without the skill the reader isn't told the HV categories exist.

**2. System-prompt asymmetry: the skill condition's system prompt is ~13,648 words longer than the no-skill one.** This is a length/context confound: more tokens in the system prompt could prime thoroughness, verbosity, or "list more items" behavior independent of the skill's content. Also the no-skill system prompt ("You are reviewing a document.") is one line — it doesn't even tell the reader it's auditing a theory, while the with-skill prompt says "You are reviewing a document. You have the skill below, a method for testing explanations, and you should use it for this audit." The asymmetry is inherent to the design (you can't give the skill without tokens), but the fix is a placebo/length-control arm: give the no-skill condition a system prompt of matched length with irrelevant content (or a different method document, file 24 as mentioned in "Not tested" — the plan says the other model is given file 24 in its place in S78 but this test uses a one-liner instead!). Actually the plan says "the other model is given file 24 in its place" — that's the S78 usage. But here the no-skill condition is a one-line prompt, not file 24. So the test deviates from the deployment usage it claims to test. Smallest fix: make the no-skill system prompt a length-matched non-HV document (file 24, or a generic "how to review a paper" text), so the only difference is the skill's content, not its presence/length.

**3. Markers mark their own model's reports.** Atria marks Atria's reports, Mimo marks Mimo's reports. Self-marking bias: a marker may be lenient toward its own model's style, or, conversely, harsh. The plan acknowledges style leakage but not self-bias. With the agreement rule (both must say FOUND), a self-favoring bias on one marker is partly cancelled by needing both — but if Atria is lenient on Atria reports, and Mimo is lenient on Mimo reports, each report gets one lenient and one neutral marker, which biases all reports toward "not found" relative to a fully neutral panel... actually it biases *all* cells equally? No — it biases toward the null across conditions if bias is uniform. Hmm, but it's not uniform: the bias direction is unknown and it's a systematic per-marker confound that interacts with per-reader tables (each reader's row is marked by a different mix). The per-marker tables are given, so the reader can see it, but the "agreement" table mixes a lenient marker for reader X with a lenient marker for reader Y. Smallest fix: exclude each marker from marking its own model's reports (i.e., Atria marks only Mimo+DeepSeek+Opus reports, Mimo marks only Atria+DeepSeek+Opus), and have the disagreements adjudicated as already planned. But then each report gets 2 markers still, neither self. That's clean. Alternatively add a third marker. Smallest change: skip jobs where marker model == reader model.

**4. Three repetitions is far too few for the stated inference.** The plan itself says a difference of one error in twelve is noise — but with 3 reps × 4 planted-per-kind, per cell per kind you have 12 possible finds per kind per cell... wait: 3 reps × 4 HV errors = 12 HV opportunities per cell. A difference of 1/12 = 8.3% is within noise. The plan's rule "difference called a finding only when it holds in the same direction on at least two readers" is a weak guard; with n=3 reps the variance is large, and there is no power analysis, no significance test. Also 3 reps of a single document means the variation is only sampling noise from the model, not generalization across documents (the plan does say "other documents than file 10" not tested, so that's acknowledged). Smallest fix: this is a judgment call — increase reps to at least 6–10 per cell for the key comparison, or at minimum report a proper uncertainty measure (exact binomial / bootstrap CI) and require the CI to exclude zero. Given cost, minimal change: add a confidence interval to every reported difference and only call a finding when the interval excludes 0 (and same direction on ≥2 readers).

**5. The agreement rule is a hidden double threshold that can manufacture false alarms or misses.** "an error counts as found when both markers say FOUND; a false alarm when both say MISTAKEN". If markers systematically disagree (one strict, one loose), nothing is ever found and nothing is ever a false alarm — the table would show zeros in both directions and the plan would read it as "no gain and no false alarms" → "abandon" or "adds nothing", when in fact it's a marking artifact. Also disagreement cells are listed but the found/false-alarm counts are asymmetric in their handling: an error one marker found and the other didn't is not counted (conservative for found) but a false alarm one says MISTAKEN and the other GENUINE is also not counted (conservative for false alarms) — actually both are dropped, which is symmetric in treatment but biases the *comparison* if the skill changes marker agreement (e.g., skill reports use skill jargon that one marker recognizes and the other doesn't — plausible! Atria may recognize HV skill terms like "held", "loose", "idle" differently than Mimo). The skill changes report *style*, which changes marker agreement, which changes the found-rate — an unblinded pathway from skill → style → marker agreement → verdict. The plan says "the per-error rules are written so that style cannot pass for finding" but marker variance is real. Smallest fix: add the tie-breaker earlier — have the Opus adjudicator resolve ALL disagreements (both FOUND/NOT and GENUINE/MISTAKEN/UNCLEAR splits), not just a subset; or use majority of 3 markers. Minimal: extend the Opus check to every disagreement cell, both found-flag and false-alarm verdicts, and count those cells by the adjudication.

Hmm, the plan does say "every disagreement is listed" and Opus re-marks every disagreement. "its call stands for the agreed table's disputed cells". OK — so Opus does resolve disagreements. But wait: does "every disagreement" include GENUINE-vs-UNCLEAR and MISTAKEN-vs-UNCLEAR? It says "re-marks every disagreement against the key" — for key errors. The false-alarm verdicts (GENUINE/MISTAKEN/UNCLEAR) disagreements — are those re-marked? Ambiguous. The smallest fix is to state explicitly that Opus adjudicates all disagreement cells including other-item verdicts, and record its verdict in the table. Actually a cleaner issue: Opus adjudicating means the final table is Opus's judgment for disputed cells — fine, but must be recorded as such (it is). So this point is partly about clarity. I'll fold it in as a lower-severity point maybe. Let me reconsider severity.

Actually there's a more serious version: **the markers are Atria and Mimo, which are also two of the three readers.** The "agreed" table requires both markers to say FOUND. If a reader is Atria and markers are Atria+Mimo, then... covered in point 3.

**6. The clean document carries one real error, D3.** So the clean document does NOT measure false alarms purely — a reader who reports D3 on the clean document is *correct*, not a false alarm. The plan handles this (D3 found of 6, both documents; key for clean doc is D3 only). Fine. But: the false-alarm count on clean is "items listed minus D3 found minus UNCLEAR"? The table says "false alarms on the clean document (both markers MISTAKEN)". OK. But there's an issue: the clean document has zero planted errors, so a reader's report on clean has 0 true positives available except D3 — the false-alarm rate is unbounded/unanchored (no denominator specified other than items listed). Minor. Also the seeded document also contains D3, so on seeded, a reader listing D3 is correct (D3 found of 6, both documents — 3 reps × 2 docs = 6). OK that's handled.

**7. The seeded document's planted errors were made "by program, each old string matched once."** Risk: the programmatic edits may have left artifacts (e.g., formatting inconsistencies, double spaces, or the replaced text differing stylistically) that a model could detect, or the edits may have broken cross-references (a swapped theorem may be referenced elsewhere). Not checkable from here, but the seeded list is sealed and its SHA recorded — fine. However: the seeded and clean documents differ in *filename and title?* "Readers see the document under its own title, 'Claude Fable Semantics', with no mention of seeding." Good — but the reader prompt appends the document content; the filename isn't in the prompt content. Good. But the marker key parsing: `key.index("## The error file 10 already carries")` and `"## Predictions, sealed"` — string-slicing the sealed list by heading names. If the sealed list's headings differ, this crashes or silently mis-slices. Not checkable without the file; flag as fragility.

**8. The Opus arm leaks the seeding.** The plan admits: the repository is in reach, the seeded file sits in `tests/` under its S80 name ("S80 Seeded authority - file 10 with eight planted errors.md"). An Opus subagent told to read only its working folder still has the repo in reach; even if obedient, the file name itself is a leak if it appears in tool output. Worse: the *clean* document for Opus is file 10 at its real path `authority/10 Claude Fable Semantics...`, and the seeded one is a copy with "planted errors" in the name. If Opus sees the filename, it knows which doc it has — and in the S condition it could even read the seeded list? No, the sealed list is outside the repo. But it could read the skill (S condition: told to read the skill's directory first — fine, that's intended). The real leak: Opus can see both files in `tests/` and `authority/`, could diff them, and could find the planted errors by comparison — in BOTH conditions (S and N), since the leak is the file presence, not the skill. That would make Opus's numbers incomparable to the API readers (who see only the document text). The plan's mitigation (search reports for "S80", "seeded", "planted" afterwards) is a post-hoc detection, not prevention: a model that found the leaks by diffing would report the errors as genuine audit findings without naming the file. Smallest fix: for Opus, copy the document into the working folder under a neutral name ("document.md") with no reference to S80/seeding, and don't give the agent any path outside that folder (or explicitly instruct it not to list/diff files, plus verify by logging the agent's tool calls). Also apply the same neutral naming to API arms (already fine). And the plan should state the working folder contains *only* the document copy; the repo being "in reach" should be removed (run the subagent with a sandboxed cwd).

**9. Runner code bugs:**

(a) `run_pool` creates one ThreadPoolExecutor per model with max_workers=3 — that's 3 in flight per provider, matching the cap. OK. But `futs` collects futures from all pools; `f.result()` raises any exception — fine. However `worker(j)` prints but doesn't guard exceptions; a worker exception propagates and kills the run mid-flight (other in-flight calls still complete but their results... they're already written by `call`). Actually the exception propagates from f.result() in the main thread, aborting the loop; remaining futures may never be waited on. Minor resumability concern.

(b) **Resumability/idempotency bug in `mark()`:** `call()` returns "skipped" if `.response.txt` exists. But in `mark()`, if a marking call *fails* (writes .error.txt and .receipt.json with failed:true, no .response.txt), a re-run will retry it — good. But if a call succeeds and later you re-run `mark` with a *new* key file, old marks are reused (skipped) — that's intended freeze. OK.

(c) **MAP.json generation is non-deterministic across partial runs** but deterministic given the same tag set order (`sorted(tags)`, seeded RNG) — fine. But: `if t not in mp.values()` — if a previous run mapped some tags and new tags appear, new ids are assigned; old ones kept. Fine. However, **the map is written to `OUT/marks/MAP.json` before the marking calls are made, i.e., the anonymization key sits in the same directory as the marks.** The plan says "is in no marker's input" — true, the marker prompt doesn't include it. OK.

(d) **Doc determination for marking: `doc = "seeded" if "_seeded_" in t else "clean"`.** For Opus reports saved as `opus_S_seeded_r1.response.txt` — contains "_seeded_" → seeded. Good. But wait, DOCS keys: "seeded" and "clean"; tags like `atria_ST_seeded_r1`. And `DOCS[j["doc"]]` for "seeded" path is `tests/S80 Seeded authority...`. Fine.

(e) **`mark()` key slicing:** `key_seeded = key[:j]` — the key file must contain "## The error file 10 already carries" and "## Predictions, sealed". If "## The error file 10 already carries" appears AFTER "## Predictions, sealed", `i > j` and `key[i:j]` is empty → silent wrong key for clean doc. Also for the seeded key, `key[:j]` includes the predictions section? No, j is the predictions heading index, so key[:j] excludes it. Good. But the seeded key also includes "## The error file 10 already carries" section which is about D3 — the plan says the seeded key is "the sealed list without its predictions". Fine.

(f) **`readers()` passes `sys_plain` for no-skill — a one-liner.** Already covered in point 2.

(g) **`call()` writes `.request.json` before the retry loop, using `raw` — fine. But `.error.txt` is written only on FINAL failure; on retryable failures the history is only in memory. If the process dies mid-retries, no record. Minor.

(h) **`call()` retry on empty content with finish=="length" sets status -4, which is not in FINAL, so it retries** — but if max_tokens is genuinely too small, it will retry 5 times and then fail with "failed" — wasteful but OK. Actually wait: empty content with finish "length" means the model spent all tokens on reasoning (thinking on, high effort) and produced no visible output. With max_tokens=48000 and reasoning_effort high, this is plausible! Especially since `max_tokens` typically caps *total* output including reasoning tokens for some providers. If a provider counts reasoning against max_tokens, high-effort thinking could exhaust 48k tokens in reasoning, yielding empty content and finish=length → retried 5 times → marked failed. The receipt records reasoning_chars only on success. This is a real design risk. Smallest fix: treat finish=="length" with empty content as a final failure (don't retry) or increase max_tokens; better: on empty-with-length, retry once with doubled max_tokens. Hmm, minimal: add "length" to FINAL? No — a length-truncated *non-empty* response is fine (finish=length with content present is kept). The bug is only empty+length. Minimal fix: if empty and finish == "length", retry once with max_tokens × 2, and record it.

Actually also: for thinking-on calls, `reasoning_effort: high` is set, but `max_tokens` 48000 — DeepSeek's API historically counts reasoning tokens against max output? DeepSeek R1 had separate reasoning content. Can't verify. Flag as risk with the minimal fix above.

(i) **`_stream` timeout: `idle=900` seconds read idle timeout; connect 30s.** A 15-minute idle timeout is fine. But `requests.post(..., timeout=(30, idle))` — with stream=True, the read timeout applies per chunk read; OK.

(j) **`_stream` returns `r.text[:4000]` for non-200 — fine. But 429 handling: retries with `time.sleep(min(120, 10 * 2 ** n))` — n=1: 20s... exponential, fine.**

(k) **In `call()`, `res["content"]` may be None?** `content = ""` initially, so it's a string. OK. But `res` could be None if exception — handled.

(l) **`mark()` uses `call(j["model"], None, user, ...)` — system=None, so messages has only the user message. But the marker task asks for "a single JSON object and nothing else". With thinking on and high effort, markers may produce reasoning then JSON — fine. But there's no JSON validation or parsing; the marks are written as raw response text and presumably hand-read later. The plan says markers return JSON; the table is built... by the owner, not by code. There is no code that builds the table! The plan says "The table" is read off — but no script aggregates marks. That's a gap: the final aggregation is manual, error-prone, and not specified in the repo. Smallest fix: add a small `table` command that parses the marker JSON responses, joins with MAP.json, applies the agreement rules, and prints the table — so the aggregation is reproducible and inspectable.

(m) **`readers(only)`: `sys.argv[3:] if len(sys.argv) > 2 and sys.argv[2] == "--only" else []` — fine.**

(n) **The pilot call:** "One pilot call (DeepSeek, NO, seeded) was made before this plan... in the scratch area, not in the outputs, and does not count." Fine — but it means DeepSeek NO seeded has been seen already; not a counting issue.

(o) **`read()` in `skill_text()` uses relative paths `SKILL_DIR = "HV Skill/authority/hard-to-vary"` and the docstring says run from repository root. But DOCS uses `S = "Semantics"` prefix, and PR uses `S` prefix, while SKILL_DIR does not.** So the skill dir is at repo-root/HV Skill/... and the Semantics stuff at repo-root/Semantics/... Inconsistent but workable if run from repo root. However, the plan file says the skill is "HV Skill/authority/hard-to-vary/" — and the docstring of s80_run says "Run from the repository root". So the skill lives outside Semantics/. OK, but the Opus agent path and the `tools/` path: `sys.path.insert(0, os.path.dirname(__file__))` and `from s80_call import call` — works. And `import` happens before the cwd matters. Fine. Just flag path fragility: if run from inside `Semantics/`, SKILL_DIR won't resolve. Minimal fix: anchor all paths to `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` plus a configured repo root.

(p) **The `mark` command's `run_pool(jobs, one)` groups by marker model — `by_model.setdefault(j["model"], ...)` — with max_workers 3 per model. 168 calls, 2 models, 3 workers each = 6 concurrent. Fine.**

(q) **Opus reports: the runner doesn't produce them; Claude runs subagents and saves them to the same folder as `opus_<cond>_<doc>_r<n>.response.txt`.** The `mark()` glob picks up `*.response.txt` — so Opus reports are included automatically. Good. But **Opus's reports have no `.request.json`/`.reasoning.txt`/`.receipt.json`** — inconsistent artifact set, and no thinking flag recorded for the table's "thinking" column (the plan says thinking can't be switched for a subagent). Also **the Opus arm has no NT/SO/NO cells, so it can't separate skill from thinking at all**, and the owner's named comparison (ST vs NO) has no Opus counterpart. The plan says Opus is in the "four readers" claim in the title ("four readers with and without it") but the table per reader only has 2 conditions for Opus. That's fine if reported as such. The bigger issue: **the Opus arm cannot answer "does the skill add anything" for Opus at the same thinking setting** because thinking varies with the agent run and isn't recorded — the S and N runs may differ in thinking by chance. Smallest fix: record the agent's thinking setting per run in a receipt, and treat Opus as a separate, weaker arm (already stated) — or drop it. I'll flag it mid-severity.

(r) **`call()` signature: `call(provider, system, user, out_dir, tag, thinking, max_tokens=48000, ...)` — in `mark`, `call(j["model"], None, user, f"{OUT}/marks", j["tag"], True, max_tokens=32000)`. Positional args — `thinking=True`, `max_tokens=32000`. Correct. In readers: `call(j["model"], sys, task+doc, f"{OUT}/readers", j["tag"], j["thinking"])` → max_tokens default 48000. OK.**

(s) **The `idle=900` and `attempts=5` are fine. But `FINAL` includes 413 (payload too large) — with a 13k-word system prompt + document + 48k max tokens, 413 is conceivable on some providers; not retried, marked failed, and then that cell is silently missing from the table. The plan has no missing-cell handling rule.** Smallest fix: state how missing/failed cells are treated (exclude and note, or re-run with reduced tokens), and have the runner report a final list of missing tags vs the full job list (a `verify` command). Also add a `verify`/`missing` subcommand. Good point: **there is no completeness check in the code.**

(t) **`reasoning_effort: high` is sent as a body field — for the no-thinking conditions it's omitted. Good. But the plan says "with thinking on, reasoning_effort: high" for markers too. Markers marking with high reasoning effort — fine.**

(u) **Temperature: provider-default.** Acknowledged in "Not tested". But provider defaults differ across providers, and within a provider the default is fixed — so cross-condition comparisons within a reader are fine. Cross-reader comparisons (which the plan does make: "two or more of the four readers") confound model differences with temperature defaults. That's inherent; plan acknowledges temperature not tested. But it matters for the "same direction on two readers" rule: the readers are not exchangeable. Minor; flag it.

(v) **The false-alarm denominator and "items listed" metric:** The table includes "items listed" and tokens/seconds. With the skill, reports will be longer (skill prescribes format — its own test found it changes report form). Longer reports → more items listed → more chances for a marker to say MISTAKEN vs GENUINE. **The false-alarm comparison is not normalized**: "false alarms not rising by more than the gain" compares raw counts of found errors (max 12) against raw false-alarm counts (unbounded). If the skill doubles report length, false alarms could double while finds go up by 1 — the rule as stated ("false alarms not rising by more than the gain") would then reject the skill on a count comparison that is really a rate comparison. Smallest fix: report false alarms *per item listed* (a rate) alongside raw counts, and define the threshold in terms of rates, or require markers to judge every listed item (they do) and compare false-alarm *rates* on the clean document per report.

(w) **Blinding is only partial by design** — the plan admits style leaks ("held", "loose", "idle"). Given markers are Atria and Mimo, and Atria is a reader: if Atria's own reports in the skill condition use skill jargon, Mimo marks them — fine; but Atria marking Mimo's skill-condition reports may recognize the skill... the bigger issue is **marker knowledge of the skill itself**: do the markers know the HV skill? If Atria knows the skill's vocabulary, it may be more generous to reports using it (or harsher). The plan doesn't say markers are ignorant of the skill, nor that they're instructed to ignore style. Smallest fix: add to the marker task an explicit instruction to mark only by the key's "Found when" rules and to ignore the report's vocabulary/method; plus (better) have the Opus adjudicator re-mark a random 10% sample as a drift check. Hmm, Opus adjudicator already re-marks disagreements. Add a calibration sample.

(x) **"Adds something" decision rule is not specified for false alarms numerically** — "false alarms not rising by more than the gain" — gain measured in found errors (out of 12 per kind, or 24 total?). Ambiguous: gain in HV found vs rise in false alarms (both counts)? The rule mixes per-kind and total. Need explicit numeric rule. Smallest fix: define gain = (found with skill − found without) over all 24 planted per cell; false-alarm rise = (FA with skill − FA without) per report averaged; require gain ≥ 2 and FA rise ≤ gain, with CIs.

(y) **The sealed list's SHA-256 is recorded in the log "now" — good. But the sealed list is the answer key; the `mark` command takes `KEY_FILE` as an argument and the key is sliced by heading strings. If the headings don't match exactly, KeyError (`key.index` raises ValueError) — the whole marking run dies.** Smallest fix: validate the key file structure (assert both headings present and i < j) before running, and fail fast with a clear message.

(z) **Predictions Q1–Q5 sealed in the same file as the key** — and `mark()` slices them out (key[:j] and key[i:j] exclude predictions). Good — but the marker for the *seeded* document gets key[:j] which is everything before "## Predictions, sealed" — that includes the D3 section and the eight planted errors. Good. The clean key gets key[i:j] = just the D3 section. Good.

(aa) **Document order and "same task for every reader" — the task is identical, good. But the document is appended after "THE DOCUMENT ============" in reader task.md, which ends with blank lines. Fine.**

(bb) **`reader task.md` says "End your report with the line END OF REPORT."** — used to detect truncation presumably. But the code doesn't check for it. A truncated report (finish=length) is saved as success if non-empty. So **truncated reports are silently counted as complete.** The marker would see a report missing END OF REPORT and could infer truncation, but there's no rule for it. Smallest fix: in `call()`, record finish_reason (done) and add a `verify` step flagging reports without "END OF REPORT" as truncated; exclude or re-run them.

(cc) **The 3-repetition design with `--only` and skip-if-exists means partial reruns are fine, but there's no lock file — running two `mark` processes concurrently would both generate ids and clobber MAP.json.** Minor. Smallest fix: write MAP.json atomically (tmp + rename) and skip if tag already mapped... it does check `if t not in mp.values()`. Race remains. Minor.

(dd) **The plan's claim: "The counts are never added across readers into one score." Good. But the "two or more of the four readers" rule IS an aggregation across readers** — a weak one with no multiplicity correction. With 4 readers × 2 thinking settings × 2 error kinds, there are many comparisons; "same direction on ≥2 readers" has a decent false-positive rate. Smallest fix: report all comparisons with CIs and state the family-wise error rate; require the effect to be consistent in sign across readers AND significant in the pooled per-reader analysis (fixed-effects style).

(ee) **DeepSeek thinking support claim** — "probe of 23 September: reasoning tokens 0 with it disabled on each". OK, verified by probe. But does DeepSeek's `deepseek-v4-pro` support `reasoning_effort: high`? Unknown; an unsupported field might cause 400 (FINAL → fail immediately, no retry). Risk: whole arms fail. Smallest fix: probe each provider with reasoning_effort before the run; treat 400 on that field as a provider-capability note and fall back to omitting it, recorded.

(ff) **Opus subagent: "The agent is given a working folder holding only the document (and the skill, for S), and told to read nothing else"** — but the seeded file sits in tests/ under its S80 name. If the working folder holds a *copy* with a neutral name, the leak is only via repo access. The plan acknowledges the leak but the mitigation (search for "S80"/"seeded"/"planted" in reports) fails if the model exploited the leak without naming it. Also **the clean-document runs for Opus would use file 10 from authority/ — and the seeded file is also in the repo; an agent with repo access in the *clean* condition could find the seeded file, read the eight errors, and then "find" them in the clean doc — creating false alarms on the clean document!** That's a particularly nasty failure mode: it inflates false alarms in the Opus arm and can't be detected by keyword search. Smallest fix: sandbox Opus (no repo access; only the working folder), copy docs in under neutral names, and inspect the agent's tool-call log (not just the report text) for reads outside the folder.

(gg) **The title says "thinking on and off" and the owner's named comparison is ST vs NO — a confounded comparison (skill AND thinking differ).** The plan does note the four cells separate the effects, and it will also read ST vs NT and SO vs NO. But the owner's decision is stated on ST vs NO, which conflates two changes. If the skill only helps with thinking on, ST vs NO could show a gain that's mostly thinking. The plan's reading rules should require the clean contrasts (ST vs NT, SO vs NO) as primary, and treat ST vs NO as secondary. Smallest fix: state in the plan that the decision rule uses the matched contrasts (ST–NT and SO–NO), and ST–NO is reported but never used for the abandon/add decision.

Let me also check:

- **`mark()` job tag: `f"{rid}_by_{m}"` — output files `marks/R001_by_atria.response.txt`. The map from rid→tag is in MAP.json. Fine. But the marker prompt includes "THE REPORT, id R001" — the marker sees the anonymous id. Good. However, the marker also sees the *document*, which for seeded is `tests/S80 Seeded authority - file 10 with eight planted errors.md` — the marker sees the full seeded document and the key. Fine. But the marker prompt includes the document *content*, not the filename — `read(DOCS[j["doc"]])`. Good, no filename leak to markers.

- **Does the reader see the document title?** The plan says readers see the document under its own title "Claude Fable Semantics". The file 10 begins with that title presumably. Good. But the seeded copy's filename isn't shown. Good.

- **The seeded document md5 recorded; clean md5 recorded. Good practice. But the runner doesn't verify md5 at run time** — if file 10 is edited between runs, the clean doc changes. Smallest fix: assert md5 of both docs at the start of `readers()` and fail if mismatched.

- **`SKILL_ORDER` includes "references/..." seven files; the plan says the skill is 8 files, 13,648 words "as sent". The system prompt concatenates them. Good. But `skill_text()` reads with relative path — if the working directory is repo root, fine.**

- **Atria and Mimo as markers marking DeepSeek and Opus reports — fine. But markers use thinking on with reasoning_effort high and max_tokens 32000. Marking 84 reports × 2 = 168 calls. Cost/time fine.**

- **The marker task says "Include every error id in the key, in its order." Good. The JSON has "report_items_total". Good. But there's no schema enforcement; if a marker returns prose, the aggregation (manual) must handle it.**

- **Agreement rule asymmetry: "an error counts as found when both markers say FOUND" — but the Opus adjudicator only re-marks disagreements. So effectively the found-count = both-found ∪ (adjudicated-found among disagreements). OK as long as "its call stands for the agreed table's disputed cells" means disagreements are resolved by Opus. Then why require both markers at all? Because Opus only sees disputed cells. Fine. But the plan text is ambiguous about whether "disagreement" includes the found-flag disagreements (marker says FOUND, other says NOT). It should. State it explicitly.**

- **One more: the clean document measures false alarms, but the seeded document also generates false alarms (reports listing non-key items on the seeded doc). The plan's false-alarm metric is only on the clean document. But the skill's effect on false alarms *while finding real errors* (the real deployment condition) is not measured — the clean doc has nothing to find, so the reader's behavior is different (fishing vs auditing). This is a construct-validity gap: false alarms on a clean doc ≠ false alarms on a doc with real errors. Smallest fix: also count and report non-key items on the seeded document as "false alarms under load" (the markers already judge every other item GENUINE/MISTAKEN/UNCLEAR — the data exists!). Just add the seeded-doc MISTAKEN count to the table. Cheap and important.

- **Tokens and seconds recorded in receipt — good. But the table's "tokens and seconds" per condition is confounded with the system prompt length (skill adds ~18k tokens input). The table should separate input vs output tokens (receipt's usage has them) — and note that input token counts differ by design.**

- **`stream_options: include_usage` — the last chunk carries usage. `last = d` updates on every chunk, so `last` is the final chunk. Good. But if the stream ends without a usage chunk, `usage` is null. Fine.**

- **`response_sha256` of content only — good. But no sha of reasoning. Minor.**

- **Retry sleep `time.sleep(min(120, 10 * 2 ** n))`: n=1→20, 2→40, 3→80, 4→120(>120 capped), 5→120. Fine.**

- **`history` records only attempt status/seconds/finish; on exception, `res` is None → `res and res["finish"]` → None. Good. But after the loop, `res` might be None only if all attempts failed, in which case we returned earlier. If status==200 on a later attempt, res is the last successful. Good. However — bug: if attempt 1 fails with an exception after partially streaming (content accumulated in the local `content` inside `_stream`, discarded) — fine, each attempt re-streams.**

- **Potential bug: `if status == 200 and not (res["content"] or "").strip():` then `status, text = (-4 if res["finish"] == "length" else -2), ...`. Then `if status == 200: break` — status no longer 200. Then `if status in FINAL or n == attempts` — -2/-4 not in FINAL; if n < attempts, sleep and retry. Good. At n == attempts → fail with error.txt. Good. But note: an empty response with finish="stop" (model said nothing) is retried 5 times. OK.**

- **Real bug hunt in `_stream`: `for ch in d.get("choices") or []:` — if a chunk has no choices (e.g., usage-only chunk), skipped. `last = d` — last is the whole chunk dict. `finish` from the first chunk with finish_reason — but if multiple choices, later ones could overwrite; only one choice used. Fine.**

- **`requests.post(..., headers=...)` with `json=body` — sets Content-Type automatically. Fine.**

- **A subtle one: `body` includes `thinking` and possibly `reasoning_effort`; for providers that reject unknown fields with 400, the FINAL set kills the arm. Already flagged (ee).**

- **Another: `call()` writes `.request.json` before attempting — includes the full system prompt with the skill (13k words) for every skill call; that's fine but large.**

- **`readers()` builds `sys_skill` once and `task` once — good. But `read(DOCS[j["doc"]])` re-reads the doc per job — trivial.**

- **Opus reports are saved by Claude manually as `opus_<cond>_<doc>_r<n>.response.txt` where cond ∈ {S, N} and doc ∈ {seeded, clean}. The `mark()` doc detection uses "_seeded_" in t — works for `opus_S_seeded_r1`. Good. But the tag format `opus_S_seeded_r1` — the readers' tags are `atria_ST_seeded_r1`. The `mark()` code only needs "_seeded_" substring. OK. However: **if Opus reports are saved without `.response.txt` or with different naming, they're silently excluded from marking** (glob). A `verify` command would catch it.**

- **The plan says 84 reports and "168 calls" for marking — 84 × 2 = 168. But the table says markers mark "every one of the 84 reports once". And the disagreement adjudication by Opus adds more calls. OK.**

- **Not tested: "The skill handed over in other ways (router only, or as file 24)" — the plan acknowledges that the no-skill arm uses a one-liner rather than file 24, which is the actual deployment comparator from S78. This is arguably the single biggest design gap: the question "does the skill add anything" in the *deployment* use means skill vs file 24 (the substitute the project actually uses). Using a one-liner instead tests "skill vs minimal instruction", a different question. I'll fold this into the system-prompt asymmetry point and make the fix: use file 24 (or a length-matched generic method) as the no-skill comparator.**

Now severity ordering. Let me pick the most serious:

1. **The no-skill arm is not the deployment comparator and is confounded by prompt-length asymmetry** (one-liner vs 13,648-word skill) — this alone can produce or mask an effect and the plan's stated use (file 24 in S78) is not what's run. Fix: length-matched placebo / file 24.

2. **The reader task prompt names the HV error kinds** ("conditions or clauses that do no work", "shields the theory from criticism") to every reader including no-skill — this gives away the skill's targeting, biasing toward the null and making "adds nothing" uninterpretable. Fix: remove those two phrases from the task for all arms (or add a task-variant arm).

3. **Markers mark their own model's reports** (Atria marks Atria, Mimo marks Mimo) — self-bias, and with the both-found rule it can systematically distort per-reader rows. Fix: exclude self.

4. **The Opus arm's seeding leak** is worse than the plan admits: with repo access, Opus can diff the clean and seeded documents in either condition, and in the clean condition it can then "find" planted errors — inflating false alarms invisibly (keyword search can't catch it). Fix: sandbox, neutral filenames, inspect tool-call logs.

5. **Three repetitions, no uncertainty quantification, multiplicity** — "same direction on two of four readers" has no control of false positives; a difference of 1/12 is called noise but no CI or test is specified; the decision rule is qualitative. Fix: report exact CIs for every difference and require CI to exclude 0; or raise reps.

6. **False alarms are only measured on the clean document, and raw counts are compared to found-counts out of 24** — unnormalized; and the skill changes report length/items listed, so raw false-alarm counts move with verbosity. Also non-key items on the seeded doc are already marked but not reported. Fix: report FA per listed item, and include seeded-doc MISTAKEN counts.

7. **No aggregation/verification code** — the table is built by hand from JSON responses; no `table` command, no completeness check, no truncation check (END OF REPORT never verified), no md5 check on documents, no key-structure validation before marking. A run can silently produce missing or truncated cells that enter the table as low scores. Fix: add `verify` + `table` commands.

8. **The decision rule's primary contrast is ST vs NO** (the owner's named comparison), which changes skill and thinking together — if the effect is mostly thinking, ST vs NO misattributes it. Fix: make ST–NT and SO–NO the decision contrasts.

9. **Marker agreement/style unblinding pathway**: the skill changes report vocabulary; markers may recognize skill terms (Atria may know the skill) and the both-found rule turns marker disagreement into zeros — the plan says disagreements go to Opus, which resolves it, but the plan is ambiguous about whether that covers found-flag disagreements and other-verdict disagreements. Fix: state that Opus adjudicates all disagreement cells, both kinds, and add a small random re-mark sample as drift check.

10. **Empty-content-with-length retry/failure risk under thinking-on high effort** (reasoning tokens may count against max_tokens on some providers → 5 wasted retries → failed cells; and 413/400 not retried could kill an entire arm). Fix: probe provider capabilities; treat empty+length as retry-with-doubled-budget or final; log missing cells.

11. **Opus arm can't separate skill from thinking** (thinking as the agent runs, unrecorded), and its artifacts lack receipts — it's a weaker arm that still feeds the "two of four readers" rule. Fix: record thinking per run; exclude Opus from the decision rule or report it separately (plan does treat it separately in the table but the decision rule counts "four readers").

Hmm wait, the decision rule says "on two or more of the four readers" — Opus is one of the four readers and can contribute to the "two readers" threshold despite having no matched-thinking contrast. That's a real flaw: a single Opus result with no thinking control could tip "two readers" to "adds something". Fix: require the two readers to be among the three API readers with matched thinking contrasts, or require Opus's contribution to be conditioned on thinking parity.

12. **Path/fragility issues** (SKILL_DIR relative to cwd; key slicing by heading strings can ValueError or silently mis-slice; MAP.json race; no atomic writes). Fix: anchor paths, assert key structure, atomic writes.

13. **Provider defaults / temperature differ across readers** — cross-reader consistency rule confounded. Minor; acknowledged partly.

14. **The "found when" rules and sealed list** — can't evaluate without the file; but note the reliance: everything hinges on the key's rules; the plan claims style cannot pass for finding, but that's only as good as the rules. Can't check. Note it as "cannot read off".

15. **Claim: "The skill is 13,648 words as sent" — fine. Claim: markers "thinking on" — fine. Claim the table will show "tokens and seconds" — the receipt has them, but no code aggregates.**

Also: does the plan claim to read off anything it can't? Yes:
- "false alarms not rising by more than the gain" — requires a false-alarm count, which requires markers to judge every item; if a marker's JSON is malformed or truncated, the count is wrong and there's no validation.
- "D3 found (of 6, both documents)" — D3 is in both docs; fine.
- The plan claims the clean document "measures false alarms: what a reader reports when nothing is planted" — but the clean document is *not* error-free (D3), and more importantly, false alarms on a clean doc ≠ false alarms in the presence of real errors. So the claim "measures false alarms" overstates: it measures false alarms in the no-signal condition only.
- The plan claims the four cells "separate the skill's effect from thinking's" — with n=3 and no interaction test, a 2×2 with 3 reps cannot separate main effects from interaction reliably; it can estimate them but with wide CIs. Claim is too strong as stated; needs the caveat that with n=3 the interaction is unidentifiable at useful precision.

Also one more runner check: in `readers()`, the `--only` filter takes `sys.argv[3:]` — if you run `python ... readers --only atria_ST_seeded_r1`, fine. But there's no way to run only Opus (it's manual). Fine.

Another: `run_pool` — `pools = [ThreadPoolExecutor(max_workers=3) for _ in by_model]` — creates 3 pools for 3 models, submits all jobs, then `for f in futs: f.result()`. If a worker raises, `f.result()` raises and the loop stops; but the other pools' threads continue (daemon? ThreadPoolExecutor threads are non-daemon; interpreter waits at exit). The process won't exit until all threads done; results are written by `call` regardless. So a crash in one job (e.g., a KeyError in `one()` for marking) aborts the wait loop but calls still complete... Actually if the main thread raised, the script exits after threads finish. The remaining futures' results are computed and files written. So partial completion is survivable. But the exception is not logged to a file — the traceback goes to stderr. Minor.

Another marking bug: in `one(j)`, `read(f"{OUT}/readers/{j['t']}.response.txt")` — if a reader job failed, `.response.txt` doesn't exist → FileNotFoundError → worker exception → that marking job fails; but since `call` is never reached, no `.error.txt`. And `run_pool`'s worker prints only on success. A failed reader report would then be *missing from the marks* silently (well, an exception propagates). With no verify command, you'd discover it when building the table by hand. Fix: skip missing inputs with a recorded reason, and verify completeness before marking.

Also `mark()` will try to mark Opus reports; if Claude hasn't produced them yet, crash. The plan says Opus runs are by Claude as subagents — timing not controlled. Fix: `mark` should skip-and-report missing inputs.

One more: **the `mark` command requires the key file path as `sys.argv[2]` — the key is opened (unsealed) at marking time, after which it's on disk in the repo? The plan says the sealed list is kept outside the repo until every reader has reported. But `mark KEY_FILE` reads it from wherever; the key is also embedded in marker prompts saved to `results/.../marks/*.request.json` — which are in the repo. So after marking, the key is effectively public in the repo (in marker request files). That's after all readers have reported, so acceptable — but the *Opus* adjudicator step and any reruns happen after; and if any reader runs are re-run after marking, the key is now in the repo and a later Opus subagent with repo access could read it. The plan says readers run before the key is opened — but resumability means someone might re-run `readers` after `mark`. Fix: state explicitly that no reader run may occur after `mark` has been executed, and have the runner refuse `readers` if any marks exist. That's a real procedural hole given the Opus leak path. Good point — include it.

Let me also double check the "eight planted errors, four of the kind the skill targets" vs "HV errors found (of 12)" — 3 reps × 4 = 12. And GEN of 12. D3 of 6 (3 reps × 2 docs). Consistent.

Now, "at most 3 calls in flight per provider" — the owner's decision. Runner honors it via per-model pools of 3. But marking: 2 marker models, 3 workers each — 3 in flight per marker provider. OK.

One more potential confound: **the document itself is the same in all cells, so position/learning effects across reps aren't an issue (no within-run feedback). Fine.**

**Position/order effects: jobs are submitted in a fixed order (model, condition, doc, rep) and run concurrently — no order randomization, but since there's no feedback loop, order doesn't matter. Fine.**

**Marker order: `sorted(mp.items())` — deterministic. Fine.**

Now let me also consider: the plan says "The skill condition adds a system prompt with the skill whole (main file, then the seven modules, each headed by its file name)". The system prompt file "reader system - with skill.md" says "You are reviewing a document. You have the skill below, a method for testing explanations, and you should use it for this audit." — it *instructs* use of the skill. The no-skill prompt doesn't instruct any method. That's the instruction confound: with-skill readers are told to use a method; without-skill readers are told nothing. Even a placebo method (irrelevant text) with the same instruction ("use the method below") would control for the instruction effect. So the placebo arm should include the instruction "you should use it" too. Include in point 1.

Also the with-skill system prompt says "a method for testing explanations" — describing the skill; the placebo should say the same about its content to be a true placebo.

Also consider **the "thinking off" + skill combination (SO)**: the skill is a long method document; with thinking off, the model has no reasoning tokens to apply a 13k-word method — the SO cell may be a degenerate cell where the skill can't help regardless. The plan reads SO vs NO as "the skill's effect at thinking off" — fine, that's a legitimate cell, but interpreting a null in SO as "skill adds nothing" would be wrong; the skill may require reasoning to apply. The plan should state that SO–NO is a lower bound on the skill's usefulness. Minor point, include as a caveat/interpretation point.

Alright, also check the claim "all three honour it (thinking): probe of 23 September, zero reasoning tokens when disabled" — good, verified. But `reasoning_effort: high` acceptance unverified. Flagged.

Now let me think about what else the plan "claims it can read off that it cannot":
- "Adds something is read off the table as: more planted errors found... with false alarms not rising by more than the gain" — with 3 reps and both-found rule, this is a noisy, threshold-free read.
- "The counts are never added across readers into one score" — but the "two or more readers" rule is an addition in disguise.
- "the sealed list... its SHA-256 recorded in log S80 now" — fine.
- "each old string matched once" — can't verify; the programmatic edit could have failed silently for some (e.g., matched once but the replacement introduced a grammatical error). Can't check; the SHA of the seeded file is given, fine.
- The plan says "A report's wording can still show the skill... marking is blind to the label, not to the style" — honest. But it doesn't account for the fact that *style is correlated with the condition*, so markers' treatment of style is a direct conduit of condition bias. The only fix is style-blind marking (paraphrase reports?) — too invasive; the per-error rules + Opus adjudication is the mitigation; strengthen by instructing markers to ignore vocabulary and mark only by the rules. Include.

Also: **the markers are given the document the reader saw. For seeded reports, markers see the seeded document and the key with all 8 errors. Fine. But markers are also readers' competitors?** No.

**A big one I should double check: the marker key for clean reports is only D3. But the clean document is file 10, which per the plan carries D3 and no other known errors. But what if a reader finds a *genuine* unknown error in file 10? The marker judges it GENUINE — counted as neither found nor false alarm. The table has "items listed" and false alarms = both-MISTAKEN. GENUINE items on the clean doc are... not false alarms, good. But then "false alarms on the clean document" excludes genuine findings — correct. OK.**

**And on the seeded document: a reader might find D3 (correct) or a genuine non-key error (GENUINE). Fine.**

**What about a reader finding a planted error but describing it as a different problem? NOT FOUND per rules. OK — that's the key's job.**

Now let me think about the count "HV errors found (of 12)": 3 reps × 4 HV. But the four HV kinds are one each (a condition made vacuous, a distinction collapsed, an idle condition, a patch). So per rep, each HV error is either found or not — 4 per rep. With 3 reps, an error found in 2 of 3 reps counts as 2. OK.

**Power**: to detect a difference in proportion from, say, 0.25 to 0.5 with n=12 per cell per kind... Fisher exact p ≈ 0.2. So underpowered. The plan's own noise statement acknowledges. The fix: more reps or explicit CI reporting. This is serious for a decision about abandoning a project. I'll rank it #5 or so.

Let me now decide the final ordering (most serious first):

1. No-skill comparator is a one-line prompt, not the deployment substitute (file 24), and is ~13,600 words shorter → the effect measured is "skill + method instruction + length" vs "bare". Both directions of bias possible; makes "abandon" and "adds" uninterpretable. Fix: length/instruction-matched placebo (file 24 or an irrelevant method text) with identical "use it" instruction.

2. The reader task names the HV error kinds to all readers, giving no-skill readers the skill's target categories for free → biases toward null; "adds nothing" can't be distinguished from "task already supplied the categories". Fix: strip the two HV-specific phrases from the task.

3. Markers mark their own model's reports + the both-found rule → self-bias can distort per-reader rows; and skill-style reports are recognized by markers (Atria may know the skill) → unblinded pathway from condition to verdict. Fix: exclude self-marks; instruct markers to ignore vocabulary; Opus adjudicates all disagreement cells (both found-flags and other-verdicts); add a small random re-mark drift check.

4. The Opus arm's leak is larger than admitted: repo access + the seeded file in tests/ lets Opus diff clean vs seeded in either condition, and in the clean condition it can "find" planted errors → invisible false-alarm inflation (keyword search cannot detect it). Also Opus's thinking is unrecorded, yet Opus counts toward the "two of four readers" decision rule. Fix: sandbox Opus (no repo access, neutral filenames, log tool calls, record thinking); exclude Opus from the decision rule or require thinking parity.

5. Underpowered: 3 reps, no CIs/tests, multiplicity across 4 readers × 2 settings × 2 kinds, and the "same direction on ≥2 readers" rule has an uncontrolled false-positive rate; the interaction (skill × thinking) is effectively unidentifiable at n=3. Fix: report exact binomial/bootstrap CIs for every contrast and require the CI to exclude 0 for a finding; pre-register the decision rule numerically; treat interaction as exploratory.

6. False-alarm metric is mis-specified: measured only on the clean (no-signal) document, compared as raw counts against found-counts out of 24 — unnormalized against items listed, and the skill changes report length/items listed, so verbosity alone moves the count. The data for a better metric already exists (MISTAKEN items on the seeded doc are marked but not reported). Fix: report FA per listed item and add seeded-doc MISTAKEN counts to the table; define the gain/FA threshold in rates.

7. No verification or aggregation code: no completeness check (failed/missing cells silently absent — 413/400 are never retried and can kill an arm), no truncation check (END OF REPORT never validated; a finish=length non-empty report is saved as success), no md5 check on the documents, no key-structure validation (string slicing can ValueError or silently mis-slice), no table builder (the final table is hand-made from marker JSON, with no schema validation). Also the key becomes public in the repo in marker request files after marking, so any later `readers` rerun (or a late Opus run) can see it. Fix: add `verify` and `table` commands; assert md5s and key headings; validate JSON; refuse `readers` once marks exist.

8. The owner's decision contrast is ST vs NO (skill and thinking change together) — a positive there is not evidence about the skill. Fix: make ST–NT and SO–NO the decision contrasts; report ST–NO as descriptive only. Also note SO–NO is a lower bound (a 13k-word method may need reasoning to apply), so a null in SO should not count toward "abandon".

9. Runner robustness details: empty content with finish=length (plausible when reasoning tokens count against max_tokens under high effort) is retried 5× then failed, wasting time and dropping a cell; `reasoning_effort: high` is unprobed per provider (a 400 kills the arm, no retry); a worker exception aborts the wait loop without logging to a file; missing reader inputs crash the marking worker without an error.txt; MAP.json write isn't atomic (concurrent reruns can clobber). Fix: probe capabilities first; on empty+length, retry once with doubled max_tokens (recorded); log exceptions to error files; skip-and-report missing inputs; write MAP.json atomically.

10. Cross-reader comparability: provider-default temperatures differ, models differ; the "same direction on two readers" rule treats non-exchangeable readers as replicates. Fix: report per-reader with CIs and require the two readers to be API readers with matched contrasts; treat temperature as a noted limitation.

Maybe also mention: the plan's claim "the clean document measures false alarms" — overstates (no-signal condition only). Folded into 6.

Also: the plan is a draft pending cross-examination — fine.

I think 10 points is good. Let me make sure each has: what's wrong, why it matters, smallest fix. And end with exactly one line: RUN AS IS / RUN WITH CHANGES / DO NOT RUN. Given points 1–4, the design as-is could produce a misleading answer to the owner's decision question — but it's not unsafe; the fixes to prompts are small (text changes), and the code fixes are additive. The correct verdict: RUN WITH CHANGES.

Wait — should it be DO NOT RUN? The question "does the skill add anything or should it be abandoned" is the owner's decision (S12). As designed, the no-skill arm is a one-liner and the task leaks the categories — the two most important confounds are cheap to fix (edit two prompt files). Running as-is risks an uninterpretable null that kills the project wrongly (or a false positive). The fixes are small. So: RUN WITH CHANGES.

Let me write it concisely but completely. I'll number 1–10 and keep each tight.