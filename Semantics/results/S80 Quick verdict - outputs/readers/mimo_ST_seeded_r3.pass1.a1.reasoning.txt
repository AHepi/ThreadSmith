The user wants an audit of the theory document "Claude Fable Semantics", using the hard-to-vield skill method. The job: find defects — false statements, proofs that don't prove their claims, claims wider than support, contradictions, other defects. Number them, give (a) location (Part + heading/label), (b) exact sentence/formula quoted, (c) what's wrong in 2-5 sentences with counterexample or failing step.

Most serious first. End with "END OF REPORT".

This is really a rigorous technical audit, but the skill method says to use hard-to-vary — which is about testing whether parts of an explanation are held in place. But the user's request is specific: find defects in a document. The skill's method is about judging explanations. I should use the method's discipline (freeze the question, test parts, look for claims wider than support, hunt the answer in the starting points, etc.) but the deliverable is a numbered defect list as specified.

Let me actually do the deep technical analysis. Let me examine the document carefully for mathematical and logical errors.

Let me go through the document part by part.

**Part II — Organizations and their changes**

D = (V, (X_v), J, B, A, L). A is a set of admitted edits, closed under partial associative composition with identity 1. L_j(a,b) ⊆ ∏_{v∈V_j} X_v. Sol_D(a,b) = {z ∈ X_D : ∀j, z|_{V_j} ∈ L_j(a,b)}.

Note: edits don't change the port set V or domains — an edit changes relations L_j(a,b). But "an edit that sets a port replaces the component assigning that port" — that's fine within the schema.

Hmm, one subtlety: L_j depends on (a,b) for each edit a and boundary b. Fine.

**Roles are derived**: "A port v is an input under A when A contains an edit that sets v directly." This is a definition. "A port is an output of component j when its value is determined by L_j given the other ports of V_j across B." OK.

**Kinds are edit-signatures**: sig_C(j) = {(a,b,L_j(a,b)) : (a,b) ∈ C}. Two components j, j' are of one kind on C when there is a bijection of their footprints under which sig_C(j) and sig_C(j') coincide.

Wait — a bijection of footprints V_j → V_{j'} should induce a map on values; "coincide" must be up to the induced map on ∏X_v. But the domains X_v may differ. This is a minor imprecision. Is it a defect? "under which sig_C(j) and sig_C(j') coincide" — the relations live in different products ∏_{v∈V_j} X_v vs ∏_{v∈V_{j'}} X_{v'}, so they can't literally coincide without the bijection acting on values. This is a real imprecision but maybe too pedantic. Hmm, the audit should report defects I can state precisely. This one is arguable. Let me look for stronger ones first.

Also note: kinds defined as equivalence classes "under this relation" — but is "same kind on C" actually an equivalence relation? Bijection of footprints with signatures coinciding up to the bijection: reflexivity (identity), symmetry (inverse bijection), transitivity (composition). Yes, equivalence.

But wait: the signature includes (a,b,L_j(a,b)) — the same (a,b) pairs for both components since C is shared. Fine.

**The causal assignment definition**: "a causal assignment has a signature that changes under intervention on its output port and under replacement of the component, and is invariant under observation edits". Hmm — but the signature of a component j is defined as the set of L_j(a,b) over (a,b) ∈ C. "Intervention on its output port" is an edit a. How does an intervention on the output port of j change sig_C(j)? Per the model, an edit that sets a port "replaces the component assigning that port; it does not add an equation beside an incompatible one." So an intervention on the output port of j replaces j itself... then L_j(a,b) for that edit a is presumably the new relation. Hmm — the definition of "causal assignment" is circular or underspecified: a component's signature changing "under intervention on its output port" — if the intervention replaces the component, then the signature of the *old* component is not even defined at that edit. Actually the way L is set up, L_j(a,b) is given for all (a,b), so an edit that "sets" port v would be reflected in L_j(a,b) being the fixed value... This is murky. This may be a defect: the three role-descriptions (cause, measurement, rule) reference "intervention on its output port", but "output" is itself derived, and the semantics of edits that replace components means the signature of the replaced component is replaced, so "signature that changes under intervention on its output port" either is vacuous or conflates components. Hmm.

Let me keep this as candidate. Let me look at Part V first, which is load-bearing.

**Part V — Account**

Component fidelity (F1): for every active component k of E with anchor λ(k) ⊆ D, and every (a,b) ∈ C,

proj_{V_k}[Sol_{λ(k)}(a,b)] = L_k(τ(a),σ(b)).

Hmm. Note Sol_{λ(k)}(a,b) is a set of valuations on ports of the subnetwork λ(k) (ports of D). But proj_{V_k} — projecting onto V_k — the ports of k in E. But the anchor λ(k) is "a subnetwork of D with a port translation". So projecting onto V_k requires translating ports from D-side to E-side. The formula as written projects Sol_{λ(k)}(a,b) onto V_k, but the solution lives over D's ports, so proj_{V_k} is ill-typed unless V_k ⊆ V_D or the port translation is applied. The transport λ "assigns each component of E a subnetwork of D with a port translation" — so the translation maps ports of the anchor to ports of k. The formula (F1) writes proj_{V_k}[Sol_{λ(k)}(a,b)] — presumably meant to be the projection onto the anchor's ports, then translated. As written it's a type error. Hmm — could be flagged as a formal defect but might be dismissed as notation. Perhaps worth flagging if the defects are thin. Let's collect.

Also (F1) equates the projection (a relation over V_k's value domains ∏_{v∈V_k} X_v in E? or in D?) with L_k(τ(a),σ(b)) ⊆ ∏_{v∈V_k} X^E_v. Type mismatch unless the port translation carries domains too. Flag as defect candidate.

Now the bigger one: (F1) as stated requires proj_{V_k}[Sol_{λ(k)}(a,b)] = L_k(τ(a),σ(b)) for every (a,b) ∈ C. But Sol_{λ(k)}(a,b) is computed with edit a and boundary b *of D*, while L_k(τ(a),σ(b)) uses the translated edit of E. OK.

Derivation 1 claims (F1) entails every component of E has the signature of its anchor. Let's check: Derivation 1's proof says sig_C(λ(k)) = {(a,b, proj_{V_k} Sol_{λ(k)}(a,b))}. But (K) defines the signature of a component j as {(a,b,L_j(a,b))} — the *relation supplied by the interpretation* L_j, not the projection of the solutions of the subnetwork. For a subnetwork λ(k) with multiple components, there is no single L; the proof replaces the signature definition with the projected solution relation. For a single-component anchor, proj_{V_j} Sol_{λ(k)}(a,b) = L_j(a,b) trivially (when λ(k) is just {j}). But for a multi-component subnetwork anchor, the signature per (K) is not defined at all — (K) defines signatures of *components*, not subnetworks. So Derivation 1's "By (K), sig_C(λ(k)) = {(a,b, proj Sol...)}" is not by (K): (K) gives sig via L_j, and proj Sol_{λ(k)} equals L_{λ(k)} only if we extend the definition. Actually for a subnetwork of components with shared hidden ports, proj_{V_k}[Sol_{λ(k)}] is generally strictly smaller than any single L_j — it's the composition/join. So Derivation 1's proof step "By (K)" is wrong: (K) defines signature via the supplied relation L_j of a component; the projected solution of a multi-component anchor is not the L of any component. So the claim "every active component k of E has the signature of its anchor" — where the anchor is a subnetwork — conflates a component signature with a subnetwork's projected relation. Also the claim in Part V "By (K), (F1) entails that every component of E has the signature of its anchor" is at best a stipulated extension of (K) to subnetworks.

Hmm, but also Derivation 1 claims "the same signature on τ[C] as its anchor λ(k) has on C, up to the port translation" — but (F1) only equates the third coordinates; the first coordinates differ: sig over C vs sig over τ[C]. "up to the port translation" — but also up to the edit translation τ and boundary translation σ. The proof says "(F1) equates the third coordinates pointwise" — fine but the claim about signature equality is up to (τ,σ) relabeling. Minor.

More importantly: Derivation 1's corollary claims a kind condition "adds nothing to (F1) on any contract. Where the contract separates two kinds, (F1) already distinguishes them". Hmm — but wait. Consider: the anchor condition would require that the anchor λ(k) is a component of D of the same kind as k. If C separates two components j, j' of D into different kinds, and E's component k is anchored to j' while playing the role of j... (F1) equates k's relation to the projection of λ(k)'s solutions. So k is faithful to whatever it's anchored to. The claim is that any anchoring is fine where kinds coincide. But the question of whether the anchor is the *right* component of D — the correspondence problem — is not addressed: (F1) says k matches its anchor, but nothing says the anchor is the part of the real thing that k is supposed to model. Hmm — that's the classic problem: F1 checks each E-component against its declared anchor in D, but λ is part of the transport being tested; a wrong λ could pair k with an unrelated subnetwork that happens to have matching relations. Actually, if the relations match under every (a,b) ∈ C, then by the semantics's own (K) they're of one kind, so the "wrong" anchor is indistinguishable from the right one at that grain. That is the intended doctrine. OK.

Now, Derivation 2: "Two candidates E, E' for the same p that both satisfy (F1), (F2), and (A) on C are one account at grain C: their components are pairwise of one kind on C and their answer profiles coincide."

This is clearly false as stated. Two candidates can satisfy F1, F2, A and have different numbers of components, different decompositions, so "their components are pairwise of one kind" fails (no bijection between sets of different sizes). Counterexample: E has one component k with footprint {x,y} and relation L; E' has two components k1 on {x,y}... hmm, careful. Let's construct: D is trivial. Let p ask the output port. Let E have two components over disjoint port sets {u} and {w} with relations fixing u=0, w=1; E' has a single component over {u,w} with relation u=0 ∧ w=1. Both project to the same Sol (X_u × X_w restricted). F2: π[Sol_D] = Sol_E: take D = E itself and π = id... Then E and E' satisfy F1 (each component matched to its counterpart(s)? but λ(k) for E' single component k over {u,w} anchored to subnetwork {j1,j2} of D=E; proj_{u,w} Sol = the product relation = L_k. Fine.) So both satisfy F1, F2, A with the same answer profiles, but E has2 components and E' has 1; "their components are pairwise of one kind" is not even well-formed (bijection of footprints between components; pairwise matching across different cardinalities fails). So Derivation 2's claim is false: the decomposition can differ. Also "Answer profiles coincide" follows from (A) trivially since both equal Ans_p. But the first conjunct "components are pairwise of one kind" does not follow — (F1) relates each candidate's components to anchors in D, not to each other; two candidates anchored to *different* subnetworks of D with different structures can both satisfy F1. Even if both anchor to the same D subnetworks... Take D with components j1 on {u}, j2 on {w}. E anchors k1→{j1}, k2→{j2}; E' anchors k→{j1,j2}. F1 for k: proj_{u,w} Sol_{{j1,j2}} = L_{j1} × L_{j2} = L_k. So E' satisfies F1 with one component. Both satisfy F2 with π = id. Both satisfy (A). E has 2 components, E' has 1: no pairwise kind matching. So Derivation 2 is refuted. Serious defect — it's a "derivation" whose claim is false, and the proof is a non sequitur ("Immediate from Derivation 1 and (A)" — Derivation 1 only says each component matches its own anchor; it says nothing about cross-candidate correspondence).

Also Derivation 2's "one account at grain C" — the consequence about underdetermination. The claim is stronger than what's proved. Great defect.

Also Derivation 2 says "both satisfy (F1), (F2), and (A)" — but (E) also requires NonCircular, NonVacuous, Coherent. The claim says "candidates ... are one account" but only assuming three of the six conditions. So a candidate could satisfy F1, F2, A and yet fail non-circular dependence — and then it's not an account at all, and the claim "are one account" is wrong on that ground too. Indeed Part0 grievance 2... hmm. Also the claim "their answer profiles coincide" — from (A) both equal Ans_p on C. OK that conjunct holds. But "one account" is not established since (E) has more conjuncts.

**Derivation 3: Selected transports are underdetermined on unseen changes.**

Claim: Let t be selected on finite history H ⊊ C. For every (a,b) ∈ C∖H there exists t', also surviving on H, with a different value at (a,b).

Proof: "The component relations L_j(a,b) are supplied independently for each (a,b). Survival on H constrains only {L_j(a,b) : (a,b) ∈ H}. Alter L_j(a,b) for one (a,b) ∉ H to any other admitted relation; the result survives on H and differs at (a,b)."

Problems: (1) The claim is about transports t and t', but the proof alters the component relations L_j — i.e., it alters the *target organization D's interpretation*, or E's? t = (π,τ,σ,λ) is a transport; its "value at (a,b)" isn't defined. What varies in the proof is L_j(a,b) — the interpretation data of an organization, not the transport. So the proof varies the wrong object. (2) Altering L_j(a,b) may break (F2)/(F1) or the constraint that L is "supplied independently" — indeed, if the interpretation must satisfy some coherence (e.g., composition), arbitrary alteration may be inadmissible. But worse: the claim says "there exists a transport t' also surviving on H with a different value at (a,b)" — if by "value" they mean π(Sol_D(a,b))... altering L_j of E changes the solutions. Hmm.

Also the claim's proof shows a different object survives on H — but survival of *what*? "Survival" was defined as fidelity on H. If we alter the relation at (a,b) ∉ H, fidelity on H is unaffected — trivially. But is the altered object still a transport between the same organizations? If L_j is part of the *world* (D), then altering it changes the world, not the transport. If L_j is part of E (the simulation), then altering L_j(a,b) at one (a,b) means the modified E still satisfies F1 on H (since F1 constrains only points in C... wait F1 is for all (a,b) ∈ C). Hold on: faithfulness on C is required for representation (R), but selection survival is fidelity on H. So t' needs fidelity on H only. Altering L_k(τ(a),σ(b)) at one point outside τ[H] keeps fidelity on H. So the proof can work if we alter E's L_k, and "different value at (a,b)" means different L_k(τ(a),σ(b)) or different π[Sol_D(a,b)] vs Sol_E. But the proof says "Alter L_j(a,b)" using j (which in the document indexes components of D... actually J indexes components of D; components of E are k). In Derivation 3 the notation L_j(a,b) with j — ambiguous. Also "The component relations L_j(a,b) are supplied independently for each (a,b)" — is that stipulated? In Part II, L is "the interpretation supplies L_j(a,b) ⊆∏" for each j, edit a, boundary b. Nothing says they're independent; in fact (F2) requires τ(1)=1 and τ(a2a1)=τ(a2)τ(a1), and coherence of Sol across edits... Actually there is no constraint in (O) linking L_j(a,b) across different (a,b) — except A being a monoid acting and... no constraint given. Hmm, so "supplied independently" is roughly consistent with (O) as defined. But (F2) for transport requires π[Sol_D(a,b)] = Sol_E(τ(a),σ(b)) and τ homomorphism. If we alter one value of L_k of E at (τ(a),σ(b)) with (a,b) ∉ H, F2 at that point fails — but we only need survival on H. But then is t' still "a transport"? A transport is just (π,τ,σ,λ) — the fidelity conditions are separate ("A transport is faithful on C when it satisfies..."). So altering L_k doesn't alter t = (π,τ,σ,λ) at all! The transport is the quadruple of maps; it has no "value at (a,b)". So the claim "there exists a transport t' ... with a different value at (a,b)" is ill-posed: transports don't have values at edit–boundary pairs; the organizations' relations do. The proof alters the interpretation L, not the transport. So Derivation 3's statement conflates transports with organizations/interpretations. That's a genuine defect: proof does not prove what it claims (it varies the wrong object), and the claim is ill-typed.

But also there's a subtler issue: the claim says "for every (a,b) ∈ C∖H there exists a transport t' ... with a different value at (a,b)". If we interpret charitably: the candidate's answer/expectation at (a,b). But also if the alteration must keep t' "also surviving on H" — the proof also assumes altering one L_j(a,b) leaves everything else intact and still admissible. If, e.g., the organizations must satisfy (F2)-type constraints... but for a selected transport, survival = fidelity on H only. OK.

Also Derivation 3's claim is used in Part 0 ("Derivation 3 that selected transports are always underdetermined on unseen changes") and Part XV ("A non-fallible selected transport... This refutes Derivation 3"). But wait — is the claim even true? Counterexample: suppose the organization E's relations are constrained by a law linking values across edits (e.g., the physical module requires L to be generated by a fixed function). In the formalism as given, L_j(a,b) is arbitrary data per (a,b) — so within the formalism the claim is true modulo the ill-typedness. Hmm, but here's a real counterexample within the formalism: if C∖H contains (a,b) but the relation at (a,b) is forced by F2... no, survival on H doesn't include F2 at (a,b) outside H? Actually "faithful on C'" for a subset C' means F1, F2, A restricted to C'? The document says "A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V" and "a survival condition requiring fidelity on H". So faithfulness on H = F1/F2/A on H. Then yes altering outside H preserves survival.

But wait — one more: the claim says "with a different value at (a,b)" — different from what? Presumably t's value. And "Alter L_j(a,b) for one (a,b) ∉ H to any other admitted relation" — if we alter L_j of D (the target/world), then the transport t might still be faithful... hmm, "any other admitted relation" — is any relation admitted? X_v nonempty domains; relations any subset. But is there a constraint that Sol_D(a,b) ≠ ∅? Not per (O). Non-vacuity only requires Sol_D(1,b_0) ≠ ∅. OK.

So Derivation 3's main defect: the object varied is the interpretation L (an organization's relation data), while the claim quantifies over transports, which are quadruples of maps with no values at (a,b). Also "the component relations are supplied independently for each (a,b)" is an assumption about the formalism that is not stated as a constraint anywhere and conflicts with (F2)-type coherence demanded of the physical module (Org)... hmm, maybe weaker.

Actually, hold on. There's an even more interesting counterexample: if C∖H's edit (a,b) equals composition of history edits... A is closed under composition and τ must be a homomorphism. But L_j(a,b) for composite a is independent per (O). But (F2) requires τ(a2a1) = τ(a2)τ(a1) and π[Sol_D(a,b)] = Sol_E(τ(a),σ(b)) — this constrains Sol across composed edits: Sol_D(a2a1, b)? Hmm, does Sol_D respect composition? (O) says Sol_D(a,b) depends on L_j(a,b); nothing requires L_j(a2a1,b) to relate to L_j(a2,b), L_j(a1,b). But (F2) requires π[Sol_D(a2a1,b)] = Sol_E(τ(a2)τ(a1), σ(b)) = Sol_E(τ(a2a1), σ(b)). That's just F2 at a2a1, consistent. So no cross-edit constraint on L. Fine — so within the formalism the relations per (a,b) are indeed independent data. OK.

So Derivation 3 defect: ill-typed claim/proof mismatch. Let me phrase it: the proof modifies L_j — the interpretation of an organization — not the transport t=(π,τ,σ,λ); a transport has no "value at (a,b)". So what is proved is that the *interpretation* can vary, not that a different transport exists with a different value. Also, if "value at (a,b)" means the expectation Ans_S(τ(a),σ(b)) (Part IV), then the alteration must change the answer of the simulation layer, which requires altering E's query-relevant relation — the proof's "alter L_j(a,b)" with j ambiguous doesn't specify whether that changes Ans. Hmm, this is a defect but maybe medium.

Hmm wait, actually, there's a cleaner counterexample to Derivation 3 as stated: Let C = H ∪ {(a,b)}. Suppose the transport t and organization E are such that... we need "there exists t' also surviving on H with a different value at (a,b)". If we read "t's value at (a,b)" as π(Sol_D(a,b))... that's determined by D and π. Varying t means varying π. Let π' differ at (a,b)? π is a single map X_D → X_E "on the stated scope" — it doesn't depend on (a,b). So t's "value at (a,b)" would have to be via Sol_E(τ(a),σ(b)) = π[Sol_D(a,b)] — determined by D and π; to change it you change π, which may break F2 at history points... The claim as written is genuinely ill-formed. Good.

**Derivation 4: Surprise requires an incomplete history.**

Claim: "A system can be surprised if and only if it holds a transport selected on a history H strictly smaller than the contract C of changes the world admits."

Proof: surprise defined as violation at (a,b) ∉ H. If no transport, no expectation hence no violation. If H = C, every occurring (a,b) is in H so no violation at (a,b) ∉ H.

Defects: (i) The "only if" direction: the claim requires a *selected* transport with H ⊊ C. But a *constructed* transport can also be violated at a change it never met — Part IV says "surprise is a violation at (a,b) ∉ H" where H is the selection history of a selected transport. If a constructed transport has no H, then per definition surprise is undefined for it, yet the claim says "a system can be surprised iff it holds a transport selected on H ⊊ C" — so a system with only constructed transports cannot be surprised, which contradicts the ordinary notion and also the definition in Part IV which defines surprise for "t be selected on history H" only. Hmm. But the claim says "if and only if it holds a transport selected on a history H strictly smaller than C" — "holds a transport" — a system could hold both. The defect: the "if" direction also fails: H ⊊ C doesn't guarantee surprise is *possible* — wait, "can be surprised" = there exists a violation at some (a,b) ∉ H that actually occurs. If the world never performs edits outside H... but C is "the contract of changes the world admits" — hmm, the claim says "the contract C of changes the world admits" — but in Part III, C is the *question's* contract, a declared subset of physically admitted edits, not "the changes the world admits". So the claim conflates C (a declared contract of a question) with the physically admitted changes. If a system's transport is selected on H with H ⊊ C but the world only ever instantiates edits in H... Actually (a,b) "actually occurring" is required for violation ("For an edit–boundary pair (a,b) ∈ C actually occurring"). So "can be surprised" requires the possible actual occurrence of some (a,b) ∈ C∖H. H ⊊ C doesn't ensure anything occurs outside H. So the "if" direction needs the extra premise that some (a,b) ∈ C∖H actually occurs (or can). The proof's "if" direction is just: surprise is defined as violation at (a,b) ∉ H — existence requires an actual occurrence. The claim "if and only if" is too strong: strictly, surprise requires (i) a transport with expectation, (ii) an actual (a,b) outside its history where fidelity fails. A system with H ⊊ C but where fidelity never fails outside H (t faithful on all C) can never be surprised despite H ⊊ C. Wait — is that right? Surprise = violation at (a,b) ∉ H. Violation = fidelity failure at (a,b). If t is faithful on all of C, then no violation anywhere, hence no surprise ever — even though H ⊊ C. So the "if" direction of "can be surprised iff H ⊊ C" is false: H ⊊ C is necessary (given definitions) but not sufficient for the *possibility* of surprise... well "can be surprised" — is it about possibility or actuality? If t is faithful on C∖H now, could it be violated later? The relations are fixed data; faithfulness at (a,b) is a static fact. So whether surprise can occur at (a,b) ∉ H depends on whether fidelity fails there — a further fact. So the "iff" fails: H ⊊ C does not make surprise possible; it only removes the definitional bar. Actually hmm, "can be surprised" might mean "is the sort of thing that surprise is defined for". Ambiguous. But the deeper defect: the definition of surprise in Part IV is only given for selected transports ("Let t be selected on history H with contract C"), yet Derivation 4's claim covers "a system" and says iff "it holds a transport selected on..." — a system with only a constructed transport that is violated at an unmet change has expectation and violation, and per Part IV's response taxonomy "a violation" occurs and "surprise is a violation at (a,b) ∉ H" — for constructed transports H is not defined, so surprise is undefined, so the semantics cannot mark surprise for constructed transports even though Derivation 4's necessity direction says a system without a selected transport cannot be surprised... wait no, Derivation 4 says "if and only if it holds a transport selected on H ⊊ C" — so a system with a constructed transport violated outside its tested range cannot be surprised per the claim. But Part X and Derivation 10 treat constructed transports being violated... hmm, Derivation 10 uses S_0 selected. OK.

Also the proof: "If H = C, every occurring (a,b) is in H" — but wait, the claim says H strictly smaller than "the contract C of changes the world admits" — if occurring (a,b) may lie outside C entirely (the world admits more changes than any question's contract), then surprise at (a,b) ∉ H can occur even when H = C, since (a,b) might be physically admitted but outside C. Per Part IV, "Let t be selected on history H with contract C. For an edit–boundary pair (a,b) ∈ C actually occurring: ... surprise is a violation at (a,b) ∉ H." So surprise is only tracked within C. But then a change outside C occurring would violate fidelity at a point outside the contract where fidelity isn't even defined ("faithful on C"). Hmm. The document's Part IV defines violation "when fidelity fails at (a,b)" for (a,b) ∈ C actually occurring. So surprise is confined to C. Then Derivation 4's "if and only if" has the sufficiency problem I noted: H ⊊ C is not sufficient since fidelity might hold everywhere on C (then no violation is possible at any (a,b)). Hmm, but if fidelity held everywhere on C∖H, the transport would be faithful on C and no surprise would ever occur. But is surprise *possible*? The word "can" is doing work. If we read "can be surprised" as "surprise is possible for it", then H ⊊ C is necessary but not sufficient (need also that some (a,b) ∈ C∖H will actually occur and fidelity fails there — or at least the possibility thereof). Hmm — actually for possibility: fidelity at (a,b) is a determinate fact (given the model). If fidelity holds at all (a,b) ∈ C∖H, then surprise cannot occur. So "can be surprised iff H ⊊ C" is false. Unless "can be surprised" means "surprise is defined for it / is of the type". I'd flag it: the "only if" (necessity) part of the proof also mishandles systems with no transport vs. constructed transports. And "a system whose history exhausts its contract cannot be surprised" (Part IV) is fine.

Hmm, is there an even starker contradiction? Part IV: "A system with no transport cannot be surprised. A system whose history exhausts its contract cannot be surprised. Surprise requires an incomplete selection history". And Derivation 4 says "if and only if it holds a transport selected on a history H strictly smaller than the contract C". But per Part IV's own definitions, a *constructed* transport with an incomplete testing record could also suffer "a violation at (a,b) ∉ H" — but H is only defined for selected. So the definition of surprise excludes constructed transports by fiat, and Derivation 4's "only if" direction then is close to definitional. But the claim "surprise requires an incomplete selection history" (Part IV) contradicts... hmm, actually the more interesting counter: Derivation 10 says "An occlusion occurs... This is surprise (Derivation 4)" and t_0 is selected — consistent.

But wait, Derivation 4's proof has an actual gap in the "only if" direction: it says "If there is no transport there is no expectation and hence no violation." But the claim's "only if" needs: if a system can be surprised then it holds a transport selected on H ⊊ C. The proof only rules out the no-transport case and the H = C case; it doesn't rule out a *constructed* or *declared* transport being the vehicle of surprise — and per the definition of surprise in Part IV (which presupposes t selected on H), surprise for non-selected transports is undefined rather than impossible. So the "iff" is partly definitional and the proof's two cases do not exhaust the possibilities (constructed transports with no H at all). That's a genuine defect: the proof has an unexcluded case (a transport with no selection history), and in that case "surprise" is undefined per the definition but the claim asserts it's impossible. Also the sufficiency gap.

Also Derivation 4 says "This is derived, not assumed (Derivation 4)" in Part IV — and indeed the definition of surprise in Part IV already *builds in* (a,b) ∉ H, so the derivation is nearly trivial/definitional. That's the "answer hidden in the starting points" defect: the result is true by definition. Combined with the iff overreach. Let me make it one defect or two? I'll present as one: the iff claim is false/ill-supported. Or maybe split: (1) sufficiency failure, (2) unexcluded case. Better to combine into a single numbered defect with a precise quote of the claim, explaining the step that fails and the counterexample. Actually the instructions say "one number for each defect". I can have separate defects where they are distinct. Let me be careful to keep each defect crisp.

**Finite monotone theorem (Part VI).**

"If Γ is finite, S is upward closed, and Γ ∈ S, then the critical singletons are exactly ⋂min S and the globally indispensable ones are exactly ⋃min S."

Definitions: CriticalBlock(B;W,p) iff W ∈ S and W∖B ∉ S. "Critical singletons" = elements d such that {d} is critical for some W? I.e., ∃W ∈ S with W∖{d} ∉ S. "Globally indispensable" = d such that Γ∖{d} ∉ S.

Claim 1: critical singletons = ⋂ min S.

Counterexample: Γ = {a,b,c} with S = upward closure of {{a,b},{a,c}}? Let's see: min S = {{a,b},{a,c}}. ⋂ min S = {a}. Critical singletons: d is critical if ∃W ∈ S: W∖{d} ∉ S. Take W={a,b}∈S, remove b: {a} ∉ S (min supports are {a,b},{a,c}; {a} not in S). So b is critical (for W={a,b}). Similarly c is critical for {a,c}. So critical singletons = {a,b,c}, while ⋂ min S = {a}. So the claim "critical singletons are exactly ⋂ min S" is false. Hmm — maybe "critical singletons" means "singletons that are critical blocks", i.e., B={d} with CriticalBlock({d};W,p) — that's what I computed. Or maybe it means "elements critical for Γ itself"? For W = Γ: Γ∖{d} ∉ S. Γ={a,b,c}, Γ∖{b} = {a,c} ∈ S. So b not critical for Γ. Γ∖{a} = {b,c} ∉ S. So a critical for Γ. So "critical for Γ" = {a} = ⋂ min S. Hmm! Indeed: with upward closure, Γ∖{d} ∉ S iff every minimal support contains d iff d ∈ ⋂ min S. So "critical singletons" (if it means critical blocks {d} for the full W=Γ) = ⋂ min S. And "globally indispensable" = d such that deleting d from Γ destroys support = Γ∖{d} ∉ S = d ∈ ⋂ min S as well... wait then both claims give the same set?

Hmm. Let's re-read the proof: "A member of a minimal support is critical for it. If d is critical for W, finiteness gives a minimal U ⊆ W; if d ∉ U, upward closure makes W∖{d} a support. Deletion of d from Γ preserves support exactly when a minimal support omits d."

So the proof establishes: d is critical for some W ∈ S iff d ∈ ⋃ min S? Let's see: "A member of a minimal support is critical for it" — if U is minimal and d ∈ U, then U∖{d} ∉ S (by minimality? minimality of U means no proper subset of U is in S — careful: "minimal member" of S means U ∈ S and no proper subset in S. So U∖{d} ∉ S, so d is critical for U). And "If d is critical for W... if d ∨∈ U (minimal support U ⊆ W), upward closure makes W∖{d} a support" — i.e., if d ∉ U then W∖{d} ⊇ U so W∖{d} ∈ S, contradiction with criticality. So d critical for W implies d ∈ U for every minimal U ⊆ W. Finiteness of Γ and upward closure guarantee a minimal U ⊆ W exists (W ∈ S, take a minimal subset in S). So: d is critical for some W ∈ S iff d belongs to some minimal support iff d ∈ ⋃ min S.

And "globally indispensable": "Deletion of d from Γ preserves support exactly when a minimal support omits d" — deletion preserves support (Γ∖{d} ∈ S) iff some minimal support omits d iff d ∉ ⋃ min S... hmm: Γ∖{d} ∈ S iff ∃ minimal U ⊆ Γ∖{d} iff ∃ minimal U with d ∉ U iff d ∉ ⋂ min S. Wait: ∃ minimal U omitting d ⟺ d ∉ ⋂ min S. So deletion of d destroys support (Γ∖{d} ∉ S) ⟺ every minimal support contains d ⟺ d ∈ ⋂ min S. So "globally indispensable" (deletion destroys support) = ⋂ min S.

So by the proof's own reasoning: critical singletons (critical for some W) = ⋃ min S, and globally indispensable = ⋂ min S. But the theorem states the REVERSE: "the critical singletons are exactly ⋂ min S and the globally indispensable ones are exactly ⋃ min S." The sets are swapped! The proof establishes the opposite of the stated theorem (unless "critical singletons" means "critical for Γ", but then critical for Γ = globally indispensable = ⋂ min S and the second claim "globally indispensable = ⋃ min S" is false).

Let me double-check with the counterexample: Γ={a,b}, S={{a},{b},{a,b}} (redundant routes example, but wait S must be upward closed in Γ: upward closure of {{a},{b}} = {{a},{b},{a,b}} — yes that's the redundant-routes S). min S = {{a},{b}}. ⋂ min S = ∅. ⋃ min S = {a,b}. Critical singletons: b is critical for W={b} ({b}∖{b}=∅ ∉ S). a critical for {a}. So critical singletons = {a,b} = ⋃ min S. Globally indispensable: Γ∖{a}={b} ∈ S so a not indispensable; similarly b. So globally indispensable = ∅ = ⋂ min S. The theorem as stated says critical singletons = ⋂ min S = ∅ — false, since a and b are each critical for some support. And "globally indispensable = ⋃ min S = {a,b}" — false. So the theorem's two sets are swapped relative to the truth (given the natural reading of "critical singletons" as "elements that are critical singletons for some W"). 

Alternatively, "critical singletons" might mean "singletons {d} that are critical blocks for the full set Γ" — then {d} critical for Γ ⟺ Γ∖{d} ∉ S ⟺ d ∈ ⋂ min S — matching the first claim — but then "critical singletons" and "globally indispensable" would denote the same set (both⋂ min S) and the second claim (⋃ min S) would be false, and moreover the redundant-routes example right below would contradict it (in redundant routes, ⋂ min S = ∅ and ⋃ min S = {a,b}, and the text says "each is contributory, neither indispensable" — contributory ≈ critical somewhere = ⋃min S; indispensable = ⋂ min S = ∅ ✓). Hmm. Under this reading, "critical singletons =⋂ min S" is right but "globally indispensable = ⋃ min S" is wrong (should be ⋂ min S). Either reading, one of the two claims is false. And the proof's steps support: "A member of a minimal support is critical for it" (so members of min supports are critical-for-some-W = ⋃ min S), and "Deletion of d from Γ preserves support exactly when a minimal support omits d" (so indispensable = ⋂ min S). So the proof's reasoning yields critical-somewhere = ⋃ min S and indispensable = ⋂ min S, while the theorem statement asserts the reverse. Under the "critical for Γ" reading, the proof's first sentence is off-target. Either way there is a genuine mismatch. Good, strong defect: the theorem as stated is false; counterexample Γ={a,b}, S = powerset-minus-∅... wait let me use the document's own redundant-routes example: Γ={a,b}, S={{a},{b},{a,b}}. min S = {{a},{b}}.

Under reading 1 (critical singletons = elements critical for some W ∈ S): theorem says =⋂ min S = ∅; but a is critical for {a} since∅ ∉ S. False.

Also check the theorem's hypothesis: Γ finite ✓, S upward closed ✓ (as defined? S is defined as {W ⊆ Γ : Account(E|W,p)} and the theorem says "If ... S is upward closed" — as a hypothesis, fine), Γ ∈ S ✓ ({a,b} ∈ S). Conclusion fails.

Also there's a subtlety in the proof: "If d is critical for W, finiteness gives a minimal U ⊆ W" — finiteness of Γ gives a minimal U ⊆ W with U ∈ S (since W ∈ S). But strictly you need S to contain a minimal element below W: with finiteness and W ∈ S, descend by removing elements — yes. But the theorem also allows infinite Γ? No, hypothesis is finite. OK.

Also "the critical singletons" — the phrase "critical singletons" is not defined in the document! CriticalBlock(B;W,p) is defined for nonempty B ⊆ W. "Critical singletons" presumably B = {d}. So "the critical singletons" = the set of d such that ∃W: CriticalBlock({d};W,p)? That's reading 1. The theorem is then false as shown. Great.

This is a mathematical error, which Part XV lists as a defeat ("A mathematical error. A counterexample to the finite monotone theorem..."). So the document defeats itself — nice point to note in the defect.

**Infinitary support.** "Γ={d_n:|x|≤ 1/n}: every unbounded index set determines x=0; no minimal support and no singleton witness exists." Hmm — d_n: the constraint |x| ≤ 1/n. An index set I ⊆ ℕ supports (determines x = 0) iff inf{1/n : n ∈ I} = 0 iff I unbounded. Correct: every unbounded set determines x=0. "No minimal support" — minimal support = a support all of whose proper subsets fail: if I is unbounded and remove one element, still unbounded, still a support. So no minimal support ✓. "no singleton witness exists" — a singleton {d_n} doesn't determine x=0 ✓. Fine. But the example is stated as "Γ={d_n:|x|≤1/n}" — d_n as an element of Γ must be a *commitment*, fine. But the claim "no minimal support" conflicts with... hmm, Part VI says "No upward closure and no minimal member are assumed" — fine. But wait: does S here have minimal members? Supports = unbounded index sets. A minimal support would be an unbounded set with no unbounded proper subset — impossible. ✓. But hold on: is the empty set of constraints... The definition of "determines x=0" — this is illustrative. OK fine.

Hmm, but is this consistent with (S)'s definition via Account? It's an illustration. Fine.

**Hard-to-vary.** "For explanatory jobs F ⊆ F′, Pres(F′) ⊆ Pres(F)" where Pres(F) = {v ∈ V : ∀f ∈ F, Account(E_v, f)}. This holds trivially by monotonicity of ∀ over set inclusion — trivial but true. But note: Account(E_v, f) — f is a "job" not a question p; Account takes (E, p). Typing sloppiness: Account(𝓔) takes a candidate 𝓔 = (E,p,t,Γ). Here "Account(E_v, f)" with f a job. Mild type error. Also Pres over V (the declared family of organization edits from (D)-boundary definition — "For a declared family 𝒱 of organization edits") — so v ∈𝒱 are edits, and E_v are versions. OK.

Also the claim "More reach constrains variation; the containment need not be strict; counting jobs is not a warrant" — fine.

**Part VII — Production and direction.**

"Stipulate H:=U_H, θ:=U_θ, L:=H cot θ." Then "The reversed calculation H = L tan θ is not [faithful under the production contract]: intervening on H changes the target's L but not the calculation's H."

Hmm — wait. In the reversed calculation, H is computed as L tan θ. If we intervene on H (the port H of the target D), the calculation E's port H is an output determined by L and θ. Under the production contract, edits intervene on H and θ (inputs of D). The transport π maps D's valuation to E's valuation. Intervening on H in D changes Sol_D but E's H is computed from L and θ... wait but in the reversed calculation, what are the ports? Presumably same ports H, θ, L. Under intervention on H (setting H := h), the target D's L = h cot θ. The reversed calculation E has component H = L tan θ. Under the same edit (setting H), hmm — "An intervention on L replaces its component and leaves H,θ unchanged" — earlier in Part II: "an edit that sets a port replaces the component assigning that port". So intervention on H in E replaces E's component assigning H, so E's H = h and L is free/undetermined? Hmm. The claim is about F2 failing: "intervening on H changes the target's L but not the calculation's H." Hmm — actually under intervention on H: D gives L = h cot θ; E (reversed) gives H = L tan θ, i.e., L free and H determined... The statement says intervening on H changes the target's L (yes, L = h cot θ changes with h) "but not the calculation's H"?? Wait intervening on H *sets* H in both. Hmm, the failure should be about the calculation's L not changing. Let me think again.

Actually the claim in Part V's "What (E) excludes": "A reversed calculation — identification presented as production — fails (F2) under the production contract: intervening on the upstream port changes the target's downstream value but not the calculation's." That's clearer: intervening on H (upstream) changes D's L (downstream) but not E's L (since E's L is... hmm, in the reversed calculation E: H = L tan θ, so L = H cot θ actually! Solving the same equation! H = L tan θ ⟺ L = H cot θ forθ ∈ (0, π/2). The relation is the SAME relation! H = L tan θ is just a rearrangement of L = H cot θ. The "reversed calculation" as an *equation* is identical as a relation over (H, θ, L). So what makes it "reversed"? The direction of computation: which port is determined. But (O)'s semantics: Sol_D(a,b) is determined by the relations L_j(a,b) and the edit structure. The forward organization has a component assigning L (footprint {H, θ, L} with the cot relation, L determined); the reversed has a component assigning H. But the *relation* is the same subset of the product! So Sol_D(a,b) would be the same... unless the edits differ. In the forward case, edits set H and θ (inputs), so the relation determines L. In the reversed case, edits set L and θ, determining H. But if the *same* contract (interventions on H and θ) is imposed on the reversed organization: edit sets H := h. "An edit that sets a port replaces the component assigning that port" — the reversed component assigns H. So the edit replaces that component with the fixed-value component H := h. Then the relation L = H cot θ is gone! So Sol_E after intervening on H has L free — L undetermined. Whereas D's (forward) component assigns L and is untouched by an edit setting H... wait, in the forward organization, H is an input port set by the edit — the edit "sets a port", "replaces the component assigning that port; it does not add an equation beside an incompatible one." In the forward org, which component assigns H? Presumably the edit-supplying component. Hmm, this is getting subtle but the mechanism is: intervention on H replaces the component that computes H. In the forward org, H is an input (set directly by the edit), so the cot-relation component (assigning L) survives and L = h cot θ is determined. In the reversed org, the tan-relation component assigns H, so setting H destroys the relation and leaves L undetermined. So Sol_E ≠ π[Sol_D] and F2 fails ✓. But the document's stated reason — "intervening on H changes the target's L but not the calculation's H" — in Part VII says "The reversed calculation H=L tanθ is not: intervening on H changes the target's L but not the calculation's H." Hmm — intervening on H sets H in both. The calculation's H *does* change (it's set to the intervention value). What fails to change is the calculation's *L*. So the stated reason misidentifies which value fails to move. Hmm — unless they mean: in the reversed calculation, intervening on H (in the world) leaves the calculation's H as computed from L tan θ... but no, an intervention sets the port. Hmm.

Wait, maybe the intended reading: The intervention is on the world's H. The calculation E computes H := L tan θ from observed L and θ. Under the intervention on the world's H, the world's L changes (L = H cot θ). But the calculation's inputs (L and θ) are fed by observations... Hmm, in the production contract, the edits are on H and θ. So under the edit, Sol_D has H = h (fixed) and L = h cot θ. The calculation's H... if the calculation's ports are connected to D's ports via π: π maps D-valuation to E-valuation. If E computes H = L tan θ and takes L, θ as inputs, then E's valuation has H = L tan θ = (h cot θ) tan θ = h. So E's H = h — it does change! And E's L = ? L is an input of E, taken from...? If π maps D's L to E's L (as input), then E's L = h cot θ which changes. Hmm, then π[Sol_D] = Sol_E? The reversal seems to be a redescription of the same relation with different input/output roles, and roles are "derived" from admitted edits. If both use the same admitted edits (interventions on H and θ), then the roles are the same and there is no difference — the "reversed calculation" would just be a rearranged equation with the same edit-signature. Hmm! That's interesting: by the document's own "roles are derived" doctrine, L := H cot θ and H := L tan θ are the same relation and, under the same contract, the same organization up to component syntax. So the claim that "the reversed calculation is not faithful under the production contract" depends on treating the two as different organizations with different input/output roles — but the semantics says roles are derived from admitted edits, not from how the equation is read. If the same edits are admitted, the two are the same organization. So the example either (a) smuggles in a role assignment (which port the component "assigns"), contradicting "roles are derived", or (b) doesn't distinguish at all.

Hmm, but actually "roles are derived" says an output of component j is a port whose value is determined by L_j given the other ports. With the single relation over {H,θ,L}, given θ and H, L is determined (for θ ∉ {0, π/2} etc.) and given θ and L, H is determined. So the same component has both H and L as "outputs" in the respective senses. The direction claim rests on which edits set which ports ("A port v is an input under A when A contains an edit that sets v directly"). Both H and L would be inputs if edits set both. The forward/reversed distinction is: forward contract has edits setting H and θ; the reversed *contract* has edits setting L and θ. So the difference lives in the contract, not in the equation. But Part VII says "It is faithful under the identification contract, whose edits alter the observed L." ✓ consistent. And "The reversed calculation H = L tan θ is not [faithful under the production contract]" — but as noted, under the production contract (edits on H and θ), what is the reversed calculation's organization? If its component relation is the same set {(H,θ,L) : H = L tan θ} = {(H,θ,L) : L = H cot θ}, then it's identical to the forward one and faithful. For it to be unfaithful, its component must "assign" H so that the edit replacing H's component destroys the relation — but which port a component "assigns" is part of the interpretation L_j data... hmm, actually (O) doesn't track "assignment"; the roles section says "a port is an output of component j when its value is determined by L_j given the other ports" — derived. And "an edit that sets a port replaces the component assigning that port" — so for edit setting H, which component is replaced? The one "assigning" H. In the forward org, the cot-component assigns L (L determined by others), so it isn't replaced when H is set; the edit replaces the component assigning H (the input mechanism). In the reversed org, the tan-component assigns H, so it's replaced when H is set. But per the derived-roles doctrine, with relation R = {(H,θ,L) : L = H cot θ}, which port does the component assign? Given the others, both H and L are determined (θ fixed). So it assigns both?! Then setting H replaces it and setting L replaces it. In the forward organization with the *same* relation, setting H would also replace the component (since it "assigns" H too) and destroy the L-determination. So the forward organization wouldn't be faithful either! Unless "assigns" means something finer-grained than "determines". There's an inconsistency here between "roles are derived" (determination-based) and "an edit that sets a port replaces the component assigning that port" (which requires a prior assignment/role). Since a relation like L = H cot θ determines each of H, L given the other two, the "assignment" is ambiguous and the claimed asymmetry between forward and reversed calculation disappears. The document's Part VII example therefore presupposes a direction (which port is "assigned") that its own Part II says is derived — and the derivation yields no direction for this relation. That's a solid internal contradiction/defect. Hmm, but is it fair? Let me re-read Part II's roles section:

"No role assignment is supplied. A port v is an input under A when A contains an edit that sets v directly. A port is an output of component j when its value is determined by L_j given the other ports of V_j across B. A port is an observation when A contains an edit that alters the relation reporting it without altering what it reports. The direction of an organization is a consequence of which edits it admits, not a stipulation about which way an equation is read."

"A port is an output of component j when its value is determined by L_j given the other ports" — with L the cot-relation over {H,θ,L}: given H and θ, L is determined → L is an output. Given L and θ, H is determined → H is an output. So both are outputs of j. Then the forward and reversed organizations are the same D with the same roles. Part VII's claim that "the reversed calculation H = L tan θ is not [faithful]" under the same contract is then false: it is the same relation and the same Sol. Unless the intended distinction is which ports the *edit* sets — but Part VII says "Under A containing interventions on H and θ, these ports are inputs and L is an output... The forward organization is faithful under this contract. The reversed calculation H=L tan θ is not: intervening on H changes the target's L but not the calculation's H."

Hmm wait, maybe the intended reversed organization E is one whose only component computes H from L and θ with L as a free input port (footprint {L, θ, H} but "assigning H"). Then intervening on H: edit sets H, replacing E's component (since it assigns H), leaving L free → E's L undetermined → π[Sol_D] (where L = h cot θ determined) ≠ Sol_E (L free). F2 fails ✓. But then E's component "assigns H" while in D the corresponding component "assigns L" — and per (K) their signatures... the relation L_j(a,b) is the same set R regardless of "assignment"! Because (O) only uses L_j(a,b) as a relation. So D's forward component and E's reversed component have the same relation data. The difference must come from *which edits replace which components* — "an edit that sets a port replaces the component assigning that port." So the model needs to know which component assigns which port — but that's not in the data (V, X, J, B, A, L)! The tuple has footprints V_j and relations L_j(a,b). "Assigning" is not formalized. So the semantics as formalized cannot distinguish the forward from the reversed organization: both are the same data. Therefore the claim "the reversed calculation is not faithful" is false in the formalism — or the formalism is missing the assignment structure needed to make the claim true. Either way it's a defect: the example's failure mode is not representable in (O) because the notion of "the component assigning a port" — on which edit-replacement depends — is not defined in the formal data. And "roles are derived" claims no role assignment is supplied, yet edit-replacement semantics ("replaces the component assigning that port") requires one. Strong defect. Counterexample to the claim as stated: take E = D = the same organization with relation R = {(H,θ,L) : L = H cot θ} on footprint {H,θ,L}; then E is trivially faithful under the production contract (π = id), yet E "is" the reversed calculation, since H = L tan θ is the same relation written the other way. So the claim "The reversed calculation H = L tan θ is not [faithful]" is false as written — the equation written in that form is the same relation.

Hmm, but maybe charitable: "reversed calculation" means an organization whose component footprint assigns H, i.e., the roles differ. The document's Part V says "A reversed calculation — identification presented as production — fails (F2) under the production contract: intervening on the upstream port changes the target's downstream value but not the calculation's." — "not the calculation's [downstream value]" — this says the calculation's downstream value (L) doesn't change. Part VII says "intervening on H changes the target's L but not the calculation's H" — the calculation's H. These two statements of the same fact contradict each other about which value fails to change! Part V: the calculation's downstream value (L) stays; Part VII: the calculation's H stays. Under intervention on H, the calculation's H is *set* by the intervention (it moves), so Part VII's account is wrong; Part V's is the plausible one. Unless "the calculation's H" means H as computed by the calculation... which is overwritten. Hmm, this is a genuine internal inconsistency between Part V's "What (E) excludes" and Part VII's "Production and direction" about which quantity fails to respond. Good defect — internal contradiction. Actually, let me quote both.

Part V: "A reversed calculation — identification presented as production — fails (F2) under the production contract: intervening on the upstream port changes the target's downstream value but not the calculation's."

Part VII: "The reversed calculation \(H=L\tan\theta\) is not: intervening on \(H\) changes the target's \(L\) but not the calculation's \(H\)."

Hmm, in Part VII: "intervening on H changes the target's L but not the calculation's H" — wait. Maybe they intend: the intervention is on the *target's* H port (world intervention), and the calculation is a separate system computing H from its inputs L, θ. The intervention changes the world's H and hence the world's L. But the calculation's H (its computed output) is fed from its L input... if the calculation's L input is observed from the world, then the world's L changed, so the calculation's H = L tan θ changes too (back to the same h!). Hmm, actually H = (h cot θ) tan θ = h. So the calculation's H does change. If the calculation's inputs are the world's L and θ only (identification setting), and we intervene on the world's H, then the world's L = h cot θ changes; the calculation computes H = L tan θ = h — coincidentally equal to the intervened H! Because the relation is invertible. So actually under intervention on H, the reversed calculation gets H right! But its L: it doesn't compute L at all (L is its input). Hmm.

OK so the real content of "reversed calculation fails under production contract": the failure shows only when the intervention is on H while L is *clamped* (or when the relation is not invertible). Hmm. Let's think about what makes identification-presented-as-production fail. In production mode, we want to compute L from H, θ. The "reversed calculation" claims to compute H from L, θ. If used as a production explanation (what produces L?), it gets the direction wrong: intervening on H changes L (in the world), but the reversed calculation has no causal path from H to L — it treats L as input. Under the production contract's edits (intervene on H), the reversed organization E: if its ports are H, θ, L with L an input port (set by... hmm, edits in the production contract set H and θ, not L). So E's port L is not set by any edit in C; E's component computes H from L and θ. Under edit "set H := h": replaces E's component assigning H → E's relations: H = h, L free. Sol_E = {(h, θ, L) : θ free?θ is set too...}. Meanwhile D (forward): H = h, θ = θ₀ (set), L = h cot θ. π[Sol_D] = {(h, θ, h cot θ)}. Sol_E = {(h, θ, L) : all L}. Not equal → F2 fails ✓. And the difference: "the calculation's L" doesn't change (it's free/unchanged by the edit), i.e., intervening on H changes the target's L but not the calculation's L. So Part V's phrasing ("the target's downstream value but not the calculation's [downstream value]") is right, and Part VII's "not the calculation's H" is wrong (the calculation's H is precisely what the intervention sets). Unless they consider the calculation's H as computed before the intervention replaces... I'll flag Part VII's sentence as misidentifying the invariant — contradicting Part V. Good.

But the deeper issue (roles not formalized / reversed calculation is the same relation) is also real. Let me decide which to report and how. I could report as two defects: (i) internal contradiction about which value fails to move; (ii) the "reversed calculation" example cannot be represented as distinct from the forward one under (O) since H = L tan θ and L = H cot θ are the same relation and "the component assigning a port" is not part of the formal data, so the claimed (F2) failure is not derivable — and "roles are derived" contradicts the assignment-based edit-replacement rule. Hmm, (ii) is subtle but genuine. Let me hold it as a defect but maybe rank lower.

**Identification (Part VII).**

"Let Z be the admitted states, g: Z → Y the measurement, f: Z → F the feature. The fibre at y is Z_y = g^{-1}(y). The feature is identified at y exactly when Z_y ≠ ∅ ∧ |f[Z_y]| = 1. (I1) It is identified on every attainable y exactly when f = f̄ ∘ g for some f̄. (I2)"

Check (I2): "identified on every attainable y exactly when f = f̄ ∘ g for some f̄". Identified on every attainable y means: for every y ∈ g(Z) (attainable), |f[Z_y]| = 1. This is equivalent to f being constant on fibres of g, which is equivalent to f = f̄ ∘ g for some f̄ : Y → F (defined on g(Z), extendable arbitrarily outside if F nonempty). Yes, (I2) is correct (with f̄ defined on the image). Fine.

"(I4) For linear A and feature c^⊤, identification is ker A ⊆ ker c^⊤." Hmm — Z = ℝⁿ (states x), measurement g(x) = Ax, feature f(x) = c^⊤ x. Fibre at y: x₀ + ker A. f constant on fibres iff c^⊤ vanishes on ker A iff ker A ⊆ ker c^⊤. ✓ Correct. Note it skips (I3) — labels (I1), (I2), (I4) with no (I3). That's a numbering gap — minor cosmetic defect, not worth reporting maybe. Actually the audit says "any other defects" — a missing (I3) label is a trivial inconsistency but maybe worth a low-priority note? Hmm, the finite monotone theorem is referenced as... Part XV: "A counterexample to the finite monotone theorem, (I2), (O1), (T2), (CT2), or Derivations 1–3" — no (I3) referenced anywhere. The gap suggests deleted content. It's a labeling inconsistency. I might include at the end as trivial if I have room. But the instructions say "Report only defects you can state this way" (with quote and precise wrongness). A missing (I3) is a defect in labeling; I can state it: "the labels jump from (I2) to (I4)". Eh. I'll consider including as a minor defect at the end.

"For the two balances \(\begin{pmatrix}1&1&0\\1&0&1\end{pmatrix}\), the kernel is spanned by (1,−1,−1); the readings identify b_B − b_A and not x."

Check: A = [[1,1,0],[1,0,1]]. Kernel: x1+x2 = 0, x1+x3 = 0 → x2 = x3 = −x1 → kernel spanned by (1,−1,−1) ✓. "the readings identify b_B − b_A and not x" — the feature c^⊤ x must satisfy ker A ⊆ ker c^⊤. With x = (x1, x2, x3)... which components correspond to what? Presumably masses and biases: "b_B − b_A" suggests the model is: balance A reads 1·m?... Hmm. Two balances, three unknowns. Typical: readings y1 = x1 + b_A? Hmm. Let me think: matrix rows [1,1,0] and [1,0,1]: y1 = x1 + x2, y2 = x1 + x3. If x = (m, b_A, b_B) with m the mass and b_A, b_B the biases... then y1 = m + b_A, y2 = m + b_B. Then y1 − y2 = b_A − b_B. The readings identify b_A − b_B (equivalently its negative). "the readings identify b_B − b_A" — sign aside, they identify the difference up to sign: c = (0, −1, 1) → c^⊤x = b_B − b_A = y2 − y1 ✓ identified. "and not x" — the vector x itself is not identified ✓ (kernel nontrivial). But wait — is "b_B − b_A" the right sign? y2 − y1 = (m + b_B) − (m + b_A) = b_B − b_A ✓ identified. OK so the example is fine if x = (m, b_A, b_B). Fine.

Hmm, but "the readings identify b_B − b_A and not x" — also they identify m + b_A (y1 itself) etc. Fine as illustration.

**Obstruction (Part VII).** "For allowed steps R and an invariant I with zRz′ ⇒ I(z) = I(z′), no allowed path joins states of different invariant value. (O1) Equal values do not establish reachability." ✓ correct. "Twenty-four indivisible tokens cannot be split equally three ways" — wait, 24 tokens CAN be split equally three ways: 24/3 = 8! The earlier skill example uses 23 coins among three people. The document says "Twenty-four indivisible tokens cannot be split equally three ways; permitting division changes the state space and does not refute the scoped result." 24 = 3 × 8, so 24 tokens CAN be shared equally among three (8 each). This is a factual/mathematical error in the example — presumably a typo of twenty-three. Nice catch — concrete defect. Quote: "Twenty-four indivisible tokens cannot be split equally three ways". Counterexample: give each party 8. Unless "split equally three ways" means something else... no. 24 is divisible by 3. So false. Good defect (probably mid-priority; it's a clear false statement but local).

**Explanations that remove structure (Part VII).** Fine-ish, flagged by the doc itself.

**Odd-order skew-symmetric matrices (Part VII).**

"Question: why is every odd-order real skew-symmetric matrix singular? Contract: remove skewness; remove oddness; remove both; field arithmetic and determinant–invertibility held fixed. The full Leibniz expansion with skewness substituted satisfies (F1) and (F2): every intermediate product is a determinant suborganization whose hidden ports project away. It satisfies (A): the answer is the absence of invertible matrices in the family, and under each contrast the family contains one."

Check: "under each contrast the family contains one [invertible matrix]": contrasts: remove skewness (all odd matrices) — contains invertible ✓ (I₃). Remove oddness (all skew-symmetric, including even order) — contains invertible: [[0,1],[-1,0]] has det = 1 ✓. Remove both ✓. OK.

"Non-circular dependence is witnessed by I₃ under removal of skewness and by \(\begin{pmatrix}0&1\\-1&0\end{pmatrix}\) under removal of oddness." OK.

Wait — but is the claim "the full Leibniz expansion with skewness substituted satisfies (F1) and (F2)" plausible? (F1) requires that for every active component k of E and every (a,b) ∈ C, the projected solution equals L_k(τ(a),σ(b)). The claim "every intermediate product is a determinant suborganization whose hidden ports project away" is hand-wavy but roughly the point of F1/F2 in this example is that the expansion computes the determinant correctly. Hmm — with skewness "substituted", i.e., the expansion with a_ji := −a_ij substituted. Then det = 0 identically for odd n. Hmm, is the Leibniz expansion an organization whose Sol matches? This is a stipulation ("Stipulate...") in an example. There might be an issue: (A) requires Ans_E = Ans_p for all (a,b) ∈ C; Ans_p is "the absence of invertible matrices in the family" — hmm, Q is "a specified set-theoretic operation... with codomain Y_p". Fine.

Hmm, one thing: "Non-circular dependence... There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ ... under which the answer profile changes..." — the definition of NonCircular in Part V is odd: "The answer follows by evaluating E under its independent boundary conditions... There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ while preserving the other boundary conditions, under which the answer profile changes, ceases to be determined in the claimed way, or is left as it was."

Wait — "changes, ceases to be determined in the claimed way, **or is left as it was**"?! If the answer being "left as it was" also counts as discharging non-circular dependence, then the condition is trivially satisfied by *any* (a,b) that removes a block and leaves the answer unchanged — which would make non-circular dependence nearly vacuous: for any E and any (a,b) that "removes or replaces a nonempty block of Γ", one of the three disjuncts always holds (the answer either changes, or ceases to be determined, or stays as it was — exhaustive!). The disjunction "changes, ceases to be determined, or is left as it was" covers every logical possibility. So the existential condition is satisfied whenever there exists any edit that removes a nonempty block of Γ at all — the answer behavior is unconstrained. That's a devastating defect: non-circular dependence as formalized is (almost) vacuous — its second sentence's disjunction is exhaustive, so it holds automatically for any candidate with any removable block. Unless "is left as it was" is meant as a disjunct that *fails* the condition... but as written with "or", it satisfies. Let me re-read exactly:

"Non-circular dependence. The answer follows by evaluating E under its independent boundary conditions. The target's answer does not appear, at the declared grain, as an unanalysed boundary input or as a component; moving an assertion from an input slot into a component named "law" does not discharge this. There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ while preserving the other boundary conditions, under which the answer profile changes, ceases to be determined in the claimed way, or is left as it was."

Hmm — maybe "or is left as it was" is a typo or a deliberate clause meaning "or [the answer] is left as it was [and that's also acceptable?]" — but that makes it vacuous. Actually maybe the intent: the answer "changes, ceases to be determined..., or is left as it was" — describing all outcomes as acceptable? That can't be the intent of a *dependence* condition. Non-circular dependence wants: removing the commitments changes the answer (the answer depends on the commitments, not smuggled in). If "is left as it was" is allowed, then the classic "p because p" candidate — where the answer is smuggled into a component named law — removing that component would... hmm.

Wait, maybe the intended meaning: "under which the answer profile changes [for the better cases], ceases to be determined in the claimed way, or is left as it was" — hmm. Or perhaps the three disjuncts are meant to be alternatives covering different acceptable outcomes of removal — but then the condition is exhaustive and vacuous. As an auditor, this is a defect: as written the disjunction is exhaustive over all possible behaviors of the answer profile under the edit, so the existential is satisfied whenever any edit removes a nonempty block of Γ; the condition does no work. And this contradicts (E)'s stated purpose ("'p because p' fails non-circular dependence") — under the written condition, "p because p" with a component asserting p would have some edit removing that component, under which the answer... changes (if the answer was computed from the component) or stays. Either way the disjunction holds, so "p because p" would satisfy the written condition. Hmm — wait, but the first two sentences also constrain: "The target's answer does not appear, at the declared grain, as an unanalysed boundary input or as a component." So "p because p" is excluded by the second sentence. OK so the condition isn't wholly vacuous. But the third sentence (the formal existential) is vacuous as written. Since (E) is a conjunction of named conditions and NonCircular's formal content is the third sentence, the vacuity matters: the existence claim does no work. Also there's tension: Part V's later paragraph "A contract consisting only of relabelings, or excluding every change under which the active commitments could matter to Q, does not satisfy non-circular dependence" — under the written existential, such a contract WOULD satisfy it (any block-removing edit leaves the answer "as it was" — third disjunct). So the claim contradicts the formal definition. Great defect — internal contradiction between NonCircular's formal clause and its glosses/claimed consequences.

Let me double check "changes, ceases to be determined in the claimed way, or is left as it was" — could "is left as it was" be intended as the *failure* case listed for contrast? Grammatically "under which the answer profile changes, ceases to be determined in the claimed way, or is left as it was" — "under which [one of these]" — the "under which" picks the acceptable outcomes. If the author intended "changes or ceases to be determined" as acceptable and "is left as it was" as... no, it's in the same list with "or". So yes, exhaustive. Excellent defect #1 material.

Also note the earlier statement in Part 0 / Part V: "(E) excludes... 'p because p' fails non-circular dependence." — consistent with sentence 2. But also "An account whose only substantive contrast is one that no physically admitted edit realizes fails non-vacuity by having a silently narrowed contract" and the Non-vacuity paragraph: "A contract consisting only of relabelings, or excluding every change under which the active commitments could matter to Q, does not satisfy non-circular dependence and is therefore not a contract on which an account can be claimed." — This attributes to non-circular dependence a job that its formal clause cannot do (as shown). Contradiction.

**Non-vacuity.** "Sol_D(1,b_0) ≠ ∅. The contract C is a declared subset of the physically admitted edits, and every physically admitted edit excluded from C is excluded by a stated scope, not silently." Hmm — "every physically admitted edit excluded from C is excluded by a stated scope" — this is a requirement about the record. Fine-ish. But there's a quantifier subtlety: "excluded by a stated scope" is a pragmatic/linguistic criterion smuggled into a formal definition — (E) is claimed to be purely structural ("None inspects a label"), but "excluded by a stated scope" inspects the *record of statements*. Contradiction with "Every conjunct is a condition on how supplied relations behave under the changes in C. None inspects a label." Hmm — "stated scope" isn't a label but it's not a condition on supplied relations either; it's a condition on the modeller's prose. That's a defect: (E) claimed to be a formal/structural definition but non-vacuity's second clause references whether an exclusion is "stated", which is not part of the mathematical data (D, C, b_0, Q, O_p, ρ_p) — no field records statedness. So (E) is not well-defined on its own data. Good defect.

Also NonCircular's clause "The target's answer does not appear, at the declared grain, as an unanalysed boundary input or as a component" — again not a formal condition on the data as given ("unanalysed" is a semantic/predicate notion not defined). And "moving an assertion from an input slot into a component named 'law' does not discharge this" — "named" — the doc says "None inspects a label" yet the clause explicitly mentions naming. Hmm, it's about not being fooled by naming, fine. But "unanalysed" is undefined. Combined defect: (E) is claimed to be a conjunction of conditions on supplied relations, but at least two of its clauses ("excluded by a stated scope", "unanalysed boundary input", "the claimed way") are not expressible in the supplied data.

**Baseline coherence.** "Sol_E(1,σ(b_0)) ≠ ∅ ... This is an independent requirement; it is not implied by the conditions above." Is it? (A) says Ans_E(τ(a),σ(b)) = Ans_p(a,b) for all (a,b) ∈ C, including (1,b_0). And Non-vacuity gives Sol_D(1,b_0) ≠ ∅. And F2 gives π[Sol_D(1,b_0)] = Sol_E(1,σ(b_0)) (since τ(1)=1). Since Sol_D(1,b_0) ≠ ∅ and π is a function, π[Sol_D(1,b_0)] ≠ ∅, hence Sol_E(1,σ(b_0)) ≠ ∅. So baseline coherence IS implied by (F2) + non-vacuity! The claim "This is an independent requirement; it is not implied by the conditions above" is false. Let me double check (F2): "π[Sol_D(a,b)] = Sol_E(τ(a),σ(b)), τ(1)=1, τ(a_2a_1)=τ(a_2)τ(a_1)". At (a,b) = (1,b_0): π[Sol_D(1,b_0)] = Sol_E(τ(1), σ(b_0)) = Sol_E(1, σ(b_0)). Non-vacuity: Sol_D(1,b_0) ≠ ∅. π is a map (function) X_D → X_E. Image of nonempty set under a function is nonempty. Hence Sol_E(1,σ(b_0)) ≠ ∅. So Coherent follows from NonVacuous ∧ (F2). The sentence "it is not implied by the conditions above" is a false statement — a mathematical error. Excellent defect.

Unless... π "on the stated scope" might be partial. "where π: X_D → X_E on the stated scope" — if π is partial and undefined on Sol_D(1,b_0)... "on the stated scope" is vague. But (F2)'s equality π[Sol_D(1,b_0)] = Sol_E(1,σ(b_0)) with the left side nonempty forces the right side nonempty regardless of partiality (assuming π[S] means image). So yes, implied. Strong defect.

Also (F1) at (1,b_0) would give L_k(1,σ(b_0)) = proj Sol_{λ(k)}(1,b_0) ⊆ ... hmm, and Sol_E(1,σ(b_0)) ⊆ ∏ L_k... and (F2) gives it equal to image of nonempty set. Yes.

**Part V (F1) type issue** — noted earlier.

**"What (E) excludes"** — "A table of observed answers has no component whose relation is replaced by an intervention; it fails (F1) under any contract containing one." Hmm — is this right? A table of observed answers... it fails F1 how? Consider E being a table (a component with the observed answers as relation). Under an intervention edit, L_k(τ(a),σ(b)) for the table component — the table's relation under intervention... Hmm, the claim is a table has no component whose relation is replaced by an intervention — but F1's failure: proj Sol_{λ(k)}(a,b) = L_k(τ(a),σ(b)) — for the table component, L_k is fixed regardless of the edit (a table doesn't respond to interventions), whereas the anchor's projected solutions do change. So F1 fails ✓. Plausible.

**Part VI** — "Fix𝓔 and a declared restriction operation. For W ⊆ Γ, let E|W retain the commitments in W with the named background fixed." Then S_{E,p} = {W ⊆ Γ : Account(E|W, p)}. Account takes a *candidate* 𝓔 = (E,p,t,Γ); here "Account(E|W,p)" — the transport t and Γ are implicit. Minor typing.

Also CriticalBlock definition: "For nonempty B ⊆ W, CriticalBlock(B;W,p) ⟺ W ∈ S_{E,p} ∧ W∖B ∉ S_{E,p}". Then "A block may be critical while no singleton in it is." ✓ true (with S not upward closed... hmm, with upward closure, if W∖{b} ∈ S for each b ∈ B but W∖B ∉ S — e.g., Γ = {a,b,c}, S = up-closure of {{a,b},{a,c},{b,c}}: W = Γ ∈ S; W∖{a} = {b,c} ∈ S; so {a} not critical; similarly; but B = {a,b}: W∖B = {c} ∉ S → B critical while no singleton is ✓.) OK true.

**Boundary (D)**: "Boundary_{E,p} = {(v,w) ∈ 𝒱² : Account(E_v,p) ≠ Account(E_w,p)}" — fine.

**Hard-to-vary in Part VI**: "For explanatory jobs F ⊆ F′, Pres(F′) ⊆ Pres(F), where Pres(F) = {v ∈ 𝒱 : ∀f ∈ F, Account(E_v,f)}." As noted, Account(E_v, f) type issue (f is a job, not a question). Trivially true otherwise. Also — hmm, "Account(E_v, f)" where earlier Account(𝓔) is one-place on a candidate. Mild.

**Part VIII — Transport results.**

"Functional transport. If π∘S_a = T_a∘π for every generator a, the same holds for every admitted finite composition with matching scopes. Proof. π S_b S_a = T_b π S_a = T_b T_a π; induct."

Check: want π S_{ba} = T_{ba} π = T_b T_a π. If S is a (anti?)homomorphism: S_b S_a = S_{?}. Given composition of edits a_2 a_1 with τ(a_2a_1) = τ(a_2)τ(a_1) (from F2), we need S_{a_2a_1} = S_{a_2} S_{a_1} presumably. The proof shows π S_b S_a = T_b T_a π, i.e., π S_b S_a = T_{ba} π. For the conclusion "the same holds for every admitted finite composition", we need S_b S_a = S_{ba}. The proof assumes the composition law for S (that S_bS_a = S_{ba}) without stating it — "with matching scopes" hints at partiality issues (partial maps don't compose cleanly when scopes disagree). The proof's step "π S_b S_a = T_b π S_a" uses the hypothesis at generator b applied to the vector S_a(x) — fine if the hypothesis is "for every generator a and all x in scope". OK the proof is basically fine modulo assuming S respects composition. Partial composition with "matching scopes" — mild. Might not report.

Hmm, but wait: there's a subtlety: the statement is "If π∘S_a = T_a∘π for every generator a, the same holds for every admitted finite composition with matching scopes." But A is "closed under a partial associative composition with identity 1" — so admitted edits include composites. "generators" — generators of what? A is a set with partial composition; "generator" is not defined in the document. Minor gap. Possibly a defect: "every generator a" — the term generator is undefined. Eh.

"Relational transport... Composition of relations preserves both directions when intermediate scopes agree." ✓ plausible.

"Approximate transport. With one-step discrepancy ε and an L-Lipschitz next-step map, e_n ≤ ε Σ_{k<n} L^k. (T2) Without a modulus, no accumulated bound follows."

Check: standard discrete Grönwall: e_{n+1} ≤ L e_n + ε → e_n ≤ ε(1 + L + ... + L^{n-1}) = ε Σ_{k<n} L^k ✓ (if e_0 = 0). Fine. Note (T2) referenced in Part XV as (T2) ✓.

**Part IX.**

"What a test refutes. For T ∧ B ∧ I ⇒ O, an established ¬O yields ¬T, since B and I are background. (K3)"

This is WRONG logically! From T ∧ B ∧ I ⇒ O and ¬O, you get ¬(T ∧ B ∧ I), i.e., ¬T ∨ ¬B ∨ ¬I. You cannot conclude ¬T unless B and I are *established true*, not merely "background". The parenthetical "since B and I are background" doesn't logically license ¬T. And the skill's own word-list says: "a failed test knocks down the bundle | what a test refutes (K3) | Part IX | From theory and background and test set-up together predicting wrongly, only 'not all three' follows." !!! The word-list explicitly interprets K3 as "only 'not all three' follows" — but the document (K3) says "yields ¬T, since B and I are background". So the document's (K3) is a logical error (denying the antecedent... no, affirming... it's the fallacy of ignoring that B and I could be false), and it contradicts the word-list gloss. This is a top-tier defect. Quote: "For T∧B∧I⇒O, an established ¬O yields ¬T, since B and I are background." Counterexample: T false... no wait. Let T be true, B false (background assumption wrong), I true, O false. Then T ∧ B ∧ I ⇒ O is vacuously true (antecedent false), ¬O established. But ¬T is false since T is true. So the inference is invalid. The correct conclusion: ¬T ∨ ¬B ∨ ¬I. "Background" is an epistemic status, not a logical guarantee. Excellent defect #1 or #2.

Also Part X / elsewhere relies on (EK) etc.

**Part IX — Bearing (K1):** "Bearing(c,z,p) ⟺ Account(𝓔_c, p_δ)" — fine-ish.

**Standing (K2)** — fine.

**Receipts** — "Negation exchanges them; missing evidence stays missing." Fine.

**Part X — Newness (N):** "With R_{<e}(s,h) = ⋃_{ξ before e} R_{β,ℓ}(s,ξ), New(s,c,h,e) ⟺ ¬∃d ∈ R_{<e}(s,h), d ≡_ℓ c." — "d ≡_ℓ c" — equivalence at grain ℓ is presumably "one kind"/isomorphism at ℓ. Not defined explicitly for contents (contents are organizations with contracts). Kinds were defined for components. d ≡_ℓ c for contents is undefined — mild. Also "ξ before e" — before in what order? h's precedence presumably. Minor.

**Origin (G)**: fine.

**Part XI — Repair (P):**

"Repair_{O,P}(ξ,ξ′;Δ) ⟺ ∃o ∈ O[¬o(ξ) ∧ o(ξ′)] ∧ ∀r ∈ P[r(ξ) ⇒ r(ξ′)] ∧ ProducedBy(Δ,ξ,ξ′;O)."

Fine — repairs at least one obligation and protects the protected ones. Note: it requires only *one* o repaired and allows other o ∈ O to remain unsatisfied — fine as a definition.

**CreateEK (EK)** — has "Account(c, p_c)" where c is a content... and earlier Account(𝓔) is a four-place candidate (E,p,t,Γ). Here "Account(c,p_c)" two-place. Typing drift. Also "the scope of Account(c,p_c) is the contract fixed at e_c" fine.

Also in (EK): "CreativeCriticalEpisode(s,Δ,h,e)" — earlier defined as "A creative critical episode contains an instance of (G) connected to its inquiry" — then (EK) separately requires Origin — redundancy, not error.

**Artistic effect (AR)**: "achievement R[a,k] ≠ ∅ ∧ R[a,k] ⊆ G[k] (AR)" — G ⊆ K × F, so G[k] = {f : (k,f) ∈ G}. R ⊆ A × K × F. R[a,k] = {f : (a,k,f) ∈ R}. Achievement: every effect produced is permitted and at least one is. Fine as a definition ("purpose achieved iff something happens and all that happens is within the purpose"). Hmm — but a purpose G[k] with the requirement R[a,k] ⊆ G[k]... this defines "does not do anything outside the purpose and does something". OK, a definition.

Hmm wait, there's an issue: "an effect organization ℛ ⊆ A × K × F; a purpose G ⊆ K × F; achievement ℛ[a,k] ≠ ∅ ∧ ℛ[a,k] ⊆ G[k] (AR)". Fine.

**Part XII — physical module.**

"(CT1)" RetReal: "∀z ∈ C ∀i ∈ dom T ∀η ∈ Exec(π,z,i;χ), η completes with o ∈ T[i] and z′ ∈ C." Fine. "Execution families are nonempty on legitimate inputs; deadlock is not vacuous success." ✓.

"(CT2) Retention fixed point. F(C) = states whose executions all complete and return into C. F is monotone; C ⊆ F(C) is the invariant form; the union of post-fixed sets is the greatest fixed point."

Check: "F(C) = states whose executions all complete and return into C" — i.e., F(C) = {z : ∀ executions from z, complete and end in C}. Is F monotone? If C ⊆ C′, then "return into C" implies "return into C′" so F(C) ⊆ F(C′) ✓ monotone.

"C ⊆ F(C) is the invariant form" — C ⊆ F(C) is the *post-fixed* point condition (usually "C ⊆ F(C)" is post-fixed or "F(C) ⊆ C" pre/fixed... conventions vary). "the union of post-fixed sets is the greatest fixed point" — Hmm! The union of post-fixed points is NOT in general a fixed point, nor is it necessarily... Actually, Knaster–Tarski: the greatest fixed point of a monotone F on a complete lattice is the union of all *post-fixed* points (sets C with C ⊆ F(C)). Yes! Knaster–Tarski: gfp(F) = ⋃{C : C ⊆ F(C)}. That's correct for monotone F on a complete lattice (powerset). ✓ Good.

But hold on — is F monotone in the required sense... yes as shown. But careful: F as defined uses "all executions complete and return into C" — F(C) ⊆ F(C′) for C ⊆ C′ ✓. Knaster–Tarski gives gfp = union of post-fixed sets ✓. And "C ⊆ F(C) is the invariant form" — states invariantly retained ✓. So CT2 is fine? The claim "the union of post-fixed sets is the greatest fixed point" is correct by Tarski. OK. But hmm — "F(C) = states whose executions all complete and return into C" — "return into C" means end state in C. But for a *fixed point* we want F(C) = C: states whose executions all complete and return into C. Note z ∈ F(C) requires executions from z return into C — and if z ∈ C⊆ F(C), fine. OK, Tarski applies. Fine.

Wait — one subtlety: the union of post-fixed sets is a post-fixed set (for monotone F) and is the greatest fixed point. Correct.

Hmm, but "C ⊆ F(C) is the invariant form" — is C ⊆ F(C) equivalent to "C is invariant (executions from C stay in C and complete)"? C ⊆ F(C) means every z ∈ C has all executions complete and return into C ✓ exactly invariance. Fine.

**Grades (CT3, CT4):** "Cap^{q,r}_Ω(ξ) ⊆ Admit^{q,r}_Θ; Cap^∞ = ⋂_{q,r} Cap^{q,r} ⊆ Poss_Θ. (CT3, CT4) Finite-grade capability does not imply possibility at all grades." Hmm — "Possibility is the absence of a law-imposed limit short of perfection" — Poss_Θ would be the class of tasks possible (no law-imposed limit). Cap^∞ = tasks achievable at all grades...⊆ Poss ✓. Fine-ish.

**Part XIII.** Universality etc. Fine as definitions. "(U3) UECS = {(M,s,Ω,β) : M ⊨ RC ∧ UU ∧ UC}" fine.

**Part XIV — Primitives.** "The semantics has two. 1. The physical module Θ... 2. The normative relation𝒩, when a question invokes one. Everything else is derived."

Hmm — but Part III defines contracts with provenance in {declared, selected, constructed} and "declared" is a stipulation by the modeller — is declaration derived from Θ? Also, the claim "Every predicate in Parts II–XIII is defined from Θ ... and 𝒩 ... together with declared indices" (Derivation 6). But what about the *modeller's declarations* — the contract C, the question's query Q, the obligations O_p? Part XIV says "Indices, not primitives. Grain ℓ, boundary β, continuity Ω, and the contract C are declared indices. Every claim is relative to them; none is a predicate that could be true or false." But Part III says "A question can be wrong... Its formulation is still an event" and Part 0 says "Questions have provenance too" and Derivation 5 treats contracts as contents that can be constructed. If C is "a declared index... none is a predicate that could be true or false", how can a contract be an organization (Derivation 5) and be true or false (Part III "A question can be wrong")? There's tension: Part XIV says contracts are "declared indices" that can't be true or false, while Part III gives contracts provenance including "constructed"/"selected" and allows that a question "may fail to pick out its alleged target" (i.e., can be wrong). Also Derivation 5 says a contract is an organization — but Part II defines an organization as D = (V, X, J, B, A, L) — while Part III defines a question p = (D, C, b_0, Q, O_p, ρ_p) with C ⊆ A × B. A contract C is a subset of A × B, NOT an organization. Derivation 5: "A contract C is an organization (a set of edits with a query), and can be the content c in (G). ... Give it ports (the edits and their boundaries), components (the closure conditions), and admitted edits (add or remove a change; alter Q). It is then a D in the sense of (O)."

Hmm — the proof stipulates a construction of an organization from a contract: ports = edits and boundaries (but ports need nonempty value domains X_v — what are the values? the edits themselves?), components = "the closure conditions" (with footprints and relations L_j? not given), admitted edits = add/remove a change. But (O) requires each component j to have a footprint V_j ⊆ V and a relation L_j(a,b) ⊆ ∏_{v∈V_j} X_v for each edit a and boundary b — the construction gives none of this data. "Give it ports (the edits and their boundaries)" — a port must have a value domain; here the "ports" are the edits themselves, which is a category error (ports carry values; edits are operations). So the proof of Derivation 5 is a sketch that does not construct the data required by (O): it never specifies X_v, J's footprints, or L_j. Hence the claim "It is then a D in the sense of (O)" is unproved. Also the claim in the statement "A contract C is an organization (a set of edits with a query)" contradicts Part III's definition where C is a subset of A × B (a set of edit–boundary pairs), and the query Q is part of the *question* p, not of the contract C. So the statement misdescribes its own definitions. Good defect.

Also Part 0 claim "This is what distinguishes it from any class in which questions are inputs: such a class cannot represent question-finding" — overreach maybe.

**Derivation 8 — Equivariance under genuine recoding.** "Transporting all carriers, relations, transports, histories, and contracts along structure-preserving bijections preserves (E), (G), (P), (EK). Proof. Each is a conjunction of equalities and existence claims over the transported data; bijections preserve them. ∎"

Hmm — (E) includes Non-vacuity's clause "every physically admitted edit excluded from C is excluded by a stated scope, not silently" and Non-circular dependence's clause about "the target's answer does not appear... as an unanalysed boundary input" — these are not "equalities and existence claims over the transported data"; they are pragmatic/semantic conditions. So the proof's claim "Each is a conjunction of equalities and existence claims" is false for (E) as defined (and (EK) includes Account). Also (G) includes New which quantifies over the repertoire — preserved by bijections ✓. But the flaw stands: the proof mischaracterizes (E). Also (EK) includes "Deploy... supporting the declared use task U as a retained capability (Part XII)" and RetReal has "law-imposed" notions — carried byΘ. Hmm, "structure-preserving bijections" of what structure? Underspecified. Medium defect.

**Derivation 9 — Output descriptions do not determine accounts.** "If M_0, M_1 have the same input–output projection and differ on an account claim, no function of the projection agrees with the claim on both. Proof. Equal inputs to a function give equal outputs. ∎" ✓ trivial but correct.

**Derivation 10.** The two-layer episode. Let me check for defects.

"Stipulate a primitive layer P: a line of cells; two things, each with a position and a velocity; continuity components; admitted edits: displace a thing, set its velocity, occlude a cell, swap the two identities. The sensory field is the occupancy of cells — which cells are filled, without which thing fills them."

"A simulation layer S_0 with a transport t_0 selected on a history H_0 containing displacements and velocity changes but no occlusions. S_0 predicts occupancy from recent occupancy. It is faithful on H_0."

"An occlusion occurs. Under occlusion, the target's thing continues to exist and to move; the occupancy field shows nothing at its cell. S_0, predicting from occupancy, expects nothing there and is violated when the thing re-emerges at a cell consistent with its velocity. This is surprise (Derivation 4)."

Hmm — "This is surprise (Derivation 4)" — but Derivation 4's claim is an iff about when surprise is possible, not a license for calling a particular event surprise. Fine.

"The swap edit is invisible in the sensory field and leaves every prediction unchanged. On any contract containing it, the two persistence components are of one kind (Derivation 2)."

Wait — "On any contract containing it, the two persistence components are of one kind (Derivation 2)" — hmm. Derivation 2 says candidates satisfying F1, F2, A are one account; kind-identity of two components is (K)/Derivation 1 territory. Also the claim "on any contract containing [the swap edit], the two persistence components are of one kind" — is that right? The two persistence components track the two things. Their signatures: under displacement of thing 1, component 1's relation changes and component 2's doesn't... hmm. Per (K), "Two components j,j′ are of one kind on C when there is a bijection of their footprints under which sig_C(j) and sig_C(j') coincide." sig_C(j) = {(a,b,L_j(a,b))} — for the SAME edits (a,b) ∈ C. The two persistence components respond differently to "displace thing 1" (component 1's ports move, component 2's don't). So their signatures over the same contract differ — unless the footprint bijection includes swapping... The definition says the signatures must coincide under a bijection of footprints — the edit–boundary pairs are the same, but the relations are over different ports. For the signatures to coincide under the bijection, we'd need L_{j1}(a,b) mapped to equal L_{j2}(a,b) for each (a,b). But "displace thing 1" changes j1's relation (its position port) and not j2's. So sig differs at a = "displace thing 1" → they are NOT of one kind on a contract containing "displace thing 1"! Hmm wait — careful. What does L_j(a,b) look like? L_j is the relation of component j under edit a. If edit a = "displace thing 1", then the persistence component for thing 1 has its position port set/replaced, so its L under that edit is different from its L under the identity edit. Component 2's L under "displace thing 1" equals its L under identity (unaffected). So sig(j1) ∋ (a, b, L_{j1}(a,b)) with L_{j1}(a,b) ≠ L_{j1}(1,b), whereas sig(j2) has L_{j2}(a,b) = L_{j2}(1,b). Under the bijection of footprints, equality of signatures would require the mapped relations to coincide — since the edits in the signature are literally the same set C of (a,b), we'd need σ∘L_{j1}(a,b) = L_{j2}(a,b) for all (a,b) ∈ C, but at a = "displace thing 1" these differ (one is displaced-relation, other not). So NOT one kind. Unless the contract's edits are unlabeled with respect to which thing — e.g., "displace thing 1" and "displace thing 2" both in C, and the bijection swaps... no — the bijection is of footprints (ports), and it must hold for each (a,b) with the same (a,b). At a = "displace thing 1", j1's relation changes and j2's doesn't. So sigs differ. Therefore the claim "On any contract containing [the swap edit], the two persistence components are of one kind" is not just unsupported — it seems false for contracts also containing displace-thing-1 vs displace-thing-2 as distinct edits... 

Hmm wait. Let me reconsider. Maybe the intended reading: the swap edit means the contract can't tell which thing is which, so the two components are of one kind *up to the swap*. But (K)'s definition compares signatures on the SAME edit set with a footprint bijection — no edit renaming. Unless τ... hmm. Actually hmm — maybe the claim is that the components are one kind on the contract *restricted to* the swap edit... "On any contract containing it" — containing the swap edit. If the contract contains ONLY the swap edit (and identity), then both components' relations under swap... Under the swap edit, thing 1 and thing 2 exchange identities. Both persistence components change (each takes the other's identity?). Hmm.

Honestly the cleanest formulation of the defect: (K) defines one-kind via a bijection of footprints under which sig_C(j) and sig_C(j') coincide, with signatures indexed by the same C. For two persistence components in a contract containing "displace thing 1", the signatures differ at that edit (one component's relation varies, the other's does not), so they are not of one kind — contrary to "On any contract containing it, the two persistence components are of one kind (Derivation 2)". Unless the contract contains only edits symmetric in the two things (like swap, or "displace both"). So "any contract containing it [the swap edit]" is too strong: a contract containing swap AND "displace thing 1" separates the components. Hmm, wait — does "displace thing 1" separate them per (K)? sig_C(j1) contains the relation of j1 under edit a1 = displace-thing-1; sig_C(j2) contains relation of j2 under a1. Since j1's footprint maps to j2's by bijection φ, coincidence requires φ(L_{j1}(a,b)) = L_{j2}(a,b). Under a1, j1's relation has the new position for thing 1... j2's relation is unchanged. These relations are over different port sets, mapped by φ (position of thing 1 ↦ position of thing 2). L_{j1}(a1,b) = relations fixing pos1 = displaced value d... L_{j2}(a1,b) = relations with pos2 = its previous value. Under φ: L_{j1}(a1,b) maps to fixing pos2 = d ≠ previous. So they differ. ✓ Separated. So the claim is false as stated.

Hmm, but maybe the intent: "the two persistence components are of one kind" on the contract where the identity-swap is the only edit that permutes them... Hmm. But also the conclusion drawn: "A claim that 'component 1 is really thing 1' is a claim that some admitted change distinguishes them, and on this contract none does." — "on this contract none does" — which contract? The one containing swap, displace, set velocity, occlusion. On THAT contract, "displace thing 1" distinguishes them (component 1 responds, component 2 doesn't). Unless "displace a thing" is not indexed to which thing... "admitted edits: displace a thing, set its velocity, occlude a cell, swap the two identities" — "displace a thing" — ambiguous: an edit that displaces whichever thing... If the edit "displace" is not thing-indexed (i.e., there's one edit "displace a thing" and it applies to both?), then... The stipulation is ambiguous, but the claim "on this contract none [no admitted change] does [distinguish them]" is clearly wrong if "displace thing 1" and "displace thing 2" are both admitted as distinct edits — displacing thing 1 leaves a mark on component 1 and not component 2. Hmm, but does it "distinguish the components"? Distinguishing components per (K) = their signatures differ = there's an edit where one's relation varies and the other's doesn't. Displacing thing 1 does exactly that. So the claim fails.

Hmm, but wait — maybe the intended point is subtler: after the swap edit, the persistence components track trajectories, and "component 1" and "component 2" can be permuted without affecting anything — i.e., the *labelling* of components is what's at issue. The swap edit exchanges the identities of the two things, and since the sensory field can't see it, the two components' *contents* (thing 1 vs thing 2) are exchangeable. The claim "component 1 is really thing 1" is about anchoring: λ(component 1) = thing 1's continuity subnetwork vs λ(component 1) = thing 2's. If the swap edit is admitted and invisible, then both anchorings give faithful transports. The right claim would be: no admitted change distinguishes the two *anchorings/transports*. But the document states "the two persistence components are of one kind" — which per (K) is about components' signatures, and is the wrong vehicle. Hmm. And "on this contract none does" — no admitted change distinguishes component 1 from component 2 — false per the above.

Hmm, hold on. Let me reconsider whether "displace thing 1" distinguishes the components in the signature sense. Components of S_1: persistence component per thing. Component 1 "carries position and velocity through occlusion" for thing 1. Its footprint: pos1, vel1 ports. Under edit "displace thing 1", what is L_{k1}(a,b)? The relation of component k1 under that edit. Displacing thing 1 changes pos1. L_{k1}(a_displace1, b) presumably reflects the displaced position. And L_{k2}(a_displace1, b) — component2's relation — unaffected: same as L_{k2}(1,b). And L_{k1}(1,b) ≠ L_{k1}(a_displace1, b). So sig(k1) ≠ φ(sig(k2)) at (a_displace1, b): we need φ(L_{k1}(a,b)) = L_{k2}(a,b) — LHS = displaced-pos relation mapped to pos2; RHS = pos2's undisturbed relation. Differ. So not one kind ✓ my analysis.

But hmm — what if the persistence component's relation L_k(a,b) is the *continuity* relation "position evolves by velocity", which is invariant under displacements (the relation is the same functional form; the edit sets the position port which is a different component)? I.e., component k1 = "persistence: pos(t+1) = pos(t) + vel(t)·dt" — its relation over its footprint. Under edit "displace thing 1" (which sets the position port via a separate setting-component), L_{k1}(a,b) is unchanged! Because the edit replaces the component *assigning* the position port... hmm, which component assigns pos1? The persistence component computes pos at the next time from current pos and vel. The displacement edit sets the current pos. If setting a port "replaces the component assigning that port", then displacing thing 1 replaces the persistence component?? That doesn't seem right either.

OK the formalization here is genuinely murky. But there's a cleaner way to see the defect in the claim: the claim "On any contract containing it, the two persistence components are of one kind" is *contradicted within the very same paragraph's own reasoning*: they say the swap edit is "invisible in the sensory field and leaves every prediction unchanged" and conclude the components are of one kind. But by (K), one-kind is signature-coincidence under ALL edits in C. If C contains an edit that moves one thing and not the other (which it does: "displace a thing" is admitted), the components respond differently...Hmm, unless "displace a thing" is a single edit schema and "the swap edit" makes the two things indistinguishable... no, displace-thing-1 vs displace-thing-2 are distinguishable by their effects on the sensory field (occupancy changes at different cells).

Actually hmm, wait. Let me think about what "of one kind" would require here more carefully. The persistence components: k1 carries thing 1's pos and vel; k2 carries thing 2's. Under "displace thing 1", k1's position changes and k2's does not. Under "displace thing 2", vice versa. Signatures over C = {1, displace1, displace2, setvel1, setvel2, occlude, swap}: sig(k1) has relations varying at displace1, setvel1, swap?, and sig(k2) at displace2, setvel2, swap?. Bijection φ: pos1↦pos2 etc. Coincidence requires φ(L_{k1}(a,b)) = L_{k2}(a,b) for every a. At a = displace1: LHS varies with displacement of thing 1; RHS is k2's relation under displace1 = unaffected. So they differ → not one kind. To make them one kind, you'd need to *translate the edits* as well (displace1 ↦ displace2) — which is what transports do (τ), but (K)'s kind relation has no edit translation. So there's a structural flaw: (K)'s definition of "one kind" cannot identify symmetric components in a world with labeled edits — while the document's examples (Derivation 10) and Part 0 grievance 2 ("At a finer level with more admitted changes they may separate") rely on it being able to. Hmm, interesting: this shows (K) is too strict — two components that intuitively are "the same kind" (two balls) are never one kind on a contract with individually-labeled interventions. That undermines Part 0's Grievance 2 answer and Derivation 10's claim.

Hmm, but is that really a defect of the *document* or of my reading? Let's re-read (K): "Two components j,j′ are of one kind on C when there is a bijection of their footprints under which sig_C(j) and sig_C(j′) coincide." sig_C(j) = {(a,b,L_j(a,b)) : (a,b) ∈ C}. The elements are triples including the edit a. For coincidence under a footprint bijection φ (extended to relations), we need {(a,b,φ(L_j(a,b)))} = {(a,b,L_{j'}(a,b))}. Since the (a,b) pairs match exactly, need φ(L_j(a,b)) = L_{j'}(a,b) for each (a,b). Yes — no edit renaming. So my analysis holds. And then Derivation 10's claim is false under (K) as defined (given the contract includes individually-labeled displace edits). Unless the stipulated P has edits "displace a thing" that are NOT thing-indexed (a single nondeterministic edit "displace a thing" that could displace either). Then both components respond identically to it and one-kind could hold. But then "the swap edit is invisible... and leaves every prediction unchanged. On any contract containing it, the two persistence components are of one kind" — with a nondeterministic "displace a thing" edit... hmm, then which thing is displaced is a boundary condition? The document's stipulation is too vague to decide. But the phrase "On any contract containing it" is the problem: take a contract containing the swap edit AND an edit "displace thing 1" (as distinct edits are admitted per "displace a thing... swap the two identities" — hmm).

Alternatively — maybe a cleaner defect in Derivation 10: it cites "(Derivation 2)" for the claim that "the two persistence components are of one kind". Derivation 2 is "Indistinguishable is identical" about two *candidates* being one account — it does not establish that two components are of one kind; that's (K) or Derivation 1. So the citation is wrong. Plus the substantive claim conflicts with (K) when the contract has thing-indexed edits. I'll state the defect as: the claim is unsupported and false under (K) on any contract admitting edits that move one thing and not the other — a counterexample: the edit "displace thing 1" changes k1's relation and not k2's, so their signatures differ.

Hmm, wait. But hold on — is that right given how L_j(a,b) works? Let me think again about whether "displace thing 1" changes L_{k1}. L_j(a,b) is "the interpretation supplies" — the relation of component j under edit a and boundary b. If edit a displaces thing 1 by setting its position port... "An edit that sets a port replaces the component assigning that port". If k1 assigns pos1 (it carries position through), then edit "displace thing 1" replaces k1 with a constant-position component. Then L_{k1}(a,b) = the fixed position relation. And L_{k2}(a,b) = its normal persistence relation. These differ structurally (one is a constant, one is a persistence law) → signatures differ → not one kind ✓ my conclusion stands. If instead the edit acts on a different component (the "setting" component for pos1), and k1's relation is unchanged... but k1 *carries* position — its footprint includes pos1. Hmm. Under either reading, at edit "displace thing 1", k1 and k2's relations respond differently (k1 is affected, k2 isn't), so the signatures fail to coincide. Unless the edit affects both symmetrically. OK.

But hmm, wait — maybe under the swap edit, "thing 1" and "thing 2" labels are attached... Ugh. Let me also consider that maybe the claim intends: "the two persistence components are of one kind" on the contract *restricted to* {swap} — and "on any contract containing it" means any contract containing swap and nothing that separates... no, "any contract containing it" is universal. Fine — I'll report it with the counterexample edit.

Actually, hmm, wait. Let me reconsider. Under the SWAP edit itself: thing 1 and thing 2 exchange identities. Then k1 and k2 both change (each persistence component now carries the other's identity?). The claim says the swap edit "leaves every prediction unchanged" (occupancy predictions identical since things look alike). "On any contract containing it, the two persistence components are of one kind (Derivation 2). A claim that 'component 1 is really thing 1' is a claim that some admitted change distinguishes them, and on this contract none does."

Hmm — "component 1 is really thing 1" — the claim is about the anchor λ(k1) = thing 1's continuity subnetwork. The two candidate transports: t with λ(k1)→thing1, λ(k2)→thing2, and t′ with λ(k1)→thing2, λ(k2)→thing1. On a contract containing swap, both transports are faithful (swap exchanges the things invisibly). So no admitted change distinguishes the two *transports*. That's the right statement. But the document says the *components* are of one kind and "no admitted change distinguishes them [the components]". As shown, edits like "displace thing 1" DO distinguish the components (k1 responds, k2 doesn't). So the document's statement is wrong at the level of (K)'s kinds; the defensible claim is about the two candidate transports/anchorings. I'll report this.

Hmm, but is it really wrong? "component 1" and "component 2" — under the swap edit, is there a fact about which persistence component is "component 1"? The components are indices j ∈ J. Component 1 tracks whatever thing... if the swap edit exchanges identities, then after the swap, component 1 tracks what was thing 2. The claim "component 1 is really thing 1" (over time, absolutely) fails after a swap — fine. The document's point: identity is exhausted by trajectory. But their formal claim "the two persistence components are of one kind" on any contract containing swap is still false per (K) given other edits. Unless... ugh. OK, decide: report as a defect — the claim as stated ("on any contract containing it") is falsified by any contract that also admits an edit acting on one thing only (e.g. "displace thing 1"): under (K) the components' signatures then differ. This is a genuine counterexample. And the citation (Derivation 2) is wrong. I'll include.

Hmm, wait. Let me reconsider once more, because this affects the flagship "relative-consistency witness". Consider the swap edit more carefully. Maybe the stipulated world has things with identity carried by cells' occupancy... The sensory field can't distinguish the things. But the *ports* of P are "persistent things with boundaries and identity" — each thing has position and velocity ports. The admitted edits include "swap the two identities". After swapping identities, thing 1 now has thing 2's position/velocity? Or the identities swap while trajectories stay? If "swap the two identities" exchanges the identity labels while leaving positions and velocities, then thing 1's position port takes the value thing 2 had. Then under this edit, both k1 and k2's relations change (each persistence component's carried values swap). Hmm.

But consider the edit "displace thing 1": k1's position port set to a new value; k2 unaffected. So sig(k1) ≠ sig(k2) unless... unless the footprint bijection φ maps pos1↦pos2 and we compare L_{k1}(a_displace1) with L_{k2}(a_displace1): L_{k1}(a_displace1, b) — the relation of k1 under the edit that displaces thing 1. If "displace thing 1" sets pos1 to some value d (a boundary/parameter), then L_{k1}(a,b_d) = {pos1 = d, vel1 = ...} while L_{k2}(a,b_d) = its persistence relation. Different. Not one kind. Confirmed.

Unless the persistence components don't include position ports... "a new organization S_1 is built with a component per thing carrying position and velocity through occlusion — a persistence component". Footprint includes pos and vel of that thing. Yes.

OK. Now — is the claim maybe right in the sense that (K)'s bijection could map and the signatures "coincide" as sets of triples where the third coordinate is a relation over the mapped ports... At a = displace1: L_{k1}(a,b) vs L_{k2}(a,b) — the relation of k2 under a is its *ordinary* relation (its ports weren't edited). For coincidence we'd need these to match under φ. If "displace thing 1" is realized as "set pos1 := d", and the persistence relation of k is "pos evolves", then L_{k1}(displace1, b) is the *replacement* (constant d) while L_{k2}(displace1,b) is the persistence relation. Differ. Yes. Report it.

Alternatively the intended reading of L_j(a,b) is that an edit's effect on a component is only via the component being *replaced* if it assigns the edited port... and a component that is replaced is no longer "the same component" — then sig_C(j) at the edit where j is replaced is... L_j(a,b) is still supplied ("for each j, edit a, and boundary b"). OK whatever — the claim cannot be verified under the given definitions and the natural reading refutes it. I'll state it carefully: under (K), with a contract that includes an edit acting on one thing alone, the signatures differ; the claim "on any contract containing it" is therefore false; and in any case Derivation 2 does not prove kind-claims.

Hmm — although — maybe I should be careful: maybe under the swap edit, the admitted edits "displace a thing" is a single edit schema and the *edit* is not indexed to a thing — i.e., "displace a thing" means "displace some thing" (nondeterministic) and the specific thing is determined by the boundary b! "For each j, edit a, and boundary b" — L_j(a,b) — the boundary b can specify which thing is displaced. Then under edit a = "displace" with b = "thing 1", L_{k1}(a,b) changes and L_{k2}(a,b) doesn't. Still separates them at (a,b) with b thing-1. Unless b can't refer to which thing... The stipulation says "admitted edits: displace a thing, set its velocity, occlude a cell, swap the two identities" — "occlude a cell" is cell-indexed. So thing-indexing seems available. OK, my counterexample stands: the pair (displace, b=thing1) separates. Fine.

Actually — hmm, wait. Let me reconsider whether this is the document's intent and whether the swap edit is meant to make even "displace thing 1" indistinguishable. If the two things are qualitatively identical and the sensory field can't tell them apart, then "displace thing 1" vs "displace thing 2" differ in *which cell's occupancy* changes — if the things are at different positions, the occupancy field reveals which. So they ARE distinguishable. But the identity-swap claim is about *identity*, not position: "component 1 is really thing 1" — over a history with a swap, trajectory-based identity is all there is. The document says "identity, at this grain, is exhausted by trajectory" — the right claim is about the two anchorings being equally faithful. OK.

Let me now also check Derivation 10's claim: "By Derivation 1, the persistence components have the signature of things on that contract: they respond to displacement and velocity edits as things do, and are invariant under occlusion as things are." Derivation 1 is about (F1) entailing signature equality between E-components and their anchors ✓ fine.

**Part 0 — the seasons... no. Part 0's claims:**

"Two components no admitted change can separate are one kind at that level, whatever labels anyone attach to them." — by (K) ✓ (definition).

"Conjecture... A genuine explanatory achievement that these four cannot represent counts against the conjecture." OK.

"What this document does not claim: ... It does not say prediction is explanation. Fidelity in this semantics is over component structure under change, not over outputs. Two systems with identical outputs and different internal routes are different organizations here" ✓ consistent with Derivation 9.

Grievance 3: "There is a theorem below (Derivation 3) that selected transports are always underdetermined on unseen changes." — "always" — Derivation 3 requires H ⊊ C and "the component relations supplied independently". OK.

Grievance 4: "A contract that quietly excludes physically possible changes to protect an account is not caught by a rule about intentions; it is caught by the requirement that the exclusion be stated, and then by any criticism that supplies the excluded change." Fine-ish.

"Where to attack this" (A): "The classic attempts — lookup tables, reversed calculations, conclusion-as-premise — all fail one of the four" — "one of the four" — but (E) has SIX conjuncts (F1, F2, A, NonCircular, NonVacuous, Coherent). Part 0 repeatedly says "four conditions of Account (Part V)". "Produce a candidate that satisfies all four conditions of Account (Part V)..." But (E) is a conjunction of six named conditions grouped into four bold headings: "Component fidelity" (F1 & F2), "Question fidelity" (A), "Non-circular dependence", "Non-vacuity", "Baseline coherence" — that's five headings and six formulas. Part 0 says "all four conditions" and "A candidate that satisfies all four while plainly explaining nothing" and Part V ends "(E) ⟺ (F1) ∧ (F2) ∧ (A) ∧ NonCircular ∧ NonVacuous ∧ Coherent" — six. And Part XV: "A candidate meeting (E)..." So "four conditions" in Part 0 contradicts the six conjuncts of (E). Hmm — maybe "four" counts: F1+F2 as "fidelity", A, NonCircular, NonVacuity = 4 with Baseline coherence added later ("This is an independent requirement")? Indeed Part V lists5 bold conditions. Part 0's "four" is inconsistent with Part V's five/six. Minor but citable: "satisfies all four conditions of Account (Part V)" vs (E) with six conjuncts. Also in "Where to attack this (A)": "Produce a candidate that satisfies all four conditions of Account (Part V) on a physically admitted contract, with a non-declared transport" — hmm interesting: "with a non-declared transport" — sufficiency attack requires a non-declared transport (selected or constructed). But (R) requires selected/constructed for representation; for *Account* (E) the provenance of t isn't constrained at all! (E) says nothing about provenance. So a declared transport satisfies (E) trivially in many cases — and Part 0's challenge (A) excludes declared transports from the counterexample search, which quietly admits that declared transports can satisfy (E) while explaining nothing ("A declared transport does not make an occurrence represent anything"). So there's a tension: (E) — the definition of "explanatory candidate... satisfies Account" — has no provenance requirement, yet the document's sufficiency claim (that satisfying (E) = explaining) is only defended against candidates with non-declared transports. If a declared transport satisfying (E) plainly explains nothing, that would refute sufficiency as stated in Part V ("Account(E)") but the document pre-excludes it in Part 0's attack spec. Hmm — is that a contradiction? Part V defines Account without provenance conditions on t. Part 0 (A) demands the counterexample have "a non-declared transport". So either (E) as defined is not sufficient (declared transports can satisfy it while explaining nothing), or the challenge misstates the claim. Let me think of the counterexample: take any real-world explanandum D and a modeller who *declares* a transport t from D to some E. If (F1),(F2),(A),NonCircular,NonVacuous,Coherent hold, does it "explain"? By the document's own Part IV, a declared transport makes no representation claim ("A declared transport does not make an occurrence represent anything; it makes a modeller assert that it does"). But Account(E) is about E being an explanatory candidate — the sufficiency claim of the constitutive conjecture is presumably "satisfying the four conditions = explaining". Since (E) ignores provenance, and the document admits declared transports don't confer representation, a candidate with a declared transport satisfies (E) without the explanation's being *about* anything... Hmm, this is philosophical. The cleaner statement of the defect: Part 0's attack condition (A) silently strengthens the claim under test by requiring a non-declared transport, while (E) as defined in Part V imposes no provenance condition on t; so either (E) is committed to counting declared-transport candidates as accounts (contradicting Part IV's denial that declaration confers representation), or (E) is missing a provenance conjunct and the written definition of Account is not the definition the document defends. Good — I can state this as a contradiction between Part V (E) and Part 0 (A)/Part IV.

Hmm, is it top-tier? It's a real internal inconsistency in the load-bearing definition. I'd put it mid-high.

Also Part V says "An explanatory candidate for question p is an organization E, a transport t = (π,τ,σ,λ) from D to E, and an identified set Γ" — note the transport is "from D to E" but (R)'s Rep has t : Org_ℓ(o) → c — transport from carrier-org to content-org. In Part V the transport is from the *target* D to the explanation E. Hmm — direction inconsistency? In (R), "admits a transport to c" from Org_ℓ(o) — i.e., from the physical carrier's organization to the content. In Part V, the account's transport goes from D (the target, the world) to E (the explanation). Both make sense in their places (representation: carrier→content; explanation: world→model). Hmm — but for an explanation, isn't the correspondence between the *explanation* E and the *target* D? F1/F2/A check that E mirrors D under edits. Fine.

But wait — there's a directional issue in (F2): "π[Sol_D(a,b)] = Sol_E(τ(a),σ(b))" — π: X_D → X_E maps target valuations to explanation valuations ✓ fine.

**Part I — "Two provenances, not one. Selection and construction both produce correspondences."** vs Part IV "Three provenances": Selected, Constructed, Declared. Part I says "Two provenances, not one" and Part 0 says correspondences can be "selected ... or constructed ... or merely declared". Part IV heading "Three provenances" and says "A transport t ... has exactly one of three provenances". Part I's heading "Two provenances, not one" with text "Selection and construction both produce correspondences. They are distinguished by their histories. Neither is reduced to the other." — this is about the two *genuine* ones vs declared as mere modelling convenience. Part IV says "exactly one of three provenances, determined by its history" — including Declared = "Neither of the above". OK consistent-ish (Declared is the null case). Not a defect.

But: Part IV says "A transport t between organizations of a physical system has exactly one of three provenances, determined by its history in the physical module" — but then: "Constructed. There is an episode (Part X) whose construction witness prepares t, and in which t, or the organization it targets, is available as a represented target; or t is the survivor of a variation-and-survival history on a population that contains it, which serves as its construction witness."

Whoa — the second disjunct: "t is the survivor of a variation-and-survival history on a population that contains it, which serves as its construction witness." But a variation-and-survival history is exactly the definition of **Selected**! ("Selected. There is a population 𝒯 of candidate transports, a variation operator μ on 𝒯, a finite history H ⊆ C ... The transport t is a member of 𝒯 that survived.") So the second disjunct of Constructed makes Constructed overlap with Selected — contradicting "exactly one of three provenances" and contradicting "Construction is not selection. A selected transport has no represented target in its history; a constructed one does" (Part X) and Part 0 Grievance 6 ("the semantics forbids that reduction in Part IV") and Part XV ("Construction reduced to selection... This collapses the two provenances"). The disjunct "or t is the survivor of a variation-and-survival history on a population that contains it, which serves as its construction witness" is either (i) identical to the Selected criterion (then "exactly one of three" is false and the classes overlap), or (ii) requires additionally that the history be a construction witness — but a construction witness is defined in Part X as identifying "the controlled processes, the incoming carriers, the bindings constructed" — a variation-and-survival history per Selected has "no member of the history represents t, H, or the survival condition". If the second disjunct's history "serves as its construction witness", the witness must represent t — but Selected explicitly requires non-representation. So if the disjunct requires a represented target, it's the first disjunct again. As written, the disjunct unmoors "constructed" from the represented-target requirement and makes it coincide with selection. Excellent defect — direct contradiction between Part IV's "exactly one of three provenances" and the definition of Constructed, and with Part X's "Construction is not selection."

Also "exactly one of three" vs. the possibility that a transport both has a construction episode and a selection history ("Construction may operate on selected material. Selection may continue to operate beneath construction.") — a transport prepared by an episode AND surviving a selection history would satisfy both Sel and Con criteria → not "exactly one". Well, "determined by its history" with priority rules unstated. Combined with the above.

Also (R): "Rep_ℓ(o,c) ⟺ ∃t [Faithful_C(t : Org_ℓ(o) → c) ∧ (Sel(t) ∨ Con(t))]" — note Sel(t) and Con(t) were written with parameters(Sel(t;𝒯,μ,H), Con(t;h,e)) — fine, existentially closed.

Also (R): "A system can represent a false theory: the transport from carrier to content is faithful while the content's transport to the world fails." ✓ consistent.

**Part IV — Expectation/surprise** — noted.

**Part III — "The respect is the query"** — "A production question has a Q that reads an output port and a C containing interventions on upstream ports." Fine.

**Part III — Contracts have provenance** — "A claim that an episode *found* a question requires ρ_p = constructed for the contract in question, with the witness." OK.

**Part II — "Kinds are edit-signatures"** — the definition issue with bijection of footprints and value domains. And the causal/measurement/rule "families of signatures" descriptions:

"- a causal assignment has a signature that changes under intervention on its output port and under replacement of the component, and is invariant under observation edits;"

Wait — "a signature that changes under ... replacement of the component"? Replacement of the component would change which j we're talking about — the signature sig_C(j) is a function of j; "changes under replacement of the component" is trivially true of any component (a different component has a different signature) or trivially false (if it's the same relation). Hmm, "a causal assignment has a signature that changes under intervention on its output port" — sig_C(j) = {(a,b,L_j(a,b))} over (a,b) ∈ C. "Under intervention on its output port" — an intervention is an edit a ∈ C. For the signature to "change under intervention", we'd have to compare signatures across contracts or across edits... The signature is a set indexed by all (a,b) ∈ C — it doesn't "change under" an edit; it *includes* every edit's relation. To say it "changes under intervention on its output port" presumably means L_j(a,b) ≠ L_j(1,b) when a intervenes on j's output port. OK, that's a readable gloss. But "a measurement has a signature invariant under interventions on the measured port" — L_j(a,b) = L_j(1,b) for a intervening on the measured port ✓. "a rule application has a signature invariant under interventions on the world and variable under edits to the rule" ✓ plausible.

Hmm, but there's a subtle issue: "a causal assignment has a signature that changes under intervention on its output port" — for a genuine causal component, intervening on its output port *replaces* the component ("an edit that sets a port replaces the component assigning that port")! So under intervention on j's output port, j is replaced and its relation is the setting-relation. Then every component that "assigns" its output port trivially "changes under intervention on its output port" — including measurements? A measurement assigns its *reading* port; intervening on the *measured* port (world side) leaves the measuring relation intact ✓. So the discriminator is which port the intervention targets. Hmm — but then "causal assignment... changes under intervention on its output port" is trivially true for every component whose output port is settable, since setting replaces the component. So this family-of-signatures criterion doesn't discriminate causes from anything. Compare "a rule application has a signature invariant under interventions on the world" — a rule's ports are cases and statuses; intervening on "the world" isn't even a port of the rule component. The three descriptions are informal ("These are descriptions of patterns in (K), not additional data") — they are vague and arguably circular (they use "its output port", "the measured port", "the world", "the rule" — the very role terms they claim to derive). Indeed! The whole point of "Roles are derived" and "kinds are edit-signatures" is that cause/measurement/rule are derivative notions. But the definitions of causal assignment / measurement / rule application *use* "its output port" (role), "the measured port" (a role: what it measures), "the world", "the rule" — i.e., they presuppose the roles they're meant to define. A measurement is defined as "a signature invariant under interventions on the measured port" — but "the measured port" is only defined once you know it's a measurement. Circular. Similarly "intervention on its output port" uses "output", which is derived from determination — but that derivation is independent. Hmm, "output" is defined in the Roles section ("A port is an output of component j when its value is determined by L_j given the other ports"). So "its output port" is defined. "The measured port" — not defined except via the measurement notion. For the measurement case: "a measurement has a signature invariant under interventions on the measured port and variable under edits to the measuring relation" — "the measuring relation" is the component itself presumably; "the measured port" is a port of... the component's footprint? If measured port = the port of the footprint that is an input (not determined by L_j)... then "interventions on the measured port" = edits setting that input port. Hmm, could be made precise. "variable under edits to the measuring relation" = edits that alter L_j itself. And the causal case: "changes under intervention on its output port" — the output port per the derived definition. And "invariant under observation edits" — observations defined in the Roles section ✓ ("A port is an observation when A contains an edit that alters the relation reporting it without altering what it reports"). Hmm OK — the definitions might be reconstructible. The circularity charge is weakest for "measured port". Eh — I might include a softer version: the three family-descriptions use role terms ("its output port", "the measured port", "the rule", "the world") that are either derived under other definitions or not defined at all ("the measured port", "the world", "the rule"), so the promised reduction of cause/measurement/rule to (K) is not actually carried out. Medium defect.

Let me also check Part II's claim "A deleted component imposes the full relation on its ports." ✓ consistent with Part VII's eliminative treatment.

"An edit that sets a port replaces the component assigning that port; it does not add an equation beside an incompatible one." — the "assigning" notion not formalized (ties into my reversed-calculation defect). And "A changed rule is a changed component" ✓.

**Roles are derived** — "A port v is an input under A when A contains an edit that sets v directly." Hmm — so inputs are ports that some edit sets. In the shadow example, H and θ are inputs ✓ L is output ✓. But "The direction of an organization is a consequence of which edits it admits" — combined with "an edit that sets a port replaces the component assigning that port" — the notion "assigning" is needed to know which component the edit replaces, and it's role-like. But per the Roles section, "output of component j" is derived via determination. For the cot-relation component over {H,θ,L}: given the other ports, L is determined (for θ ∉ {0,π/2}); also H is determined givenθ, L. So the component assigns both H and L?! Then "an edit that sets a port replaces the component assigning that port" — setting H replaces the cot-component → the relation L = H cot θ disappears → the forward organization is NOT faithful under interventions on H. That directly contradicts Part VII's "The forward organization is faithful under this contract." So the formalism has a real bug: whether the forward organization is faithful depends on which component the edit replaces, and "the component assigning that port" is ambiguous — with the derived notion of assignment (determination), the cot-component assigns both H and L and is replaced by the intervention on H, destroying the shadow computation. So Part VII's central example fails to be derivable from Part II's rules. Unless "assigns" = "has it as an output in the sense of determination given other ports" and when multiple components assign the same port... For a single component with relation R = {L = H cot θ}: is L determined given H,θ? Yes. Is H determined given L,θ? Yes (H = L tan θ). So the component assigns both. The example needs a formalism where an equation is oriented (L := H cot θ) — precisely the "stipulation about which way an equation is read" that Part II's "Roles are derived" denies is needed. This is a strong internal contradiction: Part II ("The direction of an organization is a consequence of which edits it admits, not a stipulation about which way an equation is read") vs. Part VII's example which requires oriented assignment (the same relation read one way is faithful and read the other way is not). And under (O) the two readings are literally the same relation, so the semantics as formalized cannot distinguish the faithful forward calculation from the unfaithful reversed one — refuting the document's claim in "What (E) excludes" that the reversed calculation is excluded.

Hmm, but wait — is that fully right? Let me think about whether the forward vs reversed organizations can be distinguished in the (O) formalism via *footprints*. Forward org: component j with footprint {H,θ,L} and relation R, plus edit-mechanism components for H and θ. Reversed org: component j′ with footprint {L,θ,H} (same set!) and relation R. Same. Yes — identical data. Unless the reversed org's footprint is {H,θ,L} but with the roles... no roles in the data. So identical. Unless the *edit* differs: in the reversed org, the edit "set L" is admitted (identification contract) and "set H" is not. But then the difference is in A (the contract), and the claim "the reversed calculation is not [faithful] under the production contract" — if we feed the reversed org the production contract (edits setting H and θ), what happens? "An edit that sets a port replaces the component assigning that port" — the assigning component of H. If we can't say which component assigns H (both readings), the model is undefined. If the cot/tan component assigns H (the tan reading), then it's replaced and faithfulness fails ✓ the document's claim. If it assigns L (the cot reading), it survives and faithfulness holds ✗. Since the data don't determine the assignment, the example underdetermines the verdict. And since (O)'s L_j(a,b) is just a relation, both readings have identical L_j — so (F1) and (F2) evaluate identically — the verdict must be the same for both. Therefore "the forward organization is faithful... The reversed calculation is not" cannot both hold in the formalism. Solid defect.

But hold on — maybe the intended formalization is that L_j(a,b) *already encodes* the edit's effect on the component: L_j(a,b) for a = "set H" is the relation imposed by j under that edit. If "set H" replaces j, then L_j(setH, b) = the full relation (no constraint) or {H = h}. So the difference between forward and reversed orgs is exactly in how L_j(setH, b) is supplied: forward supplies L_j(setH,b) = {L = h cot θ} (j survives), reversed supplies L_j(setH,b) = full relation (j replaced). But then L_j data differs between the two organizations — and the *world* D's L_j is what it is. The point of "Roles are derived" is that this L_j(a,b) data determines everything. So "the reversed calculation" = an organization whose L_j(setH,b) is the full relation (it computes H, so setting H destroys its constraint). And "the forward" = an organization whose L_j(setH,b) = the cot constraint. But now — the two organizations have *different L data* and the same bare relation at (1,b). So they're distinguishable ✓. But then "the reversed calculation H = L tan θ" is not characterized by its equation but by its L_j(a,b) profile — and the doc's statement "The reversed calculation H=L tanθ is not [faithful]: intervening on H changes the target's L but not the calculation's H" — under this reading, intervening on H destroys the reversed org's constraint entirely: both its L and its H are... H is set (changes to h) and L is free. The target's L changes (to h cot θ) and the calculation's L is undetermined — so Part V's phrasing is right and Part VII's ("not the calculation's H") is wrong. And the claim "roles are derived" is fine since L_j(a,b) data carries the orientation. Hmm! So the orientation lives in the L_j(a,b) data (how the component responds to edits), which is exactly "edit-signature". OK so the forward/reversed distinction IS representable — via differing L_j(a,b) at the setting-edit. Then my "identical data" objection fails if we allow the two organizations to have different L profiles. But then the document's phrase "The reversed calculation H=L tanθ" — presenting the distinction as one of equation-writing — is misleading but not fatal; the real distinction is in the edit-response data. Hmm. But then Part VII's *stated reason* ("intervening on H changes the target's L but not the calculation's H") remains wrong: intervening on H sets the calculation's H (it changes to the intervention value); what the calculation fails to produce is a determined L. Actually hmm — wait. Let me re-read once more: "The reversed calculation \(H=L\tan\theta\) is not: intervening on \(H\) changes the target's \(L\) but not the calculation's \(H\)."

Hmm — maybe "the calculation's H" means: the calculation computes H from L and θ; intervening on the world's H (setting it to h) changes the world's L to h cot θ... and the calculation's computed H = L tan θ = (h cot θ) tan θ = h — which DOES change (to h). So "not the calculation's H" is wrong twice over. Unless the intervention doesn't feed through to the calculation's L input... In the production contract the calculation's inputs are H and θ? The reversed calculation takes L and θ as inputs. If the intervention is on H and the calculation's L input is held fixed (identification-style observation)... ugh.

Simplest robust statement of the defect: Part V and Part VII give contradictory accounts of what fails under intervention in the reversed-calculation example: Part V says the calculation's *downstream* value (L) fails to move; Part VII says the calculation's *H* fails to move. Under an intervention that sets H, the calculation's H moves (it is set), and what is left undetermined/unmoved is its L. So Part VII's stated reason is wrong and contradicts Part V. That's clean and defensible. I'll report it that way, and can mention the deeper issue (the same relation written either way; the distinction lives in edit-response data, and the example as written ("the reversed calculation H = L tan θ") suggests equation-reading determines direction, contradicting "not a stipulation about which way an equation is read"). Maybe fold into one defect or two. Let me fold: the primary quoted sentence with the contradiction and the reason it fails.

**Now Derivation 1 issues.** Let me re-examine to state a defect precisely.

Derivation 1 Claim: "If a transport satisfies (F1) on C, then every active component k of E has the same signature on τ[C] as its anchor λ(k) has on C, up to the port translation."

Proof: "By (K), sig_C(λ(k)) = {(a,b,proj_{V_k} Sol_{λ(k)}(a,b))}..."

But (K) defines the signature of a *component* j as {(a,b,L_j(a,b))}. The anchor λ(k) is a *subnetwork* of D (possibly with several components and hidden ports), for which (K) supplies no signature at all. The proof silently replaces L_j(a,b) by the projected solution of the whole subnetwork — which equals L_j only when the subnetwork is a single component. For multi-component anchors, proj Sol is the join of several relations and is not any component's L. So the first sentence of the proof is not "By (K)". Consequently the claim ("every active component k has the same signature as its anchor") is not established for multi-component anchors under (K)'s definition of signature (which is only defined for components), and the claim that "By (K), (F1) entails that every component of E has the signature of its anchor" (Part V) is false/undefined when λ(k) is a genuine subnetwork. Also — even for single-component anchors — the claim says "the same signature on τ[C]" — the proof notes F1 equates third coordinates "pointwise" — but the first coordinates differ ((a,b) vs (τ(a),σ(b))), so the signatures coincide only after applying (τ,σ) to the indices and the port translation to the relations — "up to the port translation" mentions only the port translation, not τ,σ. Minor.

Also — hmm, another issue with Derivation 1: F1 says proj_{V_k}[Sol_{λ(k)}(a,b)] = L_k(τ(a),σ(b)) — the left side is the projection onto... V_k (E's ports) of a solution over D's ports — ill-typed without applying the port translation (as I noted). And the proof's "sig_C(λ(k)) = {(a,b,proj_{V_k} Sol_{λ(k)}(a,b))}" — again mixing. And moreover (K)'s signature of the anchor at (a,b) would use L_{j}(a,b) — the *supplied* relation — while proj Sol_{λ(k)}(a,b) is a *derived* quantity. For a single-component anchor, proj_{V_j} Sol_{{j}}(a,b) = L_j(a,b) ✓ fine.

So Derivation 1's defect: the proof's key identification is not "By (K)" when anchors are subnetworks with hidden ports — (K) gives no signature to subnetworks, and the projected solution relation is generally not equal to any component relation (it is the existentially-quantified join of the anchor's components, strictly smaller than each). Counterexample: anchor with two components sharing a hidden port: components j1 on {x,h} with h = x, j2 on {h,y} with y = 2h. Then proj_{x,y} Sol = {y = 2x}, while L_{j1} = {h = x} over {x,h}. The signature of the anchor under (K) (undefined, or if you take either component) ≠ the projected relation. Hence "By (K), sig_C(λ(k)) = {(a,b,proj Sol...)}" is false as stated. Good, precise.

Then the corollary: "A condition 'each component must anchor to a component of the same kind' adds nothing to (F1) on any contract." — the corollary's "Where the contract separates two kinds, (F1) already distinguishes them" — hmm, but F1 says nothing about which anchor λ(k) is chosen; a wrong anchoring is caught only if F1 fails for that anchoring. Fine.

But here's a deeper problem with the corollary and Part V's "Why there is no anchoring condition": the claim is that an anchoring condition is "either redundant or unevaluable... There is no third case." But consider: two components j, j′ of D of *different* kinds on C (separated by some edit), and E's component k anchored to j′. F1 then forces k's relation to track j′'s. Is that "wrong"? The account would model the wrong part of the world — e.g., model the thermometer reading as the cause of fever. But F1 is satisfied with λ(k) = thermometer-j′. So (E) would count "the reading causes the fever" as an account — no wait, the anchoring λ says k stands for j′ (the reading), and the account's answer profile must match (A). Hmm — the anchoring condition ("each component of E must anchor to a component of D of the same kind") — since F1 forces same-kind (via Derivation 1's claim), any k anchored to j is automatically the same kind as j. So the condition really is redundant *if* Derivation 1 holds. But the real protection against anchoring the wrong part of the world is (A) + F2 (the whole match). OK.

But here's the thing: the doc says the anchoring condition would be "unevaluable" where C separates... hmm: "Where C contains a change separating two kinds, (F1) already fails for a component anchored to the wrong one." — Is that right? If k is anchored to j′ (of kind K′) but "should" be anchored to j (kind K), F1 with λ(k) = {j′} succeeds (k matches j′). F1 does NOT fail — the anchoring is just to a different part of D. F1 would fail only if λ(k) = {j} while k's relation matches j′'s. So the sentence "Where C contains a change separating two kinds, (F1) already fails for a component anchored to the wrong one" is only true if "the wrong one" means the anchor's relation differs from k's — but if k's relation genuinely matches the wrong part of the world, F1 holds and the account still models the wrong part. Whether that's a defect depends on the intent: the "anchoring condition" they discuss is "each component of E must be tied to a component of D of the same kind" — i.e., kind-matching between k and λ(k). Under Derivation 1, F1 forces that. OK so the sentence is about kind-matching, fine-ish given Derivation 1. But given Derivation 1 is broken for subnetwork anchors, the corollary inherits the flaw. I'll report Derivation 1's proof flaw and mention the corollary.

Hmm, but actually — there's still something to say: is kind-preservation under F1 even true for single-component anchors? sig of k on τ[C] = {(τ(a),σ(b), L_k(τ(a),σ(b)))} and sig of j on C = {(a,b,L_j(a,b))}, and F1 gives L_k(τ(a),σ(b)) = proj_{V_k} Sol_{λ(k)}(a,b) = L_j(a,b) (single-component anchor, with port translation applied). So signatures match up to relabeling ✓. Fine for single-component anchors. For multi-component anchors, "the signature of its anchor" is undefined by (K). ✓ my defect.

**Now — check Derivation 2 more carefully** for the defect statement. Claim: "Two candidates 𝓔, 𝓔′ for the same p that both satisfy (F1), (F2), and (A) on C are one account at grain C: their components are pairwise of one kind on C and their answer profiles coincide."

Counterexample: as constructed above (E with 2 components vs E′ with 1). Also even with same number of components: E's components anchored to subnetworks {j1}, {j2} and E′'s to {j1, j2}... Let me build a minimal explicit counterexample:

Let D have ports x, y and two components: j1 with footprint {x}, L_{j1} = {(0)} (x=0)... hmm let me use relations. Let D: V = {x,y}, J = {j1, j2}, V_{j1} = {x}, V_{j2} = {y}, L_{j1}(1,b) = {x = 0}, L_{j2}(1,b) = {y = 1}. Sol_D = {(0,1)}.

Candidate 𝓔: E with ports x,y and two components k1 (footprint {x}, L = {x=0}), k2 (footprint {y}, L={y=1}); λ(k1)={j1}, λ(k2)={j2}; π = id. F1 ✓ (projections match). F2: π[Sol_D] = {(0,1)} = Sol_E ✓. Question: Q reads... say the full valuation; Ans equal ✓ (A). NonCircular? Hmm — for the full (E) we'd need more, but Derivation 2 only assumes F1, F2, A. Fine — but the claim says "are one account at grain C" — "one account" presumably means satisfying (E) and being the same account. Even granting the F1/F2/A parts:

Candidate 𝓔′: E′ with the same ports x,y but ONE component k′ with footprint {x,y}, L = {x=0 ∧ y=1}; λ(k′) = {j1,j2} (a subnetwork with hidden... no hidden ports here; footprint {x,y}). F1: proj_{x,y} Sol_{{j1,j2}}(a,b) = {(0,1)} = L_{k′} ✓. F2: π=id, Sol_{E′} = {(0,1)} = π[Sol_D] ✓. A ✓.

Now "their components are pairwise of one kind on C": 𝓔 has two components, 𝓔′ has one. No bijection between {k1,k2} and {k′}. So the claim's first conjunct is false. Even if we matched k′ to k1: footprints {x,y} vs {x} — bijection impossible. So Derivation 2's claim is false. Also its proof ("Immediate from Derivation 1 and (A)") is a non sequitur: Derivation 1 relates components to their own anchors within one candidate; nothing relates𝓔's components to 𝓔′'s.

Also the phrase "one account at grain C" — Derivation 2 conflates "indistinguishable" with "identical": two candidates with different component structures (decompositions) can satisfy the same conditions. The consequence paragraph ("Underdetermination of an account by a contract is not a failure...") rides on the false claim. Also note the title "Indistinguishable is identical" — but the claim only covers candidates sharing F1/F2/A — and even then the decompositions can differ (as shown), so "identical" is false. Also — even answer profiles coincide is trivially from (A). Also: "at grain C" vs "at grain ℓ" — (K) uses contract C; the phrase "at grain C" mixes grain and contract indices. Minor.

Great — Derivation 2 is a headline defect.

**Also check Part 0's grievance 2** — "A content is a whole organization, which can contain components that no change at that level separates — and then they are one kind at that level" ✓ consistent.

**Part 0's constitutive conjecture includes "(iv) their physical realization"** — and Part XIV's primitives include Θ. OK.

**Part 0: "Nothing in the definition of an explanation asks whether a piece of the explanation 'is the same kind of thing' as a piece of the world."** — consistent with Part V's "Why there is no anchoring condition". OK.

**Check (A)'s reference "all four conditions of Account"** vs (E)'s six — count defect. Let me also check Part V's count: headings: Component fidelity (F1 & F2), Question fidelity (A), Non-circular dependence, Non-vacuity, Baseline coherence = five conditions, six formulas. Part 0 says "all four conditions of Account (Part V)" (twice: "the four conditions" in (A) and "satisfies all four"). Also "Produce a candidate that satisfies all four conditions of Account (Part V) on a physically admitted contract, with a non-declared transport, and that plainly explains nothing. This would show change-fidelity is not enough." And Part IV's phrase "the five checks" — no wait that's the skill. The document's Part V: "(E) ⟺ (F1) ∧ (F2) ∧ (A) ∧ NonCircular ∧ NonVacuous ∧ Coherent". So "four conditions" is wrong. Also Part 0: "An explanatory candidate... satisfying all four" hmm. And Grievance... Also "Where to attack this (B)": fine.

Also note (E)'s six conjuncts vs Part 0's "change-fidelity is not enough" — fine.

Also Part V says "each a condition on supplied relations under the changes in C" then Baseline coherence: "Sol_E(1,σ(b_0)) ≠ ∅" — at the baseline only, and Non-vacuity's "stated scope" clause. The claim "Every conjunct is a condition on how supplied relations behave under the changes in C. None inspects a label." — contradicted by "excluded by a stated scope" (a condition on the record of statements) and "as an unanalysed boundary input" (a condition on grain/analysis, not on relations). Good defect.

**Part VI's "Finite monotone theorem"** — headline defect (sets swapped / claim false).

**Part IX (K3)** — headline defect (invalid inference).

**Obstruction "Twenty-four tokens"** — clear false statement.

**Baseline coherence "not implied"** — clear false statement.

**Non-circular dependence's exhaustive disjunction** — headline defect.

**Derivation 3** — the claim/proof mismatch (varies L, not t; "value at (a,b)" undefined for transports). Also maybe the claim is false as stated: "For every (a,b) ∈ C∖H there exists a transport t′, also surviving on H, with a different value at (a,b)" — if we read "value at (a,b)" as the expectation Ans_S(τ(a),σ(b)) or π(Sol_D(a,b))... Take a case where C∖H = {(a,b)} and where F1/F2 are not required outside H... The claim intends: selection leaves unseen changes undetermined. As formalized: transports have no values at (a,b). Let me also double-check whether the intended reading could make the claim false even charitably: Suppose the transport must satisfy (F2)-like constraints that tie values across edits (e.g., if τ must be a homomorphism and Sol must respect composition — no such requirement). Suppose instead that "surviving on H" requires fidelity on H only ✓. Then altering L_k at one unseen (a,b) keeps survival. But is the altered t′ a transport? t is the quadruple; altering L alters E (the organization), not t. So there is no t′ ≠ t. The claim "there exists a transport t′ ... with a different value" is false/ill-posed: what exists is a different *interpretation* or *organization* paired with the same t. Alternatively, if "value at (a,b)" means "the value the transport assigns" — π(Sol_D(a,b)) — that is determined by π and D and cannot be changed without changing π, and changing π changes values at history points too (π is edit-independent!) — π: X_D → X_E is a single map not indexed by (a,b). So "a different value at (a,b)" while preserving fidelity on H requires changing π only at points that occur only at (a,b)... Since π is unindexed, you can change π at valuations that appear only in Sol_D(a,b) for (a,b) ∉ H. Hmm — possible in some cases but not guaranteed (e.g., if Sol_D(a,b) ⊆ ⋃_{h∈H} Sol_D(h) — the unseen edit produces only valuations already seen). Counterexample to the claim (charitable reading via π): Let X_D = {0,1}, X_E = {0,1}; H = {(1,b_0)} with Sol_D(1,b_0) = {0} and C = H ∪ {(a,b)} with Sol_D(a,b) = {0} as well (an edit that changes nothing at the level of valuations, e.g. a relabeling... hmm, non-vacuity excludes relabeling-only contracts but C here includes the baseline). Then any π surviving on H maps0 ↦ (whatever Sol_E(τ(1),σ(b_0)) is). Any π′ differing at (a,b) must differ on Sol_D(a,b) = {0}, i.e., differ at 0, which breaks fidelity on H. So no t′ with different value at (a,b) survives on H. Under the "value = π[Sol_D(a,b)]" reading, the claim fails. Under the "value = L_k(τ(a),σ(b))" reading (altering the interpretation), the proof alters the model, not the transport. Either way the claim as stated ("there exists a transport t′") is false or unproved. Good — I can give this counterexample. Hmm, but careful: in my counterexample, is the "value at (a,b)" of t different... Let me set Sol_E(τ(a),σ(b)) — if t′ must be a transport between the same D and E, then its "value at (a,b)" = π′[Sol_D(a,b)]. With Sol_D(a,b) = Sol_D(1,b_0) = {0}, any π′ surviving on H has π′(0) = π(0). So no differing value. So the claim's universal "For every (a,b) ∈ C∖H there exists..." fails. But maybe the claim intends E and D to be fixed and the *transport* varied... yes. So my counterexample works under that reading. And under the reading where L is varied, the proof varies the wrong object (the organization's interpretation), and moreover the claim would then be about varying E, not t. 

Also Part0 grievance 3 asserts "There is a theorem below (Derivation 3) that selected transports are *always* underdetermined on unseen changes" and Part XV lists "A non-fallible selected transport. A selection history H ⊊ C whose survivor is determined on C∖H. This refutes Derivation 3." — my counterexample is exactly such a case (with the caveat about readings). Nice: Part XV's own defeat condition is met by a trivial case. Well — "whose survivor is determined on C∖H" — in my example the survivor's value at (a,b) is determined (equal to its value at history points) because Sol_D(a,b) = Sol_D(1,b_0). So Derivation 3 is refuted per the document's own Part XV criterion — on the π reading. Hmm, but on the L reading (relations supplied independently), Derivation 3 holds by fiat of the stipulation "The component relations L_j(a,b) are supplied independently for each (a,b)" — which is an unstated assumption (it follows from the absence of constraints in (O), so arguably true in the formalism — but then the claim is about varying L = varying the interpretation/organization, not "a transport t′"). So the defect: the proof varies component relations (interpretation data) while the claim quantifies over transports; a transport is a quadruple of maps with no "value at (a,b)". As stated the claim is ill-typed; and if "value at (a,b)" is read as the transported solution-set π[Sol_D(a,b)], the claim is false when Sol_D(a,b) is determined by history points. Good.

**Part IV's claim of surprise iff** — Derivation 4 issues. Let me consolidate: Claim: "A system can be surprised if and only if it holds a transport selected on a history H strictly smaller than the contract C of changes the world admits." Problems: (i) "the contract C of changes the world admits" conflates the question-contract C (a declared subset, Part III) with the world's admitted changes (Θ). (ii) The "if" direction fails: H ⊊ C does not make surprise possible — surprise also requires that fidelity actually fail (or be able to fail) at some (a,b) ∈ C∖H and that such a pair occur; a transport faithful on all of C with H ⊊ C can never be surprised. (iii) The "only if" direction has an unexcluded case: per Part IV's own definition, surprise presupposes a *selected* transport ("Let t be selected on history H"), so the necessity direction is true by definition rather than derived, and the proof only handles "no transport" and "H = C" — it never considers a constructed or declared transport, for which surprise is undefined rather than impossible. Also Part IV says "This is derived, not assumed (Derivation 4)" — but the definition of surprise already contains "at (a,b) ∉ H", so the derivation's content is nearly nil (the answer is in the starting points). I'll state this as one or two defects. Maybe one defect on the iff with the counterexample for sufficiency, plus mention the definitional circularity.

Hmm, let me think about whether "can be surprised iff H ⊊ C" is really false. "Can be surprised" = it is possible that a surprise occurs. Surprise = violation at (a,b) ∉ H (with (a,b) ∈ C actually occurring). Violation = fidelity fails at (a,b). Whether fidelity fails at (a,b) is a determinate fact about t and the target. If t is faithful at all (a,b) ∈ C, no surprise is possible — so the "if" direction is false. But maybe "can be surprised" is dispositional/generic: "is the kind of system for which surprise is defined". But then the iff is trivial/definitional. Also the claim says "if and only if it holds a transport selected on a history H strictly smaller than the contract C of changes the world admits" — note "the contract C of changes the world admits" — if C is the world's admitted changes (Θ-level), while Part IV defines surprise with respect to the question contract C of the transport's question... The document's C is "The contract C ⊆ A × B is the set of admitted edit–boundary pairs the claim ranges over" (Part III) — a declared subset of physically admitted edits. Derivation 4's phrase "the contract C of changes the world admits" misdescribes C. If C is a declared contract and the world admits changes outside C, then a system with H = C can still meet an occurring (a,b) outside C... but surprise is only defined for (a,b) ∈ C ("For an edit–boundary pair (a,b) ∈ C actually occurring"). Hmm — so surprise is undefined outside C. Then "a system whose history exhausts its contract cannot be surprised" — but the world can throw changes at it outside the contract and it will have expectations violated... The semantics can't call that surprise. That's a real limitation contradicting Part0/Part IV's rhetoric ("Surprise requires an incomplete selection history, which is to say it requires that the world admit changes the system's correspondence was never shaped against" — "the world admit changes" ≠ "C contain changes"). OK.

I'll write Derivation 4's defect focusing on: the "if" direction is false (counterexample: a transport faithful on all of C, selected on H ⊊ C, can never be surprised), and the claim's necessity direction is built into the definition of surprise (Part IV defines surprise as violation at (a,b) ∉ H), so "This is derived, not assumed" is false — the result is definitional. Plus the conflation of C with the world's admitted changes. Maybe split into two defects: (1) false "if" direction; (2) the "derived, not assumed" claim is wrong (circularity). I might merge as one "Derivation 4" defect to keep the list manageable, but the instructions want one number per defect and the two are distinct defects in the same place. Hmm. I could state them as one defect with the quote of the claim and explain both the failing step and the counterexample. 2-5 sentences for (c). Let me keep each defect focused; I can have two numbered entries for the same heading if they're distinct defects. Let me see total count and prioritize.

Let me now also scan for more defects in Parts I, XI, XII, XIII.

**Part I — "Fallibility without falsehood-as-work."** — doctrine, fine.

**Part XI — Repair (P)** — "∃o ∈ O[¬o(ξ) ∧ o(ξ′)]" fine. (P) "does not rank alternatives. Losses outside P must be exposed." fine.

**Part XI — (EK)** — "Account(c,p_c)" typing. Also (EK) requires "¬o(ξ) ∧ o(ξ′)" for an epistemic obligation and "Origin" at e_c ⪯_h e. Fine. Hmm — (EK)'s definition of "Created explanatory knowledge" requires CreativeCriticalEpisode(s,Δ,h,e) ∧ Repair... ∧ ∃... — fine.

Hmm — one thing in Part XI: "An epistemic obligation requires a correct account, or the correction of a use through one, to be deployable." — fine.

**Part XII — "Possibility is the absence of a law-imposed limit short of perfection on performance and retention; it is not one successful trajectory."** fine.

"(CT2) ... F is monotone; C ⊆ F(C) is the invariant form; the union of post-fixed sets is the greatest fixed point." — Wait, "the union of post-fixed sets is the greatest fixed point" — for monotone F on a complete lattice, the greatest fixed point = the union of all post-fixed points (x ≤ F(x)). ✓ Tarski. But careful: is F monotone given its definition "F(C) = states whose executions all complete and return into C"? F(C) = {z : ∀η ∈ Exec(π,z,i;χ)??...} hmm — F depends also on the protocol and inputs. Monotone in C ✓. But — one subtlety: Tarski's theorem needs F monotone on a complete lattice; the powerset of states ✓. And "the union of post-fixed sets" is indeed the greatest *fixed* point ✓. OK fine. Actually — hmm, one more check: "post-fixed" conventionally means x ≤ F(x) ✓ matches "C ⊆ F(C) is the invariant form". OK correct.

Hmm — but "C ⊆ F(C) is the invariant form" — and then "the union of post-fixed sets is the greatest fixed point" — the greatest fixed point is the largest C with F(C) = C. And it equals⋃{C : C ⊆ F(C)} ✓ standard. Fine.

**Part XIII — Barriers, Universality** — definitions. "A finite list of failures is not a barrier proof; a bypass refutes a proposed barrier." fine.

Hmm (U1): "∀c ∈ 𝔈_Θ ∃χ, Enable(s,U_c,χ) ∧ Can(ξ_0,U_c;χ)" — fine.

**Part XIV — Dependence order.** "(F1), (F2), (A) depend on (O), (Q), (K)." — Hmm, (A) depends on (Q) ✓ and (F1)/(F2) on (O) ✓ and (K)? (F1) doesn't use (K)... well Derivation 1 links them. "(R) depends on (F1)–(F2) and physical provenance" ✓. "Deploy depends on (R) and (CT1)" ✓. "Build depends on histories and (E)" — hmm, Build (Part X) depends on Deploy? "prepares a represented organization for explanatory use" — uses representation (R) and Account? "Build depends on histories and (E)" per Part XIV — but Part X's Build says "prepares a represented organization for explanatory use of c" — "represented" invokes (R), and "explanatory use" — plausibly (E). Minor.

"(N), (G) depend on Deploy and Build" ✓. "(P), (EK) depend on (G), (E), Deploy" ✓. "(K1) depends on (E)" ✓.

Also Part XIV: "Indices, not primitives. Grain ℓ, boundary β, continuity Ω, and the contract C are declared indices. Every claim is relative to them; none is a predicate that could be true or false." — Contradiction with Part III (contracts have provenance; can be found/constructed; "A question can be wrong") and Derivation 5 (a contract can be a content, constructed, new) — if C is a mere declared index that cannot be true or false, it cannot have provenance (selected/constructed vs declared — Part III says ρ_p ∈ {declared, selected, constructed}), nor can it "fail to pick out its alleged target" (be wrong). Part III says "A question may fail to pick out its alleged target, assume an incompatible baseline, or combine incompatible requirements. Its formulation is still an event." — so questions have truth/failure conditions. Part XIV says contracts are indices, "none is a predicate that could be true or false" — direct contradiction. Good defect.

Also Part 0: "Questions have provenance too. A question — its target, its scope of admitted changes, what it asks — can be found as well as answered" ✓ vs Part XIV. Yes.

Hmm — Part XIV might intend: "Every claim is relative to them; none is a predicate that could be true or false" — "none" refers to the indices themselves (as indices they're not predicates). But Part III gives contracts provenance as a property of how they got there — a contract's provenance is a factual claim ("A selected contract is the surviving member of a population under a variation-and-survival history") — that IS a predicate that could be true or false about the contract. And "A question can be wrong" — a question "may fail to pick out its alleged target" — that's a truth-evaluable defect. So the claim "none is a predicate that could be true or false" contradicts Part III. Also the contract C is described in Part XIV as "declared" index — while Part III allows selected and constructed contracts. "Grain ℓ, boundary β, continuity Ω, and the contract C are declared indices" — but a contract can be *constructed* (Part III: "A constructed contract is the result of an episode with a construction witness"; Derivation 5). So calling C a "declared index" contradicts the three provenances of contracts. Good.

**Part XVI Derivation 6:** "Every predicate in Parts II–XIII is defined from Θ (including Org_ℓ) and, where invoked, 𝒩, together with declared indices. Proof. By the dependence order of Part XIV, following each definition to its base." — But the definitions also use the modeller's declarations beyond "indices": contracts' provenance includes "declared" (a stipulation by the modeller — not derived from Θ), obligations O_p ("a question may... combine incompatible requirements"; "claimed obligations O and protected obligations P" — where do obligations come from? "a normative relation 𝒩, when a question invokes one" — obligations might come from 𝒩 or be declared), "a declared restriction operation" (Part VI), "a declared family 𝒱 of organization edits" (Part VI), "the declared use task U", "an independently specified J_p" (Part XIII), "a declared scope", "the declared grain"... Lots of "declared" inputs. The claim "together with declared indices" lumps these in — but obligations, use tasks, families of edits, and restriction operations are not among the four indices named in Part XIV (ℓ, β, Ω, C). So Derivation 6's claim ("two primitives plus declared indices") understates the primitive/declared inputs: the semantics also needs declared obligations, declared families𝒱, restriction operations, use tasks U, independently specified J_p and C_I, the "stated scope" records. Also the provenance notions Sel/Con require a *history* — physical ✓Θ. Hmm. And "Dec(t)" — declaration by the modeller — is not derived from Θ: (R) says representation requires Sel or Con — fine. But Dec is a provenance value determined by "Neither of the above" — derived negatively ✓. But the *modeller's act of declaration* enters via contracts'ρ_p = declared. OK — Derivation 6's proof is a one-line appeal to the dependence order which itself lists "declared indices" — but the categories named in the definitions exceed the four indices. Medium defect.

Also Derivation 6's consequence: "There is no residual predicate meaning 'explains,' 'represents,' 'is a cause,' or 'is knowledge.'" — hmm, but (E) is literally a definition of what it is to be an explanatory account... The claim is that no *primitive* predicate with that meaning remains; fine-ish.

**Derivation 7** — fine (consistency of indexed claims). "The claim '𝓔 is an account on C' and the claim '𝓔 is not an account on C′' are claims at different indices and may both be true." ✓.

**Derivation 8** — the mischaracterization of (E) as "a conjunction of equalities and existence claims" (it contains the "stated scope" and "unanalysed" clauses, which are not preserved-point claims). Also (EK) includes Deploy with "retained capability" involving law-imposed limits (CT1/possibility) — "structure-preserving bijections" of what? IfΘ isn't transported, then terms like "law-imposed limit" aren't preserved by bijections of carriers alone. The claim "Transporting all carriers, relations, transports, histories, and contracts along structure-preserving bijections preserves (E), (G), (P), (EK)" — (E) includes Non-vacuity's requirement that "every physically admitted edit excluded from C is excluded by a stated scope" — transporting along bijections preserves what's *stated* only if the record is transported too — "all carriers, relations, transports, histories, and contracts" — not the *statements*. Eh — I'd rather use the "equalities and existence claims" mischaracterization as the defect, noting Non-vacuity's "stated scope" and Non-circular's "unanalysed" clauses aren't of that form. Medium.

Hmm, wait — also (E)'s NonCircular existential is "There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ..." — "removes or replaces" — again not formalized (what edit "removes a block of Γ"?). More unformalized vocabulary inside the supposedly formal conditions. I'll fold this into the NonCircular/Non-vacuity defect or the "(E) is claimed structural but contains non-formal clauses" defect. Let me structure:

Defect: "(E) claims: 'Every conjunct is a condition on how supplied relations behave under the changes in C. None inspects a label.' But Non-vacuity's clause requires that exclusions be 'excluded by a stated scope', and Non-circular dependence requires that the answer not appear 'as an unanalysed boundary input' and speaks of edits that 'remove or replace a nonempty block of Γ' — none of these is defined in the data (V, X, J, B, A, L, C, Q), and 'stated' concerns the modeller's prose, which no field records. So (E) is not a well-defined predicate on its own data, and the claim that it inspects no labels/records is false."

Good.

**Also check Part V's (F2) claim in "What (E) excludes" about reversed calculations** vs Part VII — the contradiction I noted.

**Check Part VII's identification claim (I2) label ordering and the missing (I3).** The text: "(I1)... (I2)... (I4) Repeating rows changes no kernel; an independent calibration can." Wait — where's (I3)? The labels used: (I1) feature identified at y; (I2) f = f̄∘g; then "(I4) For linear A and featurec^⊤, identification is ker A ⊆ ker c^⊤. Repeating rows changes no kernel; an independent calibration can." Hmm, actually re-reading: "(I1) It is identified on every attainable y exactly when f = f̄ ∘ g for some f̄. (I2) For linear A and feature c^⊤, identification is ker A ⊆ ker c^⊤. (I4)" — the parenthetical labels come AFTER the statements. Let me re-read exactly:

"The fibre at y is Z_y = g^{-1}(y). The feature is identified at y exactly when Z_y ≠ ∅ ∧ |f[Z_y]| = 1. (I1) It is identified on every attainable y exactly when f = f̄ ∘ g for some f̄. (I2) For linear A and feature c^⊤, identification is ker A ⊆ ker c^⊤. (I4) Repeating rows changes no kernel; an independent calibration can."

So labels: (I1) after the first claim, (I2) after the second, (I4) after the third. (I3) is missing — the linear claim is labeled (I4) and there's no (I3). Also Part XV references "(I2)" only. So the label (I3) is skipped. Trivial cosmetic defect. And note the linear statement is labeled (I4) but referenced nowhere; Part XV lists "(I2), (O1), (T2), (CT2)" as the mathematical items open to counterexample — skipping (I1) and (I4) — inconsistent. Eh. I might include the missing (I3) as a minor defect at the very end, or skip it. The audit says "and any other defects" but "Report only defects you can state this way" (with location, quote, wrongness). The missing (I3): quote "(I4) For linear A and feature c⊤..." wrong: the labels jump from (I2) to (I4), so the label (I4) refers to nothing and the numbering promises a missing (I3). It's trivial. I'll include at the end as a low-priority numbering defect — maybe. Let me decide later based on total count.

**Check Part VI's infinitary example:** "Γ = {d_n : |x| ≤ 1/n}: every unbounded index set determines x = 0" — hmm, "unbounded index set" — a set of indices I⊆ ℕ unbounded. The constraints {|x| ≤ 1/n : n ∈ I} determine x = 0 iff inf_{n∈I} 1/n = 0 iff I unbounded ✓. "no minimal support and no singleton witness exists" ✓. But "(B) records the collective contribution" — (B) is CriticalBlock(B;W,p) which quantifies over *nonempty B ⊆ W* — in the infinitary case, is the whole-index-set block "critical"? W = Γ (all constraints): W ∈ S ✓ (determines x=0). W∖B for any proper subset B... if B is finite, W∖B still unbounded → still determines → not critical. If B = Γ, W∖B = ∅ → doesn't determine → critical. So CriticalBlock(Γ; Γ, p) holds ✓. And no proper sub-block is critical... wait B = Γ∖{finite} : W∖B = finite set → not determining → critical ✓. So blocks of cofinite... anyway "no singleton witness" ✓ consistent. Fine. Hmm — but wait: "no minimal support and no singleton witness exists. (B) records the collective contribution." But (B) requires W ∈ S_{E,p} — and Account... fine as illustration. Also — the claim "no minimal support" — minimal *support* = minimal member of S. S = {W : W unbounded}. Minimal members: none ✓ (every unbounded set has an unbounded proper subset). ✓.

Hmm — but Part VI says "No upward closure and no minimal member are assumed" and then the theorem assumes upward closure. Fine.

But hold on — is S upward closed in general? S = {W : Account(E|W,p)}. Adding commitments can break the account (interference!). Part VI's own "Interference" example: S = {{a}} with Γ = {a,b} — not upward closed ({a,b} ∉ S). ✓ consistent with "No upward closure assumed". And the theorem assumes upward closure as a hypothesis. Fine.

**Check the "Finite monotone theorem" hypothesis "Γ ∈ S"** — fine.

Let me also verify my swapped-sets reading once more, since it's a headline defect. Statement: "the critical singletons are exactly ⋂ min S and the globally indispensable ones are exactly ⋃ min S."

Proof text: "A member of a minimal support is critical for it. If d is critical for W, finiteness gives a minimal U ⊆ W; if d ∉ U, upward closure makes W∖{d} a support. Deletion of d from Γ preserves support exactly when a minimal support omits d."

From the proof: (a) members of minimal supports are critical (for that support) → every member of ⋃ min S is a "critical singleton" (critical for some W). (b) If d is critical for W then d ∈ every minimal U ⊆ W — in particular d ∈ some minimal support. So d critical (somewhere) ⟹ d ∈ ⋃ min S. Combined: critical singletons (somewhere) = ⋃ min S. (c) "Deletion of d from Γ preserves support exactly when a minimal support omits d" → deletion destroys support (d indispensable) ⟺ every minimal support contains d ⟺ d ∈ ⋂ min S. So globally indispensable = ⋂ min S.

The theorem states the exact reverse. So either the theorem statement has its two sets swapped, or "critical singletons" means "singletons critical for the full Γ", in which case critical-for-Γ = ⋂ min S (matches first clause) but then "globally indispensable" (deletion destroys support) is the same set⋂ min S, and the second clause (⋃ min S) is false. On any reading, one clause is false. And the proof supports the swapped reading. Solid.

Counterexample to give: Γ = {a,b}, S = { {a}, {b}, {a,b} } (the document's own redundant-routes S). min S = {{a},{b}}. Under the proof's own notion (critical for some W): a is critical for W={a} since ∅ ∉ S. So the critical singletons = {a,b}, but ⋂ min S = ∅. And neither a nor b is globally indispensable (deleting either leaves a support), yet ⋃ min S = {a,b}. Both clauses of the theorem fail. 

And Part XV lists "A mathematical error. A counterexample to the finite monotone theorem... " as a defeat condition — so this defect defeats the document on its own terms. Worth noting.

Hmm — one caution: "the critical singletons" — maybe it means "the elements that are singletons in⋂ ... " ugh. Or "critical singletons" = the singletons {d} such that CriticalBlock({d}; Γ, p)? Under this reading: CriticalBlock({d};Γ) ⟺ Γ ∈ S ∧ Γ∖{d} ∉ S ⟺ d ∈ ⋂ min S ✓ first clause true! And "globally indispensable ones" = same notion? "Deletion of d from Γ preserves support exactly when a minimal support omits d" — the proof's last sentence is about deletion from Γ = the same as critical-for-Γ. So under this reading, the proof's last sentence establishes clause 1 (= ⋂ min S) but clause 2 ("globally indispensable = ⋃ min S") contradicts the proof's own last sentence ("deletion preserves support exactly when a minimal support omits d" → deletion destroys support ⟺ d ∈ ⋂ min S, not ⋃). So clause 2 is false and contradicts the proof. AND the proof's first sentence ("A member of a minimal support is critical for it") would then be irrelevant/mismatched. So on the second reading: clause 2 is false, contradicted by the proof's own last sentence. Either way — defect. I'll present the counterexample via the document's own redundant-routes instance and note both readings.

Hmm, but also "globally indispensable" might mean "critical for every W ∈ S"? d is critical for every support W: for Γ={a,b}, S={{a},{b},{a,b}}: is a critical for W={b}? {b}∖{a} = {b} ∈ S → not critical. So a not globally indispensable-under-this-reading;⋃ min S = {a,b} ≠ ∅. Hmm. Let me test this reading's truth: d critical for every W ∈ S. If min S = {{a},{b},{c...}}. Take Γ = {a,b}, S = upclosure of {{a,b}} = {{a,b}}: min S = {{a,b}}. ⋃ min S = {a,b} = ⋂ min S. d critical for every W ∈ S: W = {a,b}; {a,b}∖{a} = {b} ∉ S → a critical ✓; b critical ✓. So "globally indispensable =⋃ min S = {a,b}" ✓ under this reading?? Let me test another: Γ = {a,b}, S = upclosure of {{a}} = {{a},{a,b}}. min S = {{a}}. ⋃ min S = ⋂ min S = {a}. "d critical for every W ∈ S": W={a}: {a}∖{a}=∅ ∉ S → a critical ✓. W = {a,b}: {a,b}∖{a} = {b} ∉ S → a critical ✓. b: W={a}: {a}∖{b} = {a} ∈ S → b not critical. So globally-indispensable-in-this-sense = {a} = ⋃ min S ✓. Hmm interesting — is "d critical for every W ∈ S" ⟺ d ∈ ⋃ min S in general? Take Γ={a,b,c}, S = upclosure of {{a,b},{c}}: min S = {{a,b},{c}}. ⋃ min S = {a,b,c}. d critical for every W ∈ S? Consider d = a and W = {c} ∈ S: {c}∖{a} = {c} ∈ S → a not critical for W={c}. So a is not "critical for every W". So this reading gives ∅-ish... which elements are critical for every W ∈ S? Need for all W ∈ S: W∖{d} ∉ S. Take W = {c}: {c}∖{d} ∉ S requires... if d = c:∅ ∉ S ✓ critical. If d = a: {c} ∈ S ✗. So only c... check c against W = {a,b,c}: {a,b} ∈ S → c not critical for that W. So NO element is critical for every W. Set = ∅. ⋃ min S = {a,b,c}. ✗. So this reading makes clause 2 false too.

OK so: the only reading making clause 1 true is "critical for Γ itself", under which clause 2 is false (and equals clause 1's set). The reading most natural given the proof's first sentence ("A member of a minimal support is critical for it" — critical for the minimal support W=U) makes clause 1 false. So: the theorem contains a false clause under every reading; and under the reading suggested by the proof's own reasoning, the two sets are swapped. Excellent.

**Let me also double-check (F1)/(F2) claim: "(F1) prevents an assembled match from hiding a wrong decomposition. (F2) prevents a set of locally correct pieces from hiding a lost shared constraint."** — fine, descriptive.

"By (K), (F1) entails that every component of E has the signature of its anchor; there is no further condition about kinds to state." — the Derivation 1 issue.

**Check Part V's NonCircular last clause once more:** "There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ while preserving the other boundary conditions, under which the answer profile changes, ceases to be determined in the claimed way, or is left as it was."

Hmm — wait. Could "changes, ceases to be determined in the claimed way, or is left as it was" be listing the possible *outcomes* of the answer profile with the intent that ANY of them is acceptable? That's what makes it vacuous. Or maybe the intent is: the answer profile "changes ... or is left as it was" describes the *edit's effect on the answer* being tracked... no. Actually maybe the intent is a badly garbled attempt at: "under which the answer profile changes [as the target's does]"? Hmm. Or "under which the answer profile changes [if the block was doing the work], ceases to be determined [if it was fixing it], or is left as it was [and then the block is idle]"? In any case as a formal condition it's exhaustive and thus vacuous — the existential is satisfied whenever there's any edit removing some block. Actually — careful: "removes or replaces a nonempty block of Γ" — does every candidate and contract admit such an edit? If C contains no edit that removes a block of Γ, then the existential fails → NonCircular fails. So the condition does constrain C (there must be at least one block-removing edit). But it constrains the *answer's behavior* not at all. The condition is equivalent to "C contains some edit that removes or replaces a nonempty block of Γ". That's the vacuity: it cannot distinguish "p because p" (answer smuggled in) from a genuine mechanism — wait, "p because p" is caught by the earlier sentence (answer not appearing as component). Hmm. But the doc explicitly says "'p because p' fails non-circular dependence" and "A contract consisting only of relabelings, or excluding every change under which the active commitments could matter to Q, does not satisfy non-circular dependence" — the latter is false under the written formal clause: such a contract still contains some edit removing a block (relabeling edits? "A contract consisting only of relabelings" — hmm, relabelings don't remove blocks... then the existential fails ✓ actually. Hmm: "A contract consisting only of relabelings" — do relabelings "remove or replace a nonempty block of Γ"? A relabeling doesn't remove a block. So no edit in C removes a block → existential fails → NonCircular fails ✓ consistent!). And "excluding every change under which the active commitments could matter to Q" — C might still contain edits that remove blocks of Γ (e.g., removing a commitment while its effect on Q is nil... wait "excluding every change under which the active commitments could matter to Q" — if C contains an edit removing a block of Γ, then per the exhaustive disjunction any outcome counts... The doc says such a contract "does not satisfy non-circular dependence" — but per the formal clause it does (as long as some edit removes a block). Contradiction ✓. Because under such a contract, removing the block leaves the answer "as it was" — third disjunct — satisfying the existential. So the document's own gloss contradicts the formal clause. Solid defect with a clear internal contradiction.

Wait, hmm, unless "is left as it was" was intended to be read as "the answer profile [must not be] left as it was" — i.e., a typo. But as written, it's a disjunct. Auditor reports the defect as written. I'll note that if the third disjunct were dropped the condition would make sense — the sentence as written is exhaustive.

Hmm, actually, let me reconsider — maybe the three disjuncts describe what happens to the answer profile in *D and E alike*, i.e., "under which the answer profile [of D and E alike] changes..." Hmm: "There exists (a,b) ∈ C that removes or replaces a nonempty block of Γ while preserving the other boundary conditions, under which the answer profile changes, ceases to be determined in the claimed way, or is left as it was." — one answer profile (Ans_E? Ans_p?). The clause is about the account's answer profile. If the intent were "the answer profile changes in E exactly as it does in D" — no. OK. Report as written.

Now, **priority ordering**. Most serious first. Let me rank by how much falls:

1. **Non-circular dependence is vacuous as formalized** (the exhaustive disjunction) — guts (E)'s anti-circularity condition, one of the load-bearing claims; contradicts the document's own glosses. High.

2. **(K3) is logically invalid** (¬O yields ¬T "since B and I are background") — a false formal claim, contradicts the correct bundle principle; Part IX is load-bearing for criticism. High.

3. **Finite monotone theorem is false** (sets swapped) — a stated theorem refuted by the document's own redundant-routes example; Part XV lists mathematical error as a defeat. High.

4. **Derivation 2 is false** ("their components are pairwise of one kind") — core doctrine "indistinguishable is identical" fails; counterexample with different decompositions. High.

5. **Baseline coherence claimed independent but implied** — false mathematical statement. Medium-high (it's contained — the error is claiming more independence than there is... actually the harm: it's a false statement, but does anything fall? The claim "This is an independent requirement; it is not implied by the conditions above" — false. Nothing else collapses. But it's a clean false statement.) Medium.

6. **Derivation 3 proof varies the wrong object** (L vs t; "value at (a,b)" undefined for transports; counterexample where Sol_D(a,b) determined by history) — plus Part 0 grievance 3 and Part XV's "non-fallible selected transport" defeated by trivial cases. Medium-high.

7. **Constructed provenance's second disjunct collapses construction into selection** — contradicts "exactly one of three", Part X, Part XV, Part 0 grievance 6. Medium-high (core architecture: the two provenances).

8. **Derivation 4's iff is over-strong and definitional** — sufficiency fails (faithful-on-C transport with H ⊊ C cannot be surprised); "derived, not assumed" false since surprise is defined with "∉ H" built in; conflates C with the world's admitted changes. Medium.

9. **Part V vs Part VII contradiction on the reversed calculation** (which value fails to move) + the deeper issue that as written the reversed equation is the same relation. Medium.

10. **Derivation 1's proof misuses (K)** for subnetwork anchors (signature undefined for subnetworks; projected join ≠ component relation) — underpins the "no anchoring condition" claim and Derivation 2. Medium.

11. **(E) claimed to inspect no labels / only supplied relations, but contains "excluded by a stated scope", "unanalysed boundary input", "removes or replaces a block"** — not predicates on the data. Medium.

12. **Part 0 "all four conditions" vs (E)'s six conjuncts** — count inconsistency. Also possibly fold with the declared-transport gap. Low-medium.

13. **Derivation 5 misdescribes contracts and underconstructs the organization** ("A contract C is an organization (a set of edits with a query)" vs Part III C ⊆ A × B; the construction never supplies X_v, footprints, L_j). Medium-low.

14. **Part XIV contradicts Part III on contracts** ("declared indices... none is a predicate that could be true or false" vs contracts have provenance and can be wrong/be constructed). Medium-low.

15. **"Twenty-four indivisible tokens cannot be split equally three ways"** — plainly false (8 each). Low-medium (clear false statement).

16. **Derivation 10's "the two persistence components are of one kind ... on any contract containing it"** false under (K) with thing-indexed edits; wrong citation (Derivation 2). Medium-low.

17. **Derivation 8 mischaracterizes (E)** as equalities and existence claims. Low.

18. **Derivation 6's two-primitives claim** understates declared inputs (obligations, families𝒱, restriction operations, use tasks). Low.

19. **Roles/kinds circularity** ("a measurement ... interventions on the measured port", "a rule application ... edits to the rule") — uses the roles it claims to derive. Low-medium.

20. **Missing (I3)** and Part XV's selective listing. Trivial.

21. **The Part 0 (A) "non-declared transport" proviso vs (E) having no provenance condition** — the sufficiency claim as tested is narrower than (E) as defined. Medium.

Also: check **Part V's (F1) ill-typed projection** (proj_{V_k} of a solution over D's ports) — formal defect. Low-medium. Could fold with Derivation 1.

Also **"Exactly one of three provenances"** vs "Construction may operate on selected material. Selection may continue to operate beneath construction." — a transport both built by an episode and surviving selection satisfies both criteria. Fold with #7.

Also **Part IV's "The one thing (R) takes from outside is Org_ℓ(o)"** — (R) also takes Faithful_C and Sel/Con which need Θ — fine.

Also — **(R) and declared transports**: "A declared transport does not make an occurrence represent anything" ✓ consistent with R.

Let me also check **Part II "Values of ports may be paths, functions, fields, proofs, or histories. Cyclic constraints are admitted. Several solutions remain several."** fine.

**Check Part VII's skew-symmetric example** once more: "The full Leibniz expansion with skewness substituted satisfies (F1) and (F2): every intermediate product is a determinant suborganization whose hidden ports project away." Hmm — F1/F2 concerns transports between the target organization D (the family of matrices with the determinant-invertibility structure) and E (the Leibniz expansion). "with skewness substituted" — substituting a_ji := −a_ij into the expansion gives an expression identically zero for odd n ✓. But wait — does it satisfy (F2)? (F2) requires π[Sol_D(a,b)] = Sol_E(τ(a),σ(b)). Under removal of skewness (a contrast edit), the expansion must revert to the ordinary determinant ✓ plausible. Hmm — "every intermediate product is a determinant suborganization whose hidden ports project away" — hand-wavy but the example is a stipulation. One possible defect: "It satisfies (A): the answer is the absence of invertible matrices in the family, and under each contrast the family contains one." — the contrasts: "remove skewness; remove oddness; remove both" — and Non-vacuity "witnessed by any nonzero odd skew matrix" ✓. Hmm — "Non-circular dependence is witnessed by I₃ under removal of skewness"