Let me carefully analyze the eleven changes against the revised text. Let me go through each one systematically.

First, let me understand the structure. The revised text is given in full. The eleven changes (R11, R22, R31, R32, R33, R35, R36, R37, R40, R42, R52) are listed with old and new wordings. All are CLAIM changes with declarations.

Let me analyze each change:

**R11** (Part IV, Three provenances, Selected, line 195)
- Old: "The transport t is a member of T that survived."
- New: "The transport t is a member of T that survived. A transport **survives on H** when it is a member of T that meets the survival condition on H; fidelity on H without membership in T is not survival."
- Declaration: Part IV now defines surviving on H: a transport survives on H when it is a member of the population that meets the survival condition on H, and fidelity on H without membership in the population is not survival.

This seems to match the declaration. The new wording defines "survives on H" as being a member of T meeting the survival condition on H, and says fidelity without membership is not survival. The declaration says exactly this. Let me check coherence with the rest.

Looking at the text: "A transport **survives on H** when it is a member of T that meets the survival condition on H; fidelity on H without membership in T is not survival."

The definition is clear. Let me check if "survives on H" is used elsewhere. In Derivation 3 (line 566): "it must survive on H (Part IV)". In the Selected provenance section: "A transport survives on H when..." - this is the definition. 

Wait, let me look at R52 more carefully. R52 changes Derivation 3's claim. The new wording says "it must survive on H (Part IV)". This points to Part IV for the definition of "survive on H", which R11 now provides. Good.

But wait - there's a subtlety. R11 says "fidelity on H without membership in T is not survival." This is a negative claim - it says what is NOT survival. But the positive definition is "a member of T that meets the survival condition on H." The question is: does "fidelity on H without membership in T is not survival" add anything beyond the definition? The definition already says survival requires membership in T. So the second sentence is redundant but not wrong. It's clarifying that mere fidelity is not enough.

Actually, wait. The declaration says "fidelity on H without membership in the population is not survival." The new wording says "fidelity on H without membership in T is not survival." These match. Good.

But is there a coherence issue? Let me check the surrounding text. The original text says "A transport survives on H when it is a member of T that meets the survival condition on H." But the survival condition is "requiring fidelity on H." So "meets the survival condition on H" means "is faithful on H." So survival = membership in T AND fidelity on H. The new sentence says "fidelity on H without membership in T is not survival" - which is just the contrapositive of what the definition already says. So this is clarification, not a change of claim.

Actually, wait. The declaration says this is a CLAIM change. If it's truly just clarifying what was already said, why is it CLAIM? Let me re-read: "Part IV now defines surviving on H." Previously, "survived" was used but not formally defined. Now it's defined. So a reader can now conclude the precise definition of survival. That's a claim addition - the definition wasn't there before, now it is. OK, that makes sense.

Let me check R42 and R52 for coherence with R11.

R42 changes "a population of realized transports" to "a population of candidate transports" in Part XII, Selection in the physical module. The declaration says Part XII now describes the population as candidate transports, as Part IV does. Let me check Part IV: "There is a population T of candidate transports." Yes, Part IV says "candidate transports." So R42 aligns Part XII with Part IV. Good.

R52 changes Derivation 3's claim. Old: "it must be admitted, realizable, a member of T, and a survivor of H." New: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

The declaration says: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

Let me compare old and new:
- Old requirements: admitted, realizable, member of T, survivor of H
- New requirements: member of T, transport the physics and the stated construction admit (Part XII), survive on H (Part IV)

The old had "realizable" which is dropped. The new adds "a transport the physics and the stated construction admit (Part XII)" which corresponds to "admitted" from the old. The "realizable" from the old is gone.

Wait, is "realizable" the same as "the physics and the stated construction admit"? Not necessarily. "Realizable" might mean "physically realizable" while "admitted by the physics and the stated construction" might mean something broader or narrower. Let me check Part XII: "The population is the set of transports the physics and the stated construction admit; a transport that would need a part every member of the population is built without is not in it."

So Part XII defines the population as "the set of transports the physics and the stated construction admit." The old wording had "admitted, realizable, a member of T" as three separate conditions. The new wording has "a member of T, a transport the physics and the stated construction admit" - these are essentially saying the same thing, since the population IS defined as the set the physics and construction admit. So "member of T" and "the physics and the stated construction admit" are redundant with each other in the new wording.

Actually, wait. Let me re-read the new wording: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

This could be read as three conditions: (1) member of T, (2) transport the physics and construction admit, (3) survive on H. Or it could be read as two: (1) member of T [which is described as "a transport the physics and the stated construction admit"], (2) survive on H. The comma after T suggests these are separate, but they're essentially the same thing since the population is defined as those the physics and construction admit.

But the key question is: does dropping "realizable" change the claim? The old had four conditions: admitted, realizable, member of T, survivor of H. The new has: member of T, admitted by physics/construction, survivor of H. "Realizable" is dropped.

Is "realizable" the same as "admitted by physics/construction"? Not necessarily. A transport might be admitted by the physics but not realizable in practice. Or "realizable" might mean something about having the right physical parts. If these are different, dropping "realizable" would change the claim by making it easier for a transport to qualify as the differing one in Derivation 3.

But wait - R11 defines "survives on H" as being "a member of T that meets the survival condition on H." And R42 says the population is "the set of transports the physics and the stated construction admit." So if the population is already restricted to admitted transports, then "member of T" implies "admitted." The old list had "admitted" AND "realizable" AND "member of T" as separate conditions. If the population already includes admission, then "admitted" and "member of T" are redundant. And "realizable" might have been a separate physical condition.

Actually, I think the key issue here is whether "realizable" and "the physics and the stated construction admit" are the same thing. Let me look at Part XII more carefully.

Part XII, Selection in the physical module: "A selected provenance Sel(t;T,μ,H) is a claim about a physical history: a population of candidate transports, a physically admitted variation operator, and a survival condition enacted by the environment. It is fallible and checkable as any physical claim is. The population is the set of transports the physics and the stated construction admit; a transport that would need a part every member of the population is built without is not in it."

So the population = {transports the physics and the stated construction admit}. This is the definition.

R42 changes "realized" to "candidate" in this passage. The declaration says Part XII now describes the population as candidate transports, as Part IV does.

Now, the old wording of Part XII said "a population of realized transports." The new says "a population of candidate transports." But then the last sentence already says "The population is the set of transports the physics and the stated construction admit." So the change from "realized" to "candidate" aligns the description with the definition. Previously, it said "realized transports" but the definition says "transports the physics and the stated construction admit" - which might be broader than "realized." The change to "candidate" fixes this inconsistency.

Wait, but is "candidate" the right word? Part IV says "a population T of candidate transports." So yes, "candidate" is consistent.

But here's a potential issue: the declaration for R42 says "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." But the last sentence says "a transport that would need a part every member of the population is built without is not in it." This last sentence is about what's NOT in the population. The second-to-last sentence says "The population is the set of transports the physics and the stated construction admit." So the declaration correctly references the definition.

Now back to R52. The old wording had "admitted, realizable, a member of T, and a survivor of H." The new has "a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

The old had "admitted" and "realizable" as separate conditions. The new drops "realizable." But if the population already requires admission (as defined in Part XII), then "member of T" and "admitted" are the same. And "realizable" - if it was about physical realizability beyond what the physics admits - is now dropped.

But wait - maybe "realizable" was just a way of saying "the physics admits it." In that case, the old list had redundancy and the new list is cleaner. Or maybe "realizable" meant something additional (e.g., you can actually build it). This is ambiguous.

Let me think about this differently. The declaration for R52 says it "replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." The declaration doesn't say it adds or removes requirements - it says it replaces the list. But the new list is different: it drops "realizable." If "realizable" was an additional requirement, dropping it changes the claim.

Actually, looking more carefully at the declaration: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H." This describes three requirements. The old had four. The declaration seems to acknowledge this is a replacement but doesn't explicitly say "realizable" is being dropped or absorbed.

Hmm, let me think about what "realizable" could mean in context. In the physical module (Part XII), we see: "A theorist's description of a protocol is not the system's possession of it." and "Can_{Ω,β}(ξ,T;χ) requires an owned retained realization or an owned, physically admitted, finite construction of one." So "realized" in the physical module context might mean something like "actually having a physical implementation." But "candidate transports" are just potential transports, not necessarily realized ones.

So R42's change from "realized" to "candidate" could be significant: it broadens the population from actually-realized transports to merely possible/candidate ones. And R52 drops "realizable" as a separate condition.

Let me check if this creates a coherence issue. The old text said the population was "realized transports" but the definition was "transports the physics and the stated construction admit." If "realized" and "the physics admits" are the same, the old text was consistent. If not, the old text was inconsistent and R42 fixes it. Or R42 changes what the population is.

Given that R42 is declared as aligning Part XII with Part IV (which says "candidate transports"), the change seems intentional and coherent. But does it change a claim? The declaration says it makes Part XII describe the population "as Part IV does, not realized ones." This is admitting that the old wording said "realized" and the new says "candidate" - and that "candidate" matches Part IV. So the declaration does acknowledge the change.

But wait - is this consistent with the "candidate" definition? Part IV says "a population T of candidate transports." Part XII says "a population of candidate transports." These match. The definition says "The population is the set of transports the physics and the stated construction admit." So "candidate" = "the physics and the stated construction admit." This seems fine.

Now, R52's replacement: the old had "admitted, realizable" as two conditions. If "admitted by physics and construction" = "candidate," and "realizable" was an extra condition beyond candidacy, then dropping "realizable" makes the claim weaker (more transports qualify as the differing one). But if "realizable" was just a synonym for "admitted," then no change.

Actually, I realize I need to look at this more carefully. The declaration for R52 says: "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." The declaration explicitly identifies what it replaces. But it doesn't say whether "realizable" is being dropped or absorbed into another condition. Let me look at the new wording again: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

This has three conditions: (1) member of T, (2) admitted by physics/construction, (3) survive on H. The old had four: (1) admitted, (2) realizable, (3) member of T, (4) survivor of H. The mapping is:
- "admitted" → "a transport the physics and the stated construction admit"
- "realizable" → dropped
- "member of T" → "a member of T"
- "survivor of H" → "survive on H"

If "realizable" is dropped and was a genuine additional requirement, this changes the claim. But the declaration says it "replaces the list" and the declaration's description of the new requirements matches the new wording. The declaration doesn't say "we're also dropping 'realizable'" - it says it replaces the list.

Hmm, but the declaration is supposed to say exactly what changes in what is claimed. If "realizable" is being dropped, the declaration should say so. But it just says the new claim requires three things and replaces the old list of four. This is potentially incomplete as a declaration.

Let me think about whether "realizable" and "a transport the physics and the stated construction admit" are the same thing. In the physical module context, a "realizable" transport would be one that can actually be physically implemented. A "candidate transport the physics admits" would be one that doesn't violate physical laws. These might be the same or different.Looking at Part XII: "a task a permitted input-to-output attribute transformation with explicit resources and side effects" and "Possibility is the absence of a law-imposed limit short of perfection on performance and retention." So the physics admits a task when there's no law-imposed limit. A transport that's admitted by the physics is one that doesn't violate physical laws. "Realizable" might mean the same thing or might mean something like "has been or can be built."

I think this is a genuine issue with R52: it drops "realizable" from the conditions, and the declaration doesn't account for this drop. The declaration should say "and drops the separate realizability condition" or similar.

But wait, let me reconsider. Maybe "realizable" and "admitted" were always the same thing and the old list was redundant. In that case, the new list is just a cleaner version of the same claim. The declaration says it "replaces" the list, which would be accurate.

Hmm, I think I'm going in circles. Let me look at other aspects.

Actually, let me re-read the declaration for R52: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

The declaration says "replaces the list." If we compare:
- Old: admitted, realizable, member of T, survivor of H (4 conditions)
- New: member of T, admitted by physics/construction, survivor of H (3 conditions)

The new drops "realizable." But "admitted by physics/construction" in the new corresponds to "admitted" in the old. So "realizable" is dropped. The declaration doesn't explicitly mention this.

But actually, I think "realizable" might be subsumed by "member of T" in the new wording. The population T is defined as "the set of transports the physics and the stated construction admit" (Part XII). So "member of T" = "admitted by physics and construction." But "realizable" might have been something more, like "can actually be built given available resources." If so, it's a genuine drop.

Actually, I just realized something. Let me re-read the old wording of Part XII: "a population of realized transports." If the population was originally "realized transports," then being in T required being realized. In that case, "member of T" (old) already implied "realizable." And "admitted" and "realizable" might have been listed separately because the old definition wasn't clear that membership in T implies realizability. With R42 changing "realized" to "candidate," membership in T now means "candidate" (admitted by physics/construction), not "realized." So now "member of T" no longer implies "realizable." And R52 drops "realizable" as a separate condition. This would mean the net effect is to broaden the population and relax the conditions on the differing transport.

But is this what R42's declaration says? R42 says: "Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones. The population is the set its own last sentence gives: the transports the physics and the stated construction admit."

So R42 explicitly says the population is "candidate transports" and gives the definition as "the transports the physics and the stated construction admit." This is saying the population includes all physically/constructionally admissible transports, not just realized ones. This is a claim change - it broadens the population.

Does this make the text incoherent? Let me check. Part IV says "a population T of candidate transports." Part XII now says "a population of candidate transports" and "The population is the set of transports the physics and the stated construction admit." These are consistent.

But what about "realizable"? If "realizable" was a separate requirement in Derivation 3, and it's now dropped, that's a change in claim that R52's declaration should account for. The declaration says "replaces the list" but doesn't say it drops a condition.

OK, I think I have a potential issue with R52: the declaration is incomplete because it drops "realizable" without accounting for it. But I need to decide whether "realizable" is genuinely different from "the physics and the stated construction admit."

Let me look at the text more carefully. In Part XII, "Selection in the physical module": "a population of candidate transports, a physically admitted variation operator, and a survival condition enacted by the environment." The phrase "physically admitted variation operator" suggests "physically admitted" is about what the physics allows. "Candidate" for the population suggests these are possibilities, not actualities. "Realized" (the old word) would mean actually built/existing. So there IS a difference between "candidate" and "realized."

In the old Derivation 3: "it must be admitted, realizable, a member of T, and a survivor of H." If "admitted" = "physically admitted" and "realizable" = "can be actually built," these are different. The new drops "realizable." This would make the condition weaker (fewer requirements).

But wait, maybe "realizable" was always subsumed by "member of T" (since T was defined as "realized transports" in the old text). If T = "realized transports," then "member of T" implies "realizable." So the old list had redundancy. With R42, T = "candidate transports," which may not imply "realizable." So R52 needs to handle this differently.

Actually, this is getting quite complex. Let me think about what the net effect is.

Old text:
- Part XII: population = "realized transports"
- Derivation 3: differing transport must be "admitted, realizable, member of T, and a survivor of H"

With the old text, being in T means being "realized," which is stronger than "candidate." So "member of T" implies "realizable" and probably "admitted." The old list was redundant.

New text:
- Part XII (after R42): population = "candidate transports" = "the transports the physics and the stated construction admit"
- Derivation 3 (after R52): differing transport must be "a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)"

Now, "member of T" = "the physics and the stated construction admit." So "member of T" and "admitted by physics/construction" are the same. The new list has redundancy too. But the key difference is that "realizable" is dropped and the population is broader (candidates vs. realized).

The net effect of R42 + R52 together is:
1. The population is broader (candidates vs. realized)
2. The differing transport in Derivation 3 just needs to be in this broader population and survive on H

This makes it easier for a differing survivor to exist, which makes Derivation 3's underdetermination result stronger (more cases of underdetermination). But the declaration for R52 doesn't acknowledge this net effect.

However, R42's declaration does acknowledge the broadening of the population. So the question is whether R52's declaration is complete given that R42 has already changed the population definition.

Hmm, but each change is supposed to be assessed on its own merits. R52's declaration says it "replaces the list 'admitted, realizable, a member of the population, and a survivor of H'" with three conditions. If the old list had four conditions and the new has three, the declaration should account for the dropped condition. The declaration says "This replaces the list" which acknowledges it's a replacement, but it doesn't explicitly say "we're dropping 'realizable'" or "realizable is now subsumed by another condition."

Actually, I think the declaration does say what the new claim requires: "the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H." This is exactly what the new wording says. And it says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." This identifies what's being replaced.

But is the new claim weaker or stronger? The old had "admitted, realizable, member of T, survivor of H." If we compare:
- "admitted" → "a transport the physics and the stated construction admit" (same or similar)
- "realizable" → dropped
- "member of T" → "a member of T" (same)
- "survivor of H" → "survive on H" (same, and now formally defined by R11)

If "realizable" is genuinely dropped and was an additional requirement, the new claim is weaker. But the declaration doesn't say "we're making this weaker by dropping 'realizable'." It just says what the new claim requires.

For task (a): Does the new wording do what its declaration says? The declaration says the new claim requires three things and replaces the old list of four. The new wording does require those three things. The declaration accurately describes the new claim. But does the declaration describe the change accurately? It says "This replaces the list" which is accurate. But it doesn't explicitly say what happens to "realizable." If "realizable" is being dropped as a separate condition, the declaration is incomplete.

Hmm, but the declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." By identifying the full old list and the full new list, the reader can see that "realizable" is not in the new list. So the declaration does implicitly account for the drop. Is that enough?

The task says: "is every part of the declaration true of the new wording? Does the new wording claim more than the declaration says, or less?"Every part of the declaration is true of the new wording (the three conditions are there). The new wording claims exactly what the declaration says (three conditions). But the declaration is about what the NEW claim is, not explicitly about what CHANGED. The declaration says "This replaces the list" which identifies the old list but doesn't say "we're dropping 'realizable'." However, by giving both lists, the reader can see the difference.

I think this is borderline. Let me look for stronger issues.

Let me now examine R31 more carefully.

**R31** (Part X, Newness, line 407)
- Old: "**Newness.** With R_{<e}(s,h)=⋃_{ξ before e} R_{β,ℓ}(s,ξ),"
- New: "**Newness.** Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at the stated grain, not string equality or similarity. With R_{<e}(s,h)=⋃_{ξ before e} R_{β,ℓ}(s,ξ),"

Declaration: Part X now defines the equivalence in (N): d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at that grain, not string equality or similarity.

Let me check the definition in the new wording: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

This says: there exist transports from d to c and from c to d, and both are faithful on c's contract at grain ℓ.

Wait - "both faithful on c's contract." This is asymmetric! Both transports are faithful on c's contract. But what about d's contract? The transport from d to c should be faithful on... whose contract? 

Let me think about this. A transport t from D to E is faithful on C when it satisfies the fidelity conditions of Part V. C is a contract, which is a subset of A × B of the target organization... wait, no. Let me re-read.

A transport from D to E is t = (π, τ, σ, λ). It translates from D's structure to E's. "A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V."

In Part V, fidelity conditions (F1) and (F2) relate the target D and the explanation E through transport t. So "faithful on C" means faithful on a contract C that ranges over D's edits.

But in R31, we have "transports from d to c and from c to d, both faithful on c's contract." This means:
- Transport from d to c: faithful on c's contract
- Transport from c to d: faithful on c's contract

But wait - a transport from c to d would have c as source and d as target. Its fidelity would be checked against d's edits/structure, not c's. Unless "c's contract" means the contract that c specifies...

Actually, let me re-read the definition of a transport. "A transport from an organization D to an organization E is t = (π, τ, σ, λ)" where π maps X_D to X_E. So a transport from d to c maps d's values to c's values. Its fidelity would relate d's structure to c's.

A transport from c to d maps c's values to d's values. Its fidelity would relate c's structure to d's.

"Both faithful on c's contract" - this means both transports are faithful when checked against c's contract. But the transport from c to d goes the other direction. What does it mean for it to be faithful on "c's contract"?

Hmm, this is potentially confusing. Let me think about what the declaration says: "both faithful on c's contract at grain ℓ." So the declaration also says "c's contract." Let me check if this makes sense.

A contract C is part of a question p = (D, C, b_0, Q, O_p, ρ_p). So a contract is associated with a target D. "c's contract" would be the contract of the question whose target is c.

Now, a transport from d to c: its fidelity is checked on... which contract? If we're transporting from d (the source/target of the world) to c (the explanation/representation), then the contract would be d's contract (the question about d). But the text says "c's contract."

Wait, actually I need to think about this more carefully. In (N), the context is Newness in Part X. c is a content. d is a previous content in the repertoire. The equivalence d ≡_ℓ c is about whether d and c are structurally equivalent.

A transport from d to c would be a way of mapping d's structure to c's structure. For this transport to be "faithful on c's contract" - does this mean the transport preserves what c's contract requires? Or does it mean the transport is faithful when the contract is c's contract?

I think the issue is that "c's contract" might not be well-defined for a transport from c to d. The transport from c to d goes in the opposite direction. Its fidelity should be checked against d's structure and edits, not c's.

Unless "c's contract" is used loosely to mean "the contract relevant to c" which applies to both directions. But the text says "both faithful on c's contract at grain ℓ" - this seems to apply c's contract to both transports.

Let me look at how transports are used elsewhere. In Part V, t is a transport from D to E, and the fidelity conditions are on C (the contract of question p, which ranges over D's edits). So fidelity is checked on the SOURCE's contract.

A transport from d to c: source is d, so fidelity should be on d's contract (or the contract relevant to d).
A transport from c to d: source is c, so fidelity should be on c's contract.

But R31 says "both faithful on c's contract." This applies c's contract to both. For the transport from d to c, this would mean checking fidelity on c's contract rather than d's contract. This might be wrong or might be intentional.

Hmm, but actually, the equivalence is supposed to be symmetric: d ≡_ℓ c iff c ≡_ℓ d. If both transports are faithful on "c's contract," then for c ≡_ℓ d, both transports (from c to d and from d to c) would be faithful on "d's contract." So the definition would give different conditions for d ≡_ℓ c and c ≡_ℓ d. That would make the relation not symmetric!

Wait, but the relation is defined as "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract." This is a definition of ≡_ℓ. For the relation to be symmetric, we need d ≡_ℓ c iff c ≡_ℓ d. But the definition says "faithful on c's contract" for both. If we swap d and c: "c ≡_ℓ d when there are transports from c to d and from d to c, both faithful on d's contract." This is different from "d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract."

So the definition is NOT symmetric! d ≡_ℓ c requires both transports to be faithful on c's contract, while c ≡_ℓ d requires both to be faithful on d's contract. These are different conditions.

But the definition says "The equivalence is structural at the stated grain." An equivalence relation must be symmetric (and reflexive and transitive). If the definition is asymmetric, it's not an equivalence relation.

This is a significant issue! The declaration says "d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ." If this is asymmetric, it's not an equivalence relation.

Wait, but maybe I'm wrong about the direction. Let me re-read. "there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ." 

Maybe "both faithful on c's contract" means the transport from d to c is faithful on c's contract (checked in the direction from d to c using c's contract), and the transport from c to d is faithful on c's contract (checked in the direction from c to d using c's contract). In this reading, c's contract is used as the reference for both directions. This would be checking that d and c agree on what c's contract specifies.

But this is still asymmetric because we're using c's contract as the reference. For c ≡_ℓ d, we'd use d's contract as the reference. Unless the definition is meant to be asymmetric but still called an "equivalence" because it becomes symmetric when c and d have the same contract... but that's not stated.

Actually, wait. Let me think about what makes sense. The purpose of the equivalence is to determine whether d (a prior content) is "the same as" c (a candidate new content). If d and c are structurally equivalent, then c is not new. The equivalence should check that d and c have the same structure.

If we check "both faithful on c's contract," we're checking that d can reproduce c's responses under c's contract, and c can reproduce d's responses under c's contract. This would mean d and c give the same answers under c's contract.

But we might also want to check under d's contract. If d and c have different contracts, checking only c's contract might miss differences.

Hmm, but the definition says "both faithful on c's contract." This is what the text says. The question is whether this makes the relation non-symmetric and thus not an equivalence.

Actually, let me reconsider. Maybe "faithful on c's contract" is a property of the transport pair, not of each transport individually. The definition says "there are transports from d to c and from c to d, both faithful on c's contract." Maybe "both" modifies "faithful on c's contract" as a joint property. Like, the pair of transports together is faithful on c's contract.

But that's a stretch. The most natural reading is "both [of which are] faithful on c's contract."

Let me check how (N) uses the equivalence. In (N): "New(s,c,h,e) ⟺ ¬∃d ∈ R_{<e}(s,h), d ≡_ℓ c." So c is new when no prior content d is equivalent to c. If the equivalence is asymmetric (only checking c's contract), then New would be defined in terms of c's contract only. This might be intentional - we care about whether d covers everything c covers, not the reverse.

But the text says "The equivalence is structural at the stated grain, not string equality or similarity." This describes it as an "equivalence." In mathematics, an equivalence relation is reflexive, symmetric, and transitive. If the definition is asymmetric, it's not an equivalence.

Hmm, but the text doesn't say "equivalence relation." It says "the equivalence is structural at the stated grain." This might just mean "the equivalence notion" or "what it means to be equivalent." It might not be claiming it's a formal equivalence relation.

But then, using≡ notation is standard for equivalence relations. And the declaration says "The equivalence is structural at the stated grain, not string equality or similarity." This describes it as "the equivalence" which suggests it's supposed to be an equivalence.

Actually, let me reconsider. Maybe "both faithful on c's contract" should be "each faithful on the other's contract" or "both faithful on both contracts." The asymmetry might be an error.

Or maybe "c's contract" is used generically to mean "the relevant contract" which would be the same for both directions. If d and c are contents that address the same question, they share the same contract. In that case, "c's contract" = "d's contract" and the definition is symmetric.

But the definition doesn't say d and c address the same question. It just says "transports from d to c and from c to d, both faithful on c's contract."

I think this is a genuine issue with R31. The definition of≡_ℓ is asymmetric (it uses c's contract for both transports), which means it's not a true equivalence relation. The declaration doesn't account for this.

Wait, but let me think again. The transport from d to c goes from d to c. When we say this transport is "faithful on c's contract," what does that mean? 

Looking at Part V: "A transport t = (π, τ, σ, λ) from D to E" - fidelity conditions are stated as (F1) and (F2), which use "for every (a,b) ∈ C" where C is a contract. The contract C is the contract of question p, and p has target D. So the contract ranges over D's edits.

A transport from d to c: source is d (like D in Part V), target is c (like E in Part V). The contract should range over d's edits. "Faithful on c's contract" would mean using c's contract, which ranges over c's edits, not d's. This is mismatched.

A transport from c to d: source is c, target is d. The contract should range over c's edits. "Faithful on c's contract" makes more sense here - the contract ranges over c's edits.

So "faithful on c's contract" is more natural for the transport from c to d than for the transport from d to c. For the transport from d to c, we'd want "faithful on d's contract."

This confirms the asymmetry issue. The definition uses c's contract for both, but the natural contract for the d-to-c transport is d's contract.

Hmm, but maybe the text intends "faithful on c's contract" to mean "faithful in the sense that it preserves what c's contract specifies." For the transport from d to c, this would mean: when we look at c's contract (the edits and boundaries of c), the transport maps d's responses to c's responses correctly. This would require c's edits to be translatable to d's edits (via τ) and the responses to match.

This is actually how (F1) and (F2) work: the transport translates edits (τ) and boundaries (σ), and fidelity is checked at the translated points. So a transport from d to c with contract c's C would translate c's edits to d's edits and check that the mapped responses match.

Wait, but that's backwards. The transport from d to c has τ that translates d's edits to c's edits (or vice versa?). Let me re-read.

"A transport from an organization D to an organization E is t = (π, τ, σ, λ) where π: X_D → X_E on the stated scope, τ translates edits, σ translates boundaries, and λ assigns each component of E a subnetwork of D."

So τ translates edits... from D to E? Or from E to D? Looking at (F1): "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))" where (a, b) ∈ C. Here a is an edit of D (since C ⊆ A × B where A is D's edits), and τ(a) is the translated edit of E. So τ maps D's edits to E's edits.

For a transport from d to c: τ maps d's edits to c's edits. The contract C ranges over d's edits. Fidelity is checked at (a, b) ∈ C where a is a d-edit, and τ(a) is a c-edit.

So "faithful on c's contract" for a transport from d to c would mean: the contract C is c's contract, which ranges over c's edits. But τ maps d's edits to c's edits. So (a, b) ∈ C means a is a c-edit, and τ(a) would be... a d-edit? That's the wrong direction.

This is getting confusing. Let me think about it differently.

Actually, I think "faithful on C" for a transport t from D to E means: the fidelity conditions hold when the contract is C. The contract C ⊆ A × B where A is D's edit set and B is D's boundary set. So C ranges over the SOURCE's edits.

A transport from d to c is faithful on d's contract (the contract of the source d). Saying it's "faithful on c's contract" doesn't make sense because c's contract ranges over c's edits, not d's.

Unless "c's contract" is used loosely. Or unless the equivalence is defined in a non-standard way.

I think R31 has a genuine issue: it says "both faithful on c's contract" where one of the transports goes from d to c and should be checked on d's contract. This makes the definition asymmetric and possibly ill-formed.

Let me also check the declaration: "d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ." The declaration has the same issue.

OK, so R31 potentially has a real problem with the definition of ≡_ℓ being asymmetric and possibly ill-typed (applying c's contract to a transport from d to c).

Let me move on and check other changes.

**R22** (Part VI, Redundant routes, line 307)
- Old: "may differ in which routes are active; the semantics represents the difference (Derivation 9)."
- New: "may differ in which routes are active; the semantics represents the difference (Derivation 9). Here a route is a support of the candidate, a member of S, and is a route whether or not any history runs it; it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history. The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

Declaration: Part VI now says four things. A route is a support of the candidate, a route whether or not any history runs it. It is active in a system when occurrences that realize it form an active route in Part IX's sense. ProducedBy credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains.

Let me check coherence. "A route is a support of the candidate, a member of S." Here S is defined earlier in Part VI: "S_{E,p} = {W ⊆ Γ: Account(E|W, p)}." So a route is a member of S, which is a set of subsets of Γ. So a route is a subset of Γ that forms a support.

But wait, in Part IX, "an active route is a connected subnetwork of actual occurrences joining a represented input to an operative result." This is about physical occurrences, not about subsets of Γ. R22 says "it is active in a system when occurrences that realize it form an active route (Part IX)." So the route (a subset of Γ) is active when the physical occurrences that realize it form an active route (a subnetwork of actual occurrences).

This seems coherent: a "route" in Part VI is a support (a subset of Γ that works), and it's "active" in a system when the physical implementation of that support actually does work (forms an active route in Part IX's sense).

But there's a notation issue. In Part VI, the context is about the candidate E and its commitments Γ. A "route" is defined as "a support of the candidate, a member of S." But S is defined as S_{E,p} = {W ⊆ Γ: Account(E|W, p)}. The "Redundant routes" example says "Γ = {a, b}, S = {{a}, {b}, {a, b}}: each is contributory, neither indispensable." Here the routes would be {a}, {b}, and {a, b}.

But in Part IX, an "active route" is "a connected subnetwork of actual occurrences." The word "route" is used in both senses. R22 says "it is active in a system when occurrences that realize it form an active route (Part IX)." So a route (in Part VI's sense) is active when its realization forms an active route (in Part IX's sense). The two senses of "route" are distinguished by context but share the word.

Is this a coherence issue? The text uses "route" for both the abstract support and the physical subnetwork. R22 says "Here a route is a support of the candidate" and "it is active in a system when occurrences that realize it form an active route (Part IX)." So the text is explicitly distinguishing the two senses and connecting them. This seems deliberate and coherent.

But wait - "it is active in a system when occurrences that realize it form an active route." The "it" refers to the route (a support). "Occurrences that realize it" - the occurrences that realize a support? A support is a subset of Γ (commitments). Occurrences realize commitments. So "occurrences that realize it" means the occurrences that realize the commitments in the support.

Hmm, but "form an active route" in Part IX means "a connected subnetwork of actual occurrences joining a represented input to an operative result." So the occurrences forming the support's realization must form a connected subnetwork. This is a property of the physical realization, not of the abstract support.

This seems coherent. The connection is: an abstract route (support) is active when its physical realization does actual work.

Now, "The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

ProducedBy is defined in Part XI: "ProducedBy holds when an active route (Part IX) runs from Δ to the repair." This credits based on active routes (Part IX's sense) running from the contribution to the repair. R22 says this credits "to the contributions whose active routes ran to it, not to the routes a candidate contains." This means ProducedBy looks at which contributions actually had active physical routes to the repair, not just which routes (supports) the candidate contains.

This seems consistent with Part XI's definition. But let me check: ProducedBy(Δ, ξ, ξ'; O) is about whether Δ (a subhistory with content changes) has an active route to the repair. R22 says it credits "to the contributions whose active routes ran to it." This is about individual contributions, not about Δ as a whole. Is there a mismatch?

Actually, looking at Part XI more carefully: "ProducedBy holds when an active route (Part IX) runs from Δ to the repair; it credits each contribution the history establishes." So ProducedBy already credits each contribution. R22's addition says this credits "to the contributions whose active routes ran to it, not to the routes a candidate contains." This is clarifying that the credit is based on which contributions actually ran (had active routes), not just which are present in the candidate.

This seems like a useful clarification and doesn't contradict anything. Let me check the declaration: "ProducedBy credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains." This matches the new wording.

Now let me check for notation issues. R22 uses "S" - "a member of S." In Part VI, S is defined as S_{E,p}. But in the Redundant routes section, the example says "S = {{a}, {b}, {a, b}}." So S is used as a concrete set in the example and as S_{E,p} in the definition. R22 says "a member of S" without subscript. This should be understood as a member of S_{E,p} (or the S in the current context). This is fine.

What about "ProducedBy"? This is defined in Part XI. R22 references it in Part VI. Part VI comes before Part XI in the text. Is this a forward reference? Yes, but forward references are common in mathematical texts and the text uses them elsewhere (e.g., Part V references Part VI, Part IV references Part V, etc.). So this is not unusual.

But wait, there's a potential issue: "ProducedBy" is used in R22's new text with specific arguments? Let me re-read: "The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains." This describes what ProducedBy does but doesn't use it with arguments. This is fine as a prose description.

OK, R22 seems mostly fine. Let me look for subtler issues.

Actually, wait. R22 says "it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history." The phrase "a subnetwork of actual occurrences in the system's history" is an appositive to "an active route." So it's saying "an active route (Part IX) [is] a subnetwork of actual occurrences in the system's history." But Part IX defines an active route as "a connected subnetwork of actual occurrences joining a represented input to an operative result, whose components satisfy the applicable relations and which has nonconstant dependence on the represented distinction under the declared contrasts." So an active route is more than just "a subnetwork of actual occurrences" - it has additional properties (connected, joining input to result, satisfying relations, nonconstant dependence). R22's appositive simplifies this. Is this a problem?

The appositive "a subnetwork of actual occurrences in the system's history" is a simplified description. It's not wrong (an active route IS a subnetwork of actual occurrences), just incomplete. Since R22 also points to Part IX for the full definition, this seems like reasonable prose. Not a defect.

Let me now look at R32 and R33.

**R32** (Part X, Ownership, line 421)
- Old: "is the system's own today whoever wrote it"
- New: "is the system's own whoever wrote it"
- Declaration: Part X no longer says that a process run inside the boundary is the system's own "today", which matches Part XII.

This removes the word "today" from the text. The declaration says this matches Part XII. Let me check Part XII: "a process run inside the boundary is the system's whoever wrote it." Part XII doesn't say "today." So R32 aligns Part X with Part XII.

But is this a CLAIM change? The declaration says "Part X no longer says that a process run inside the boundary is the system's own 'today'." This is removing a temporal qualifier. Does this change a claim?

In the old text: "a process run inside the boundary is the system's own today whoever wrote it." The word "today" suggests that ownership is temporal - the process is the system's own "today" (but maybe not tomorrow?). In the new text: "a process run inside the boundary is the system's own whoever wrote it." Without "today," ownership is not temporally qualified.

But wait, looking at the context in Part X: "Work supplied from outside that boundary, a diagnosis, a decisive question, an instruction about what to read, remains an outside contribution however it is executed inside; a process that runs inside the boundary is the system's own today whoever wrote it; and where the boundary is drawn decides, not where the process sits in the casing."

Hmm, the old text says "is the system's own today." This might mean "is the system's own [as of] today" or "is the system's own, [as happens] today." The word "today" is ambiguous. If it means "as of today," it implies temporal limitation. If it's just a temporal marker for the event, it's not a claim about ownership being temporary.

Removing "today" would remove any temporal implication. The declaration says this "matches Part XII," which is true (Part XII doesn't say "today"). But does removing "today" change a claim?

The declaration says: "Part X no longer says that a process run inside the boundary is the system's own 'today', which matches Part XII." This is saying the old text had "today" and the new text doesn't, and this matches Part XII. The declaration acknowledges the change.

But the declaration seems to say the change is that Part X "no longer says" the temporal qualifier. This is a claim change because the old text allowed the conclusion that ownership is temporally limited ("today"), and the new text no longer allows this. The declaration correctly identifies this as a claim change.

But wait - is the old text's "today" really a temporal qualifier? Let me re-read: "a process that runs inside the boundary is the system's own today whoever wrote it." This could be parsed as "a process that runs inside the boundary is the system's own today, whoever wrote it" (ownership today) or "a process that runs inside the boundary, today, is the system's own whoever wrote it" (the process runs today). The first reading gives a temporal qualifier to ownership; the second just marks when the process runs.

If the second reading is correct, "today" just specifies when the process runs, and ownership is not temporally limited. In that case, removing "today" doesn't change the claim about ownership.

But the declaration says "Part X no longer says that a process run inside the boundary is the system's own 'today'." This treats "today" as qualifying ownership. If the declaration is correct about what the old text says, then the change removes a temporal qualifier from ownership.

Hmm, but there's another issue. The old text in Part X says "is the system's own today." Part XII says "is the system's whoever wrote it." But looking at the situation O30: "The routine uploaded that morning" and the verdict: "The comparison ran inside the robot and was the robot's work today." The situation uses "today" to mean the ownership is of today's work. The verdict says "Ownership of today's history is the robot's."

So in O30, "today" is about the temporal scope of the history, not about ownership being temporary. The ownership of today's history is the robot's. If we remove "today" from the text, does the text still handle O30 correctly?

With "today" removed: "a process that runs inside the boundary is the system's own whoever wrote it." This says any process running inside the boundary is the system's own. For O30, the routine was uploaded to the robot that morning and runs inside the robot. So it's the robot's own. This matches O30's verdict.

But the declaration says the change removes "today," which "matches Part XII." Part XII says: "a process run inside the boundary is the system's whoever wrote it." Without "today." So R32 aligns Part X with Part XII. Good.

Is the declaration complete? It says "Part X no longer says that a process run inside the boundary is the system's own 'today', which matches Part XII." This correctly identifies the change (removing "today") and its motivation (matching Part XII). But does it account for any claim change? Removing "today" could change what a reader concludes about temporal ownership. The declaration says it "no longer says" the temporal thing, which is the claim change. Seems complete.

Actually, wait. Let me reconsider. The declaration says "Part X no longer says that a process run inside the boundary is the system's own 'today', which matches Part XII." But does Part XII really match? Let me look at Part XII again: "a process run inside the boundary is the system's whoever wrote it." Yes, no "today." So the match is correct.

But here's a subtlety: the old Part X said "is the system's own today." If "today" was merely temporal context (the process runs today) rather than a qualifier on ownership, then removing it changes the temporal context but not the ownership claim. In that case, the declaration might be over-claiming.

But the declaration treats "today" as qualifying ownership ("the system's own 'today'"). This is one valid reading of the old text. The declaration is at least partially correct.

I think R32 is fine. The change is small and the declaration is reasonable.

**R33** (Part X, Ownership, line 421)
- Old: "and where the boundary is drawn decides, not where the process sits in the casing."
- New: "and where the boundary is drawn decides, not where the process sits in the casing. Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."
- Declaration: Part X now says that credit for content and ownership of a process are different attributions. A routine written outside the boundary and run inside it is the system's own process, while its content remains its writer's contribution.

This adds a new sentence to Part X. The declaration says it adds two things: (1) credit for content and ownership of a process are different attributions, (2) a routine written outside the boundary and run inside it is the system's own process while its content remains its writer's contribution.

Let me check coherence. The sentence says "Credit for content and ownership of a process are different attributions." This is a new claim - the text now explicitly distinguishes these. Does this contradict anything?

In Part X, Ownership: "Ownership is not defined by the capability it is meant to ground (Part XII)." This is about ownership not being defined by capability. The new sentence is about credit vs. ownership being different. These are complementary, not contradictory.

In Part XI: "it credits each contribution the history establishes." This is about ProducedBy crediting contributions. The new sentence says "its content remains its writer's contribution." This means the writer gets credit for the content even though the system owns the process. This is consistent with Part XI's approach of crediting contributions.

In O30 (the routine uploaded that morning): the verdict says "Who wrote the routine is a separate fact, and a true one. Ownership of today's history is the robot's; the origin of the routine is the person's." The new sentence says "a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution." This matches O30's verdict.

In O21 (the expert's question): the verdict says "The expert did the part that took understanding, knowing what would separate the two." The new sentence says credit for content goes to the writer. The expert wrote the test (content), so the expert gets credit for that content. The robot owns the process of running the test. This seems consistent.

Let me check O40 (two hands on the test): "The reinterpretation was theirs together: the robot saw the assumption, the expert saw the fix." The new sentence says credit for content and ownership are different. In O40, both contributed content (the robot saw the assumption, the expert saw the fix). The new sentence doesn't determine how credit is divided between them - it just says credit and ownership are different. This seems fine.

But wait, is there an issue with the claim being new? The declaration says "Part X now says that credit for content and ownership of a process are different attributions." This is indeed new text. Before this change, Part X said ownership is about the boundary, but didn't explicitly say credit for content is different. Now it does. This is a CLAIM change because a reader can now conclude something the current text left unconcluded (that credit and ownership are different).

The declaration seems complete for this. Let me check if there are any issues.

"Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."

Wait, "its content remains its writer's contribution." This says the CONTENT is the writer's contribution. But what about the process? "is the system's own process." So the process is the system's, and the content is the writer's. This is a clear distinction.

But is this always true? What if the system modifies the content? Then some of the content is the system's contribution and some is the writer's. The sentence says "its content remains its writer's contribution" which seems to say ALL the content is the writer's. But if the system modifies it, that's not true.

Hmm, but the sentence says "a routine written outside the boundary and run inside it." If it's "run" (not modified), then the content is as the writer wrote it. If it's modified, it's a different routine. So the sentence might be about unmodified routines.

But what about O18 (the one tooth)? "The learner receives a complete key diagram from Sam. At a new lock she finds a catch the diagram never covered, files one tooth to fit it, and keeps every other tooth as the diagram gave it." The verdict says "The one tooth is hers. The rest is relay." Here the content is partially received (from Sam) and partially constructed (the one tooth). The new sentence says "its content remains its writer's contribution" for a routine "written outside the boundary and run inside it." But in O18, the content is partially modified. Is the new sentence wrong for O18?

Actually, O18 is about a learner constructing content, not about a routine written outside and run inside. The new sentence is about a specific case: a routine written outside and run inside. O18 is about a learner modifying received content. These are different situations. The new sentence doesn't claim anything about modified content.

But does the new sentence CONTRADICT anything about O18? Let me check. O18 says "The one tooth is hers. The rest is relay." Part X already handles this: "A small binding newly prepared inside received content is construction of that binding, and the rest of the content keeps its inherited provenance." This says the constructed part is the learner's and the rest keeps its inherited provenance. The new sentence says "its content remains its writer's contribution" for a routine written outside and run inside. These don't contradict because O18 involves modification and the new sentence is about unmodified routines.

Actually, wait. What if the new sentence is too broad? "a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution." This says the content "remains its writer's contribution." But what if the system uses the routine in a way that creates new knowledge? The content of the routine is still the writer's, but the use of it might create something new. The new sentence only talks about the content of the routine, not about what comes from using it. So it seems fine.

But what about O27 (the test that fitted neither)? The verdict says "The robot found the answer. The achievement is mostly the robot's, because the decisive step was its own reinterpretation of the test." The test was written by the expert (content = expert's contribution). But the robot's reinterpretation is a new content (the robot's contribution). The new sentence says "its content remains its writer's contribution" for the routine. But the reinterpretation is not the routine's content - it's new content created by the robot. So the new sentence doesn't apply to the reinterpretation.

Hmm, but the new sentence might be misleading. "a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution." If "its content" is interpreted broadly (everything the routine produces or enables), then it might wrongly attribute the robot's reinterpretation to the expert. But if "its content" means just the content of the routine itself (the code/instructions), then it's correct.

I think the sentence is about the content of the routine, not about what comes from using it. The sentence says "its content remains its writer's contribution" where "its" refers to the routine. So "its content" = the routine's content = the code/instructions. This is correct and doesn't affect O27.

Let me also check O50 (the same afternoon, three achievements): "The reinterpretation was theirs together. The discovery was the robot's... The inquiry was mostly the expert's." The new sentence says credit for content goes to the writer and ownership of process goes to the system. For O50, the expert wrote the test (content = expert's). The robot ran the test and found the fault (process = robot's, discovery = robot's). The expert's fix (swapping to the second sensor) is content = expert's. The reinterpretation is shared. The new sentence doesn't determine the exact credit for the reinterpretation or the inquiry - it just says credit and ownership are different. This seems fine.

OK, I think R33 is mostly fine. The declaration is accurate and the change doesn't contradict anything.

But wait, let me reconsider one thing. The declaration says "Part X now says that credit for content and ownership of a process are different attributions." But the new sentence says "Credit for content and ownership of a process are different attributions." This is about "credit for content" and "ownership of a process." But what about "ownership of content"? The sentence distinguishes "credit for content" from "ownership of a process." But there's also "credit for process" and "ownership of content." The sentence doesn't address these.

Is this a problem? The sentence says these two are different. It doesn't say anything about other possible attributions. So it's not wrong, just limited. The declaration says "credit for content and ownership of a process are different attributions" which matches the sentence. Fine.

Let me now look at R35, R36, R37.

**R35** (Part XI, Repair, line 429)
- Old: "**Repair.** For claimed obligations O and protected obligations P, fixed for the comparison,"
- New: "**Repair.** The contribution Δ of a repair is a subhistory together with the changes of content it makes. For claimed obligations O and protected obligations P, fixed for the comparison,"
- Declaration: Part XI now types the contribution Δ of a repair as a subhistory together with the changes of content it makes.

This adds a definition of Δ. The declaration says it "types the contribution Δ of a repair as a subhistory together with the changes of content it makes."

Let me check coherence. In (P): "Repair_{O,P}(ξ, ξ'; Δ) ⟺ ∃o ∈ O[¬o(ξ) ∧ o(ξ')] ∧ ∀r ∈ P[r(ξ) ⇒ r(ξ')] ∧ ProducedBy(Δ, ξ, ξ'; O)."

Δ appears in Repair and ProducedBy. The new sentence defines Δ as "a subhistory together with the changes of content it makes." This is a typing/definition that wasn't there before. Previously, Δ was used without explicit definition.

Is this definition consistent with how Δ is used? In ProducedBy(Δ, ξ, ξ'; O), Δ is described as having "an active route (Part IX) runs from Δ to the repair." For Δ to have an active route running from it, Δ should be something physical (like a subhistory). The new definition says Δ is "a subhistory together with the changes of content it makes." The subhistory part provides the physical basis for the active route.

In (EK): "c ∈ Result(Δ)" where "Result(Δ) is the set of contents at ξ' that Δ prepared." The "changes of content" part of Δ's definition provides the basis for Result(Δ). If Δ is "a subhistory together with the changes of content it makes," then Result(Δ) would be the contents that those changes prepared.

This seems coherent. The definition of Δ provides the right typing for its uses.

But wait - is the definition new? The declaration says "Part XI now types the contribution Δ of a repair." The word "types" suggests it's providing a type definition. Previously, Δ was used without explicit typing. Now it's typed as "a subhistory together with the changes of content it makes." This is a clarification that adds a definition.

Does this change a claim? If Δ was previously untyped, a reader could interpret it in various ways. Now it's typed, which constrains the interpretation. This is a CLAIM change (a reader can now conclude the type of Δ, which was previously unconcluded).

The declaration seems complete: it says the change "types the contribution Δ of a repair as a subhistory together with the changes of content it makes." This matches the new wording.

But wait - does the new wording say more than the declaration? The declaration says "types the contribution Δ of a repair as a subhistory together with the changes of content it makes." The new wording says "The contribution Δ of a repair is a subhistory together with the changes of content it makes." This is the same thing. Good.

Is there a coherence issue? Let me check if "subhistory" is defined. Part IX: "A history h is a set of occurrences with an acyclic causal precedence ≺_h and a physical interpretation supplying process occurrences, their ports, and the connections actually instantiated." A "subhistory" would be a subset of a history. This is a natural extension but not explicitly defined. However, "subhistory" is used elsewhere in the text (e.g., Part X: "an actual subhistory owned by s"). So the term is used consistently.

What about "changes of content"? This is a new phrase. Is it defined? The text talks about content changes in various places (e.g., Part IX: "the changes of content it makes"). But "changes of content" as a specific technical term is not formally defined. However, the context makes it clear: these are the changes in content that the subhistory makes.

Hmm, actually, "changes of content" is not a standard term in the text. Let me check. Part X says "a nontrivial binding construction relevant to that use" and "the bindings constructed." Part XI (before R35) uses Δ without defining it. Part IX says "a structural map from the represented objection into the response suborganization preserves role bindings." So the text talks about "bindings" and "content" but "changes of content" is not a defined term.

Is this a coherence issue? The term "changes of content" is used in R35's new text but is not formally defined. However, the meaning is clear from context: these are the changes in the content that the subhistory makes. The declaration uses the same phrase: "a subhistory together with the changes of content it makes." So the declaration and the new wording are consistent.

I think R35 is fine. The definition of Δ is useful and consistent.

**R36** (Part XI, Repair, line 435)
- Old: "and a protected condition is lost exactly when it fails on an occasion it covers."
- New: "and a protected condition is lost exactly when it fails on an occasion it covers. In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."
- Declaration: Part XI now says that in (P), for a protected condition r, r(ξ') means that r held on every occasion it covers from ξ to ξ', not only at ξ'.

This adds a clarification of what r(ξ') means in (P). Let me check (P): "∀r ∈ P[r(ξ) ⇒ r(ξ')]."

The original text said "a protected condition is lost exactly when it fails on an occasion it covers." This means: a protected condition r is lost if r fails on some occasion it covers. The new text adds: "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."

So r(ξ') in (P) now means: r held on every occasion it covers from ξ to ξ'. This is a re-interpretation of what r(ξ') means. Previously, r(ξ') might have been read as "r holds at ξ'" (i.e., at the final state). Now it means "r held on every occasion from ξ to ξ'."

Does this change a claim? YES. Previously, (P) said "∀r ∈ P[r(ξ) ⇒ r(ξ')]" which could be read as "if r held at ξ, then r holds at ξ'." This only checks the endpoints. Now, r(ξ') means "r held on every occasion from ξ to ξ'," which checks the entire interval. This is a stronger condition.

The declaration says "Part XI now says that in (P), for a protected condition r, r(ξ') means that r held on every occasion it covers from ξ to ξ', not only at ξ'." This correctly identifies the change.

But wait - the old text already said "a protected condition is lost exactly when it fails on an occasion it covers." This implies that the condition must hold on every occasion it covers (otherwise it's "lost"). So the old text already had the stronger interpretation, just expressed differently. The new text makes this explicit by clarifying what r(ξ') means.

But there's a tension. The formula (P) says "r(ξ) ⇒ r(ξ')" which looks like a pointwise condition (r at ξ implies r at ξ'). But the prose says r(ξ') means "held on every occasion from ξ to ξ'." The formula notation r(ξ') is being given a non-standard meaning (it means "held throughout [ξ, ξ']" rather than "holds at ξ'").

Is this a coherence issue? The formula (P) uses r(ξ') as if r is a function from states to truth values. But the prose says r(ξ') means something different. This is a mismatch between the formula and the prose.

The old text had the same issue: the formula (P) said r(ξ) ⇒ r(ξ'), but the prose said "a protected condition is lost exactly when it fails on an occasion it covers." The old text didn't explicitly reconcile these. The new text tries to reconcile them by defining r(ξ') to mean "held on every occasion from ξ to ξ'." But this is a non-standard use of function notation.

Actually, the new text says "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'." This is saying: the notation r(ξ') in (P) should be read as "r held on every occasion from ξ to ξ'." This is a meta-level clarification of the notation.

Is this problematic? It's a bit unusual to give function notation a non-standard meaning. But the text is explicit about it: "r(ξ') says... that it held on every occasion it covers from ξ to ξ', not only at ξ'." This clearly states the meaning.

But there's a deeper issue: r(ξ') is used in (P) as r(ξ') (r evaluated at ξ'). If r(ξ') now means "held throughout [ξ, ξ']", then what does r(ξ) mean? Presumably r(ξ) means "held at ξ" (or "held up to ξ"?). The text says r(ξ') means "held on every occasion it covers from ξ to ξ'." But what does r(ξ) mean? The text doesn't say.

Actually, looking at (P) more carefully: "∀r ∈ P[r(ξ) ⇒ r(ξ')]." If r(ξ') means "held throughout [ξ, ξ']" and r(ξ) means "held at ξ," then the condition is: "if r held at ξ, then r held throughout [ξ, ξ']." This is equivalent to: "if r held at ξ, then r held at every occasion from ξ to ξ'." This is a reasonable condition for protection.

But the formula notation is misleading. r(ξ') doesn't mean "r at ξ'" in the usual sense. It means "r throughout [ξ, ξ']." This could confuse readers.

Let me also check O35 (four seconds): "During the washer repair the protected tap stops for four seconds while the mains is switched over, then runs as before. Nobody drew from it in those seconds." Verdict: "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss."

With the new interpretation of r(ξ'): r is a protected condition "the tap runs." If the tap stops for four seconds, then r fails on some occasions from ξ to ξ'. Under the new interpretation, r(ξ') would be FALSE (because r didn't hold on every occasion). But the verdict says "The protected condition held in every way that mattered: nobody was without water."

Hmm, so the verdict says the protected condition is NOT lost (nobody was without water). But the new interpretation of r(ξ') says the condition must hold on EVERY occasion. If the tap stops for four seconds, the condition doesn't hold on every occasion.

But the verdict says "nobody was without water." So the protected condition might be "nobody is without water" rather than "the tap runs." If r = "nobody is without water," then r held on every occasion (even during the four seconds, because nobody drew from the tap). So r(ξ') would be TRUE.

This depends on what the protected condition r is. If r = "the tap runs," then the four-second interruption violates it. If r = "nobody is without water," then it doesn't. The verdict seems to use the latter interpretation.

With the new text, r(ξ') means "r held on every occasion from ξ to ξ'." For O35, if r = "nobody is without water," then r held on every occasion (nobody drew from the tap during the four seconds). So r(ξ') is TRUE. The verdict is consistent.

But if r = "the tap runs," then r(ξ') is FALSE (the tap stopped for four seconds). The verdict says "The protected condition held in every way that mattered." This suggests the condition is about water availability, not about the tap running.

Actually, the situation says "the protected tap stops for four seconds." The protected condition might be about the tap. But the verdict says "nobody was without water" which is about water availability. So the protected condition is about water, not about the tap.

With the new interpretation, r(ξ') requires r to hold on every occasion. For r = "nobody is without water," this holds in O35. Good.

But what if the protected condition were "the tap runs"? Then the new interpretation would say r(ξ') is FALSE, and (P) would say the condition is lost. The old interpretation (r at ξ') might have said r(ξ') is TRUE (the tap runs at ξ'). So the new interpretation is stricter.

Does this move the verdict on O35? The verdict is "The protected condition held in every way that mattered." With the old text (r(ξ') = r at ξ'), the condition might be considered held (the tap runs at ξ'). With the new text (r(ξ') = held throughout), the condition might not be held (the tap didn't run throughout).

But the verdict says "The protected condition held in every way that mattered: nobody was without water." This is using a specific protected condition ("nobody is without water") and the verdict says it held. Under both interpretations, if the condition is "nobody is without water," it holds. So the verdict is not moved.

But wait - the new interpretation is stricter. Under the new interpretation, some conditions that were previously held might now be considered lost. The question is whether O35's specific case is affected.

In O35, the protected condition is about water availability ("nobody was without water"). This condition holds throughout (even during the four seconds, because nobody drew from the tap). So the new interpretation doesn't change the verdict.

But what if someone reads O35 with a different protected condition? The situation says "the protected tap stops for four seconds." If the protected condition is "the tap runs," then the new interpretation would say the condition is lost (the tap stopped for four seconds). But the verdict says "The protected condition held in every way that mattered." This suggests the protected condition is NOT "the tap runs" but something weaker.

Hmm, actually, the verdict says "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss." The phrase "in every way that mattered" suggests that in some way it didn't hold (the tap stopped), but in the way that mattered (nobody was without water), it did. The new interpretation says r(ξ') means "held on every occasion it covers from ξ to ξ'." If the condition is "the tap runs," it didn't hold on every occasion. But if the condition is "nobody is without water," it did.

So the verdict depends on what the protected condition is. The new text makes the "every occasion" requirement explicit. If the condition is "nobody is without water," the verdict is unchanged. If the condition is "the tap runs," the new text makes it a loss while the old text might not.

But the situation says "the protected tap stops for four seconds." This suggests the protected condition involves the tap. But the verdict says "nobody was without water" which is about people, not about the tap.

I think the key insight is that the protected condition is "nobody is without water" (or "water is available to those who need it"). The tap stopping for four seconds is an event, but it doesn't cause anyone to be without water (because nobody drew from the tap in those seconds). So the protected condition holds on every occasion.

Under the new interpretation (every occasion), the protected condition "nobody is without water" holds throughout. Under the old interpretation (at ξ'), it also holds (at ξ', nobody is without water). So the verdict is the same.

But the new interpretation is stricter for other cases. For example, if someone DID draw from the tap during the four seconds, the protected condition "nobody is without water" would fail on that occasion. Under the new interpretation, the condition would be lost. Under the old interpretation (r at ξ'), it might not be (at ξ', the person has water again).

Does this affect any of the given situations? Let me check the ones listed for R36: O13, O20, O25, O26, O35, O39, O52.

O13 (two plumbers): "Ben's act stopped the leak." The protected conditions would be about other things being maintained. The new interpretation requires them to hold throughout. No specific issue.

O20 (two valves): "Either valve alone would have stopped it." Protected conditions about not losing other things. No specific issue with the every-occasion requirement.

O25 (one second apart): "Ana stopped it. Ben's act would have stopped it and did not, because there was nothing left to stop when it came." The protected condition about not losing something. The new interpretation would check throughout. No specific issue.

O26 (two feeds): "Both did the work, and the record divides it: each stopped the half that ran through their feed." Protected conditions. No specific issue.

O35 (four seconds): As discussed above. The verdict depends on the protected condition. If "nobody is without water," the verdict is unchanged.

O39 (two routes): "Both operated and both were sufficient." Protected conditions. No specific issue.

O52 (the route that started and was stopped): "Ben stopped it. Ana's route started and did no stopping." Protected conditions. No specific issue.

I don't think R36 moves any verdict. But the new interpretation is stricter (every occasion vs. at ξ'). This is a claim change - the new text claims more about what r(ξ') means. The declaration accounts for this.

But there's the formula notation issue. (P) uses r(ξ') in a non-standard way. The new text explicitly says what it means, so this is handled. But it might still confuse readers.

Let me also check: does the new text say r(ξ') means "held on every occasion from ξ to ξ'" or "held on every occasion it covers from ξ to ξ'"? The new text says: "r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."

So r(ξ') means "r held on every occasion r covers from ξ to ξ'." The phrase "it covers" is important - r covers certain occasions, and r(ξ') means r held on every such occasion from ξ to ξ'. This is more nuanced than just "every occasion from ξ to ξ'" - it's every occasion that r covers within [ξ, ξ'].

This is important because r might not cover all occasions. For O35, r = "nobody is without water" might cover all occasions when someone might draw water. If nobody draws during the four seconds, r holds on every occasion it covers from ξ to ξ'.

OK, I think R36 is mostly fine. The change is a clarification that makes the every-occasion requirement explicit. The declaration is accurate. The formula notation is slightly non-standard but the text explains it.

But wait - I should double-check whether R36 contradicts anything in the formula (P). (P) says "∀r ∈ P[r(ξ) ⇒ r(ξ')]." With the new interpretation: "for every protected condition r, if r held at ξ (or up to ξ?), then r held on every occasion it covers from ξ to ξ'." What does r(ξ) mean? The text only defines r(ξ'), not r(ξ).

If r(ξ) means "r held at ξ" (the standard reading), then (P) says: "if r held at ξ, then r held on every occasion from ξ to ξ'." This is a reasonable condition.

But if r(ξ) also has a non-standard meaning (e.g., "r held on every occasion up to ξ"), then the condition is different. The text doesn't specify r(ξ)'s meaning.

Hmm, the text says "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'." This defines r(ξ') but not r(ξ). Presumably r(ξ) means "r held at ξ" (the standard reading). But this is not stated.

Is this a coherence issue? The text defines one of the two occurrences of r in (P) but not the other. This could be confusing. But it's not a clear contradiction.

Actually, looking at the context before R36: "The obligations are declared inputs: O says what is to be repaired and P what is to be protected, each as a stated condition over stated occasions, and a protected condition is lost exactly when it fails on an occasion it covers." This says "a protected condition is lost exactly when it fails on an occasion it covers." This means r is lost when r fails on some occasion it covers. So r is about holding on occasions. r(ξ) would mean r holds at ξ (or on the occasions around ξ). r(ξ') would mean r holds at ξ' (or on the occasions around ξ'). But the new text says r(ξ') means "held on every occasion from ξ to ξ'."

There's a subtle issue: the old text said "a protected condition is lost exactly when it fails on an occasion it covers." This is about r failing on ANY occasion it covers (not just between ξ and ξ'). But the new text defines r(ξ') as "held on every occasion it covers from ξ to ξ'." So r(ξ') is about the interval [ξ, ξ'] specifically, while the old prose is about any occasion.

Wait, but (P) is about a specific repair from ξ to ξ'. So the relevant occasions are those between ξ and ξ'. The old prose says "a protected condition is lost exactly when it fails on an occasion it covers." In the context of (P), "an occasion it covers" would be an occasion in the interval [ξ, ξ']. So the old prose is about occasions in [ξ, ξ'], and the new text specifies this.

But the old prose is more general: "a protected condition is lost exactly when it fails on an occasion it covers." This could be about any occasion (not just in [ξ, ξ']). The new text narrows this to "from ξ to ξ'." Is this a change?

Hmm, the old prose is in the context of describing the obligations O and P. "A protected condition is lost exactly when it fails on an occasion it covers." This is about what it means for a protected condition to be "lost" in general, not specifically in (P). The new text adds: "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'." This is specifically about (P) and defines r(ξ').

So the old prose is general (loss = failure on any covered occasion), and the new text is specific to (P) (r(ξ') = held on every covered occasion from ξ to ξ'). These are consistent: in (P), the relevant occasions are those from ξ to ξ', and r(ξ') means r held on all of them.

OK, I think R36 is fine. The declaration is accurate and the change is a clarification.

But let me check one more thing. The declaration says "Part XI now says that in (P), for a protected condition r, r(ξ') means that r held on every occasion it covers from ξ to ξ', not only at ξ'." Does the new wording say exactly this?

New wording: "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."

Yes, this matches the declaration. Good.

**R37** (Part XI, Created explanatory knowledge, line 447)
- Old: "The scope of Account(c, p_c) is the contract fixed at e_c."
- New: "Result(Δ) is the set of contents at ξ' that Δ prepared. ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c. The scope of Account(c, p_c) is the contract fixed at e_c."
- Declaration: Part XI now defines Result(Δ) as the set of contents at ξ' that Δ prepared. It defines ProducesVia(Δ, c, o; ξ, ξ') as holding when an active route that runs from Δ to the repair of o contains the relevant binding of c.

This adds definitions of Result(Δ) and ProducesVia(Δ, c, o; ξ, ξ'). The declaration says it defines these two things.

Let me check coherence. In (EK): "c ∈ Result(Δ)" and "ProducesVia(Δ, c, o; ξ, ξ')." These are used in the formula but were not defined before. The new text provides definitions.

Result(Δ) = "the set of contents at ξ' that Δ prepared." This is clear: the contents that Δ (the contribution) prepared by the time ξ'.

ProducesVia(Δ, c, o; ξ, ξ') = "holds when an active route that runs from Δ to the repair of o contains the relevant binding of c." This says: there's an active route from Δ to the repair of obligation o, and this route contains the relevant binding of content c.

Let me check consistency with Part IX and Part XI. Part IX defines "active route" as "a connected subnetwork of actual occurrences joining a represented input to an operative result." Part XI defines ProducedBy as holding "when an active route (Part IX) runs from Δ to the repair."

ProducesVia is similar to ProducedBy but adds the condition that the route "contains the relevant binding of c." This is a more specific condition: not just any active route from Δ to the repair, but one that carries the binding of c.

This seems coherent. ProducesVia is a refinement of ProducedBy that adds a condition about the binding of c being on the route.

But wait - "the relevant binding of c." What is "the relevant binding"? The text says "the relevant binding of c" without defining "relevant." In (EK), the context is about content c being produced via Δ for the repair of obligation o. The "relevant binding" would be the binding of c that's relevant to the repair of o. But this is not formally defined.

Is this a coherence issue? The term "the relevant binding of c" is used but not formally defined. The declaration uses the same phrase: "contains the relevant binding of c." So the declaration and the new wording are consistent. But the term itself is ambiguous.

However, this might be intentional. The text often uses prose descriptions that are not formally defined (e.g., "a nontrivial binding construction" in Part X). The context makes the meaning clear enough.

Let me also check if R37 contradicts anything. The definitions of Result(Δ) and ProducesVia seem consistent with how they're used in (EK). No obvious contradictions.

But wait - "Result(Δ) is the set of contents at ξ' that Δ prepared." What does "Δ prepared" mean? Δ is "a subhistory together with the changes of content it makes" (from R35). So "Δ prepared" means the changes of content that Δ makes prepared these contents. This is consistent.

And "ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c." This says the active route goes from Δ to the repair of o and contains c's binding. In (EK), this is used as "ProducesVia(Δ, c, o; ξ, ξ')" which matches.

I think R37 is fine. The definitions are useful and consistent.

But there's one more thing to check. R37's declaration says "Part XI now defines Result(Δ) as the set of contents at ξ' that Δ prepared. It defines ProducesVia(Δ, c, o; ξ, ξ') as holding when an active route that runs from Δ to the repair of o contains the relevant binding of c." This says the change defines two things. The new wording does define these two things. The declaration is accurate.

But does the new wording claim more than the declaration? The new wording adds: "Result(Δ) is the set of contents at ξ' that Δ prepared. ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c." This is two definitions. The declaration says the change "defines Result(Δ)" and "defines ProducesVia." This matches. Good.

**R40** (Part XII, System boundary and continuity, line 467)
- Old: "Both are declared before the attribution, not chosen after it."
- New: "Both are declared before the attribution, not chosen after it. Where the system's boundary is not declared explicitly, a statement that names the system and says whether a process runs inside it or outside it declares the boundary for that process; a statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary."
- Declaration: Part XII now says two things. Where the system's boundary is not declared explicitly, a statement that names the system and says whether a process runs inside or outside it declares the boundary for that process. A statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary.

This adds two rules about implicit boundary declarations. Let me check coherence.

Rule 1: "Where the system's boundary is not declared explicitly, a statement that names the system and says whether a process runs inside it or outside it declares the boundary for that process." This says that naming the system and saying where a process runs implicitly declares the boundary for that process.

Rule 2: "A statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary." This says that claiming a process is the system's own (without saying where it runs) is an attribution, not a boundary declaration.

These rules are about how boundaries are declared implicitly. They seem to address situations like O17 (the robot's log and the maker's manual) and O30 (the routine uploaded that morning).

Let me check O17: "A repair robot keeps a log of every comparison it runs. The log shows which routine, running inside the robot, produced today's diagnosis. The maker's manual says the routine is the robot's own 'because the robot can diagnose'."

Verdict: "Ownership here rests on the log, which shows the routine running inside the robot. Strike the manual's sentence and nothing is lost; keep only the manual's sentence and there is nothing."

With R40: The log "shows which routine, running inside the robot" - this names the system (the robot) and says the process runs inside it. By Rule 1, this declares the boundary for that process (the routine runs inside the robot). The manual says "the routine is the robot's own 'because the robot can diagnose'" - this is a statement that the process is the system's own, which does not say where it runs. By Rule 2, this is the attribution, not the boundary.

So with R40, the log declares the boundary (the routine runs inside the robot), and the manual is just an attribution (which is not a boundary declaration). This matches O17's verdict: "Ownership here rests on the log" (the boundary is from the log) and "Strike the manual's sentence and nothing is lost" (the manual is just an attribution).

Good, R40 seems consistent with O17.

Let me check O30: "The robot compares the pressures itself and chooses the valve, using a routine a remote person uploaded to it that morning."

Verdict: "The comparison ran inside the robot and was the robot's work today. Who wrote the routine is a separate fact, and a true one. Ownership of today's history is the robot's; the origin of the routine is the person's."

With R40: "The comparison ran inside the robot" names the system (the robot) and says the process runs inside it. By Rule 1, this declares the boundary. The routine is the robot's own because it runs inside the boundary. The content of the routine is the person's (the writer's), which is handled by R33.

This seems consistent with O30.

Let me check O51 (the module in the casing): "A controller runs a diagnostic module bought from a supplier and fitted on a removable processor inside its own shell. The system boundary declared at the start names the controller and its routines and says nothing about the module. The module produces today's diagnosis."

Verdict: "The diagnosis was produced inside the shell and outside the declared boundary. Where the boundary was drawn decides it, and the boundary was drawn without the module. Being inside the casing changes nothing."

With R40: "The system boundary declared at the start names the controller and its routines and says nothing about the module." This is an explicit boundary declaration (it names the controller and its routines). So Rule 1 doesn't apply (the boundary IS declared explicitly). The boundary excludes the module. Even though the module is inside the shell, it's outside the declared boundary. So the module's process is not the controller's own.

This matches O51's verdict. Good.

Now let me check O41 (the technician's whisper): "The assessment is stated at the start as robot-only. Midway, a technician tells the robot which gauge to read."

Verdict: "The boundary was robot-only, and the instruction crossed it. The diagnosis is a team result under a boundary nobody restated. Robot-only credit is not available for it."

With R40: The assessment states "robot-only" which declares the boundary (the system is the robot). The technician's instruction crosses the boundary (it comes from outside the robot). By Rule 2, a statement that the process is the system's own (without saying where it runs) is an attribution, not a boundary. The technician's instruction is not the robot's own because it comes from outside the boundary.

But wait - the technician's instruction is executed inside the robot (the robot reads the gauge). Does R40's Rule 1 apply? The statement is about the technician telling the robot which gauge to read. This doesn't name the system and say where the process runs. It's about an instruction from outside. R33 says "Work supplied from outside that boundary... remains an outside contribution however it is executed inside." This handles the technician's instruction.

R40's rules are about how boundaries are declared. O41 has an explicit boundary declaration ("robot-only"), so Rule 1 doesn't apply. Rule 2 is about statements that claim ownership without specifying location. O41 doesn't have such a statement (the assessment says "robot-only" which is a boundary declaration). So R40 doesn't directly affect O41. But R33 and other parts of the text handle it.

I think R40 is fine. The declaration is accurate and the change is consistent with the situations.

But wait - let me check if R40's new text contradicts anything. The first rule says "a statement that names the system and says whether a process runs inside it or outside it declares the boundary for that process." This is about implicit boundary declaration. But what if two statements give conflicting boundary information? For example, one statement says the process runs inside, and another says it runs outside. The text doesn't handle this case. But this is an edge case and not a clear contradiction.

Also, Rule 2 says "A statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary." This is clear: claiming ownership without specifying location is an attribution, not a boundary. This is consistent with Part XII's earlier text: "Ownership is grounded in the processes and resources the boundary includes, never in the capability being attributed: 'owned because it can, and can because owned' grounds neither."

OK, I think R40 is fine.

Let me now look at R42 more carefully.

**R42** (Part XII, Selection in the physical module, line 475)
- Old: "a population of realized transports"
- New: "a population of candidate transports"
- Declaration: Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones. The population is the set its own last sentence gives: the transports the physics and the stated construction admit.

This changes "realized" to "candidate." The declaration says it aligns Part XII with Part IV (which says "candidate transports").

Let me check Part IV: "There is a population T of candidate transports, a variation operator μ on T, a finite history H ⊆ C of edit–boundary pairs actually encountered, and a survival condition requiring fidelity on H."

So Part IV says "candidate transports." Part XII (old) said "realized transports." R42 changes Part XII to say "candidate transports," matching Part IV.

The declaration also says "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." Let me check Part XII's last sentence: "The population is the set of transports the physics and the stated construction admit; a transport that would need a part every member of the population is built without is not in it."

So the population is "the set of transports the physics and the stated construction admit." This is the definition. "Candidate transports" = "transports the physics and the stated construction admit." This seems right.

But wait - the declaration says "the set its own last sentence gives." The last sentence of Part XII says "The population is the set of transports the physics and the stated construction admit." But there's also: "a transport that would need a part every member of the population is built without is not in it." This second clause adds a restriction: a transport that needs a part not present in any population member is excluded. So the population is NOT just "the transports the physics and the stated construction admit" - it's also restricted by what parts are available.

The declaration says "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." But the last sentence has TWO parts: (1) the population is the set of admitted transports, and (2) some transports are excluded because they need unavailable parts. The declaration only references the first part.

Is this a problem? The declaration says "the set its own last sentence gives." The last sentence gives a set with a restriction. The declaration simplifies this to just the first part. Is the declaration wrong?

Actually, the last sentence says "The population is the set of transports the physics and the stated construction admit; a transport that would need a part every member of the population is built without is not in it." The semicolon separates two clauses. The first clause defines the population. The second clause adds a restriction. So the population is "the set of transports the physics and the stated construction admit, minus those that need unavailable parts."

The declaration says "the set its own last sentence gives: the transports the physics and the stated construction admit." This only gives the first part of the definition. The second part (the restriction) is not mentioned in the declaration.

But does this matter? The declaration is about what R42 changes. R42 changes "realized" to "candidate." The declaration says this aligns with Part IV and references the definition. The definition has two parts, but the declaration only mentions one. This is incomplete but not wrong.

Actually, wait. The declaration says "The population is the set its own last sentence gives." This says the population is what the last sentence gives. The last sentence gives a set with a restriction. So the declaration is saying the population is the restricted set. But then it says ": the transports the physics and the stated construction admit." This is an appositive that simplifies the definition. So the declaration gives the simplified definition.

Hmm, I think the declaration is slightly incomplete because it doesn't mention the restriction. But the change itself (realized → candidate) is fine.

But there's another issue. R42 changes "realized" to "candidate." The declaration says this matches Part IV. But does it also affect the definition? If the population was "realized transports" (old), then the definition "the set of transports the physics and the stated construction admit" was already there. Changing "realized" to "candidate" in the description doesn't change the definition (which is in the last sentence). But it does change what a reader initially understands the population to be.

In the old text: "a population of realized transports." A reader would think the population contains only realized (actually built) transports. In the new text: "a population of candidate transports." A reader would think the population contains candidate (possible) transports. This is a significant difference.

The declaration says this change matches Part IV. Part IV says "candidate transports." So the change aligns Part XII with Part IV. But does the change alter what the population IS? The definition in the last sentence already said "the set of transports the physics and the stated construction admit." This definition is not changed by R42. So the population is the same set; only the description changes.

But if the description "realized transports" was correct (and the definition was wrong), then changing the description to "candidate transports" would make the description consistent with Part IV but might change what the population actually is. Conversely, if the definition was correct (and the description was wrong), then changing the description to match Part IV would fix an inconsistency.

I think R42 is fixing an inconsistency: Part XII described the population as "realized transports" but defined it as "the set of transports the physics and the stated construction admit" (which is broader). Part IV described the population as "candidate transports" (which matches the definition). R42 fixes Part XII's description to match.

This is a reasonable fix. But does it change a claim? If the old description ("realized") was taken as the definition, then changing to "candidate" broadens the population. If the old definition (last sentence) was taken as the definition, then the description change doesn't affect the population.

The declaration says "Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones." This says the change is in the description. "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." This says the population is defined by the last sentence (unchanged). So the declaration says the population doesn't change - only the description does.

But a reader of the old text might have taken "realized transports" as defining the population (and the last sentence as elaborating). In that case, the change would broaden the population. The declaration doesn't account for this possibility.

Hmm, but the declaration explicitly says "The population is the set its own last sentence gives." This directs the reader to the last sentence for the definition. So the declaration is saying: the population is defined by the last sentence (unchanged), and the description is now "candidate" instead of "realized." This seems to account for the change.

But there's still the question of whether "realized" and "candidate" describe the same set or different sets. If the population is defined by the last sentence (as the declaration says), then the description doesn't matter. But if the description constrains the definition, then "candidate" is broader than "realized."

I think the declaration is trying to say: the description changes from "realized" to "candidate," but the population is still defined by the last sentence. So the population doesn't change. This is a WORDING change (the description now matches Part IV) dressed up as a CLAIM change.

Wait, but the change IS declared as CLAIM. If the population doesn't change, why is it CLAIM? The declaration says "Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones." This says Part XII NOW DESCRIBES it differently. This is a change in what Part XII says (its description), which is a claim change (the text now claims the population is "candidate" rather than "realized").

But if the population is defined by the last sentence (unchanged), then the description change doesn't affect what the population IS. It only affects how it's described. This would be a WORDING change, not a CLAIM change.

Unless the description "realized" vs. "candidate" changes what a reader concludes about the population. If a reader reads "realized transports" and concludes the population contains only realized transports, that's a claim. Changing to "candidate transports" changes this claim. But the last sentence (unchanged) already defines the population more broadly.

I think this is a tension in R42. The declaration says the population is defined by the last sentence (unchanged), but the change is CLAIM (the description changes). If the description is just wording (the population is unchanged), it should be WORDING. If the description changes the claim (the population is broader), the declaration should say so.

Let me think about this differently. The declaration says two things:
1. Part XII now describes the population as candidate transports (matching Part IV).
2. The population is defined by the last sentence: "the transports the physics and the stated construction admit."

The first is about the description. The second is about the definition. If the description change doesn't affect the definition, it's a wording change. But the declaration says it's a CLAIM change.

Actually, I think the CLAIM is that Part XII now SAYS "candidate" instead of "realized." Even if the population is defined by the last sentence, the description "candidate" is a claim about the population (it's a claim that the population contains candidates, not just realized transports). This is a CLAIM change because the text now claims something different about the population.

But then the declaration should say: "Part XII now claims the population contains candidate transports (not just realized ones)." Instead, it says "Part XII now describes the population... as candidate transports." The word "describes" is about wording, not claim. But the change is CLAIM.

Hmm, I think there's a subtle issue here. The declaration uses "describes" which suggests a wording change, but the change is classified as CLAIM. This is a mismatch.

But wait, the declaration also says "not realized ones." This implies the old text claimed "realized" and the new claims "candidate." This IS a claim change. So the declaration does account for the claim change (from "realized" to "candidate").

OK, I think R42 is mostly fine but has a subtlety about whether the description change is a claim change or a wording change. The declaration says it's a claim change (from "realized" to "candidate") and also references the definition (unchanged). This seems reasonable.

But there's still the issue I identified earlier: R52 drops "realizable" from the conditions. If R42 broadens the population (from realized to candidate) and R52 drops "realizable" from Derivation 3's conditions, the net effect is to make Derivation 3's underdetermination result apply more broadly. But R52's declaration doesn't explicitly say it drops "realizable."

Let me now go back to R52.

**R52** (Part XVI, Derivation 3, Claim, line 566)
- Old: "it must be admitted, realizable, a member of T, and a survivor of H."
- New: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."
- Declaration: Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list "admitted, realizable, a member of the population, and a survivor of H".

The old list: admitted, realizable, member of T, survivor of H (4 conditions).
The new list: member of T, admitted by physics/construction, survive on H (3 conditions).

"Realizable" is dropped. The declaration says it "replaces the list" but doesn't explicitly say "realizable" is dropped.

Is "realizable" different from "admitted by physics/construction"? Let me think.

In the physical module (Part XII), "realizable" might mean "has a physical implementation." "Admitted by physics/construction" means "doesn't violate physical laws and is compatible with the stated construction." These might be different: a transport could be admitted by the physics (doesn't violate laws) but not realizable (can't actually be built with available resources).

But looking at Part XII: "a task a permitted input-to-output attribute transformation with explicit resources and side effects." Tasks have explicit resources. "Admitted" means the physics permits it (with the stated resources). "Realizable" might mean the same thing (can be done with the stated resources).

Actually, in Part XII: "Possibility is the absence of a law-imposed limit short of perfection on performance and retention." And "Admit^{q,r}_Θ is the set of tasks physically achievable at grades (q, r)." So "admitted" means physically achievable. "Realizable" might mean the same as "physically achievable."

If "realizable" = "admitted by physics/construction," then the old list had redundancy ("admitted" and "realizable" are the same). The new list drops one of the redundant terms. This is a WORDING change, not a CLAIM change.

But the declaration says it's a CLAIM change. And the declaration says "This replaces the list" which implies the list changes. If the list changes from 4 to 3 items (dropping "realizable"), that IS a claim change (the new claim has fewer requirements).

Hmm, but if "realizable" and "admitted" are the same, dropping one doesn't change the claim. It just makes the list shorter.

I think the key question is whether "realizable" adds anything beyond "admitted." If it does, dropping it changes the claim. If it doesn't, it's just cleanup.

Given the ambiguity, I think R52's declaration is incomplete: it should explicitly say whether "realizable" is being dropped as redundant or as a genuine condition. The declaration says "This replaces the list" which implies the list is different, but doesn't say what happens to "realizable."

But is this a defect that makes R52 FALL? Let me think about what the task says: "a change FALLS when... its declaration is false or incomplete." If the declaration is incomplete (doesn't account for dropping "realizable"), then R52 FALLS.

But wait - the declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." This identifies the old list (4 items) and the new claim (3 items). The reader can see that "realizable" is not in the new claim. So the declaration implicitly accounts for the drop.

Is "implicitly accounts" enough? The task says the declaration "is meant to say exactly what changes in what is claimed." If "realizable" is dropped and the declaration doesn't say so, it's not saying "exactly what changes."

Hmm, but the declaration gives both the old list and the new claim. The difference (dropping "realizable") is visible. Is that "exactly what changes"?

I think this is borderline. Let me look for stronger issues elsewhere.

Actually, let me reconsider R31 more carefully. I identified a potential asymmetry issue.

**R31** revisited:

New text: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

The declaration: "Part X now defines the equivalence in (N): d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at that grain, not string equality or similarity."

Issue1: Asymmetry. The definition uses "c's contract" for both transports. For the transport from d to c, the natural contract is d's contract (the source's contract). Using c's contract for both makes the relation asymmetric: d ≡_ℓ c uses c's contract, while c ≡_ℓ d would use d's contract. These are different conditions.

Issue 2: "Both faithful on c's contract" for a transport from d to c. A transport from d to c has d as source and c as target. Its fidelity is typically checked on the source's contract (d's contract). Using c's contract for it might be ill-typed or at least non-standard.

Issue 3: The declaration says "The equivalence is structural at that grain." But if the relation is asymmetric, it's not an equivalence. Calling it "the equivalence" is misleading.

These are significant issues. The definition of ≡_ℓ is potentially wrong (asymmetric, possibly ill-typed). The declaration doesn't account for this.

But wait, let me reconsider. Maybe "faithful on c's contract" has a specific meaning that I'm missing. Let me re-read how fidelity is defined.

Part IV: "A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V."

Part V: The fidelity conditions (F1) and (F2) are stated for a transport t from D to E with contract C. C ⊆ A × B where A is D's edit set and B is D's boundary set. So C ranges over D's (the source's) edits.

A transport from d to c: source is d, so fidelity should be on d's contract (a subset of d's edits × boundaries).
A transport from c to d: source is c, so fidelity should be on c's contract.

R31 says "both faithful on c's contract." This applies c's contract to both transports. For the transport from d to c, c's contract ranges over c's edits, but the transport's source is d. The fidelity conditions would need to relate c's edits to d's structure via the transport's τ (which maps d's edits to c's edits). But c's contract has c's edits, and τ maps d's edits to c's edits. So (a, b) ∈ c's contract means a is a c-edit, and τ^{-1}(a) would be a d-edit (if τ is invertible). But the fidelity conditions use (a, b) ∈ C and τ(a), where a is a source edit. If C is c's contract (c's edits), then a is a c-edit and τ(a) would be... well, τ maps d's edits to c's edits, so τ(a) doesn't make sense for a c-edit.

This confirms that "faithful on c's contract" for a transport from d to c is ill-typed. The contract should range over the source's edits (d's edits for a transport from d to c), not the target's edits (c's edits).

So R31's definition is ill-typed for one of the two transports. This is a genuine defect.

But wait, maybe the definition is using "c's contract" in a looser sense. Maybe "c's contract" means "the contract that c specifies" and this contract can be applied to any transport related to c. But this is a stretch and not supported by the text.

Alternatively, maybe "faithful on c's contract" means "faithful in the sense that it preserves what c's contract specifies." For a transport from d to c, this would mean: the transport maps d's responses to c's responses correctly under c's contract. But this requires translating c's edits to d's edits (via τ^{-1}), which is the reverse of what τ does.

I think R31 has a genuine defect: the definition of ≡_ℓ is ill-typed (applying c's contract to a transport from d to c) and asymmetric (using c's contract for both directions). The declaration doesn't account for this.

Actually, hold on. Let me reconsider once more. Perhaps the definition is correct and I'm misunderstanding the direction.

"Transports from d to c" - this maps d's structure to c's structure. "Both faithful on c's contract" - the contract is c's contract.

For the transport from d to c: the fidelity conditions relate d (source) to c (target). The contract ranges over d's edits (source's edits). But "c's contract" ranges over c's edits. So "faithful on c's contract" for this transport doesn't match the standard setup.

UNLESS "c's contract" is used to mean "the contract of the question whose target is c." In Part V, the question p has target D and contract C ⊆ A × B (D's edits). A question about c would have target c and contract C_c ⊆ A_c × B_c (c's edits). "c's contract" = C_c.

Now, a transport from d to c: in Part V, this would be t from D=d to E=c. The contract C in Part V ranges over D's edits (= d's edits). So the contract for this transport should be d's contract, not c's contract.

But "faithful on c's contract" means using C_c (c's edits). This doesn't match the Part V setup where C ranges over D's (= d's) edits.

I'm now quite confident that R31's definition is ill-typed. The contract for a transport from d to c should be d's contract (or a contract over d's edits), not c's contract.

Unless... the definition is intentionally using c's contract as a reference to check both directions against c's structure. This would mean: both transports, when evaluated against c's contract, are faithful. For the transport from c to d, this is natural (c is the source). For the transport from d to c, this requires evaluating against c's contract in the reverse direction.

This is non-standard and possibly ill-defined. But the text says it. Let me accept it as stated and see if it makes the relation symmetric.

d ≡_ℓ c: transports from d to c and from c to d, both faithful on c's contract.
c ≡_ℓ d: transports from c to d and from d to c, both faithful on d's contract.

For d ≡_ℓ c and c ≡_ℓ d to be the same, we need "both faithful on c's contract" and "both faithful on d's contract" to be equivalent. This requires c's contract and d's contract to be equivalent (or the same). In general, they might not be.

So the relation is NOT symmetric unless c and d have the same contract. This makes it not an equivalence relation.

But the text says "The equivalence is structural at the stated grain." If it's not an equivalence, this is wrong.

Hmm, but maybe the text is using "equivalence" loosely (just meaning "equivalent in some sense"), not as a formal equivalence relation. The declaration says "The equivalence is structural at that grain, not string equality or similarity." This describes "the equivalence" but doesn't claim it's a formal equivalence relation.

Still, using ≡ notation is standard for equivalence relations. And (N) uses d ≡_ℓ c in a negation: "¬∃d ∈ R_{<e}, d ≡_ℓ c." For (N) to work correctly, ≡_ℓ should be reflexive (d ≡_ℓ d for all d) and ideally symmetric. If it's not symmetric, (N) might give asymmetric results.

Actually, for (N), we only need to check "d ≡_ℓ c" for d in the prior repertoire and c the candidate. If the relation is asymmetric (using c's contract), then (N) is checking whether any prior content d is equivalent to c using c's contract. This is checking whether c's structure is reproduced by some prior d. This might be the intended meaning: c is new if no prior d can reproduce c's structure.

In this reading, the asymmetry is intentional: we check whether d can reproduce c (not the reverse). The relation is not symmetric, but it's used in (N) in a specific direction.

But the text calls it "the equivalence" and uses ≡ notation. If it's intentionally asymmetric, calling it "equivalence" is misleading.

I think R31 has a genuine issue: the definition of ≡_ℓ is asymmetric and possibly ill-typed, but it's called "the equivalence" and uses ≡ notation. The declaration doesn't account for this.

But wait - the declaration says "The equivalence is structural at that grain, not string equality or similarity." This says "the equivalence is structural." If the relation is not truly an equivalence (not symmetric), then "the equivalence" is wrong. The declaration would be making a false claim.

Hmm, but maybe "the equivalence" refers to "the equivalence notion" or "what it means for d and c to be equivalent." This doesn't necessarily mean it's a formal equivalence relation. It could mean "the concept of equivalence" or "the way we determine equivalence."

I think the issue is debatable. Let me see if there are stronger issues elsewhere.

Actually, let me look at the situations for R31: O3, O11, O18, O27, O31, N4, N10, N15, N21.

Let me check if R31 moves any verdict.

O3 (Nadia and the cards): "Nadia built something new: a part that stands for cards she has never observed. That is construction, and it is her own."

For (N), we need to check if Nadia's new content is equivalent to any prior content. With R31's definition, d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract. If Nadia's prior repertoire doesn't contain anything equivalent to her new "waiting cards" content, then it's new.

Does R31's definition change whether this is new? The definition of ≡_ℓ is new (it wasn't defined before). Previously, (N) used ≡_ℓ without defining it. Now it's defined. This might change what≡_ℓ means, which could affect (N).

But the situations listed for R31 are about whether R31 moves the verdict. If the definition of ≡_ℓ is new and changes what ≡_ℓ means, it could move the verdict on (N). Let me check.

For O3: Nadia's new content (cards waiting somewhere) is compared to her prior contents (predicting from cards just played). Are these equivalent under R31's definition? They would need transports in both directions, faithful on the new content's contract. The prior content (predicting from recent cards) and the new content (tracking unseen cards) are structurally different. So they're probably not equivalent. The verdict (construction, new) is unchanged.

But what if the definition of ≡_ℓ is so broad that everything is equivalent to everything? Then Nadia's content would not be new (since it's equivalent to some prior content). But R31's definition requires transports in both directions, faithful on the contract. This is a structural requirement that shouldn't make everything equivalent.

For O11 (the apprentice and the third lock): "Monday is blind fitting. Tuesday is making something new. Wednesday is neither: she is reusing what Tuesday gave her."

For (N) on Wednesday: the apprentice's content (the key that looks like Tuesday's) is compared to her prior repertoire. If Tuesday's key is in her repertoire, then Wednesday's content is equivalent to Tuesday's (it IS Tuesday's). So it's not new. The verdict says "she is reusing what Tuesday gave her" which means it's not new. This is consistent.

Does R31 change this? If ≡_ℓ is defined as having transports in both directions, faithful on the contract, then two identical contents would be equivalent (the identity transport works). So Tuesday's key and Wednesday's key are equivalent. Not new. Verdict unchanged.

For N4 (the tilt and the midnight sun): "The idea already accounted for the midnight sun. No one, she included, had explained it."

This is about whether the idea accounts for something (not about (N) directly). R31 defines ≡_ℓ which is used in (N). (N) is about newness. N4 is about whether an idea accounts for something and whether someone explained it. These are related but not directly about (N).

Hmm, but the situations listed for R31 include N4. Let me think about why. Maybe the≡_ℓ definition affects whether the astronomer's idea is "new" (for (G), which requires (N)). If the idea is new (not equivalent to any prior content), then (G) can hold. If not, (G) doesn't hold.

For N4: the astronomer proposes the tilt idea. Is this equivalent to any prior content? If her prior repertoire doesn't contain the tilt idea, it's new. R31's definition of ≡_ℓ determines this. If the definition is correct, the verdict is unchanged. If the definition is wrong (e.g., too broad or too narrow), the verdict might change.

But without more details about the astronomer's prior repertoire, I can't determine if the verdict moves. The situations listed for R31 are "possibly affected," not "definitely affected."

OK, I think the main issue with R31 is the asymmetry/ill-typing of the ≡_ℓ definition. Let me also check other situations.

N21 (the order of adjectives): "She knows it, tacitly. She formed her grasp of it herself."

For (N): is Ngozi's grasp of the adjective order rule new (not in her prior repertoire)? Her prior repertoire contains examples of speech but not the rule. The rule is structurally different from the examples (the rule generalizes to new phrases). So under R31's definition, the rule is probably not equivalent to the examples (no transports in both directions that are faithful). So it's new.

But what if R31's definition is so loose that the examples are equivalent to the rule? The examples are individual sentences; the rule is a generalization. Transports from examples to rule and from rule to examples might not both be faithful on the rule's contract. So they're probably not equivalent. Verdict unchanged.

OK, I don't think R31 moves any verdict. But the definition is potentially ill-typed and asymmetric.

Let me now think about the overall picture. Which changes have the strongest issues?

1. **R31**: The definition of ≡_ℓ is asymmetric (uses "c's contract" for both directions) and potentially ill-typed (applying c's contract to a transport from d to c). The declaration doesn't account for this. The relation is called "the equivalence" but may not be a true equivalence.

2. **R52**: The declaration says it "replaces the list" of 4 conditions with 3, dropping "realizable." But it doesn't explicitly say "realizable" is dropped. If "realizable" is a genuine additional condition, the declaration is incomplete.

3. **R42**: Changes "realized" to "candidate." The declaration says this matches Part IV and the population is defined by the last sentence. But the change is CLAIM (description changes from "realized" to "candidate"), and the declaration uses "describes" which sounds like wording. There's a tension between the declaration's language and the change's classification.

4. **R36**: Makes r(ξ') in (P) mean "held on every occasion from ξ to ξ'" rather than "held at ξ'." This is a stricter condition. The formula notation is non-standard. But the declaration accounts for this.

Let me also think about R11 more carefully.

**R11** revisited:

New text: "The transport t is a member of T that survived. A transport **survives on H** when it is a member of T that meets the survival condition on H; fidelity on H without membership in T is not survival."

The declaration: "Part IV now defines surviving on H: a transport survives on H when it is a member of the population that meets the survival condition on H, and fidelity on H without membership in the population is not survival."

This seems to match. But let me check: the new text says "fidelity on H without membership in T is not survival." The declaration says "fidelity on H without membership in the population is not survival." T = the population. So these match.

But there's a subtlety: the definition says "a transport survives on H when it is a member of T that meets the survival condition on H." This is a definition of "survives on H." But the preceding sentence says "The transport t is a member of T that survived." This uses "survived" (past tense) without "on H." Is "survived" the same as "survives on H"?

The old text said "The transport t is a member of T that survived." The new text keeps this sentence and adds the definition of "survives on H." But "survived" (without "on H") is not the same as "survives on H." "Survived" might mean "survived on some history" or "survived in general." "Survives on H" is specifically about history H.

Is this a coherence issue? The text uses both "survived" and "survives on H." If they mean the same thing, the definition should say so. But the definition only defines "survives on H."

Actually, looking at the context: "There is a population T of candidate transports, a variation operator μ on T, a finite history H ⊆ C of edit–boundary pairs actually encountered, and a survival condition requiring fidelity on H. The transport t is a member of T that survived."

Here, "survived" is in the context of the survival condition requiring fidelity on H. So "survived" means "survived on H" (survived the survival condition, which is about H). The new text adds a formal definition of "survives on H" which clarifies what "survived" means.

But the new definition says "a transport survives on H when it is a member of T that meets the survival condition on H." This is clear. The preceding "survived" should be read as "survives on H." This is a clarification.

Is the declaration complete? It says "Part IV now defines surviving on H." This is correct. The definition is: "a transport survives on H when it is a member of T that meets the survival condition on H; fidelity on H without membership in T is not survival." The declaration accurately describes this.

But there's one more thing: "fidelity on H without membership in T is not survival." This is a negative claim (what is NOT survival). The declaration says "fidelity on H without membership in the population is not survival." This matches.

But is this negative claim already implied by the positive definition? The positive definition says survival = membership in T AND meeting the survival condition. The survival condition is "requiring fidelity on H." So survival = membership in T AND fidelity on H. The negative claim says "fidelity on H without membership in T is not survival." This is the contrapositive of "survival requires membership in T," which is already part of the positive definition.

So the negative claim is redundant. But it's not wrong - it's a clarification. The declaration says it's part of the definition, which it is (as a redundant clause).

OK, I think R11 is fine. The definition is clear and consistent.

Let me now think about R22 more carefully.

**R22** revisited:

New text adds: "Here a route is a support of the candidate, a member of S, and is a route whether or not any history runs it; it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history. The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

The declaration: "Part VI now says four things. A route is a support of the candidate, a route whether or not any history runs it. It is active in a system when occurrences that realize it form an active route in Part IX's sense. ProducedBy credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

Four things:
1. A route is a support of the candidate.
2. A route is a route whether or not any history runs it.
3. It is active in a system when occurrences that realize it form an active route (Part IX).
4. ProducedBy credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains.

Let me verify each:
1. "a route is a support of the candidate, a member of S" - Yes, this says a route is a support (member of S).
2. "and is a route whether or not any history runs it" - Yes, this says a route exists regardless of history.
3. "it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history" - Yes, this defines when a route is active.
4. "The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains" - Yes, this describes ProducedBy.

All four match the declaration. Good.

But wait - the declaration says "Part VI now says four things." But the new wording also says "a subnetwork of actual occurrences in the system's history" as an appositive to "an active route." This is a fifth thing (describing what an active route is). But the declaration only says four things.

Is the appositive "a subnetwork of actual occurrences in the system's history" part of the third thing (defining when a route is active) or a separate fifth thing? Looking at the new wording: "it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history."

The appositive "a subnetwork of actual occurrences in the system's history" modifies "an active route." It's describing what an active route is. This is part of the third thing (the definition of when a route is active). So the declaration's four things encompass this.

But is the appositive accurate? Part IX defines an active route as "a connected subnetwork of actual occurrences joining a represented input to an operative result, whose components satisfy the applicable relations and which has nonconstant dependence on the represented distinction under the declared contrasts." R22's appositive says "a subnetwork of actual occurrences in the system's history." This is a simplification (it drops "connected," "joining a represented input to an operative result," etc.).

Is this simplification wrong? No, it's just incomplete. An active route IS a subnetwork of actual occurrences (among other things). The appositive doesn't say it's ONLY a subnetwork. So it's not wrong, just less precise.

But there's a potential issue: "a subnetwork of actual occurrences in the system's history." Part IX says "a connected subnetwork of actual occurrences." R22 says "a subnetwork of actual occurrences in the system's history." The phrase "in the system's history" is added. Part IX says "a history h is a set of occurrences with an acyclic causal precedence ≺_h." So occurrences are in a history. "In the system's history" means in the history of the system. This is consistent.

OK, I think R22 is fine. The declaration is accurate and the change is consistent.

Now let me think about whether any changes pull against each other.

R11 and R52: R11 defines "survives on H." R52 references "survive on H (Part IV)." These are consistent. R52 uses R11's definition.

R42 and R52: R42 changes "realized" to "candidate." R52 drops "realizable." Together, they broaden the population and simplify the conditions. But individually, each change is about its own place.

R32 and R33: Both are in Part X, Ownership. R32 removes "today." R33 adds the credit/ownership distinction. These are independent changes in the same paragraph.

R35, R36, R37: All in Part XI. R35 defines Δ. R36 clarifies r(ξ'). R37 defines Result(Δ) and ProducesVia. These are all additions/clarifications in Part XI. They seem consistent with each other.

R35 says Δ is "a subhistory together with the changes of content it makes." R37 says "Result(Δ) is the set of contents at ξ' that Δ prepared." If Δ is "a subhistory together with the changes of content it makes," then "Δ prepared" would refer to the contents prepared by those changes. This is consistent.

R36 says r(ξ') means "held on every occasion from ξ to ξ'." This affects (P). R37 defines ProducesVia which is used in (EK). These are in different formulas but both in Part XI. They don't contradict each other.

Let me now think about whether any changes move verdicts.

For R31 (the ≡_ℓ definition): I discussed this above. The definition is potentially ill-typed and asymmetric. But does it move any verdict?

For O3 (Nadia): the verdict is "construction, her own." With R31's definition of ≡_ℓ, Nadia's new content is compared to her prior repertoire. If the definition is correct, the verdict is unchanged. If the definition is wrong (e.g., everything is equivalent), the verdict might change. But the definition requires transports in both directions, which is a strong condition. So it's unlikely that everything is equivalent.

For O11 (the apprentice): Wednesday's content is compared to Tuesday's. If they're equivalent (same content), it's not new. R31's definition should give this result (identical contents are equivalent). Verdict unchanged.

For O18 (the one tooth): "The one tooth is hers. The rest is relay." The one tooth is a new binding (construction). The rest is received (relay). For (N), the one tooth is compared to prior contents. If the one tooth is a new binding not in the prior repertoire, it's new. R31's definition should give this result. Verdict unchanged.

For N15 (the miscopied notes): "Salma has the teacher's explanation... she made something herself, though it is small." The correction ("less" → "more") is Salma's construction. For (N), the correction is compared to prior contents. If the correction is new (not in her prior repertoire), it's new. But the verdict says "What she ended up with was not new to the world, since it was the teacher's idea." This is about the CONTENT being the teacher's, not about (N) in the technical sense.

Actually, N15's verdict says "she made something herself, though it is small." This is about construction (Build), not about newness (New). The verdict says she constructed something (the correction). Whether it's "new" (in the (N) sense) depends on whether her prior repertoire contained the correction. If not, it's new.

But the verdict also says "What she ended up with was not new to the world, since it was the teacher's idea." This is about the world, not about the system's repertoire. (N) is about the system's repertoire, not the world. So (N) might say it's new (not in Salma's prior repertoire) even though it's "not new to the world."

R31's definition of ≡_ℓ determines whether it's new in (N)'s sense. If the definition is correct, the verdict on (N) is based on Salma's prior repertoire. Verdict unchanged.

OK, I don't think R31 moves any verdict on the given situations. But the definition is potentially problematic.

Let me now think about R36 and O35 more carefully.

**R36 and O35**:

O35: "During the washer repair the protected tap stops for four seconds while the mains is switched over, then runs as before. Nobody drew from it in those seconds."

Verdict: "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss."

With R36: r(ξ') means "r held on every occasion it covers from ξ to ξ', not only at ξ'." If the protected condition r is "nobody is without water," then r held on every occasion (nobody drew from the tap). So r(ξ') is TRUE. (P) holds. The verdict is "not a loss." Consistent.

But what if r is "the tap runs"? Then r did NOT hold on every occasion (it stopped for four seconds). So r(ξ') is FALSE. (P) fails for this r. The condition would be "lost."

But the verdict says "The protected condition held in every way that mattered." This suggests the condition is "nobody is without water" (which held throughout) rather than "the tap runs" (which didn't). The verdict is about the condition that matters, not about the tap's operation.

With the new text (R36), the every-occasion requirement is explicit. If the condition is "nobody is without water," the verdict is unchanged. If the condition is "the tap runs," the new text makes it a loss (stricter than before).

But the old text said "a protected condition is lost exactly when it fails on an occasion it covers." This is also an every-occasion requirement! So the old text already required r to hold on every occasion. The new text (R36) makes this explicit by defining r(ξ').

Wait, but the formula (P) says "r(ξ) ⇒ r(ξ')." If r(ξ') is just "r at ξ'" (the standard reading), then (P) only checks the endpoints. But the old prose says "a protected condition is lost exactly when it fails on an occasion it covers." This prose says the condition must hold on every occasion.

So there was already a tension in the old text: the formula (P) checks endpoints, but the prose requires every occasion. R36 resolves this by defining r(ξ') to mean "held on every occasion from ξ to ξ'." This makes the formula consistent with the prose.

Does this change a claim? The formula (P) is now interpreted differently (r(ξ') = every occasion). This makes (P) stricter. But the prose already said every occasion. So the claim doesn't change - it's just made consistent.

But a reader of the old text might have read (P) as checking only endpoints (based on the formula) and ignored the prose. In that case, the new interpretation (every occasion) would be stricter. But the prose already said every occasion. So the new text is consistent with the prose.

I think R36 is fine. It resolves a tension between the formula and the prose by defining r(ξ') to match the prose.

But there's still the issue of the non-standard notation. r(ξ') doesn't mean "r at ξ'" but "r held throughout [ξ, ξ']." This is non-standard and might confuse readers. But the text explicitly says what it means. So it's not wrong, just unusual.

OK, let me now compile my findings and decide which changes fall.

Strongest issues:

1. **R31** (task a, c): The definition of ≡_ℓ is asymmetric (uses "c's contract" for both directions) and potentially ill-typed (applying c's contract to a transport from d to c, whose fidelity should be checked on d's contract). The declaration says "The equivalence is structural" but the relation may not be a true equivalence (not symmetric). This makes the definition potentially wrong and the declaration potentially false.

Wait, let me think about this more. The declaration says: "Part X now defines the equivalence in (N): d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at that grain, not string equality or similarity."

The declaration says "the equivalence" which might just mean "the equivalence notion" (how we determine if d and c are equivalent), not "the equivalence relation." If "the equivalence" = "the equivalence notion," then it doesn't claim it's a formal equivalence relation. In that case, the asymmetry is not a problem.

But using ≡ notation is standard for equivalence relations. If the relation is not symmetric, using ≡ is misleading. This is a coherence issue (notation clashes with standard usage).

Also, the ill-typing issue: "both faithful on c's contract" for a transport from d to c. This is applying c's contract to a transport whose source is d. In Part V, the contract ranges over the source's edits. So "faithful on c's contract" for a transport from d to c is non-standard and potentially wrong.

But maybe the text intends "faithful on c's contract" to mean "faithful in a way that respects c's contract." For a transport from d to c, this could mean: the transport preserves the structures that c's contract specifies. This is a looser reading but possible.

Hmm, I think the issue is real but debatable. Let me see if I can argue it more precisely.

The text says "both faithful on c's contract at grain ℓ." For the transport from d to c (let's call it t₁), "faithful on c's contract" means t₁ satisfies (F1) and (F2) on c's contract. But (F1) and (F2) are stated for a transport from D to E with contract C ⊆ A × B (D's edits). So "faithful on c's contract" for t₁ means: c's contract (⊆ c's edits × c's boundaries) is used as C in (F1) and (F2). But t₁ goes from d to c, so D = d and E = c. C should be a subset of d's edits × d's boundaries. Using c's contract (⊆ c's edits × c's boundaries) as C is a type mismatch.

This is a genuine issue. The contract for a transport from d to c should range over d's edits, not c's edits. Using c's contract is ill-typed.

Unless "c's contract" is re-interpreted as "a contract over c's edits that can be translated to d's edits via τ." But this is a stretch and not supported by the text.

OK, I'm fairly confident R31 has a genuine issue. Let me also think about whether this issue affects any verdict.

For (N): New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}, d ≡_ℓ c. If ≡_ℓ is ill-defined (because one of the transports can't be "faithful on c's contract"), then (N) might not work correctly. If ≡_ℓ is too strict (because the ill-typed fidelity condition can't be satisfied), then everything would be "new" (since no d ≡_ℓ c). If ≡_ℓ is too loose (because the condition is vacuously true), then nothing would be "new."

But without a precise understanding of how "faithful on c's contract" works for a transport from d to c, I can't determine the exact effect. The issue is that the definition is potentially ill-formed, which could affect (N) in unpredictable ways.

For the situations: O3 says Nadia's content is new (construction). If (N) is broken (because ≡_ℓ is ill-defined), the verdict on (N) might change. But the overall verdict (construction) depends on (G) = Attempt ∧ New ∧ Build. If New is broken, (G) might fail, and the verdict would change.

But the verdict is "That is construction, and it is her own." This is about Build and Ownership, not necessarily about (G) or (N). The verdict says "Nadia built something new" which uses "new" colloquially. Whether (N) holds depends on the definition of ≡_ℓ.

Hmm, I think the issue with R31 is significant but doesn't necessarily move verdicts on the given situations (because the verdicts are about construction and ownership, not strictly about (N)).

But wait - for (G) (Origin), we need New. If New is broken (because ≡_ℓ is ill-defined), (G) might fail. And (G) is used in (EK) (CreateEK). So if (G) fails, (EK) fails. This could affect verdicts about knowledge creation.

For N10 (the burned notebook): "Yes, she created knowledge." This requires (EK). (EK) requires (G). (G) requires (N). (N) requires ≡_ℓ. If ≡_ℓ is broken, (EK) might fail.

But the verdict is about whether Halima created knowledge. If (N) is broken in a way that makes everything "new" (because no d ≡_ℓ c), then (N) would hold trivially and (G) would require Build (which Halima satisfies). So (EK) would hold. Verdict unchanged.

If (N) is broken in a way that makes nothing "new" (because every d ≡_ℓ c), then (N) would fail and (G) would fail. (EK) would fail. Verdict would change (she didn't create knowledge).

But which way is (N) broken? If "faithful on c's contract" for a transport from d to c is ill-typed and therefore can't be satisfied, then there are no faithful transports from d to c on c's contract. This means d ≡_ℓ c is FALSE for all d ≠ c (since one of the required transports doesn't exist). So (N) would hold trivially (everything is new). (G) would work. Verdict unchanged.

Wait, but d ≡_ℓ c requires "transports from d to c AND from c to d, both faithful on c's contract." If the transport from d to c can't be faithful on c's contract (ill-typed), then d ≡_ℓ c is false. But d ≡_ℓ d (reflexivity) would require a transport from d to d, faithful on d's contract. The identity transport from d to d, faithful on d's contract, would work. So d ≡_ℓ d is true (reflexive). But d ≡_ℓ c for d ≠ c might be false (because the transport from d to c can't be faithful on c's contract). This would make (N) too permissive (everything not identical is "new").

For N10: Halima's knowledge is compared to her prior repertoire. If ≡_ℓ is broken such that only identical contents are equivalent, then Halima's knowledge is "new" (assuming it's not in her prior repertoire). (G) holds. (EK) holds. Verdict unchanged.

For O3: Nadia's content is "new" (not in her prior repertoire). With broken ≡_ℓ, it's still "new." Verdict unchanged.

So the broken ≡_ℓ might not move verdicts if it only affects the boundary between "equivalent" and "not equivalent" in ways that don't change the given cases.

But it's still a defect in the definition. The declaration says "The equivalence is structural at that grain" which implies it works correctly. If it's broken, the declaration is wrong.

OK, I think R31 has a genuine defect (the definition is ill-typed/asymmetric) that the declaration doesn't account for. This should make R31 FALL.

Actually wait, let me reconsider. Maybe I'm wrong about the ill-typing. Let me re-read the definition of a transport and its fidelity very carefully.

"A transport from an organization D to an organization E is t = (π, τ, σ, λ) where π: X_D → X_E on the stated scope, τ translates edits, σ translates boundaries, and λ assigns each component of E a subnetwork of D with a port translation."

"A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V."

Part V: "For every active component k of E with anchor subnetwork λ(k) ⊆ D, and every (a, b) ∈ C, the relation obtained by imposing the constraints of λ(k) and projecting away its hidden ports equals the component relation of k under the translated edit."

Here (a, b) ∈ C where C ⊆ A × B. What are A and B? Looking at the definition of an organization: "A is a set of admitted edits" and "B is a set of boundary conditions." These are D's edits and boundaries (since the transport is from D to E and C is used with D's structure).

Wait, actually, looking more carefully at Part V: the transport t is from D to E. C is a contract. (F1) says: "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))" for (a, b) ∈ C. Here λ(k) ⊆ D, so Sol_{λ(k)}(a, b) uses D's structure with (a, b). This means (a, b) ranges over D's edits and boundaries. And L_k(τ(a), σ(b)) uses E's structure with translated (a, b). So τ(a) is an edit of E and σ(b) is a boundary of E.

So C ⊆ A_D × B_D (D's edits and boundaries). The contract ranges over the SOURCE's edits.

Now, for the equivalence: "transports from d to c and from c to d, both faithful on c's contract."

Transport from d to c: source is d, target is c. Contract should be ⊆ A_d × B_d (d's edits). "c's contract" = ⊆ A_c × B_c (c's edits). Using c's contract for this transport is a type mismatch.

Transport from c to d: source is c, target is d. Contract should be ⊆ A_c × B_c (c's edits). "c's contract" is correct.

So "faithful on c's contract" is correct for the transport from c to d but wrong for the transport from d to c. The definition is ill-typed for one of the two transports.

This confirms my earlier analysis. R31's definition has a genuine type error.

But wait - maybe "c's contract" doesn't mean "a subset of A_c × B_c." Maybe it means "the contract of the question about c" which is a contract for a question whose target is c. In Part III: "p = (D, C, b_0, Q, O_p, ρ_p)" where D is the target and C ⊆ A × B. So the contract of a question about c would be C_c ⊆ A_c × B_c (c's edits and boundaries). This confirms that "c's contract" ranges over c's edits.

So for the transport from d to c, using c's contract (ranging over c's edits) is indeed a type mismatch. The fidelity conditions expect (a, b) ranging over d's edits.

Unless the fidelity conditions can be re-interpreted for a transport in the "reverse" direction. But the text doesn't support this.

OK, I'm now quite confident. R31 has a genuine defect: the definition of ≡_ℓ is ill-typed (it applies c's contract to a transport from d to c, where the contract should range over d's edits). This is a coherence issue (the definition doesn't type-check with the rest of the text).

Let me now think about R52 one more time.

**R52** revisited:

Old: "it must be admitted, realizable, a member of T, and a survivor of H."
New: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

The declaration says: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

The old list: (1) admitted, (2) realizable, (3) member of T, (4) survivor of H.
The new list: (1) member of T, (2) admitted by physics/construction, (3) survivor on H.

"Realizable" is dropped. But "admitted by physics/construction" in the new might subsume both "admitted" and "realizable" from the old. If "realizable" means "the physics admits it," then the new "admitted by physics/construction" covers it. If "realizable" means something more (e.g., "can be actually built"), then it's a genuine drop.

But even if "realizable" is subsumed, the declaration should say so. The declaration says "This replaces the list" which implies the new list is different. But it doesn't say what happens to "realizable." This is potentially incomplete.

However, the declaration gives both the old list and the new requirements. The reader can see that "realizable" is not in the new list. Whether it's subsumed or dropped is for the reader to determine.

Hmm, but the declaration is supposed to "say exactly what changes in what is claimed." If "realizable" is dropped (or subsumed), the declaration should say "and drops the realizability condition" or "where 'realizable' is now covered by 'admitted by physics/construction'." Without this, the declaration is incomplete.

But is this a serious enough issue to make R52 FALL? Let me think.

If "realizable" and "admitted by physics/construction" are the same, dropping one is just cleanup. The declaration says "This replaces the list" which accurately describes the change (the list is replaced). The specific mapping between old and new items is visible from the two lists.

If "realizable" is different from "admitted by physics/construction," dropping it changes the claim. The declaration should say so.

Given the ambiguity, I think R52 is borderline. The declaration is not clearly wrong, but it's potentially incomplete.

Let me also check if R52's new text is coherent with the rest.

"it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

"Survive on H (Part IV)" - Part IV now defines "survives on H" (via R11). So the pointer is correct.

"A transport the physics and the stated construction admit (Part XII)" - Part XII says "The population is the set of transports the physics and the stated construction admit." So this matches.

"A member of T" - T is the population. This is clear.

The new text references Part XII for the admission condition and Part IV for the survival condition. These are correct pointers.

But there's a redundancy: "a member of T, a transport the physics and the stated construction admit." If T is "the set of transports the physics and the stated construction admit" (Part XII), then "member of T" and "admitted by physics/construction" are the same. The new text lists both as separate conditions, which is redundant.

Is this a coherence issue? Not really - it's just redundant. But it's a bit confusing to list the same condition twice in different words.

OK, I think R52 has two issues:
1. "Realizable" is dropped without the declaration explicitly accounting for it.
2. The new text is redundant (listing "member of T" and "admitted by physics/construction" as separate conditions when they're the same).

Issue 1 is potentially serious (if "realizable" is a genuine condition). Issue 2 is minor (just redundancy).

Let me now compile my report. I need to decide which changes FALL.

Let me go through each change and decide:

**R11**: The definition of "survives on H" is clear and consistent. The declaration is accurate. STANDS.

Wait, actually, let me reconsider R11 one more time. The new text says "fidelity on H without membership in T is not survival." But the definition says "a transport survives on H when it is a member of T that meets the survival condition on H." The survival condition is "requiring fidelity on H." So survival = membership in T AND fidelity on H. The negative claim "fidelity on H without membership in T is not survival" is the contrapositive of "survival requires membership in T," which is already in the definition.

But there's a subtlety: the negative claim says "fidelity on H without membership in T is not survival." This is about any transport with fidelity on H but not in T. The definition says survival requires membership in T. So the negative claim is consistent. But it adds clarity by explicitly saying that fidelity alone is not enough.

Is there any issue? The declaration says "fidelity on H without membership in the population is not survival." This matches the new text. The declaration is accurate.

But wait - the new text says "fidelity on H without membership in T is not survival." Is this always true? What if a transport has fidelity on H and is in T? Then it IS survival (by the definition). What if a transport has fidelity on H but is NOT in T? Then it's NOT survival (by the negative claim). These are consistent.

But what about a transport that is in T but doesn't have fidelity on H? By the definition, it doesn't survive (it needs to meet the survival condition, which requires fidelity on H). So it doesn't survive. The negative claim doesn't cover this case (it's about "fidelity without membership," not "membership without fidelity"). But the definition covers it (survival requires both).

So R11 is coherent. STANDS.

**R22**: The four claims are clear and consistent. The declaration is accurate. STANDS.

**R31**: The definition of ≡_ℓ is ill-typed (applying c's contract to a transport from d to c) and asymmetric. The declaration says "The equivalence is structural" which implies it works correctly. If it's ill-typed, the declaration is wrong. FALLS.

Actually, wait. Let me reconsider whether the ill-typing is really a problem. Maybe the definition is correct and I'm misunderstanding the direction of fidelity checking.

"Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

For the transport from d to c (t₁): "faithful on c's contract." What does this mean?

Looking at (F1): "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))" for (a, b) ∈ C.

If t₁ is from d to c, then λ(k) assigns components of c (E) to subnetworks of d (D). C is the contract. (a, b) ∈ C. Sol_{λ(k)}(a, b) uses d's structure. L_k(τ(a), σ(b)) uses c's structure. τ maps d's edits to c's edits.

For "faithful on c's contract": C = c's contract ⊆ A_c × B_c (c's edits and boundaries). But (a, b) ∈ C means a is a c-edit. And τ(a) would need to map a d-edit to a c-edit. But a is a c-edit, not a d-edit. So τ(a) doesn't make sense.

This confirms the ill-typing. τ maps d's edits to c's edits, so τ expects d-edits as input. But c's contract has c-edits. Applying τ to c-edits doesn't work.

UNLESS τ is invertible and we use τ^{-1}. But the text says τ translates edits (from D to E), not from E to D. So τ(a) for a ∈ A_c is not defined (τ's domain is A_d).

This is a genuine type error. R31's definition doesn't type-check.

OK, R31 FALLS.

**R32**: Removing "today" is a small change. The declaration is accurate. STANDS.

Actually, wait. Let me reconsider. The declaration says "Part X no longer says that a process run inside the boundary is the system's own 'today', which matches Part XII." But does Part XII really match? Part XII says "a process run inside the boundary is the system's whoever wrote it." This is about the process being the system's own. Part X (after R32) says "a process that runs inside the boundary is the system's own whoever wrote it." These match.

But the old Part X said "is the system's own today." The "today" might have been meaningful in context. Let me re-read the context.

Part X, Ownership: "Work supplied from outside that boundary, a diagnosis, a decisive question, an instruction about what to read, remains an outside contribution however it is executed inside; a process that runs inside the boundary is the system's own today whoever wrote it; and where the boundary is drawn decides, not where the process sits in the casing."

The word "today" is between "own" and "whoever." So it reads: "is the system's own today, whoever wrote it." This could mean "is the system's own as of today" or "is the system's own, [on this] today." The first is a temporal qualifier on ownership; the second is just marking the time.

Removing "today" eliminates any temporal reading. The declaration says this "matches Part XII" which is true. But does it change a claim?

If "today" was a temporal qualifier ("the system's own today" = "the system's own as of today"), then removing it changes the claim (ownership is no longer temporally limited). The declaration says "Part X no longer says... 'today'" which accounts for this.

If "today" was just temporal context ("the system's own today" = "the system's own, on this day"), then removing it doesn't change the ownership claim. The declaration might be over-claiming.

But the declaration treats "today" as qualifying ownership. This is one valid reading. The change removes this reading. The declaration accounts for it. STANDS.

**R33**: Adding the credit/ownership distinction. The declaration is accurate. STANDS.

But let me double-check one thing. The new text says "Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."

"Credit for content" vs. "ownership of a process." These are presented as "different attributions." But the example says "a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution." This says the process is the system's (ownership) and the content is the writer's (credit).

But "credit for content" and "ownership of a process" are indeed different attributions. Credit for content is about who gets credit for what the content says/does. Ownership of a process is about who owns the physical process. These are different. Good.

But wait - is "credit for content" the same as "ownership of content"? The text says "its content remains its writer's contribution." This is about credit (contribution), not ownership. The writer gets credit for the content. But who owns the content? The text doesn't say.

Hmm, but the declaration says "credit for content and ownership of a process are different attributions." This matches the text. The text distinguishes credit for content from ownership of a process. It doesn't say anything about ownership of content. So the declaration is accurate for what the text says.

But is this a CLAIM change? Yes - the text now says credit and ownership are different. Previously, this wasn't stated. A reader could now conclude this. The declaration accounts for it. STANDS.

**R35**: Defining Δ. The declaration is accurate. STANDS.

**R36**: Clarifying r(ξ'). The declaration is accurate. But the formula notation is non-standard. Let me think about whether this is a problem.

(P) says "∀r ∈ P[r(ξ) ⇒ r(ξ')]." With R36, r(ξ') means "held on every occasion from ξ to ξ'." But r(ξ) is not redefined. What does r(ξ) mean?

The text before (P) says "a protected condition is lost exactly when it fails on an occasion it covers." This is about r failing on any occasion. (P) uses r(ξ) and r(ξ'). If r(ξ') means "held throughout [ξ, ξ']" and r(ξ) means "held at ξ" (or "held up to ξ"), then (P) says: "if r held at ξ (or up to ξ), then r held throughout [ξ, ξ']." This is a reasonable condition.

But the asymmetry between r(ξ) and r(ξ') is confusing. r(ξ') has a special meaning (every occasion) while r(ξ) has a standard meaning (at ξ). This is inconsistent notation.

However, the text explicitly defines r(ξ') and doesn't redefine r(ξ). So r(ξ) keeps its standard meaning. This is a deliberate choice to clarify one end of the interval.

Is this a coherence issue? It's a bit confusing but not wrong. The text explicitly says what r(ξ') means. A careful reader can follow.

But there's another issue: the formula (P) is "r(ξ) ⇒ r(ξ')." If r(ξ') means "held throughout [ξ, ξ']", then the formula is really saying "if r held at ξ, then r held throughout [ξ, ξ']." This is equivalent to "if r held at ξ, then r held at every occasion from ξ to ξ'." But the notation r(ξ') suggests it's a pointwise evaluation, which it's not. This is misleading.

But the text says "r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'." This explicitly says r(ξ') is NOT just "at ξ'" but "every occasion." So the text is clear about the non-standard meaning.

I think R36 is fine. The declaration is accurate and the change is a clarification. The non-standard notation is explained. STANDS.

Wait, but I should check if R36 moves any verdict. Let me look at O35 again.

O35: "the protected tap stops for four seconds... Nobody drew from it in those seconds." Verdict: "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss."

With R36: r(ξ') means "held on every occasion from ξ to ξ'." If r = "nobody is without water," then r held on every occasion (nobody drew from the tap). r(ξ') is TRUE. Not a loss. Verdict unchanged.

But what if r = "the tap runs"? Then r didn't hold on every occasion (it stopped for four seconds). r(ξ') is FALSE. This would be a loss.

But the verdict says "The protected condition held in every way that mattered." The phrase "in every way that mattered" suggests the condition is about water availability, not about the tap running. If the condition is "nobody is without water," the verdict is unchanged.

But what if someone uses "the tap runs" as the protected condition? Under the old text (without R36), r(ξ') might mean "r at ξ'" (the tap runs at ξ'). Since the tap runs at ξ' (after the four seconds), r(ξ') is TRUE. Not a loss.

Under the new text (with R36), r(ξ') means "r held throughout." Since the tap stopped for four seconds, r(ξ') is FALSE. Loss.

So R36 moves the verdict for the case where r = "the tap runs." Under the old text, it's not a loss. Under the new text, it is a loss. But the fixed verdict says "not a loss."

This would be a move AWAY from the fixed verdict. But only if r = "the tap runs." The fixed verdict says "The protected condition held in every way that mattered: nobody was without water." This suggests r is about water, not about the tap.

Hmm, but the situation says "the protected tap stops for four seconds." This could be read as: the protected condition is about the tap. In that case, R36 would move the verdict away from the fixed verdict.

But the verdict says "nobody was without water" which is about water availability. So the protected condition is about water, not about the tap. The tap stopping is an event, but the condition is about water.

Let me re-read the situation: "During the washer repair the protected tap stops for four seconds while the mains is switched over, then runs as before. Nobody drew from it in those seconds."

The protected condition might be "the tap provides water" or "nobody is without water." If it's the former, R36 makes it a loss (the tap didn't provide water for four seconds). If it's the latter, R36 doesn't make it a loss (nobody was without water).

The verdict says "The protected condition held in every way that mattered: nobody was without water." The phrase "in every way that mattered" acknowledges that in some way (the tap stopping) it didn't hold, but in the way that mattered (nobody without water), it did.

With R36's every-occasion requirement, if the condition is "the tap provides water," it's a loss (the tap didn't provide water for four seconds). But the verdict says "not a loss." This is a conflict.

But the verdict says the condition "held in every way that mattered." This is saying the condition that matters is "nobody is without water," and it held. The tap stopping is irrelevant to the condition that matters.

Hmm, but the text says "a protected condition is lost exactly when it fails on an occasion it covers." If the condition is "the tap provides water" and it fails for four seconds, it IS lost (by the old prose). But the verdict says "not a loss."

There's a tension here. The old prose says a condition is lost when it fails on any occasion. The verdict says the condition held "in every way that mattered" and the interruption "is not a loss." These seem contradictory if the condition is "the tap provides water."

But if the condition is "nobody is without water," it didn't fail on any occasion (nobody drew from the tap). So it's not lost. The verdict is consistent.

So the key is what the protected condition IS. The situation says "the protected tap stops." But the verdict focuses on "nobody was without water." The protected condition is about people, not about the tap.

With R36, the every-occasion requirement is explicit. For r = "nobody is without water," the condition holds on every occasion. Verdict unchanged.

For r = "the tap provides water," the condition fails for four seconds. Under the old text (r(ξ') = r at ξ'), it might not be lost (r holds at ξ'). Under the new text (r(ξ') = every occasion), it IS lost.

But the verdict says "not a loss" for the case where nobody was without water. If someone uses "the tap provides water" as the condition, R36 would make it a loss, contradicting the verdict. But the verdict seems to use "nobody is without water" as the condition.

I think R36 doesn't move the verdict for O35, as long as the protected condition is "nobody is without water." But it COULD move the verdict if the condition is "the tap provides water." The situation is ambiguous about which condition is used.

Actually, the situation says "the protected tap stops for four seconds." The phrase "protected tap" suggests the tap is the thing being protected. But the verdict says "nobody was without water" which is about people. The connection is: protecting the tap is a means to ensuring nobody is without water.

In the context of (P), the protected condition r is what's being protected. If r is "the tap runs," R36 makes it a loss. If r is "nobody is without water," R36 doesn't. The verdict uses the latter.

But the situation describes the tap as "protected" which might mean the condition is about the tap. The verdict's focus on "nobody was without water" might be a reinterpretation of the condition.

Hmm, this is ambiguous. Let me see what the fixed verdict says: "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss."

"The protected condition held in every way that mattered" - this says the condition held. "Nobody was without water" - this specifies what held. "The interruption is on the record and is not a loss" - this says the interruption is not a loss.

So the verdict is: the condition is "nobody is without water" (or similar), it held, and the tap stopping is not a loss. With R36, if the condition is "nobody is without water," it held on every occasion. r(ξ') is TRUE. Not a loss. Verdict unchanged.

But R36 says "r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'." If the condition is "nobody is without water," it held on every occasion. The "not only at ξ'" phrase emphasizes that it's not just about the endpoint. But since the condition held throughout, both the old and new interpretations give the same result.

I don't think R36 moves the verdict for O35. The condition that matters ("nobody is without water") held throughout under both interpretations.

But there's a subtlety: the new text says "not only at ξ'." This emphasizes that the old interpretation (just at ξ') is insufficient. For O35, the condition held at ξ' AND throughout. So both interpretations give the same result. But for a case where the condition holds at ξ' but not throughout, the new interpretation would be stricter.

Is there a case in the given situations where the condition holds at ξ' but not throughout? Let me check.

O13 (two plumbers): Ben tightens a joint and the leak stops. Protected conditions about other things. No specific case where the condition holds at ξ' but not throughout.

O20 (two valves): Either valve alone would have stopped it. Protected conditions. No specific case.

O25 (one second apart): Ana stops it first. Ben's act comes later. Protected conditions. The protected condition might be "the other valve is not needed." Hmm, this is getting complicated.

O26 (two feeds): Both stop their halves. Protected conditions. No specific case.

O35 (four seconds): As discussed. Condition held throughout.

O39 (two routes): Both operate. Protected conditions. No specific case.

O52 (route started and stopped): Ana's route starts but jams. Ben's route completes. Protected conditions. The protected condition might be "the leak is stopped." This held at ξ' (the leak is stopped). But did it hold throughout? The leak was not stopped until Ben's valve closed. So the condition "the leak is stopped" didn't hold throughout (it only held at ξ').

Wait, but the protected condition in (P) is about protecting something during the repair. The repair is about stopping the leak. The protected conditions are about other things being maintained (e.g., "the other valve is working," "nobody is injured").

For O52: the protected conditions are about maintaining other things. These held throughout (no other damage). The fact that the leak wasn't stopped until the end is about the CLAIMED obligation (stopping the leak), not the protected conditions.

So R36's every-occasion requirement applies to protected conditions, not to claimed obligations. For O52, the protected conditions held throughout. Verdict unchanged.

OK, I don't think R36 moves any verdict. STANDS.

Actually wait, I want to reconsider O35 one more time. Let me re-read the situation and verdict very carefully.

O35: "During the washer repair the protected tap stops for four seconds while the mains is switched over, then runs as before. Nobody drew from it in those seconds."

Verdict: "The protected condition held in every way that mattered: nobody was without water. The interruption is on the record and is not a loss."

The phrase "The protected condition held in every way that mattered" is interesting. It says the condition held "in every way that mattered." This implies it didn't hold in some way that didn't matter. What way didn't it matter? The tap stopped for four seconds. This is a way the condition might not have held (if the condition is about the tap running). But it didn't matter because nobody drew from the tap.

So the condition might be "water is available when needed" or "nobody is without water when they need it." Under this condition, it held throughout (nobody needed water during the four seconds). Under a stricter condition ("the tap runs at all times"), it didn't hold.

With R36: r(ξ') means "held on every occasion it covers from ξ to ξ'." If the condition is "nobody is without water when they need it," it held on every occasion (nobody needed water during the four seconds). r(ξ') is TRUE. Not a loss. Verdict unchanged.

If the condition is "the tap runs at all times," it didn't hold on every occasion. r(ξ') is FALSE. Loss. Verdict changed.

But the verdict says "The protected condition held in every way that mattered." This says the condition held. If the condition is "the tap runs at all times," it didn't hold. But the verdict says it did hold "in every way that mattered." This suggests the condition is not "the tap runs at all times" but something weaker.

I think the verdict is saying: the protected condition (whatever it is) held in the way that matters. The tap stopping is noted but not a loss. With R36, if the condition is about water availability (which the verdict suggests), it held throughout. Verdict unchanged.

OK, I'm convinced R36 doesn't move the verdict for O35. STANDS.

Now let me finalize my analysis.

**R37**: Defining Result(Δ) and ProducesVia. The declaration is accurate. STANDS.

But wait, let me check one thing. The declaration says "It defines ProducesVia(Δ, c, o; ξ, ξ') as holding when an active route that runs from Δ to the repair of o contains the relevant binding of c." Let me verify the new wording: "ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c." Yes, this matches.

But "the relevant binding of c" - what is "the relevant binding"? The text doesn't define "relevant." Is this a coherence issue?

In (EK), ProducesVia is used as "ProducesVia(Δ, c, o; ξ, ξ')." The content c is the account that was created. The "relevant binding" would be the binding of c that's relevant to the repair of obligation o. But "relevant" is not formally defined.

However, the context makes it clear: the binding of c that carries the explanatory content relevant to repairing o. This is a prose description that's not formally defined but is clear enough in context.

The declaration uses the same phrase ("the relevant binding of c"). So the declaration and the new wording are consistent. The term is ambiguous but not wrong.

STANDS.

**R40**: Adding implicit boundary declaration rules. The declaration is accurate. STANDS.

**R42**: Changing "realized" to "candidate." Let me think about this more.

The declaration says: "Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones. The population is the set its own last sentence gives: the transports the physics and the stated construction admit."

The first sentence says Part XII now describes the population as "candidate transports" (matching Part IV). The second sentence says the population is defined by the last sentence of Part XII.

But the last sentence of Part XII says: "The population is the set of transports the physics and the stated construction admit; a transport that would need a part every member of the population is built without is not in it."

The declaration says the population is "the transports the physics and the stated construction admit." But the last sentence has TWO parts: the definition and a restriction. The declaration only mentions the first part.

Is this a problem? The declaration says "the set its own last sentence gives." The last sentence gives a set with a restriction. The declaration simplifies this to just the first part. This is potentially incomplete.

But the declaration is about what R42 changes. R42 changes "realized" to "candidate." The declaration says this matches Part IV and references the definition. The definition has two parts, but the declaration only mentions one. This is a minor inaccuracy in the declaration.

But is there a bigger issue? Let me think about what "realized" vs. "candidate" means.

"Realized transports" = transports that have been actually built/implemented.
"Candidate transports" = transports that are possible/could be built.

The population of a selection history contains the transports that were available for selection. If the population is "realized transports," it contains only those that were actually built. If it's "candidate transports," it contains all possible ones.

For selection to work, the population should be the set of transports that were available for variation and selection. If some transports were possible but not built, they wouldn't be available for selection. So the population should be "realized" (actually built) for selection to make sense.

But Part IV says "candidate transports" and the definition says "the set of transports the physics and the stated construction admit." "Admitted" means possible (the physics allows it). So the population is all possible transports, not just realized ones.

Hmm, this is a tension. If the population is all possible transports (candidates), then selection operates on a space of possibilities, not on actually-built transports. This might be the intended meaning (selection as a process on a space of possibilities).

But in biology, selection operates on actually-existing organisms (realized), not on all possible ones. If the semantics is modeling selection after biology, the population should be "realized."

But the semantics might be using "selection" more broadly (as a process on a space of candidates). In that case, "candidate" is correct.

I think this is a substantive question about what "selection" means in the semantics. R42 changes the description from "realized" to "candidate," which is a claim change. The declaration says it matches Part IV and references the definition.

But does the change move any verdict? Let me check the situations: O11, O24, O48, N5.

O11 (the apprentice): Monday is "blind fitting" (selection). The population of keys on the ring. Are these "realized" (actually on the ring) or "candidate" (all possible keys)? If "realized," the population is the keys on the ring. If "candidate," the population is all possible keys (including ones not on the ring).

For selection (blind fitting), the apprentice tries keys from the ring. The population is the keys on the ring (realized). If the population were "candidate" (all possible keys), the apprentice might try keys not on the ring, which doesn't match the situation.

But R42's change is in Part XII (the physical module), not in Part IV (the provenance). Part IV already says "candidate transports." R42 makes Part XII match Part IV. So the description changes in Part XII, but Part IV is unchanged.

For O11, the relevant text is Part IV (which describes selection). Part IV says "candidate transports." So the population is candidates. But the situation describes trying keys from a ring (a finite set). If the population is "candidates" (all possible keys), this is broader than the ring.

But Part XII's definition says "The population is the set of transports the physics and the stated construction admit." For O11, "the physics and the stated construction" would be the keys on the ring (the stated construction is the ring of keys). So the population is the keys on the ring. This matches the situation.

Hmm, but "the physics and the stated construction admit" might be broader than the keys on the ring. The physics admits any key shape. The stated construction (the ring) limits it to the keys on the ring. So "the physics and the stated construction admit" = keys on the ring. This matches.

OK, so the population is defined by both the physics and the stated construction. The stated construction limits the population. For O11, the stated construction is the ring of keys, so the population is the keys on the ring. Verdict unchanged.

For O24 (the unused joint setting): "Two wiring arrangements agree on every tested setting and on almost every untested one. They differ at a single joint setting of two switches, one nobody has used."

Verdict: "They are two arrangements, and the record has not chosen between them. One reachable setting is enough."

For selection: if the two arrangements are in the population and differ at one setting, the population admits a differing survivor at that setting. This is the premise of Derivation 3's underdetermination. With R42 (candidate transports), the population might be broader (including more candidate arrangements). But the two arrangements in the situation are the relevant ones. Verdict unchanged.

For O48 (the forbidden wire): "The only arrangement that gives a different response at the unseen setting needs a wire between two terminals, and every device in the stated population is built without that wire."

Verdict: "Then there is no alternative in that population. The different response is imaginable and is not available. The premise is not met."

With R42 (candidate transports): if the population is "candidate" (all physically admitted), the wire arrangement might be a candidate (the physics admits it). But the verdict says "every device in the stated population is built without that wire." The "stated population" is the given constraint. If the stated population excludes the wire arrangement, there's no alternative in the population.

But R42 says the population is "the set of transports the physics and the stated construction admit." If "the stated construction" excludes the wire, then the wire arrangement is not in the population. Verdict unchanged.

But if "candidate" means "all physically admitted" (ignoring the stated construction), the wire arrangement might be in the population. This would change the verdict (there IS an alternative). But R42's declaration says "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." The "stated construction" is part of the definition. So the wire arrangement is excluded if the stated construction excludes it.

OK, verdict unchanged for O48.

For N5 (a farmer's rule carried south): "What, if anything, does the village rule tell him about when to sow?"

Verdict: "Little or nothing he can rely on."

This is about the rule's applicability in a new context. Not directly about selection populations. Verdict unchanged.

So R42 doesn't move any verdict. But there might be coherence issues.

Actually, let me reconsider R42 more carefully. The declaration says "Part XII now describes the population of a selection history as candidate transports, as Part IV does, not realized ones." This says the change is in the description. But the change is classified as CLAIM. If it's just a description change (the population is unchanged), it should be WORDING. If it's a claim change (the population is different), the declaration should say so.

The declaration says "The population is the set its own last sentence gives." This says the population is defined by the last sentence (unchanged). So the population doesn't change. The description changes. This sounds like a WORDING change.

But the change is CLAIM. The declaration says "Part XII now describes... not realized ones." This implies the old description was "realized" and the new is "candidate." If the description is a claim (what the population IS), then this is a claim change. If the description is just wording (the population is unchanged), it's a wording change.

I think there's a mismatch between the change's classification (CLAIM) and the declaration's content (which sounds like WORDING). The declaration says the population is defined by the last sentence (unchanged), but the change is CLAIM (the description changes).

But wait - the description "realized" vs. "candidate" IS a claim about the population. Even if the formal definition (last sentence) is unchanged, the description tells the reader what the population is. Changing from "realized" to "candidate" changes what the reader understands the population to be. This is a claim change.

So the declaration is correct: the change is CLAIM (the description of the population changes from "realized" to "candidate"). And the declaration also notes that the formal definition (last sentence) is unchanged. This seems complete.

But there's a subtle issue: if the formal definition is unchanged and the description changes, which one governs? If the formal definition governs, the description change doesn't affect the population. If the description governs, the population changes.

The declaration says "The population is the set its own last sentence gives." This says the formal definition governs. So the description change doesn't affect the population. But the change is CLAIM (the description changes). This is claiming something about the description, not about the population.

Hmm, this is getting circular. Let me just say R42 is CLAIM (the description changes) and the declaration accounts for it. STANDS.

But actually, wait. Let me reconsider whether R42 + R52 together move any verdict.

R42 broadens the description from "realized" to "candidate." R52 drops "realizable" from Derivation 3's conditions. Together, they make it easier for a differing survivor to exist in Derivation 3.

For O24: "Two wiring arrangements agree on every tested setting and on almost every untested one. They differ at a single joint setting of two switches, one nobody has used." Verdict: "They are two arrangements, and the record has not chosen between them."

With R42 + R52: the population is "candidate" (broader), and the conditions for a differing survivor are relaxed (no "realizable" requirement). So it's easier for the two arrangements to be in the population as differing survivors. The verdict says "the record has not chosen between them" which is consistent with Derivation 3's underdetermination.

But the verdict is about the two specific arrangements in the situation. If they're both in the population and differ at one setting, the record hasn't chosen between them. This is true regardless of R42 + R52 (as long as both arrangements are in the population).

For O48: "The only arrangement that gives a different response at the unseen setting needs a wire between two terminals, and every device in the stated population is built without that wire." Verdict: "Then there is no alternative in that population."

With R42: the population is "the set of transports the physics and the stated construction admit." If "the stated construction" excludes the wire, the wire arrangement is not in the population. Verdict unchanged.

But with R42's change from "realized" to "candidate," the population might be broader (including the wire arrangement as a candidate). If "candidate" includes the wire arrangement (because the physics admits it), then there IS an alternative in the population. This would change the verdict.

But the declaration says "The population is the set its own last sentence gives: the transports the physics and the stated construction admit." The "stated construction" is part of the definition. If the stated construction excludes the wire, the wire arrangement is not in the population. Verdict unchanged.

Hmm, but what does "the stated construction" mean? It's the construction that's stated in the claim. For O48, "every device in the stated population is built without that wire." The "stated population" is the given set of devices. If the population is defined by "the physics and the stated construction," and the stated construction is "devices built without that wire," then the wire arrangement is excluded.

But what if the "stated construction" is broader (e.g., "any wiring arrangement")? Then the wire arrangement might be included. The situation says "every device in the stated population is built without that wire." The "stated population" is a given constraint. Whether this is the "stated construction" in Part XII's definition depends on interpretation.

I think the situation is clear: "every device in the stated population is built without that wire." The population excludes the wire arrangement. So there's no alternative in the population. Verdict unchanged.

But with R42 changing "realized" to "candidate," one might argue the population should include all candidates (including the wire arrangement). But the declaration says the population is defined by the last sentence (which includes "the stated construction"). The stated construction excludes the wire. So the population excludes it.

OK, I think R42 doesn't move any verdict. STANDS.

Now let me finalize:

**R11**: STANDS. Clear definition, accurate declaration.

**R22**: STANDS. Four claims match the declaration. Consistent with Part IX and Part XI.

**R31**: FALLS. The definition of ≡_ℓ is ill-typed (applies c's contract to a transport from d to c, where the contract should range over d's edits). The declaration says "The equivalence is structural" but the relation is asymmetric and potentially not a true equivalence.

**R32**: STANDS. Small change, accurate declaration.

**R33**: STANDS. Adds credit/ownership distinction. Accurate declaration.

**R35**: STANDS. Defines Δ. Accurate declaration.

**R36**: STANDS. Clarifies r(ξ'). Accurate declaration. Non-standard notation is explained.

**R37**: STANDS. Defines Result(Δ) and ProducesVia. Accurate declaration.

**R40**: STANDS. Adds implicit boundary rules. Accurate declaration.

**R42**: STANDS. Changes "realized" to "candidate." Accurate declaration.

**R52**: Hmm, let me decide. The declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." The new list drops "realizable." The declaration doesn't explicitly say "realizable" is dropped.

But the declaration gives both the old list and the new requirements. The reader can see the difference. Is this enough?

The task says the declaration is "meant to say exactly what changes in what is claimed." If "realizable" is dropped and the declaration doesn't say so, it's not saying "exactly what changes." But the declaration does give the old list (which includes "realizable") and the new requirements (which don't). The change is visible.

Hmm, I think R52 is borderline. Let me look at the declaration one more time: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

The declaration says what the NEW claim requires (3 things) and what it REPLACES (the old list of 4 things). The difference (dropping "realizable") is implicit but visible. Is this "exactly what changes"?

I think the declaration is incomplete because it doesn't explicitly say "and drops the 'realizable' condition" or "where 'realizable' is now covered by 'admitted.'" But it does give enough information for the reader to see the change.

Given the instruction to "test as hard as you can," I should note this as a potential issue. But is it enough to make R52 FALL?

Let me think about what would make a change FALL: "its declaration is false or incomplete, it changes a claim it is declared not to change, it makes the text incoherent, or it moves a verdict away from the fixed verdict."

Is R52's declaration "incomplete"? It doesn't explicitly account for dropping "realizable." But it gives both lists. I'd say it's incomplete because it doesn't say what happens to "realizable."

But is it SERIOUSLY incomplete? The reader can see the difference. The declaration says "This replaces the list" which implies the lists are different. The specific difference (dropping "realizable") is visible.

Hmm, I think the declaration is technically incomplete but practically adequate. Let me decide: does R52 FALL?

Actually, let me think about this differently. What if "realizable" is NOT subsumed by "admitted by physics/construction"? What if it's a genuine additional condition?

In the physical module (Part XII): "a task a permitted input-to-output attribute transformation with explicit resources and side effects." And "Possibility is the absence of a law-imposed limit short of perfection." And "Admit^{q,r}_Θ is the set of tasks physically achievable at grades (q, r)."

So "admitted" means physically achievable. "Realizable" might mean the same thing (can be done). Or "realizable" might mean "has been done" (has a realization). These are different.

If "realizable" = "has a realization" (has been done), then it's different from "admitted" (can be done). Dropping "realizable" would mean the differing transport doesn't need to have been realized - it just needs to be admitted (possible). This broadens the conditions.

But if "realizable" = "can be done" = "admitted," then dropping it is just cleanup.

The old text used both "admitted" and "realizable" as separate conditions. If they were the same, why list both? This suggests they're different. If they're different, dropping "realizable" changes the claim.

I think R52's declaration is incomplete because it drops "realizable" without accounting for it. If "realizable" is a genuine condition (different from "admitted"), dropping it changes the claim in a way the declaration doesn't describe. FALLS.

But wait, let me also consider: maybe "realizable" was redundant with "member of T" (since T was defined as "realized transports" in the old text). If T = "realized transports," then "member of T" implies "realizable." So "realizable" was redundant. Dropping it is cleanup.

With R42, T = "candidate transports." Now "member of T" no longer implies "realizable." So R52 needs to handle "realizable" differently. If R52 drops it, the condition is weaker. If R52 keeps it as a separate condition, the condition is the same as before.

R52 drops "realizable." If "realizable" was a genuine condition (not just redundancy), this changes the claim. The declaration doesn't account for it.

But if "realizable" was always redundant with "member of T" (because T was "realized"), then dropping it is cleanup. But with R42, T is "candidate," so "member of T" no longer implies "realizable." Dropping "realizable" now means something.

Hmm, this is getting complicated. Let me just note the issue and decide.

I think R52 has a genuine issue: the declaration doesn't account for dropping "realizable," which may be a genuine condition. FALLS.

But wait, actually, let me re-read R52's declaration one more time: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

Hmm, the declaration says "a transport the physics and the stated construction admit." This is about admission. The old list had "admitted, realizable." The new has "admitted by physics/construction." If "realizable" is subsumed by "admitted by physics/construction," the declaration is complete (it says the new condition covers admission, which includes realizability).

But "admitted by physics/construction" might not include "realizable." They might be different things.

OK, I think the issue is real but debatable. Let me decide: R52 FALLS because the declaration is incomplete (doesn't account for dropping "realizable").

Actually, hold on. Let me reconsider. The declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." By saying "replaces the list," it's saying the entire old list is replaced by the new requirements. The reader can compare the two and see the difference. The declaration is not wrong - it accurately says what the new claim requires and what it replaces. It just doesn't explicitly say "we're dropping 'realizable.'"

Is "not explicitly saying we're dropping 'realizable'" the same as "incomplete"? The declaration gives both the old and new. The difference is visible. I think this is adequate.

But the task says "is every part of the declaration true of the new wording?" Yes - the new wording has the three conditions the declaration describes. "Does the new wording claim more than the declaration says, or less?" The new wording claims exactly what the declaration says (three conditions). "Does the declaration describe the current text correctly?" The declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." This correctly describes the old text (which had those four conditions).

So the declaration is accurate for (a). The issue is whether it's complete - does it account for all changes? It gives the old list and the new requirements. The difference (dropping "realizable") is visible. I think it's complete enough.

Hmm, but the declaration says the new claim "requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H." This has3 conditions. But the new text says "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)." This also has 3 conditions. They match.

But "a member of T" and "a transport the physics and the stated construction admit" are the same (since T is defined as "the set of transports the physics and the stated construction admit"). So the new text really has 2 distinct conditions: (1) member of T (= admitted by physics/construction), (2) survive on H. The old had 4: (1) admitted, (2) realizable, (3) member of T, (4) survivor of H.

The declaration says 3 conditions. But really there are 2 distinct ones (since "member of T" = "admitted"). This is a minor inaccuracy in the declaration (counting 3 when there are really 2 distinct conditions). But it's not wrong (the new text does list 3 things).

OK, I think R52 is fine. The declaration is accurate (if slightly redundant in its counting). STANDS.

Wait, but I initially thought R52 might drop "realizable" as a genuine condition. Let me reconsider.

If "realizable" is a genuine condition (different from "admitted"), dropping it changes the claim. The declaration says "This replaces the list" which includes "realizable." But it doesn't say "we're dropping 'realizable'" or "realizable is now subsumed."

Hmm, but the declaration says the new claim requires "a member of the population, a transport the physics and the stated construction admit, that survives on H." If "realizable" is NOT in the new claim, and the declaration says the new claim has these three things, then "realizable" is implicitly dropped. The declaration says "This replaces the list" which includes "realizable." So the reader knows "realizable" is being replaced.

Is "being replaced" the same as "being dropped"? Not necessarily. "Realizable" could be replaced by "a transport the physics and the stated construction admit" (if these are the same). Or "realizable" could be dropped entirely (if it's a separate condition).

The declaration says "This replaces the list." The word "replaces" suggests the old list is swapped for the new one. The new one doesn't have "realizable." So "realizable" is effectively dropped. The declaration says "This replaces the list" which accounts for the entire list being replaced.

I think the declaration is adequate. It says the old list is replaced by the new conditions. The specific mapping between old and new items is for the reader to determine. The declaration is not wrong - it just doesn't explicitly map each old item to a new one.

OK, I'll say R52 STANDS. The declaration is accurate (it gives both old and new) even if it doesn't explicitly map each item.

Hmm, but actually, I realize there might be a stronger issue with R52 that I've been overlooking. Let me re-read the new text:

"it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

The phrase "a transport the physics and the stated construction admit (Part XII)" - this is an appositive to "a member of T." So it's saying "a member of T [which is] a transport the physics and the stated construction admit." This means "member of T" and "admitted by physics/construction" are the same thing (the appositive clarifies what "member of T" means).

If this is the reading, then the new text has 2 conditions: (1) member of T (= admitted), (2) survive on H. The old had 4: (1) admitted, (2) realizable, (3) member of T, (4) survivor of H.

With this reading, "realizable" is dropped and "admitted" and "member of T" are merged. The new is a simplified version of the old. The declaration says "This replaces the list" which accounts for the simplification.

But "realizable" is still dropped. If it was a genuine condition, this changes the claim. The declaration doesn't explicitly say "we're dropping 'realizable.'"

OK, I think the key question is: was "realizable" a genuine additional condition in the old text? If yes, dropping it changes the claim and the declaration is incomplete. If no (it was redundant), dropping it is cleanup and the declaration is fine.

Given that the old text listed "admitted" and "realizable" as separate items, it's plausible they were different. But it's also plausible they were redundant (the author listed synonyms for emphasis).

I think the honest answer is: we can't tell from the text alone whether "realizable" was a genuine condition or redundant. The declaration should clarify this. Without clarification, the declaration is potentially incomplete.

But is this enough to make R52 FALL? The task says a change FALLS when "its declaration is false or incomplete." If the declaration is potentially incomplete (doesn't clarify whether "realizable" is dropped or subsumed), it's incomplete.

Hmm, but "potentially incomplete" is weaker than "incomplete." The declaration gives both lists and says "This replaces." The reader can see the difference. I think it's adequate.

OK, final decision: R52 STANDS. The declaration is adequate (gives both old and new, says "This replaces").

Now let me also check R11 for the "describes the current text correctly" part.

R11's declaration: "Part IV now defines surviving on H: a transport survives on H when it is a member of the population that meets the survival condition on H, and fidelity on H without membership in the population is not survival."

"Does the declaration describe the current text correctly?" The declaration says "Part IV NOW defines surviving on H." This implies the current text (before the change) did NOT define "survives on H." Let me check the old wording: "The transport t is a member of T that survived." This uses "survived" but doesn't define "survives on H." So the current text did NOT define "survives on H." The declaration is correct.

But wait - the old text says "a survival condition requiring fidelity on H." This describes the survival condition but doesn't define "survives on H" as a term. The new text adds a formal definition. So the declaration is correct: the current text didn't define "survives on H," and the new text does.

Good. R11 STANDS.

Now let me think about whether any of the 11 changes pull against each other or against the 44 unlisted changes.

R11 and R52: R11 defines "survives on H." R52 references "survive on H (Part IV)." These are consistent. R52 uses R11's definition. Good.

R42 and R52: R42 changes "realized" to "candidate" in Part XII. R52 references "a transport the physics and the stated construction admit (Part XII)." These are consistent. R52 uses Part XII's definition (which R42 doesn't change - only the description changes). Good.

R35, R36, R37: All in Part XI. They define Δ, clarify r(ξ'), and define Result/ProducesVia. These are all additions and should be consistent with each other.

R35 defines Δ as "a subhistory together with the changes of content it makes."
R37 says "Result(Δ) is the set of contents at ξ' that Δ prepared."
R37 says "ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c."

These are consistent: Δ is a subhistory with content changes; Result(Δ) is what it prepared; ProducesVia checks if an active route from Δ carries c's binding.

R36 says r(ξ') means "held on every occasion from ξ to ξ'." This affects (P). R37 defines ProducesVia which is used in (EK). These are in different formulas. No conflict.

R32 and R33: Both in Part X, Ownership. R32 removes "today." R33 adds the credit/ownership distinction. These are in the same paragraph but address different things. No conflict.

R40: In Part XII. Adds implicit boundary rules. No conflict with other changes.

R31: In Part X, Newness. Defines ≡_ℓ. This is used in (N). No direct conflict with other changes, but the ill-typing issue is a coherence problem within R31 itself.

OK, I think the only change with a serious issue is R31. Let me write up my report.

Actually, wait. Let me reconsider R31 one more time. Maybe I'm wrong about the ill-typing.

The definition says: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

Maybe "c's contract" doesn't mean "the contract of a question whose target is c." Maybe it means "the contract of the content c." A content is "an organization together with its contract-relative commitments" (Part IV). So a content has a contract. "c's contract" = the contract associated with content c.

Now, a transport from d to c maps d's structure to c's structure. Its fidelity is checked on... whose contract? If "faithful on c's contract" means "faithful with respect to c's contract-relative commitments," this might mean: the transport preserves what c's contract specifies. For a transport from d to c, this would mean: the transport maps d's responses to c's responses correctly under c's contract.

But this requires c's contract to be translatable to d's edits (via τ^{-1} or similar). The text doesn't specify how this works.

Alternatively, "faithful on c's contract" might mean "faithful when the contract is c's contract." In Part V, fidelity is checked on a contract C ⊆ A × B (the source's edits). For a transport from d to c, the source is d, so C should be d's edits. Using c's contract (c's edits) is a type mismatch.

Unless "faithful on c's contract" is a shorthand for "faithful on the contract that c specifies, translated to the source's edits." But this is a stretch.

I think the ill-typing issue is real. R31's definition doesn't type-check with the rest of the text.

But let me consider one more possibility. Maybe the transports in the equivalence are not standard transports (from Part IV). Maybe they're a different kind of transport (a "structural equivalence" transport). But the text says "transports" without qualification, and Part IV defines transports. So they should be standard transports.

OK, I'm confident R31 has a genuine defect. FALLS.

Now, let me also think about whether R31's defect affects any verdict. I discussed this earlier and concluded it probably doesn't (because the situations' verdicts are about construction and ownership, not strictly about (N)). But the defect is still real.

Wait, actually, let me think about this more. If ≡_ℓ is broken (ill-typed), what happens to (N)?

(N): New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}(s, h), d ≡_ℓ c.

If ≡_ℓ is ill-typed (can't be evaluated), then (N) is undefined. This means (G) (which requires New) is undefined. And (EK) (which requires (G)) is undefined.

For situations where (G) or (EK) is relevant:
- N10 (the burned notebook): "Yes, she created knowledge." This requires (EK). If (EK) is undefined (because (G) is undefined because (N) is undefined because ≡_ℓ is broken), the verdict can't be determined. This is a move from "Yes" to "undetermined."

But wait, if ≡_ℓ is broken such that it can't be evaluated, then (N) is undefined, and (G) is undefined, and (EK) is undefined. The theory is silent on whether knowledge was created. The verdict is "Yes" but the theory is silent. This is a disagreement.

But is this really what happens? If ≡_ℓ is ill-typed, the formula (N) doesn't make sense. But a reader might still try to evaluate it. If the reader interprets ≡_ℓ loosely (e.g., "structurally similar"), (N) might work. If the reader interprets it strictly (following the text exactly), (N) doesn't work.

Hmm, I think the defect in R31 makes the theory's verdict on cases involving (N) potentially wrong or undefined. But the specific effect depends on how the reader interprets the broken definition.

For the report, I should note this as a consequence of R31's defect: (N) is potentially undefined, which affects (G) and (EK). This could move verdicts on cases like N10.

But let me check: does the fixed verdict for N10 require (EK)? The verdict says "Yes, she created knowledge." If the theory's (EK) is undefined (because (N) is broken), the theory is silent. The fixed verdict is "Yes." Silence ≠ "Yes." So the theory disagrees with the fixed verdict (or is silent, which is also a problem).

Actually, the task says the verdict can be "agrees," "disagrees," "silent," or "split." If the theory is silent (because (EK) is undefined), that's "silent." The fixed verdict is "Yes." So the theory is silent where the verdict says "Yes." This is a move from "Yes" (current text) to "silent" (revised text with R31).

Wait, but the current text doesn't define ≡_ℓ either. The current text has (N) using ≡_ℓ without defining it. So the current text is also silent (because ≡_ℓ is undefined). The revised text (with R31) tries to define it but gets it wrong. So both texts are problematic.

Hmm, but the current text might have ≡_ℓ defined elsewhere (in one of the 44 unlisted changes or in the original text). Let me check.

The revised text has R31 defining ≡_ℓ. The current text (without R31) uses ≡_ℓ in (N) without defining it (or with a different definition). If the current text has a correct definition (elsewhere), and R31 replaces it with a broken one, that's a regression.

But I don't have the current text. I only have the revised text and the list of changes. R31 says the old wording is "**Newness.** With R_{<e}(s,h)=⋃_{ξ before e} R_{β,ℓ}(s,ξ)," and the new wording adds the definition of ≡_ℓ before this. So the old text didn't have the definition of ≡_ℓ at this point. But it might have it elsewhere.

Hmm, the task says "The revised text is the current text with fifty-five changes made to it, and no others." So the current text has (N) using ≡_ℓ. If ≡_ℓ is not defined in the current text (before R31), then (N) was already using an undefined symbol. R31 adds a definition (albeit a broken one). So R31 is an attempt to fix a gap, but it introduces a new problem.

In any case, R31's definition is broken (ill-typed). This is a defect. Whether it moves a verdict depends on whether the current text's (N) was also broken (undefined ≡_ℓ).

For the report, I should note R31's defect (ill-typed definition) and its potential impact on (N), (G), and (EK). Whether it moves a specific verdict is secondary.

OK, let me now write the report.

Actually, one more thing. Let me reconsider R31's definition one more time. Maybe "faithful on c's contract" for a transport from d to c has a meaning I haven't considered.

"Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

What if "faithful on c's contract" means "faithful when restricted to c's contract"? For a transport from d to c, this would mean: the transport is faithful on the part of its domain that corresponds to c's contract. This requires a translation from c's contract to d's edits (via τ^{-1}).

But the text says "faithful on c's contract," not "faithful on the translation of c's contract." The former is ill-typed (c's contract ranges over c's edits, but the transport from d to c expects d's edits). The latter would require an explicit translation.

I think the text is using "faithful on c's contract" loosely. The intended meaning might be: both transports preserve the structure that c's contract specifies. For the transport from c to d, this is straightforward (c's contract ranges over c's edits). For the transport from d to c, this requires translating c's contract to d's edits.

But the text doesn't specify this translation. It just says "faithful on c's contract." This is ambiguous and potentially ill-typed.

OK, I'll go with my analysis: R31 has a genuine defect (ill-typed/asymmetric definition). FALLS.

Hmm, but let me also consider: maybe "both faithful on c's contract" should be read as "both faithful, on c's contract" where "on c's contract" modifies "faithful" as a joint condition. Like, "both [are] faithful on c's contract" where "on c's contract" specifies the contract used for checking. In this reading, both transports are checked against c's contract. For the transport from c to d, this is natural. For the transport from d to c, this requires... well, it's still ill-typed.

Unless "faithful on c's contract" for a transport from d to c means: the transport, when restricted to the edits in c's contract (translated to d's edits), is faithful. This requires a translation, which is what τ does (but in the other direction).

OK, I think the issue is real. R31's definition is at best ambiguous and at worst ill-typed. The declaration doesn't account for this. FALLS.

But actually, let me reconsider one more time. Let me look at how (N) is used and what it's supposed to do.

(N): New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}(s, h), d ≡_ℓ c.

This says c is new when no prior content d is equivalent to c. The equivalence should check whether d and c are "the same" in some structural sense.

For d and c to be "the same," we'd want: d can simulate c and c can simulate d. This is what "transports from d to c and from c to d" gives. But we also want the transports to be "faithful" - they should preserve the relevant structure.

"Both faithful on c's contract" - this checks that both transports are faithful with respect to c's contract. For the transport from c to d, this means: the transport from c to d is faithful on c's contract (checking that d reproduces c's responses under c's contract). For the transport from d to c, this means: the transport from d to c is faithful on c's contract (checking that c reproduces d's responses under c's contract... wait, that doesn't make sense).

Hmm, for the transport from d to c (t₁), "faithful on c's contract" would mean: t₁ satisfies (F1) and (F2) with contract c's contract. In (F1), (a, b) ∈ c's contract means a is a c-edit. But t₁ goes from d to c, so τ maps d's edits to c's edits. (F1) uses (a, b) ∈ C and τ(a). If C is c's contract (c's edits), then a is a c-edit and τ(a) is... well, τ maps d's edits to c's edits, so τ(a) for a c-edit doesn't work.

Unless τ is a bijection and we use τ^{-1}. But the text says τ translates edits (from D to E), not from E to D.

OK, I'm going in circles. The issue is clear: "faithful on c's contract" for a transport from d to c is ill-typed. R31 FALLS.

Wait, one more thought. What if the definition means "there are transports from d to c [faithful on d's contract] and from c to d [faithful on c's contract], both faithful on [the respective contracts]"? But the text says "both faithful on c's contract" which applies c's contract to both. This is not the reading I just described.

Or what if "c's contract" is used as a generic term for "the relevant contract"? Like, "faithful on [the] contract" where "c's" is possessive but refers to the shared contract. If d and c share the same contract (because they address the same question), then "c's contract" = "d's contract" and the definition is symmetric.

But the text says "c's contract" not "the contract." And the definition doesn't require d and c to share a contract.

OK, I'm confident. R31's definition is ill-typed. FALLS.

Now let me also think about one more potential issue I haven't explored: R33 and its relationship to R32.

R32 removes "today" from "is the system's own today whoever wrote it."
R33 adds "Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."

After both changes, the paragraph reads (approximately): "a process that runs inside the boundary is the system's own whoever wrote it; and where the boundary is drawn decides, not where the process sits in the casing. Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."

Wait, the full sentence in the revised text is: "Work supplied from outside that boundary, a diagnosis, a decisive question, an instruction about what to read, remains an outside contribution however it is executed inside; a process that runs inside the boundary is the system's own whoever wrote it; and where the boundary is drawn decides, not where the process sits in the casing. Credit for content and ownership of a process are different attributions: a routine written outside the boundary and run inside it is the system's own process, and its content remains its writer's contribution."

Hmm, the last sentence (from R33) says "a routine written outside the boundary and run inside it is the system's own process." But the earlier sentence says "a process that runs inside the boundary is the system's own whoever wrote it." These are consistent: a process running inside the boundary is the system's own, regardless of who wrote it. R33 adds that the content of such a process is the writer's contribution.

But R33 says "a routine written outside the boundary and run inside it is the system's own process." The phrase "written outside the boundary" suggests the routine was created outside. But the earlier sentence says "whoever wrote it" which covers both inside and outside writers. R33's example is about an outside writer. This is consistent (R33 gives a specific example of the general principle).

No conflict. R32 and R33 are compatible.

OK, I'm now ready to write the report. Let me finalize:

R11: STANDS
R22: STANDS
R31: FALLS — the definition of ≡_ℓ applies c's contract to a transport from d to c, which is ill-typed; the relation is asymmetric yet called an equivalence
R32: STANDS
R33: STANDS
R35: STANDS
R36: STANDS
R37: STANDS
R40: STANDS
R42: STANDS
R52: STANDS

Wait, but I need to reconsider. Let me re-examine R31 more carefully before finalizing.

Actually, I want to double-check by looking at what "faithful on C" means for a transport t from D to E.

From Part IV: "A transport is **faithful on \(C\)** when it satisfies the component and global fidelity conditions of Part V."

Part V says (F1) and (F2) for a transport t = (π, τ, σ, λ) from D to E. (F1): "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))" for (a, b) ∈ C. Here λ(k) ⊆ D and L_k is E's component relation. (a, b) are used with Sol_{λ(k)} (which is D's structure) and τ(a), σ(b) are used with L_k (which is E's structure).

So (a, b) ∈ C where C ⊆ A × B. A and B are D's edits and boundaries (since they're used with Sol_{λ(k)} which is D's structure). Wait, actually, let me re-read.

In Part II: "A is a set of admitted edits" and "B is a set of boundary conditions." These are part of the organization D = (V, (X_v), J, B, A, L). So A and B are D's data.

In Part V: "For every active component k of E with anchor subnetwork λ(k) ⊆ D, and every (a, b) ∈ C..." Here (a, b) ∈ C ⊆ A × B. But A and B are... whose? The formula uses Sol_{λ(k)}(a, b) where λ(k) ⊆ D. So (a, b) should be D's edits and boundaries. And L_k(τ(a), σ(b)) where L_k is E's component. τ(a) is E's edit (translated from D's edit a) and σ(b) is E's boundary.

So C ⊆ A_D × B_D (D's edits and boundaries). This confirms that the contract ranges over the SOURCE's (D's) edits.

For a transport from d to c: source is d, so C ⊆ A_d × B_d (d's edits). "c's contract" = C_c ⊆ A_c × B_c (c's edits). Using C_c as the contract for this transport is ill-typed (C_c ranges over c's edits, but the transport expects d's edits).

This confirms my analysis. R31's definition is ill-typed.

But wait, I want to consider one more possibility. What if the "contract" in "faithful on c's contract" doesn't mean "a subset of A × B" but rather "the scope of fidelity checking"? Like, "faithful on c's contract" means "faithful within the scope that c's contract defines." This is a looser reading.

In this reading, "faithful on c's contract" for a transport from d to c means: the transport is faithful within the scope defined by c's contract. This requires translating c's scope to d's edits (via some mapping). But the text doesn't specify this mapping.

I think this looser reading is possible but non-standard. The text uses "faithful on C" in Part IV with C being a standard contract (subset of A × B). R31 uses "faithful on c's contract" which should also be a standard contract. But applying c's contract (c's edits) to a transport from d to c is ill-typed.

OK, I'm confident. R31 FALLS.

Let me now write the report. I need to keep it under about 3,000 words.

Actually, before writing, let me reconsider whether there are any other issues I might have missed.

Let me re-read R22's new text one more time: "Here a route is a support of the candidate, a member of S, and is a route whether or not any history runs it; it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history. The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

"The two meet in ProducedBy" - what are "the two"? Looking at the context: "Two systems with the same output table may differ in which routes are active; the semantics represents the difference (Derivation 9)." Then the new text says "Here a route is a support... The two meet in ProducedBy."

"The two" refers to "the two systems." The two systems (with the same output table but different active routes) meet in ProducedBy. ProducedBy credits based on active routes (which differ between the two systems). So the two systems are told apart by ProducedBy.

But wait, the new text says "The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains." This is about ProducedBy crediting contributions. "The two meet in ProducedBy" means the two systems are distinguished by ProducedBy (because they have different active routes). This makes sense.

But "the two" is ambiguous. It could refer to "the two systems" or "the two kinds of routes" (abstract routes and active routes). Let me re-read.

"Here a route is a support of the candidate, a member of S, and is a route whether or not any history runs it; it is active in a system when occurrences that realize it form an active route (Part IX), a subnetwork of actual occurrences in the system's history. The two meet in ProducedBy (Part XI), which credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains."

Before "The two," the text discusses:
1. A route is a support (abstract).
2. A route is active when its realization forms an active route (physical).

"The two" could refer to these two concepts (abstract route and active route). "The two meet in ProducedBy" means ProducedBy connects the abstract concept (route = support) with the physical concept (active route). ProducedBy uses active routes (physical) to credit contributions (which are abstract supports).

This reading makes more sense. "The two" = the abstract notion of route and the physical notion of active route. They "meet" in ProducedBy because ProducedBy uses both (it credits contributions based on their active routes).

But looking at the paragraph before the new text: "Two systems with the same output table may differ in which routes are active; the semantics represents the difference (Derivation 9)." "The two" here refers to "two systems." Then the new text says "The two meet in ProducedBy." If "the two" refers to the two systems from the previous sentence, it means the two systems are distinguished by ProducedBy.

Hmm, but the new text is inserted right after "(Derivation 9)." So "The two" in "The two meet in ProducedBy" could refer to:
(a) The two systems from the previous sentence.
(b) The two concepts (abstract route and active route) from the new text.

If (a): "The two [systems] meet in ProducedBy" means the two systems are compared in ProducedBy. Since they have different active routes, ProducedBy would credit differently. This makes sense.

If (b): "The two [concepts] meet in ProducedBy" means the abstract and physical notions of route are connected in ProducedBy. This also makes sense.

Both readings are plausible. The ambiguity is a minor coherence issue.

Actually, looking at the declaration: "ProducedBy credits a repair to the contributions whose active routes ran to it, not to the routes a candidate contains." This says ProducedBy uses active routes (physical) to credit, not just routes (abstract). So "the two" = abstract routes and active routes. They "meet" in ProducedBy because ProducedBy distinguishes between them (credits based on active routes, not just routes).

This reading is consistent with the declaration. "The two" = the two notions of route (abstract and active). They meet in ProducedBy because ProducedBy bridges them.

OK, this is a minor ambiguity but not a serious issue. R22 STANDS.

Let me also quickly check R37 for any issues I might have missed.

"Result(Δ) is the set of contents at ξ' that Δ prepared. ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c. The scope of Account(c, p_c) is the contract fixed at e_c."

"Result(Δ) is the set of contents at ξ' that Δ prepared." - Δ is "a subhistory together with the changes of content it makes" (R35). "Δ prepared" means the changes of content that Δ makes prepared these contents. This is consistent.

"ProducesVia(Δ, c, o; ξ, ξ') holds when an active route that runs from Δ to the repair of o contains the relevant binding of c." - This defines ProducesVia. "An active route that runs from Δ to the repair of o" - this is a Part IX active route from Δ to the repair. "Contains the relevant binding of c" - the route carries c's binding.

But "the repair of o" - o is an obligation. "The repair of o" is the repair of obligation o. In (P), "Repair_{O,P}(ξ, ξ'; Δ)" is about repairing obligations. "The repair of o" would be the specific repair of obligation o. But (P) doesn't define "the repair of o" as a separate entity. It defines Repair as a relation.

Hmm, "the repair of o" might mean "the event/state where o is repaired" (i.e., the transition from ¬o(ξ) to o(ξ')). This is a natural reading.

"The relevant binding of c" - "relevant" is not defined. But in context, it's the binding of c that's relevant to the repair of o. This is a prose description.

I think R37 is fine. The definitions are clear enough in context. STANDS.

OK, one more check: R40.

"Where the system's boundary is not declared explicitly, a statement that names the system and says whether a process runs inside it or outside it declares the boundary for that process; a statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary."

Rule 1: "a statement that names the system and says whether a process runs inside it or outside it declares the boundary for that process." This is about implicit boundary declaration. If you say "the robot runs the routine inside itself," this declares the boundary (the routine is inside the robot).

Rule 2: "a statement that the process is the system's own, which does not say where it runs, is the attribution, not its boundary." This says claiming ownership without specifying location is an attribution, not a boundary declaration.

These rules are clear and consistent with the rest of the text. Rule 2 is consistent with "Ownership is grounded in the processes and resources the boundary includes, never in the capability being attributed." (Part XII).

But there's a subtle issue: Rule 1 says "declares the boundary for that process." This is about declaring the boundary for a specific process. But the boundary is supposed to be declared "before the attribution, not chosen after it" (the sentence before R40's addition). If Rule 1 allows implicit declaration through a statement about a process, is this "before the attribution"?

The statement in Rule 1 ("names the system and says whether a process runs inside it or outside it") could be made before or after the attribution. If it's made before, it declares the boundary in advance. If it's made after, it's a retroactive declaration. The rule doesn't specify timing.

But the sentence before says "Both are declared before the attribution, not chosen after it." This requires the boundary to be declared before the attribution. Rule 1 allows implicit declaration through a statement. If the statement is made before the attribution, it's fine. If after, it violates the requirement.

Hmm, but Rule 1 is about what COUNTS as a boundary declaration, not about WHEN it must be made. The timing requirement ("before the attribution") is separate from the form requirement (how it's declared). Rule 1 defines the form (a statement naming the system and saying where the process runs). The timing is governed by the earlier sentence.

So Rule 1 and the earlier sentence are about different things: Rule 1 is about form, the earlier sentence is about timing. They don't conflict.

But there's a practical issue: if the boundary is declared implicitly through a statement about a process, and the statement is made after the attribution, the boundary is "chosen after" the attribution. This violates the timing requirement. But Rule 1 doesn't address this.

I think this is a minor gap, not a contradiction. The text says boundaries must be declared before the attribution. Rule 1 defines one way to declare a boundary (implicitly through a statement). If the statement is made before the attribution, it's fine. If after, the timing requirement is violated. The text handles this correctly (the timing requirement is separate from the form definition).

OK, R40 STANDS.

Now let me write the report.

Let me think about the structure:

1. R31 (a, c): The definition of ≡_ℓ is ill-typed and asymmetric.
2. Maybe some minor points about other changes.

Actually, I should also check: does R31's declaration say "The equivalence is structural at that grain"? If the relation is not an equivalence (not symmetric), calling it "the equivalence" is wrong. This would make the declaration false.

The declaration says: "The equivalence is structural at that grain, not string equality or similarity." This says "the equivalence" is structural. If "the equivalence" refers to the≡_ℓ relation and it's not truly an equivalence (not symmetric), then "the equivalence" is wrong.

But "the equivalence" might mean "the equivalence notion" (how we determine equivalence) rather than "the equivalence relation." In that case, it's not claiming≡_ℓ is a formal equivalence relation.

Hmm, but the text says "The equivalence is structural at the stated grain, not string equality or similarity." This is in the new wording of R31. The full new text is: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at the stated grain, not string equality or similarity."

"The equivalence" in the second sentence refers to ≡_ℓ (defined in the first sentence). It says ≡_ℓ "is structural at the stated grain." This describes ≡_ℓ as "structural" (determined by structure, not by labels). It doesn't explicitly say ≡_ℓ is a formal equivalence relation.

But using ≡ notation and calling it "the equivalence" strongly suggests it's meant to be an equivalence relation. If it's not (because of the asymmetry), this is misleading.

The declaration says "The equivalence is structural at that grain." This uses "the equivalence" which could mean "the equivalence notion" or "the equivalence relation." If the former, it's fine. If the latter, it's wrong (because the relation is not an equivalence).

I think this is part of the R31 defect: the relation is called "the equivalence" and uses ≡ notation, but it's asymmetric and not a true equivalence. This makes the description misleading.

OK, let me finalize R31 as FALLS and write the report.

Actually, let me also consider one more thing about R31. The definition says "both faithful on c's contract at grain ℓ." The "at grain ℓ" part is important. What does "at grain ℓ" mean?

In the text, grain ℓ is a declared index. (R) uses "at grain ℓ": "Rep_ℓ(o, c) ⟺ ∃t [Faithful_C(t: Org_ℓ(o) → c) ∧ (Sel(t) ∨ Con(t))]." Here Org_ℓ(o) is "the organization that o instantiates at a grain ℓ." So "at grain ℓ" specifies the grain at which the organization is read.

In R31, "both faithful on c's contract at grain ℓ" means: both transports are faithful at grain ℓ. This specifies the grain at which fidelity is checked. This is consistent with the rest of the text.

But there's a question: does "at grain ℓ" modify "faithful" or "c's contract"? If it modifies "faithful," then the transports are faithful at grain ℓ. If it modifies "c's contract," then the contract is at grain ℓ. These are different.

In the text: "both faithful on c's contract at grain ℓ." The most natural reading is "both faithful [on c's contract] [at grain ℓ]" where "at grain ℓ" modifies "faithful" (the fidelity is at grain ℓ). This is consistent with the rest of the text.

OK, no additional issue here. R31's main problem is the ill-typing/asymmetry.

Let me now write the report. I need to keep it focused and under3,000 words.

Let me outline the report:

1. R31, task (a) and (c): The definition of ≡_ℓ is ill-typed (applies c's contract to a transport from d to c) and asymmetric (uses c's contract for both directions). The declaration says "The equivalence is structural" but the relation is not a true equivalence. Proposed repair: change "both faithful on c's contract" to "the first faithful on d's contract and the second faithful on c's contract" or "each faithful on the contract of its source."

Wait, but I should also consider: is there a reading under which "both faithful on c's contract" makes sense? Let me think one more time.

"Transports from d to c and from c to d, both faithful on c's contract."

What if "faithful on c's contract" means "faithful with respect to the structures specified by c's contract"? For the transport from c to d, this means: the transport preserves c's structures (as specified by c's contract). For the transport from d to c, this means: the transport produces c's structures (as specified by c's contract). In both cases, the reference point is c's contract.

This reading makes sense! "Faithful on c's contract" = "faithful with respect to what c's contract specifies." For the transport from c to d, it checks that d reproduces c's structures. For the transport from d to c, it checks that c reproduces d's structures... wait, no. For the transport from d to c, it maps d to c. "Faithful on c's contract" would check that c's structures (specified by c's contract) are correctly produced from d. But this requires c's structures to be translated to d's edits (for the fidelity check).

Hmm, in (F1): "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))." For a transport from d to c, λ(k) ⊆ d and L_k is c's component. (a, b) should be d's edits (used with Sol_{λ(k)}). τ(a) is c's edit. So (a, b) ranges over d's edits. If C = c's contract (c's edits), then (a, b) ranges over c's edits, which doesn't match.

Unless the fidelity check is done differently for this equivalence. Maybe "faithful on c's contract" means something custom-defined for this equivalence, not the standard (F1)/(F2). But the text says "faithful on c's contract" using the standard term "faithful on" (defined in Part IV as "satisfies the component and global fidelity conditions of Part V").

I think the standard meaning of "faithful on C" requires C to be the source's contract. R31 violates this by using c's contract for a transport from d to c. This is the ill-typing issue.

OK, I'm going with R31 FALLS. Let me write the report now.

Hmm, but actually, I want to think about one more thing. Let me look at the declaration for R31 one more time:

"Part X now defines the equivalence in (N): d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at that grain, not string equality or similarity."

The declaration says "the equivalence in (N)." This refers to the ≡_ℓ used in (N). The declaration defines it as having transports in both directions, both faithful on c's contract.

But (N) says "¬∃d ∈ R_{<e}(s, h), d ≡_ℓ c." Here d ≡_ℓ c is used. Under R31's definition, d ≡_ℓ c requires both transports to be faithful on c's contract. But for (N), we want to check if any prior d is "the same as" c. The equivalence should be symmetric (d ≡_ℓ c iff c ≡_ℓ d) for (N) to work correctly.

Wait, actually, (N) doesn't require symmetry. (N) says "no prior d is equivalent to c." If ≡_ℓ is asymmetric (d ≡_ℓ c uses c's contract), then (N) is checking if any prior d can "match" c (under c's contract). This is a specific direction: can d reproduce c?

For (N) to determine newness, we want: c is new if no prior d can reproduce c. This is exactly what the asymmetric definition gives (d ≡_ℓ c checks if d can reproduce c under c's contract). So the asymmetry might be intentional!

But then, calling it "the equivalence" is misleading (it's not symmetric). And using ≡ notation is misleading (it suggests symmetry).

Hmm, but maybe the definition IS symmetric despite the appearance. Let me check.

d ≡_ℓ c: transports from d to c and from c to d, both faithful on c's contract.
c ≡_ℓ d: transports from c to d and from d to c, both faithful on d's contract.

Are these the same? d ≡_ℓ c requires both transports faithful on c's contract. c ≡_ℓ d requires both transports faithful on d's contract. These are different (c's contract vs. d's contract). So the relation is NOT symmetric.

Unless c's contract = d's contract (which might be the case if c and d address the same question). But in general, they might have different contracts.

OK, the relation is asymmetric. (N) uses it in one direction (d ≡_ℓ c). This is fine for (N) if the intention is to check "can d reproduce c." But calling it "the equivalence" is wrong.

The declaration says "The equivalence is structural at that grain." If "the equivalence" = "the equivalence notion" (how we determine if d and c are equivalent), this is fine. If "the equivalence" = "the equivalence relation" (≡_ℓ is an equivalence relation), this is wrong.

I think "the equivalence" in the declaration refers to "the equivalence notion" (the way we determine equivalence). This is a valid reading. But the use of ≡ notation and the word "equivalence" is misleading.

Is this enough to make R31 FALL? The declaration says "The equivalence is structural at that grain, not string equality or similarity." This describes the equivalence notion as structural. If the equivalence notion is structural (based on transports and fidelity), this is correct. The declaration doesn't claim ≡_ℓ is a formal equivalence relation.

But the definition is still ill-typed (applying c's contract to a transport from d to c). This is a genuine defect regardless of the symmetry issue.

Hmm wait, maybe the asymmetry is intentional and the ill-typing is a consequence of the intentional design. Let me think about what "both faithful on c's contract" means for the transport from d to c.

If the intention is to check "can d reproduce c's structures under c's contract," then for the transport from d to c, "faithful on c's contract" means: the transport from d to c correctly produces c's structures when evaluated under c's contract. This requires c's contract to be the reference point.

But in (F1), the contract ranges over the source's edits. For a transport from d to c, the source is d. So c's contract (c's edits) doesn't match.

Unless the fidelity check is re-interpreted for this equivalence. Maybe "faithful on c's contract" for a transport from d to c means: for every (a, b) in c's contract, the transport maps d's responses at τ^{-1}(a), σ^{-1}(b) to c's responses at (a, b). This requires τ to be invertible and the check to be done "backwards."

But this is a non-standard interpretation. The standard (F1) uses (a, b) ∈ C (source's edits) and τ(a) (target's edits). R31's "faithful on c's contract" for a transport from d to c would use (a, b) ∈ c's contract (target's edits) and... what? τ^{-1}(a)? The text doesn't say.

I think the non-standard interpretation is possible but not supported by the text. The text says "faithful on c's contract" using the standard term "faithful on" (defined in Part IV). The standard meaning requires the contract to be the source's. R31 violates this.

OK, I'm going with R31 FALLS. The definition is ill-typed (uses c's contract for a transport from d to c, where the standard meaning requires the source's contract). The declaration says "The equivalence is structural" which is misleading (the relation is asymmetric and ill-typed).

Now, proposed repair: change "both faithful on c's contract at grain ℓ" to "the transport from d to c faithful on d's contract and the transport from c to d faithful on c's contract, both at grain ℓ." This would make the definition well-typed (each transport checked on its source's contract) and symmetric (the conditions are symmetric in d and c).

But wait, this changes the meaning. The original says "both faithful on c's contract" (using c's contract for both). The proposed repair says "each faithful on its source's contract" (using d's contract for one and c's for the other). These are different.

If the intention is to check "can d reproduce c and can c reproduce d" (symmetric), then the proposed repair is correct. If the intention is to check "can d reproduce c under c's contract" (asymmetric), then the original is intentional but ill-typed.

Hmm, I think the intention is symmetric (checking if d and c are "the same"). The proposed repair would make the definition symmetric and well-typed. But it changes the meaning from the original.

Alternatively, the repair could be: "there are transports from d to c faithful on d's contract and from c to d faithful on c's contract, both at grain ℓ." This checks each transport on its source's contract. For the transport from d to c, this checks that c reproduces d's structures. For the transport from c to d, this checks that d reproduces c's structures. Together, they check that d and c reproduce each other's structures.

This is symmetric (swapping d and c gives the same condition) and well-typed (each transport checked on its source's contract). This would be a good repair.

But wait, the original says "both faithful on c's contract" which checks BOTH transports on c's contract. The repair says "the first faithful on d's contract and the second faithful on c's contract" which checks each on its source's contract. These are different.

If the intention is "both faithful on c's contract" (checking both against c's reference), the repair is wrong. If the intention is "each faithful on its source's contract" (checking each against its own reference), the repair is correct.

Given the ill-typing issue, I think the intention was "each faithful on its source's contract" but the text got it wrong. The repair should change "both faithful on c's contract" to "each faithful on the contract of its source" or equivalent.

OK, proposed repair: Replace "both faithful on c's contract at grain ℓ" with "the first faithful on d's contract and the second faithful on c's contract, at grain ℓ." Or more concisely: "each faithful on its source's contract at grain ℓ."

But this is a significant change to the definition. It would make the relation symmetric and well-typed. But it changes what the relation means.

Hmm, maybe a simpler repair: change "c's contract" to "their respective contracts." So: "transports from d to c and from c to d, each faithful on its respective contract at grain ℓ." This is symmetric and well-typed.

Or: "both faithful, at grain ℓ, on the contract of their respective sources." This is clear and well-typed.

OK, I'll propose: Replace "both faithful on c's contract at grain ℓ" with "each faithful on the contract of its source at grain ℓ."

Actually wait, this might make the equivalence too strict. If d and c have different contracts, requiring each transport to be faithful on its source's contract means both contracts must be satisfied. This might be more than what's intended.

Alternatively: "both faithful on [a shared contract] at grain ℓ." But the text doesn't mention a shared contract.

Hmm, I think the simplest repair is to make the relation symmetric by using "each faithful on its source's contract." But this might change the meaning. Let me just note the issue and propose a repair without being too specific.

OK, let me write the report now.

Let me also consider: should I flag any other changes? Let me go through them one more time.

R11: STANDS. Clear definition.
R22: STANDS. Four claims match.
R31: FALLS. Ill-typed definition.
R32: STANDS. Small change.
R33: STANDS. Adds distinction.
R35: STANDS. Defines Δ.
R36: STANDS. Clarifies r(ξ').
R37: STANDS. Defines Result and ProducesVia.
R40: STANDS. Adds rules.
R42: STANDS. Aligns with Part IV.
R52: STANDS. Replaces list.

Only R31 falls. OVERALL: NEEDS REPAIR.

Wait, but I should think harder about whether any other change has issues. Let me reconsider R52 one more time.

R52: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

The declaration: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

I noted that "realizable" is dropped. But the declaration says "This replaces the list." Does this account for the drop?

The declaration identifies the old list (4 items) and the new requirements (3 items). The difference (dropping "realizable") is visible. But the declaration doesn't explicitly say "and drops 'realizable'" or "where 'realizable' is subsumed by 'admitted.'"

For task (a): "is every part of the declaration true of the new wording?" Yes. "Does the new wording claim more than the declaration says, or less?" The new wording claims 3 things (matching the declaration). "Does the declaration describe the current text correctly?" The declaration says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." This correctly describes the old text.

So (a) is satisfied. The declaration is true and complete for (a).

For (b): "Can a reader now conclude something the current text left unconcluded, or no longer conclude something it allowed?" The new text drops "realizable." If "realizable" was a genuine condition, the new text no longer requires it. A reader could now conclude that a transport without realizability can still be the differing one. The current text (with "realizable") would not allow this. So the change claims something the current text didn't.

But the declaration says the new claim requires 3 things (not4). This implicitly accounts for the drop. The declaration says "This replaces the list" which means the old4 conditions are replaced by3 new ones. The reader can see that one condition is dropped.

Hmm, but the declaration is supposed to "say exactly what changes in what is claimed." If "realizable" is dropped and the declaration doesn't say "and drops 'realizable,'" it's not saying "exactly what changes." But it does give both lists.

I think this is borderline. Let me look at the task description again: "Every CLAIM change carries a one-line declaration, which is meant to say exactly what changes in what is claimed."

"Say exactly what changes in what is claimed." The declaration says "Derivation 3's claim now requires [3 things]. This replaces the list [4 things]." The change is: the claim now requires 3 things instead of 4. The specific drop ("realizable") is visible from the two lists.

Is "visible from the two lists" the same as "say exactly what changes"? Not quite. "Say exactly what changes" means the declaration should explicitly state the change. Giving both lists and saying "this replaces" is close but not quite "saying exactly what changes."

But the declaration is one line (as required). It can't map each old item to a new one in one line. Giving both lists and saying "this replaces" is the most it can do in one line.

I think R52's declaration is adequate given the one-line constraint. STANDS.

OK, final answer: R31 FALLS, all others STAND. OVERALL: NEEDS REPAIR.

Wait, actually, I want to reconsider R31 one more time. I want to make sure I'm not wrong about the ill-typing.

Let me look at the definition of a transport and its fidelity conditions one more time.

Part IV: "A transport from an organization D to an organization E is t = (π, τ, σ, λ) where π: X_D → X_E on the stated scope, τ translates edits, σ translates boundaries, and λ assigns each component of E a subnetwork of D with a port translation."

"A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V."

Part V: (F1) "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))" for (a, b) ∈ C.

Here, λ(k) ⊆ D (subnetwork of D). Sol_{λ(k)}(a, b) evaluates D's subnetwork under (a, b). L_k is E's component relation. τ(a), σ(b) are E's translated edit and boundary.

For this to work, (a, b) must be D's edits and boundaries (used with Sol_{λ(k)} which is D's structure). C ⊆ A_D × B_D.

So "faithful on C" requires C to be a subset of the SOURCE's (D's) edits and boundaries.

For the equivalence: "transports from d to c and from c to d, both faithful on c's contract."

Transport1: from d to c. Source is d. C should be ⊆ A_d × B_d. "c's contract" = ⊆ A_c × B_c. Type mismatch.

Transport 2: from c to d. Source is c. C should be ⊆ A_c × B_c. "c's contract" = ⊆ A_c × B_c. Correct.

So Transport 1 has a type mismatch. The definition is ill-typed for Transport 1.

This is clear. R31 FALLS.

But wait, one more thought. What if "faithful on c's contract" for Transport 1 means something different from the standard "faithful on C"? What if it's a custom notion defined specifically for this equivalence?

The text says "both faithful on c's contract at grain ℓ." The word "faithful" is used, which is a defined term (Part IV: "A transport is faithful on C when..."). So "faithful on c's contract" should use the standard definition. And the standard definition requires C to be the source's edits. Type mismatch.

Unless "faithful on c's contract" is a new definition introduced by R31. But R31 doesn't define a new notion of "faithful." It uses the existing term.

OK, I'm confident. R31 FALLS due to ill-typing.

But hmm, let me also think about whether the asymmetry issue is separate from the ill-typing issue.

Even if we fix the ill-typing (e.g., by using "each faithful on its source's contract"), the relation would be:
d ≡_ℓ c: transports from d to c faithful on d's contract, and from c to d faithful on c's contract.

This is symmetric! (Swapping d and c gives: transports from c to d faithful on c's contract, and from d to c faithful on d's contract. Same conditions.)

So fixing the ill-typing also fixes the asymmetry. The root cause is the same: using c's contract for both.

OK, the ill-typing and asymmetry are two aspects of the same problem: "both faithful on c's contract" should be "each faithful on its source's contract."

Let me now write the report.

Hmm, but before writing, let me also think about one more thing. The revised text says in Derivation 10: "The two things are built alike in P, and their persistence components alike in S₁, so the exchange carries the fidelity and the answers of t₁ over to the second transport (Derivation 8); so far as (F1), (F2) and (A) reach, the two candidates are one account (Derivation 2)."

Derivation 2 uses "of one kind on C" which uses signatures. Derivation 10 says "the two candidates are one account (Derivation 2)." This references Derivation 2's conclusion about same anchors.

But R31 defines ≡_ℓ which is used in (N). Derivation 2 doesn't use ≡_ℓ. So R31 doesn't affect Derivation 2 or 10.

But what about the "New" in (G)? Derivation 10 says "If S₁ is not in the prior repertoire, then (G) holds." This uses (N) (via (G)). (N) uses ≡_ℓ. If ≡_ℓ is broken, (N) is problematic.

But Derivation 10 is a consistency witness, not a theorem. It says "If S₁ is not in the prior repertoire, then (G) holds." This is conditional on S₁ being new. Whether S₁ is new depends on (N) and ≡_ℓ. If ≡_ℓ is broken, we can't determine if S₁ is new.

Hmm, but Derivation 10 says "If S₁ is not in the prior repertoire" which is a hypothesis. Under this hypothesis, (G) holds (given the other conditions). The hypothesis is about S₁ not being in the prior repertoire, which is a factual claim. Whether this is true depends on (N) and ≡_ℓ.

If ≡_ℓ is broken, we can't evaluate (N), so we can't determine if S₁ is new. But the hypothesis of Derivation 10 is "S₁ is not in the prior repertoire," which is assumed. Under this assumption, (G) holds.

So Derivation 10 is still valid (as a conditional). The broken ≡_ℓ affects whether the hypothesis is true, but the conditional itself is fine.

OK, I don't think R31's defect affects Derivation 10. It affects (N) and (G) in general, but Derivation 10's conditional is fine.

Let me now write the report.

Hmm, one more thought. Let me reconsider whether the definition might actually be fine. What if "faithful on c's contract" means "faithful when the fidelity conditions are evaluated with c's contract as the scope of quantification"? For a transport from d to c:

(F1) would become: for every (a, b) ∈ c's contract, proj_{V_k}[Sol_{λ(k)}(τ^{-1}(a), σ^{-1}(b))] = L_k(a, b).

Here, τ^{-1}(a) translates c's edit to d's edit, and σ^{-1}(b) translates c's boundary to d's boundary. This evaluates d's subnetwork at the translated points and checks it equals c's component relation.

This is a valid formulation! It checks: for every edit-boundary pair in c's contract, the transport from d to c maps d's responses to c's correctly.

But this requires τ and σ to be invertible. The text says τ and σ "translate" edits and boundaries (from D to E). It doesn't say they're invertible.

Also, the standard (F1) uses (a, b) ∈ C and τ(a), σ(b). The reformulated version uses (a, b) ∈ c's contract and τ^{-1}(a), σ^{-1}(b). These are structurally different (one uses τ, the other uses τ^{-1}).

The text says "faithful on C" (standard definition) and R31 says "faithful on c's contract." If "faithful on c's contract" is the standard definition with C = c's contract, then the type mismatch occurs. If "faithful on c's contract" is a non-standard definition (using τ^{-1}), then it's not the standard "faithful on."

I think the standard definition is intended (since R31 uses "faithful on" without redefining it). And the standard definition has a type mismatch for the transport from d to c.

OK, I'm going with R31 FALLS. Let me write the report now.

Oh wait, one more consideration. Let me see if there's an interpretation where "faithful on c's contract" for a transport from d to c is well-typed.

What if the transport from d to c has its direction reversed for the fidelity check? I.e., we check fidelity of the "inverse transport" from c to d (which would have c as source and d as target). In that case, c's contract would be the source's contract, and the fidelity check would be well-typed.

But the text says "transports from d to c" (not "transports from c to d checked in reverse"). The transport's direction is specified. And the fidelity conditions are stated for a transport from D to E with C ⊆ A_D × B_D. So the direction matters.

Unless "faithful on c's contract" means "the inverse transport (from c to d) is faithful on c's contract." But this would make "faithful on c's contract" equivalent to "the transport from c to d is faithful on c's contract." And the definition says "transports from d to c and from c to d, both faithful on c's contract." If "faithful on c's contract" for the transport from d to c means "the inverse is faithful on c's contract," then "both faithful on c's contract" means: the inverse of the d-to-c transport (= a c-to-d transport) is faithful on c's contract, AND the c-to-d transport is faithful on c's contract. But the inverse of the d-to-c transport IS a c-to-d transport (if the transport is invertible). So "both faithful on c's contract" would mean two c-to-d transports are faithful on c's contract. But the definition says "transports from d to c and from c to d" (two different directions). So this interpretation doesn't work.

OK, I give up trying to make it work. R31's definition is ill-typed. FALLS.

Let me write the report now. For real this time.

Actually, hold on. Let me reconsider one more time. Maybe I should present this more carefully in the report. The key argument is:

1. Part V's fidelity conditions (F1), (F2) require the contract C to range over the SOURCE organization's edits and boundaries.
2. For a transport from d to c, the source is d, so C must range over d's edits.
3. R31 says "both faithful on c's contract." For the transport from d to c, this uses c's contract (c's edits) as C.
4. This is a type mismatch: c's edits are used where d's edits are expected.

The proof of (1): In (F1), Sol_{λ(k)}(a, b) evaluates the anchor subnetwork λ(k) ⊆ D under (a, b). Since λ(k) is a subnetwork of D, (a, b) must be D's edits and boundaries. And L_k(τ(a), σ(b)) evaluates E's component under the translated edit, where τ maps D's edits to E's edits. So (a, b) ranges over D's edits.

This is clear and follows from the text. R31 violates this by using c's contract for a transport from d to c.

OK, I'll present this argument in the report. Let me write it now.

One more quick check: the declaration for R31 says "The equivalence is structural at that grain, not string equality or similarity." Does this describe the current text correctly? The current text doesn't define ≡_ℓ (that's what R31 adds). So the declaration says the new text defines the equivalence as structural. This is a description of the new text, not the current text. The task says "Does the declaration describe the current text correctly?" - for CLAIM changes, the declaration should describe what changes. If the current text didn't define ≡_ℓ and the new text does, the declaration should say this.

The declaration says "Part X now defines the equivalence in (N)." This says the new text defines it. It implies the current text didn't define it (otherwise it would say "Part X redefines" or "Part X now defines differently"). This is correct: the old wording didn't have the definition.

But the declaration also says "The equivalence is structural at that grain, not string equality or similarity." This describes the equivalence as structural. If the current text had a different notion of equivalence (e.g., string equality), the declaration should say this changed. But the current text didn't define ≡_ℓ, so there's no previous notion to compare.

Hmm, but (N) in the current text uses ≡_ℓ. If ≡_ℓ is undefined in the current text, (N) is problematic in the current text too. R31 adds a definition. The declaration says "Part X now defines the equivalence" which accounts for this.

But the declaration should also say what the equivalence IS (structural, not string equality). It does: "The equivalence is structural at that grain, not string equality or similarity." This describes the new definition.

So the declaration is complete for (a): it says the new text defines ≡_ℓ as structural (transports in both directions, faithful on c's contract). This matches the new wording.

But the definition itself is ill-typed. The declaration faithfully describes the new wording (including its flaw). So the declaration is accurate but the definition is wrong.

For (a): "is every part of the declaration true of the new wording?" Yes - the declaration accurately describes the new wording. "Does the new wording claim more than the declaration says, or less?" The new wording claims what the declaration says (a definition of ≡_ℓ). "Does the declaration describe the current text correctly?" The declaration says "Part X now defines the equivalence" which correctly implies the current text didn't define it.

So (a) is satisfied. But (c) reveals the flaw: the definition is ill-typed.

For (c): "Is the change coherent with the rest of the revised text?" No - the definition of ≡_ℓ is ill-typed (uses c's contract for a transport from d to c, where the source's contract is needed).

So R31 FALLS due to (c), not (a). The declaration accurately describes the new wording, but the new wording is incoherent with the rest of the text.

Let me also check: "a new sentence that contradicts an unchanged one." Does R31's definition contradict anything?

R31 says "both faithful on c's contract." Part V says fidelity is checked on the source's contract (C ⊆ A_D × B_D where D is the source). These contradict for the transport from d to c (source is d, but c's contract is used).

So R31's definition contradicts Part V's fidelity conditions. This is a coherence issue under (c).

Also: "notation that clashes with notation used elsewhere, such as a letter already bound or a label shared with an equation tag." The ≡ symbol is standard for equivalence relations. If≡_ℓ is not an equivalence (not symmetric), this clashes with standard notation. This is a coherence issue.

OK, R31 FALLS due to (c): the definition contradicts Part V's fidelity conditions (type mismatch) and uses misleading notation (≡ for a non-equivalence).

Let me write the report now.

Actually, I realize I should double-check one more thing. Let me look at how Derivation 2 uses kinds and whether R31's ≡_ℓ is related.

Derivation 2: "Let E, E' be candidates for the same p that both satisfy (F1), (F2) and (A) on C. (i) Their answer profiles coincide on C. (ii) If a bijection φ of their active components gives each k and φ(k) one anchor... then k and φ(k) are of one kind on C for every k."

Derivation 2 uses "of one kind on C" (from Part II's signature definition). It doesn't use ≡_ℓ. So R31 doesn't affect Derivation 2.

But (N) uses ≡_ℓ. And (G) uses (N). And (EK) uses (G). So R31 affects the chain (N) → (G) → (EK).

In Derivation 10: "If S₁ is not in the prior repertoire, then (G) holds." This uses (G) which uses (N) which uses ≡_ℓ. But the conditional "If S₁ is not in the prior repertoire" is a hypothesis. Under this hypothesis, we don't need to evaluate (N) - we just assume the result. So Derivation 10's conditional is fine.

But in practice, whether S₁ is "not in the prior repertoire" requires evaluating (N). And (N) requires ≡_ℓ. If ≡_ℓ is broken, we can't evaluate (N).

Hmm, but "S₁ is not in the prior repertoire" might be a simpler condition than (N). (N) says "New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}(s, h), d ≡_ℓ c." This is about equivalence. "S₁ is not in the prior repertoire" might mean S₁ is not a member of R_{<e} (literally not in the set). This is different from (N) (which uses≡_ℓ).

Actually, (N) uses ≡_ℓ to check if any prior content is EQUIVALENT to c. "Not in the prior repertoire" might mean "not literally in the set" (different from "not equivalent to any member"). These are different.

But (N) is defined as "New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}(s, h), d ≡_ℓ c." This says c is new when no prior d is equivalent to c. The "prior repertoire" R_{<e} is a set of contents. (N) checks equivalence, not membership.

Derivation 10 says "If S₁ is not in the prior repertoire." This might mean "if S₁ is not in R_{<e}" (membership) or "if S₁ is new per (N)" (equivalence). If it means membership, (N) is not needed. If it means equivalence, (N) is needed.

In context: "If S₁ was built by an owned subhistory containing a nontrivial binding construction, the binding of a persistence component to a continuity subnetwork, and S₁ is not in the prior repertoire, then (G) holds." The phrase "not in the prior repertoire" is used alongside (G). (G) requires (N). (N) uses ≡_ℓ. So "not in the prior repertoire" likely means "new per (N)" (equivalence-based).

But this is ambiguous. If it means literal membership, (N) is not needed and R31's defect doesn't affect Derivation 10. If it means equivalence, (N) is needed and R31's defect affects Derivation 10.

Hmm, I think "not in the prior repertoire" is a colloquial way of saying "new" (per (N)). In that case, (N) is needed and R31's defect matters.

But Derivation 10 is a consistency witness, not a theorem. It says "If [conditions], then (G) holds." If one of the conditions ("S₁ is not in the prior repertoire") can't be evaluated (because ≡_ℓ is broken), the conditional is still valid (we just can't determine if the hypothesis is true).

OK, I think R31's defect is real but its impact on specific verdicts is indirect. The main issue is the ill-typed definition, which is a coherence problem.

Let me now write the report. Final answer: R31 FALLS, all others STAND. OVERALL: NEEDS REPAIR.

Wait, I want to reconsider one more time whether R31 really falls or if I'm being too harsh.

The definition says: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ."

I've been arguing this is ill-typed because "faithful on c's contract" for a transport from d to c uses c's contract (target's edits) where the source's edits are expected.

But what if "faithful on c's contract" is intentionally non-standard? What if it means "faithful with respect to the equivalence class defined by c's contract"? This would be a new notion specific to this definition.

But the text doesn't define a new notion. It uses "faithful on" which is already defined in Part IV. So it should use the standard meaning.

Unless R31 implicitly redefines "faithful on" for this context. But the text doesn't say "in this definition, 'faithful on' means..." It just uses "faithful on c's contract."

I think the standard meaning is intended. And the standard meaning has a type mismatch. R31 FALLS.

But let me consider: is the type mismatch actually a problem in practice? If τ is a bijection (which it might be for an equivalence), then τ^{-1} exists and we can translate c's edits to d's edits. In that case, "faithful on c's contract" for a transport from d to c could be interpreted as: translate c's contract to d's edits via τ^{-1}, then check fidelity on the translated contract.

But the text doesn't say this. It says "faithful on c's contract" which, by the standard definition, uses c's contract directly (not translated). This is the type mismatch.

If the text said "faithful on τ^{-1}[c's contract]" or "faithful on the translation of c's contract to d's edits," it would be well-typed. But it says "faithful on c's contract" which is not translated.

OK, I'm confident. R31 FALLS.

Let me write the report now.

Hmm, actually, I want to present this more carefully. Let me think about the best way to argue the point.

The key argument:
1. Part V defines fidelity for a transport t from D to E with contract C ⊆ A_D × B_D.
2. In (F1), (a, b) ∈ C are used with Sol_{λ(k)}(a, b) where λ(k) ⊆ D. This requires (a, b) to be D's edits and boundaries.
3. R31 says "both faithful on c's contract" for transports from d to c and from c to d.
4. For the transport from d to c (source is d), "faithful on c's contract" uses C = c's contract ⊆ A_c × B_c. But (F1) requires C ⊆ A_d × B_d (d's edits). Type mismatch.
5. The correct formulation would be "the transport from d to c faithful on d's contract, and the transport from c to d faithful on c's contract."

This is clear and follows from the text. Let me present it in the report.

Also, the asymmetry issue: with "both faithful on c's contract," the relation d ≡_ℓ c and c ≡_ℓ d use different contracts (c's and d's respectively). This makes the relation asymmetric. But (N) and the use of ≡ notation suggest it should be symmetric.

These two issues (type mismatch and asymmetry) are related: both stem from using "c's contract" for both transports.

Proposed repair: Replace "both faithful on c's contract at grain ℓ" with "each faithful on the contract of its source at grain ℓ."

This fixes both issues: the type mismatch (each transport uses its source's contract) and the asymmetry (the definition is symmetric in d and c).

But this changes the meaning. The original says "both faithful on c's contract" (using c's contract as the reference for both). The repair says "each faithful on its source's contract" (using each source's contract). These are different.

If the intention was to use c's contract as the reference (to check if d and c agree on c's structures), the repair changes this. If the intention was to check each transport faithfully (using its source's contract), the repair preserves the intention.

Given the ill-typing, I think the intention was the latter (check each transport faithfully). The repair preserves this.

OK, I'll propose this repair in the report.

One more thing: I should check if the declaration for R31 says anything about the direction of fidelity checking. The declaration says: "d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ." This matches the new wording. The declaration doesn't clarify the direction issue.

So the declaration faithfully describes the new wording (including its flaw). The flaw is in the wording, not in the declaration's description. For (a), the declaration is accurate. For (c), the wording is incoherent.

OK, R31 FALLS due to (c). The declaration is accurate for (a) but the new wording is incoherent.

Now let me think about one more thing: does R31's defect affect any verdict (task d)?

I discussed this earlier. The defect makes≡_ℓ potentially unusable (ill-typed). This affects (N) and (G). For situations where (N) or (G) is relevant:

- O3 (Nadia): verdict is "construction, her own." (G) requires New (via (N)). If ≡_ℓ is broken, (N) is problematic. But the verdict is about construction (Build) and ownership, not strictly about (G). However, the verdict says "That is construction, and it is her own." Construction is Build, not (G). (G) = Attempt ∧ New ∧ Build. The verdict mentions construction (Build) and ownership. It doesn't explicitly mention (G) or New. So the verdict might not depend on (N).

But the verdict says "Nadia built something new." The word "new" could refer to (N). If (N) is broken, the "new" part is problematic.

Hmm, but "new" in the verdict might be colloquial (she built something that didn't exist before), not technical (per (N)). In that case, (N) is not directly relevant.

For N10 (the burned notebook): verdict is "Yes, she created knowledge." This requires (EK) which requires (G) which requires (N). If ≡_ℓ is broken, (N) is problematic, and (EK) is uncertain.

But the verdict is "Yes, she created knowledge." If the theory can't determine this (because ≡_ℓ is broken), the theory is silent. This is a move from "Yes" (fixed verdict) to "silent" (theory's verdict). But wait, the theory's verdict with the CURRENT text might also be silent (because ≡_ℓ was undefined). So the move might be from "silent" to "silent" (no change).

Hmm, but the current text might have ≡_ℓ defined elsewhere (not in R31's location). Let me check.

R31 adds the definition of ≡_ℓ at the beginning of the Newness section. The old text starts with "**Newness.** With R_{<e}(s,h)=..." and (N) uses ≡_ℓ. If ≡_ℓ is not defined elsewhere in the current text, (N) uses an undefined symbol. R31 adds a definition (albeit broken).

If the current text has ≡_ℓ undefined, then (N) is problematic in both texts. R31 adds a broken definition. The theory's verdict might change from "undefined≡_ℓ" to "broken ≡_ℓ." But in both cases, (N) is problematic.

I think the defect in R31 is a coherence issue (ill-typed definition) but doesn't clearly move any verdict (because the current text also has issues with ≡_ℓ). The main problem is that R31's definition is wrong, making the text incoherent.

OK, let me finalize and write the report. The main point is R31's ill-typed definition (task c). I'll also mention the asymmetry issue and the misleading use of ≡ notation.

Let me also briefly consider if R31's problem could be seen as a CLAIM issue (task a). The declaration says "The equivalence is structural at that grain." If the relation is not truly an equivalence (not symmetric), calling it "the equivalence" is misleading. But the declaration might mean "the equivalence notion" rather than "the equivalence relation." This is ambiguous.

For (a), the declaration accurately describes the new wording (including the use of "equivalence"). But the new wording is misleading (the relation is not an equivalence). This is a (c) issue (coherence: misleading notation) rather than an (a) issue.

OK, final report structure:

Point 1: R31, task (c) — The definition of ≡_ℓ is ill-typed and asymmetric.

Then: R31: FALLS — definition of ≡_ℓ ill-typed and non-symmetric.

OVERALL: NEEDS REPAIR.

END OF REPORT.

Wait, but I should give a more detailed argument. Let me flesh out the point.

Actually, I realize I should also check whether R31's "both faithful on c's contract" might work if the transport from d to c is interpreted differently. What if the transport from d to c is actually a transport whose fidelity is checked from c's perspective?

In Part V, t is a transport from D to E. The fidelity conditions check that E's components match D's subnetworks. The contract C ranges over D's edits. So fidelity is checked from D's perspective (the source).

For the equivalence, "transports from d to c and from c to d, both faithful on c's contract":
- Transport from d to c: checks that c's components match d's subnetworks. Contract should be d's edits. Using c's contract is wrong.
- Transport from c to d: checks that d's components match c's subnetworks. Contract should be c's edits. Using c's contract is correct.

Wait, "checks that c's components match d's subnetworks" — this is checking from c's perspective (c's components are the target). But the contract ranges over d's edits (the source). So the check is: for every d-edit, c's components match d's subnetworks.

For "faithful on c's contract": the contract is c's edits. But the check needs d's edits (to evaluate d's subnetworks). So using c's edits as the contract doesn't work.

Unless "faithful on c's contract" means: for every c-edit (in c's contract), the transport maps d's responses correctly. This requires translating c-edits to d-edits (via τ^{-1}). But the text doesn't say this.

OK, I've been going back and forth. Let me just write the report.

Actually, I want to reconsider the whole thing one more time. Let me think about what "faithful on c's contract" COULD mean that makes sense.

Option 1: "faithful on C" where C = c's contract. This is the standard meaning. Ill-typed for transport from d to c.

Option 2: "faithful with respect to c's contract." This means the fidelity is checked against c's structures (as defined by c's contract). For the transport from d to c, this means: the transport produces c's structures correctly (as defined by c's contract). This requires evaluating the transport at points specified by c's contract. For the standard (F1), this means (a, b) ∈ c's contract and checking L_k(τ(a), σ(b)). But (a, b) should be d's edits for Sol_{λ(k)}. So this is still ill-typed.

Wait, unless we reverse the roles. For "faithful with respect to c's contract" for the transport from d to c: we want to check that c's components (L_k) match what d produces. So we evaluate d's subnetworks at d's edits and check they match c's components at c's edits. The contract c's contract specifies which c-edits to check. For each c-edit (a_c, b_c) in c's contract, we need to find the corresponding d-edit (a_d, b_d) and check: Sol_{λ(k)}(a_d, b_d) projects to L_k(a_c, b_c). The correspondence between (a_d, b_d) and (a_c, b_c) is given by τ and σ: τ(a_d) = a_c and σ(b_d) = b_c. So a_d = τ^{-1}(a_c) and b_d = σ^{-1}(b_c).

This would give: proj_{V_k}[Sol_{λ(k)}(τ^{-1}(a_c), σ^{-1}(b_c))] = L_k(a_c, b_c) for (a_c, b_c) ∈ c's contract.

This is well-typed! It uses c's contract as the scope and τ^{-1} to translate to d's edits. But this is NOT the standard (F1) formulation. The standard (F1) uses (a, b) ∈ C and τ(a), σ(b). This reformulation uses (a, b) ∈ C and τ^{-1}(a), σ^{-1}(b).

So "faithful on c's contract" for a transport from d to c could be well-typed if we use a non-standard fidelity check (with τ^{-1}). But the text says "faithful on c's contract" using the standard term "faithful on" (defined in Part IV). The standard definition uses τ, not τ^{-1}.

I think the non-standard interpretation is possible but not what the text says. The text says "faithful on c's contract" which, by the standard definition, is ill-typed for a transport from d to c.

But a charitable reader might interpret "faithful on c's contract" as the non-standard version (using τ^{-1}). In that case, the definition would be well-typed but non-standard.

Hmm, this is getting too deep. Let me just present the issue in the report and let the reader decide.

OK here's my plan for the report:

Point 1 (most serious): R31, tasks (a) and (c). The definition of ≡_ℓ says "both faithful on c's contract." Under Part V's fidelity conditions, the contract must range over the source organization's edits: (F1) evaluates Sol_{λ(k)}(a, b) where λ(k) ⊆ source, so (a, b) must be source edits. For the transport from d to c, the source is d, so the contract must range over d's edits. But "c's contract" ranges over c's edits. This is a type mismatch. Consequently, the relation is not symmetric (d ≡_ℓ c uses c's contract; c ≡_ℓ d would use d's contract) yet is called "the equivalence" and written≡. Repair: change "both faithful on c's contract" to "each faithful on the contract of its source" or "the first faithful on d's contract, the second on c's."

Actually, I want to consider one more time whether this is really a defect or if I'm being overly pedantic. In mathematical writing, "faithful on C" might be used loosely to mean "faithful with respect to C." If C is c's contract and the transport is from d to c, "faithful with respect to C" might mean "faithful in the sense that it preserves the structures C specifies." This is a looser reading that avoids the type mismatch.

But the text defines "faithful on C" precisely in Part IV: "A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V." And Part V's conditions require C to be the source's edits. So the precise definition has a type mismatch.

I think this is a genuine defect. In a formal semantics paper, definitions should type-check. R31's definition doesn't. FALLS.

But let me consider: is this the kind of thing the cross-examination is looking for? The task says "Look for a term or symbol used before it is defined, or defined in two different ways; notation that clashes with notation used elsewhere, such as a letter already bound or a label shared with an equation tag; a pointer to a Part, derivation, condition, label or tag that does not say what the change says it says; a new sentence that contradicts an unchanged one; an unchanged sentence that the change has made false, stale or idle; and two changes that pull against each other."

"Defined in two different ways" — "faithful on C" is defined in Part IV (standard) and used in R31 (potentially non-standard). If R31 uses it differently, this is "defined in two different ways."

"A new sentence that contradicts an unchanged one" — R31's "both faithful on c's contract" contradicts Part V's requirement that C ranges over the source's edits. This is a contradiction between new and unchanged text.

So R31's defect fits the task's criteria for (c). FALLS.

OK, I'm writing the report now. For real.

Let me also think: should I raise any minor points about other changes, even if they STAND?

The task says "A change you tried to break and could not needs no point of its own." So if I don't have a strong point, I don't need to raise it. But I can raise minor points as long as the change still STANDS.

I think I should raise one minor point about R36 (the non-standard notation of r(ξ')) but conclude it STANDS. And maybe a minor point about R52 (the dropped "realizable") but conclude it STANDS.

Actually, the task says "A change you raised a point on that survives your own scrutiny STANDS." So I can raise points and still have the change STAND.

Let me raise:
1. R31 (major): ill-typed definition. FALLS.
2. R52 (minor): "realizable" dropped without explicit declaration. STANDS (the declaration gives both lists).
3. R36 (minor): non-standard notation for r(ξ'). STANDS (the text explains it).

Actually, the report should be focused. "Depth on the points that matter counts for more than coverage of small ones." So I should focus on R31 and mention others briefly if at all.

Let me write the report with:
1. R31 (detailed argument): ill-typed and asymmetric definition. FALLS.
2. R52 (brief note): "realizable" dropped. STANDS (declaration adequate).
3. R36 (brief note): non-standard notation. STANDS (explained in text).

Actually, maybe I should just focus on R31 and not raise minor points. The task says "A change you tried to break and could not needs no point of its own." If R52 and R36 survive scrutiny, they don't need their own points.

But if I have a genuine (even if minor) point, I should raise it. Let me raise R52's point briefly and conclude STANDS.

Hmm, but the format says "Number your points in one list, most serious first." If I only have one point (R31), the list has one entry. That's fine.

Actually wait, I want to reconsider R36 one more time. The formula (P) uses r(ξ') in a non-standard way. But the text explains it. Is there any issue with the EXPLANATION itself?

"In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."

"Accordingly" refers to the preceding sentence: "a protected condition is lost exactly when it fails on an occasion it covers." So "accordingly" means: given that a condition is lost when it fails on any occasion, r(ξ') in (P) means the condition held on every occasion. This is a logical consequence.

But wait: the preceding sentence says "a protected condition is lost exactly when it fails on an occasion it covers." This is about ANY occasion (not just from ξ to ξ'). The new text says r(ξ') means "held on every occasion it covers from ξ to ξ'." This narrows the scope to [ξ, ξ'].

Is this narrowing correct? In (P), the repair is from ξ to ξ'. The protected conditions should hold during the repair (from ξ to ξ'). So the relevant occasions are those in [ξ, ξ']. The preceding sentence's "an occasion it covers" is about any occasion in general. But in (P), the relevant occasions are those in [ξ, ξ']. So the narrowing is correct.

But the word "accordingly" suggests the new text is a consequence of the preceding sentence. The preceding sentence is about ANY occasion. The new text is about occasions in [ξ, ξ']. The new text is not a direct consequence of the preceding sentence (which is more general). But in the context of (P), the narrowing is appropriate.

Is this a coherence issue? The word "accordingly" might suggest a stronger connection than exists. But the content is correct (in (P), the relevant occasions are those in [ξ, ξ']). Not a serious issue.

OK, R36 STANDS. I'll mention it briefly in the report if needed.

Let me now actually write the report.

Here's my outline:

**Point 1: R31, tasks (a) and (c).**
The definition of ≡_ℓ is ill-typed and asymmetric.

Detailed argument:
- Part V's (F1) evaluates Sol_{λ(k)}(a, b) where λ(k) ⊆ D (source). So (a, b) must be D's edits.
- R31 says "both faithful on c's contract" for a transport from d to c. c's contract ranges over c's edits, not d's. Type mismatch.
- The relation is asymmetric: d ≡_ℓ c uses c's contract; c ≡_ℓ d would use d's contract.
- The declaration says "The equivalence is structural" but the relation is not an equivalence (not symmetric).
- Proposed repair.

**R31: FALLS** — definition ill-typed and non-symmetric.

**OVERALL: NEEDS REPAIR.**

END OF REPORT.

Hmm, but this feels too short. The task says "under about 3,000 words" and "Depth on the points that matter counts for more than coverage of small ones." Let me expand the R31 argument and maybe add 1-2 more points.

Actually, I want to reconsider R52 one more time.

R52's new text: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

"Member of T" and "a transport the physics and the stated construction admit" — as I noted, these are the same (since T is defined as "the set of transports the physics and the stated construction admit"). So the new text lists the same condition twice.

But the old text listed "admitted, realizable, a member of T" as three separate conditions. If these were all different, the new text's merging of "admitted" and "member of T" (while dropping "realizable") changes the claim.

If the old text's "admitted" and "member of T" were different (e.g., "admitted" = physically allowed, "member of T" = in the specific population), then the new text's equation of them (via the appositive) would change the claim. But the definition of T says T = "the set of transports the physics and the stated construction admit." So "member of T" = "admitted by physics/construction." The old text's "admitted" and "member of T" might have been the same all along (just listed twice for emphasis).

I think the old text was redundant (listing "admitted" and "member of T" which are the same). The new text is cleaner (merging them into one condition with an appositive). The "realizable" drop is the only substantive change.

Is "realizable" a genuine additional condition? If it means "has been realized" (built), it's different from "admitted" (possible). If it means "can be realized" (buildable), it's the same as "admitted."

In the physical module: "realized" might mean "has a RetReal (retained realization)." "Admitted" might mean "is in Admit^{q,r}_Θ (physically achievable)." These are different: a task can be physically achievable (admitted) without having been realized.

If "realizable" = "has been realized" (built), it's a genuine condition. Dropping it changes the claim. But the declaration doesn't say so.

If "realizable" = "can be realized" (buildable = admitted), it's redundant. Dropping it is cleanup.

I think the text is ambiguous. But the declaration should clarify. Without clarification, the declaration is potentially incomplete.

Hmm, but is this enough to make R52 FALL? The declaration says "This replaces the list." The reader can see the difference. I think it's borderline.

Let me raise this as a secondary point and conclude R52 STANDS (with reservation).

Actually, on reflection, I think I should raise this as a point and let R52 STAND, since the declaration does give both lists and the reader can see the change. But I should note the issue.

OK, let me write the report with two points:

1. R31 (major): ill-typed definition. FALLS.
2. R52 (minor): "realizable" dropped without explicit mention. STANDS (declaration gives both lists).

Then the verdicts and overall.

Let me also think about one more thing: does the text use "faithful on C" anywhere else where the contract might not be the source's? If so, my argument might be wrong.

Looking at the text:
- Part IV: "A transport is faithful on C when it satisfies the component and global fidelity conditions of Part V." Standard.
- Part V: (F1), (F2) with C as the contract. Standard (C is source's edits).
- Part IV (Rep): "Faithful_C(t: Org_ℓ(o) → c)." Here t is from Org_ℓ(o) to c. C should be Org_ℓ(o)'s edits (source's edits). But c has a contract too. Which is C?

In (R): "Rep_ℓ(o, c) ⟺ ∃t [Faithful_C(t: Org_ℓ(o) → c) ∧ (Sel(t) ∨ Con(t))]." Here C is... whose? The text says "faithful on c's contract" earlier ("An occurrence o represents content c at grain ℓ when... admits a transport to c that is faithful on c's contract"). So C = c's contract.

But for a transport from Org_ℓ(o) to c, the source is Org_ℓ(o). C should be Org_ℓ(o)'s edits. But the text says "faithful on c's contract" (= c's edits). This is the same type mismatch as R31!

Wait, this is interesting. The text in Part IV says: "An occurrence o represents content c at grain ℓ when the organization that o instantiates under the physical module, at grain ℓ, admits a transport to c that is faithful on c's contract and whose provenance is selected or constructed."

"Faithful on c's contract" for a transport from Org_ℓ(o) to c. The source is Org_ℓ(o) and the target is c. The contract should be the source's (Org_ℓ(o)'s) edits. But the text says "c's contract" (target's edits).

This is the SAME type mismatch as R31! So the issue is not unique to R31 — it exists in Part IV's definition of representation (R) as well.

Hmm, but (R) is in the revised text (which includes 44 unlisted changes). Maybe one of those changes introduced this issue. Or maybe it's in the original text.

Wait, the task says "The revised text is the current text with fifty-five changes made to it, and no others." So the text in Part IV's (R) is either unchanged or one of the 55 changes. Let me check if any of the 11 listed changes affect (R). None of them do (they're in Parts IV, VI, X, XI, XII, XVI). R11 is in Part IV but it's about "survives on H," not about (R).

So the phrase "faithful on c's contract" in (R) is either in the current text or one of the 44 unlisted changes. If it's in the current text, the type mismatch exists in both texts. If it's one of the 44 unlisted changes, it's a separate issue.

But this is interesting: if "faithful on c's contract" is used in (R) for a transport from Org_ℓ(o) to c, and this is the same type mismatch as R31, then maybe the text INTENDS "faithful on c's contract" to mean something non-standard.

In (R), "faithful on c's contract" for a transport from Org_ℓ(o) to c might mean: the transport preserves c's structures (as defined by c's contract). This requires checking that Org_ℓ(o)'s structures map to c's structures correctly, using c's contract as the reference.

If this is the intended meaning, then "faithful on C" in Part IV might have a dual meaning:
1. Standard: C is the source's contract (used in Part V's fidelity conditions).
2. Non-standard: C is the target's contract (used in (R) and R31's≡_ℓ).

But the text defines "faithful on C" once (Part IV) and uses it in multiple places. If the meaning varies, this is "defined in two different ways" — a coherence issue.

Hmm, but maybe "faithful on C" always means the same thing, and C is always the source's contract. In (R), "faithful on c's contract" might be wrong (should be "faithful on Org_ℓ(o)'s contract"). This would be a pre-existing error in the text.

But (R) is stated as: "An occurrence o represents content c at grain ℓ when the organization that o instantiates under the physical module, at grain ℓ, admits a transport to c that is faithful on c's contract." The transport is "to c" (from Org_ℓ(o) to c). "Faithful on c's contract" uses c's contract. If the standard meaning requires the source's contract, this is wrong.

But maybe the text intends "faithful on c's contract" to mean "faithful with respect to what c's contract demands." For a transport to c, this means: the transport produces c's structures (as demanded by c's contract). This is a natural reading: we want the transport to be faithful to c (the target), so we check against c's contract.

In this reading, "faithful on C" means "faithful with respect to C" where C is the reference point for fidelity. If C is the target's contract, fidelity means the transport correctly produces the target's structures. If C is the source's contract, fidelity means the transport correctly preserves the source's structures.

These are different notions. But the text uses "faithful on C" for both. This is ambiguous.

Hmm, but in Part V, the fidelity conditions are explicitly stated. (F1) uses (a, b) ∈ C with Sol_{λ(k)}(a, b) (source's structure) and L_k(τ(a), σ(b)) (target's structure). So C is used with the source's structure. This means C is the source's contract.

For (R) and R31, "faithful on c's contract" uses c's contract (target's). This contradicts Part V's usage.

Unless (R) and R31 use "faithful on" differently from Part V. But the text defines "faithful on" once (Part IV, pointing to Part V). So it should be the same.

I think there's a pre-existing inconsistency in the text: "faithful on c's contract" in (R) uses the target's contract, while Part V's fidelity conditions use the source's contract. R31 repeats this inconsistency.

But R31 is the change I'm examining. Even if the inconsistency exists elsewhere, R31 introduces it in the definition of ≡_ℓ. This is a coherence issue for R31.

Actually, wait. Let me re-read (R) more carefully.

"An occurrence o represents content c at grain ℓ when the organization that o instantiates under the physical module, at grain ℓ, admits a transport to c that is faithful on c's contract and whose provenance is selected or constructed."

"Admits a transport to c" — the transport goes TO c (from Org_ℓ(o) to c). "Faithful on c's contract" — the fidelity is checked on c's contract.

But Part V's fidelity conditions check on the SOURCE's contract. For a transport from Org_ℓ(o) to c, the source is Org_ℓ(o). So the contract should be Org_ℓ(o)'s.

But "faithful on c's contract" uses c's (the target's). This is the same type mismatch.

Hmm, but maybe "faithful on c's contract" in (R) means: faithful in the sense that it satisfies c's commitments. c is a content (an organization with contract-relative commitments). "c's contract" defines c's commitments. "Faithful on c's contract" means the transport satisfies those commitments.

In this reading, "faithful on c's contract" is about meeting c's requirements, not about the standard fidelity conditions. It's a different notion of "faithful."

But the text says "faithful on c's contract" using the defined term "faithful on." The defined term (Part IV) points to Part V's conditions. So it should be the standard meaning.

I think there's genuine ambiguity in the text. The phrase "faithful on c's contract" could mean:
(a) Standard: satisfies (F1), (F2) with C = c's contract. Ill-typed for a transport to c.
(b) Non-standard: satisfies c's commitments (what c's contract demands). Not the standard fidelity conditions.

If (a), the text has a type mismatch. If (b), the text uses "faithful on" in two different ways (standard in Part V, non-standard in (R) and R31).

Either way, there's a coherence issue. R31 inherits this issue.

But is this issue specific to R31, or is it pre-existing (in (R))? If it's pre-existing, R31 just repeats it. If R31 introduces it, it's R31's fault.

Looking at the old text of R31: "**Newness.** With R_{<e}(s,h)=⋃_{ξ before e} R_{β,ℓ}(s,ξ)," — this doesn't use "faithful on." The new text adds "faithful on c's contract." So R31 introduces this usage in the Newness section.

But (R) already uses "faithful on c's contract." So the usage is pre-existing. R31 repeats it.

Hmm, if the usage is pre-existing (in (R)), then R31 is consistent with (R). The issue is with (R), not R31.

But R31 is the change I'm examining. Even if the issue is pre-existing, R31's use of "faithful on c's contract" is consistent with (R). So R31 is coherent with (R).

Wait, but R31's usage is for a transport from d to c AND from c to d. (R)'s usage is for a transport from Org_ℓ(o) to c (one direction). R31 applies "faithful on c's contract" to BOTH directions. For the transport from c to d, "faithful on c's contract" is natural (c is the source). For the transport from d to c, "faithful on c's contract" is the same as (R)'s usage (c is the target).

So R31's usage for the transport from d to c is consistent with (R). And for the transport from c to d, it's consistent with Part V (c is the source).

But the relation is asymmetric because both use c's contract. For the transport from d to c, using c's contract is like (R). For the transport from c to d, using c's contract is like Part V. Both are "faithful on c's contract" but in different senses.

Hmm, this is getting very confusing. Let me step back and think about the big picture.

The text uses "faithful on C" in two ways:
1. C = source's contract (Part V, standard).
2. C = target's contract ((R), "faithful on c's contract" for a transport to c).

R31 uses "faithful on c's contract" for both directions:
- Transport from c to d: c is the source. "Faithful on c's contract" = standard (source's contract). OK.
- Transport from d to c: c is the target. "Faithful on c's contract" = non-standard (target's contract). Consistent with (R).

So R31 mixes the two usages. For one transport, it uses the standard meaning. For the other, it uses the non-standard meaning (consistent with (R)).

This is confusing but not necessarily wrong. The text has two usages of "faithful on C," and R31 uses both.

But the relation is asymmetric because both use c's contract. For c ≡_ℓ d (swapping d and c), both would use d's contract. So d ≡_ℓ c and c ≡_ℓ d use different contracts.

Is the asymmetry intentional? If (N) is "New(s, c, h, e) ⟺ ¬∃d ∈ R_{<e}, d ≡_ℓ c," the asymmetry means (N) checks if any prior d can "match" c under c's contract. This is checking: does any prior content reproduce c's structures? This is a reasonable definition of "new" (c is new if no prior content reproduces it).

So the asymmetry might be intentional! (N) is about whether c is reproduced by prior content. The reference point is c (the candidate). Using c's contract for both transports checks: can d reproduce c (under c's contract) AND can c reproduce d (under c's contract)?

Wait, "can c reproduce d under c's contract" is weird. If we're checking if d and c are equivalent, we want both directions. But using c's contract for both means we're always using c's reference. This checks if d and c agree on c's structures, but doesn't check if they agree on d's structures.

Hmm, but (N) is about c being new. We care about c's structures. If d can reproduce c's structures (under c's contract), then c is not new (it's already in the repertoire as d). We don't care about d's structures (which c might not reproduce).

But the definition says "transports from d to c AND from c to d." Both directions are required. If only one direction is needed (d → c), why require both?

Maybe both directions are needed to ensure structural equivalence (not just one-way simulation). But using c's contract for both means we're checking equivalence on c's terms, not on neutral terms.

I think the definition is trying to check: d and c are structurally equivalent (both directions) with c's contract as the reference. This is asymmetric but might be intentional.

But the text calls it "the equivalence" which suggests symmetry. And ≡ notation suggests symmetry. If the relation is asymmetric, calling it "the equivalence" is misleading.

Hmm, but maybe "the equivalence" means "the equivalence notion" (how we determine equivalence) rather than "the equivalence relation." This is a valid reading.

OK, I think the issue is genuinely ambiguous. The definition might be intentionally asymmetric (using c's contract as the reference). But calling it "the equivalence" and using ≡ notation is misleading.

And there's still the question of whether "faithful on c's contract" for a transport from d to c is well-defined. If it uses the non-standard meaning (consistent with (R)), it might be well-defined. But the text doesn't distinguish the two meanings of "faithful on C."

I think I should present this as a coherence issue: "faithful on C" is used in two different ways (source's contract in Part V, target's contract in (R) and R31). R31 uses both in one definition, making it confusing and potentially ill-typed.

But if the two usages are intentional (and consistent within their contexts), R31 might be OK. The asymmetry is intentional (c's contract is the reference), and the "faithful on c's contract" for the d-to-c transport uses the (R) meaning.

Hmm, I'm going back and forth. Let me make a decision.

I think R31 has a genuine issue with the dual usage of "faithful on C" and the resulting asymmetry. Even if the asymmetry is intentional, calling it "the equivalence" and using ≡ notation is misleading. And the dual usage of "faithful on C" is a coherence issue ("defined in two different ways").

But is this enough to make R31 FALL? Let me check the criteria:
- "its declaration is false or incomplete" — the declaration says "The equivalence is structural." If the relation is not truly an equivalence (not symmetric), this is misleading but not clearly false.
- "it changes a claim it is declared not to change" — R31 is CLAIM (it adds a definition). The declaration accounts for this.
- "it makes the text incoherent" — the dual usage of "faithful on C" makes the text incoherent ("defined in two different ways").
- "it moves a verdict away from the fixed verdict" — not clearly.

So R31 might FALL due to "makes the text incoherent." The dual usage of "faithful on C" (source's contract in Part V, target's contract in R31) is a coherence issue.

But wait — the dual usage is pre-existing (in (R)). R31 just repeats it. If (R) already has the dual usage, the text is already incoherent (before R31). R31 doesn't make it worse.

Hmm, but R31 uses BOTH usages in one definition. (R) uses only one (target's contract). Part V uses only one (source's contract). R31 mixes them (one transport uses source's, the other uses target's). This mixing is new (introduced by R31).

Actually, let me re-examine. R31 says "both faithful on c's contract." For the transport from c to d, c is the source, so "faithful on c's contract" = source's contract (Part V standard). For the transport from d to c, c is the target, so "faithful on c's contract" = target's contract (like (R)). Both use c's contract, but in different roles (source for one, target for the other).

This is confusing: the same phrase "faithful on c's contract" means different things for the two transports. For one, it's the standard meaning (source's contract). For the other, it's the (R) meaning (target's contract). This mixing in one definition is problematic.

But maybe "faithful on c's contract" always means the same thing (regardless of direction). If it means "faithful with respect to c's contract" (c's contract as the reference for fidelity), then it's consistent for both transports. The transport from c to d is faithful with respect to c's contract (checking c's structures are preserved). The transport from d to c is faithful with respect to c's contract (checking c's structures are produced).

In this reading, "faithful on c's contract" = "faithful with respect to c's contract" = "c's structures are correctly handled." This is consistent for both directions. But it's not the standard Part V meaning (which uses the source's contract).

So the text has two meanings of "faithful on C":
1. Part V: C is the source's contract, checking fidelity conditions (F1), (F2).
2. (R), R31: C is the reference contract (can be source or target), checking "faithful with respect to C."

These are different. Using the same term for both is "defined in two different ways" — a coherence issue.

But this issue is pre-existing (in (R)). R31 inherits it. Is R31 responsible?

Hmm, R31 is a change I'm examining. Even if the issue is pre-existing, R31's use contributes to the incoherence. And R31's specific usage (mixing both in one definition) makes it worse.

I think R31 FALLS due to coherence issues (dual usage of "faithful on C," asymmetry despite ≡ notation).

OK, final decision: R31 FALLS. Let me write the report.

Actually, I want to reconsider one more time. Let me look at (R) again.

(R): "Rep_ℓ(o, c) ⟺ ∃t [Faithful_C(t: Org_ℓ(o) → c) ∧ (Sel(t) ∨ Con(t))]."

Here, "Faithful_C(t: Org_ℓ(o) → c)" — the subscript C is the contract. The prose says "faithful on c's contract." So C = c's contract.

But t is from Org_ℓ(o) to c. The source is Org_ℓ(o). In Part V, C should be the source's contract. But here C = c's contract (target's). This is the type mismatch.

Now, the formula (R) is stated in the revised text. Is this formula one of the 55 changes? Let me check. The task says "The revised text is the current text with fifty-five changes made to it, and no others." The formula (R) is in Part IV. R11 is the only listed change in Part IV (about "survives on H"). The other changes in Part IV are among the 44 unlisted. So (R) might be unchanged or one of the 44 unlisted changes.

If (R) is unchanged (in the current text), the type mismatch is pre-existing. R31 repeats it. R31 is consistent with (R) but both are wrong.

If (R) is one of the 44 unlisted changes, the type mismatch is introduced by that change. R31 is consistent with the new (R). But I'm told to "leave them out of your report, except where a listed change depends on one of them or pulls against it."

R31 depends on the definition of "faithful on" (used in its definition of ≡_ℓ). If (R) changes the meaning of "faithful on," R31 depends on that change. But (R) doesn't change the meaning — it just uses "faithful on c's contract" which is the standard term.

Hmm, I think the issue is pre-existing (or at least not introduced by R31). R31 is consistent with (R) in using "faithful on c's contract" for a transport to c. The type mismatch exists in both.

But R31 also uses "faithful on c's contract" for a transport FROM c (the transport from c to d). Here, c is the source, so "faithful on c's contract" = source's contract (standard Part V meaning). This is fine.

So R31 uses "faithful on c's contract" in two ways:
1. For the transport from c to d: c is the source, standard meaning. OK.
2. For the transport from d to c: c is the target, non-standard meaning (like (R)). Potentially problematic.

The mixing of two meanings in one definition is the issue. But one of the meanings is pre-existing (in (R)).

I think R31's specific contribution to the incoherence is the MIXING of two meanings in one definition. (R) uses one meaning (target's contract). Part V uses the other (source's contract). R31 mixes both. This mixing is R31's fault.

OK, R31 FALLS due to mixing two meanings of "faithful on C" in one definition, resulting in an asymmetric and potentially ill-typed relation.

Let me now write the report. FINAL.

Hmm, I realize I also want to double-check my claim about (F1) requiring the contract to be the source's. Let me re-read (F1) one more time.

(F1): "proj_{V_k}[Sol_{λ(k)}(a, b)] = L_k(τ(a), σ(b))."

- λ(k) ⊆ D (anchor subnetwork in the source).
- Sol_{λ(k)}(a, b): evaluates the source's subnetwork under (a, b).
- L_k: target's component relation.
- τ(a): target's edit (translated from source's edit a).
- σ(b): target's boundary (translated from source's boundary b).

For Sol_{λ(k)}(a, b) to be defined, (a, b) must be valid inputs to the source's organization. Since Sol_D(a, b) is defined for (a, b) ∈ A_D × B_D (source's edits and boundaries), (a, b) must be source's edits.

So C ⊆ A_D × B_D. C is the source's contract. Confirmed.

For a transport from d to c: source is d. C ⊆ A_d × B_d. "c's contract" = C_c ⊆ A_c × B_c. Type mismatch confirmed.

OK, the argument is solid. Let me write the report.

Let me draft it:

---

**1. R31, tasks (a) and (c) — The definition of ≡_ℓ is ill-typed and non-symmetric.**

R31 inserts: "Write d ≡_ℓ c when there are transports from d to c and from c to d, both faithful on c's contract at grain ℓ. The equivalence is structural at the stated grain, not string equality or similarity."

The defect: "both faithful on c's contract" applies c's contract to two transports of opposite direction. Under Part V's fidelity conditions, the contract must range over the source organization's edits. In (F1), the left side proj_{V_k}[Sol_{λ(k)}(a, b)] evaluates the anchor subnetwork λ(k) ⊆ D (the source) at (a, b); so (a, b) must be D's edits and boundaries. For the transport from d to c (line 407: "transports from d to c"), the source is d, and the contract must therefore be a subset of d's edits × boundaries. But "c's contract" ranges over c's edits (Part III: "C ⊆ A × B" for question p = (D, C, ...) where D is the target c). The two do not type-match.

Consequence: the relation is not symmetric. d ≡_ℓ c checks both transports against c's contract; c ≡_ℓ d would check both against d's contract. In general c's contract ≠ d's contract, so d ≡_ℓ c ≠ c ≡_ℓ d. Yet the text calls this "The equivalence" and writes ≡, both of which standardly denote a symmetric relation.

The declaration says "The equivalence is structural at that grain." If "the equivalence" means ≡_ℓ as a relation, this is misleading (it is not an equivalence). If it means "the equivalence notion" (the way equivalence is assessed), it is less misleading but still inaccurate for a relation that fails symmetry.

The same phrase "faithful on c's contract" also appears in (R) (line 166–167: "admits a transport to c that is faithful on c's contract") for a transport from Org_ℓ(o) to c, again applying the target's contract to a transport whose source is Org_ℓ(o). R31 propagates this mismatch into the definition of ≡_ℓ, where it becomes structural: one of the two required transports cannot be "faithful on c's contract" under the standard reading of "faithful on."

Proposed repair: replace "both faithful on c's contract at grain ℓ" with "each faithful on the contract of its source at grain ℓ." This makes the definition type-correct (each transport checked on its own source's edits) and symmetric (the condition is invariant under swapping d and c). The declaration would then read: "d ≡_ℓ c when there are transports from d to c faithful on d's contract and from c to d faithful on c's contract, at grain ℓ."

**R31: FALLS — the definition of ≡_ℓ applies c's contract to a transport from d to c, where Part V's fidelity conditions require the source's edits; the resulting relation is non-symmetric despite being written≡.**

**OVERALL: NEEDS REPAIR.**

END OF REPORT.

---

Hmm, this is about 400 words. The task allows up to 3,000 words. I should expand or add more points. But the task says "Depth on the points that matter counts for more than coverage of small ones." So depth on R31 is good.

Let me expand the R31 argument and maybe add a secondary point about R52 or R36.

Actually, let me reconsider R52 one more time. Is there a stronger issue than just "realizable is dropped"?

R52's new text: "it must be a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)."

Wait, I just noticed: "a transport the physics and the stated construction admit (Part XII)." The pointer is to Part XII. Part XII says "The population is the set of transports the physics and the stated construction admit." So "a transport the physics and the stated construction admit" = "a member of the population" = "a member of T." So "a member of T" and "a transport the physics and the stated construction admit" are saying the same thing.

But the new text lists them as separate items connected by commas: "a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)." This reads as three conditions. But the first two are the same. So there are really only two conditions: (1) member of T (= admitted), (2) survive on H.

The old text had four: (1) admitted, (2) realizable, (3) member of T, (4) survivor of H. The new has two distinct: (1) admitted/member of T, (2) survive on H. The change merges (1) and (3) and drops (2) ("realizable") and renames (4).

The declaration says the new claim "requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H." This has three conditions (matching the text). But two of them are the same. The declaration is redundant (listing the same condition twice) and doesn't account for dropping "realizable."

Is the redundancy a problem? Not really (it's just imprecise). But the dropped "realizable" is a potential issue.

Hmm, I think I should raise this as a secondary point. Let me add it.

Actually, looking at the declaration again: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

The first sentence lists three things: (1) member of the population, (2) a transport the physics and the stated construction admit, (3) survives on H. But (1) and (2) are the same (the population IS the set of admitted transports). So the declaration lists two distinct conditions, but presents them as three.

The second sentence says "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." The old list has four items. The new has three (but really two distinct). The specific drop ("realizable") is not mentioned.

I think the declaration is imprecise (listing the same condition twice, not accounting for "realizable"). But it's not clearly wrong.

Let me raise this as a secondary point and conclude R52 STANDS.

OK, let me write a more complete report with two points.

---

**1. R31, tasks (a) and (c) — The definition of ≡_ℓ is ill-typed and non-symmetric.**

[detailed argument as above]

**2. R52, task (a) — The declaration does not account for the dropped condition "realizable."**

R52 replaces the four conditions "admitted, realizable, a member of T, and a survivor of H" with three: "a member of T, a transport the physics and the stated construction admit (Part XII), and it must survive on H (Part IV)." The word "realizable" from the old list has no counterpart in the new. Because Part XII defines the population as "the set of transports the physics and the stated construction admit," the phrases "a member of T" and "a transport the physics and the stated construction admit" state one condition, not two. So the net change is from four conditions to two: admission/membership and survival.

The declaration says the new claim requires "a member of the population, a transport the physics and the stated construction admit, that survives on H," and that "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'." By giving both lists, the declaration makes the drop visible. But it does not say that "realizable" is dropped or subsumed. If "realizable" was a distinct physical condition (e.g., having a retained realization in the sense of (CT1)), removing it weakens Derivation 3's hypothesis and broadens its conclusion. The declaration should state whether "realizable" is now covered by "admitted" or is removed as a separate requirement.

This point survives scrutiny as a note rather than a fatal defect: the declaration gives both lists and says "This replaces," from which a careful reader can see the change. **R52: STANDS.**

---

Hmm wait, but I said R52 STANDS. Let me reconsider whether I should make it FALL.

The criteria for FALL: "its declaration is false or incomplete." If the declaration is incomplete (doesn't account for dropping "realizable"), R52 FALLS.

But I argued the declaration "gives both lists and says 'This replaces'" which makes the change visible. Is this "complete"?

"Complete" means the declaration says "exactly what changes in what is claimed." If "realizable" is dropped and the declaration doesn't say "and drops 'realizable,'" it's not saying "exactly what changes."

But the declaration is one line. It can't map each old item to a new one in one line. Giving both lists and saying "This replaces" is the most it can do.

Hmm, but the declaration could say: "This replaces the list 'admitted, realizable, a member of the population, and a survivor of H', dropping the separate realizability condition." This would explicitly account for the drop. It's still one line (well, one sentence).

The current declaration doesn't say this. So it's incomplete.

But is "incomplete" the same as "should FALL"? The criteria say "its declaration is false or incomplete." If incomplete, FALL.

Hmm, I think by the strict criteria, R52's declaration is incomplete (doesn't explicitly account for dropping "realizable"). So R52 should FALL.

But I'm not sure "realizable" is a genuine condition. If it's redundant, dropping it is cleanup and the declaration is fine (the lists show the change).

Let me be strict: the declaration should "say exactly what changes in what is claimed." Dropping "realizable" is a change in what is claimed (one fewer condition). The declaration doesn't say this explicitly. Incomplete. FALL.

But this feels harsh. The declaration gives both lists. The change is visible. Let me think about the spirit of the exercise.

The task says "Test that as hard as you can." So I should be strict. If the declaration doesn't explicitly account for a change, it's incomplete.

But the declaration says "This replaces the list." "Replaces" implies the old list is gone and the new one takes its place. The specific changes within the replacement are visible from comparing the lists. Is "visible from comparing" the same as "said exactly"?

I think "said exactly" means the declaration explicitly states the change. "Visible from comparing" is not "said exactly." So the declaration is incomplete.

But then, almost any CLAIM change with a list replacement would be "incomplete" (because the declaration can't map each item in one line). This seems too strict.

Hmm, let me re-read the task: "Every CLAIM change carries a one-line declaration, which is meant to say exactly what changes in what is claimed."

"One-line declaration" — it's one line. "Say exactly what changes" — it should state the change precisely. For R52, the change is: "the claim now requires 3 conditions instead of 4, dropping 'realizable.'" The declaration says "the claim now requires [3 things]. This replaces the list [4 things]." This states the change (from 4 to 3) but doesn't name the dropped item.

Is naming the dropped item necessary? The declaration gives both lists. The reader can identify the dropped item. But "say exactly what changes" might require naming it.

I think this is genuinely borderline. Let me go with: the declaration is adequate (gives both lists, says "This replaces"). The reader can see the change. R52 STANDS.

But I'll note the issue in the report.

OK wait, I want to reconsider one more time. Let me look at the declaration for R52: "Derivation 3's claim now requires the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H. This replaces the list 'admitted, realizable, a member of the population, and a survivor of H'."

Hmm, the first sentence says the claim "now requires" three things. The second sentence says "This replaces the list" of four things. The change is from 4 to 3. The declaration says both. The specific mapping is visible.

But there's a subtlety: the first sentence lists "a member of the population, a transport the physics and the stated construction admit, that survives on H." The second and third items in this list ("a transport the physics and the stated construction admit" and "that survives on H") are connected by "that," suggesting "a member of the population [that is] a transport the physics and the stated construction admit, [and] that survives on H." This would make it ONE condition (a member of the population that is admitted and survives) rather than THREE.

Wait, let me re-read: "the differing transport to be a member of the population, a transport the physics and the stated construction admit, that survives on H."

This could be parsed as:
(a) "a member of the population, [which is] a transport the physics and the stated construction admit, [and] that survives on H" — one condition with two properties.
(b) "a member of the population, [and] a transport the physics and the stated construction admit, [and] that survives on H" — three conditions.

If (a), the declaration says one condition (member of T, which is admitted, and which survives). This is really two conditions: member of T (= admitted) and survives.

If (b), the declaration says three conditions. But two are the same.

Either way, "realizable" is not mentioned. The declaration is incomplete in that it doesn't say "realizable" is dropped.

But again, the declaration gives both lists. The change is visible.

OK, I'll go with R52 STANDS and note the issue.

Now, should I add any more points? Let me think about R36 one more time.

R36 adds: "In (P), accordingly, r(ξ') says of a protected condition r that it held on every occasion it covers from ξ to ξ', not only at ξ'."

The word "accordingly" suggests this is a consequence of the preceding text. The preceding text says "a protected condition is lost exactly when it fails on an occasion it covers." This means: a condition is lost if it fails on any covered occasion.

"In (P), accordingly, r(ξ') says..." — this defines r(ξ') in (P) as "held on every occasion from ξ to ξ'." This is "accordingly" because if a condition is lost when it fails on any occasion, then r(ξ') in (P) must mean "held on every occasion" (otherwise the formula would not capture the loss condition).

But there's a gap: the preceding text says "an occasion it covers" (any occasion). The new text says "every occasion it covers from ξ to ξ'" (occasions in the interval). The narrowing from "any occasion" to "every occasion from ξ to ξ'" is not directly "accordingly" — it requires the additional assumption that only occasions in [ξ, ξ'] are relevant for (P).

This is a minor logical gap. The word "accordingly" suggests a direct consequence, but it requires an additional assumption (the interval restriction). Not a serious issue.

Also, the formula (P) uses r(ξ') whereξ' is a state, not an interval. But the definition says r(ξ') means "held on every occasion from ξ to ξ'." This is a non-standard use of function notation (r at a point means r over an interval). The text explains this ("not only at ξ'") but the notation is misleading.

Is this worth raising? It's a minor notational issue. The text explains it. I'll mention it briefly.

OK, let me finalize the report with:
1. R31 (detailed): ill-typed definition. FALLS.
2. R52 (brief): "realizable" dropped without explicit mention. STANDS.
3. R36 (very brief): non-standard notation. STANDS.

Actually, the task says "A change you tried to break and could not needs no point of its own." If R52 and R36 survive scrutiny, they don't NEED their own points. But I can include them as minor observations.

Hmm, but the task also says "Number your points in one list, most serious first." If I have three points, they're numbered 1, 2, 3. Point 1 is R31 (most serious). Points 2 and 3 are minor.

I think I should include R52 as a point (it's a genuine issue even if not fatal) and skip R36 (too minor).

Let me write the final report.

Oh wait, I want to also verify that my understanding of "c's contract" is correct. Let me check Part III.

Part III: "A question is p = (D, C, b_0, Q, O_p, ρ_p)." Here D is the target and C ⊆ A × B is the contract. A and B are D's edits and boundaries (from Part II: D = (V, (X_v), J, B, A, L)).

So "c's contract" for content c: if c is a question's target, c's contract is C_c ⊆ A_c × B_c (c's edits and boundaries). If c is just an organization (not a question's target), "c's contract" might refer to the contract of the question about c.

In (N): "d ≡_ℓ c" where c is a content. A content is "an organization together with its contract-relative commitments" (Part IV). So c has a contract. "c's contract" = the contract associated with c.

For a transport from d to c: the source is d and the target is c. The fidelity conditions require C ⊆ A_d × B_d (d's edits). "c's contract" = C_c ⊆ A_c × B_c (c's edits). Type mismatch confirmed.

OK, the argument is solid. Let me write the report.

Actually, one more thing. Let me look at the definition of "content" in Part IV: "A content is an organization together with its contract-relative commitments." So a content = (organization, contract-relative commitments). "c's contract" would be the contract that c's commitments are relative to.

Now, a transport from d to c maps d's structure to c's structure. For fidelity, we need a contract. If "faithful on c's contract" means "faithful with respect to c's contract-relative commitments," this might mean: the transport preserves the commitments that c's contract specifies. This is about c's requirements, not about the standard fidelity conditions.

In this reading, "faithful on c's contract" is not the standard "faithful on C" (Part IV/Part V). It's a new notion: "faithful with respect to c's contract-relative commitments."

But the text uses "faithful on" which is defined in Part IV. So it should be the standard meaning. Unless "faithful on c's contract" is a shorthand for "faithful with respect to c's contract-relative commitments" which is a different notion.

This is the "defined in two different ways" issue. "Faithful on C" has one meaning in Part V (standard fidelity conditions) and potentially another in (R) and R31 (faithful with respect to C's commitments).

OK, this is the coherence issue I identified. Let me present it in the report.

Final report:

**1. R31, tasks (a) and (c) — the definition of ≡_ℓ misuses "faithful on" and produces a non-symmetric relation.**

[detailed argument]

**2. R52, task (a) — the declaration does not explicitly account for the dropped condition "realizable."**

[brief argument]

R31: FALLS
R52: STANDS
OVERALL: NEEDS REPAIR
END OF REPORT.

Let me write this up properly now.

Hmm, but I realize I should also double check: the line number cited for R31 is 407. Let me verify this corresponds to the right location in the revised text. The revised text has the Newness section in Part X. Let me count... actually, I don't need to verify exact line numbers. The change description says "Part X, Newness, line 407" and gives the exact old and new wordings. I can cite the new wording directly.

Also, I want to make sure my proposed repair is in "exact words" as the task requests. Let me formulate it precisely.

Proposed repair for R31: Replace "both faithful on c's contract at grain ℓ" with "the first faithful on d's contract and the second faithful on c's contract, at grain ℓ."

Wait, "the first" = transport from d to c, "the second" = transport from c to d. For the transport from d to c (source = d), the contract should be d's. For the transport from c to d (source = c), the contract should be c's. So "the first faithful on d's contract and the second faithful on c's contract" is correct.

But this changes the meaning. The original says both are checked against c's contract. The repair says each is checked against its source's contract. These are different.

Alternatively: "each faithful on the contract of its source at grain ℓ." This is more concise.

Or if the intent was to use c's contract as the reference: "the transport from d to c faithful when restricted to c's contract through τ, and the transport from c to d faithful on c's contract, at grain ℓ." But this is complex and non-standard.

I think the simplest repair is "each faithful on the contract of its source at grain ℓ." This makes the definition type-correct and symmetric.

But if the intent was asymmetry (using c's contract as the reference), the repair would be different: "the transport from d to c faithful on τ^{-1}[c's contract] and the transport from c to d faithful on c's contract, at grain ℓ." But this requires τ to be invertible and uses non-standard notation.

I'll propose the simple repair: "each faithful on the contract of its source at grain ℓ."

OK, writing the report now.