# Workflow - project story

*The Workflow project's own record, kept here from 22 September 2026. Numbering is this project's own, W1 first. See READ ME FIRST in this folder.*

Authority: W8, the model of an LLM agent in the language of file 11, frozen 22 September 2026 (log W6). The plan is W3 read with W5 (tests/), frozen.

## The goal
A workflow that embodies Semantics file 10 (HV decision H6). The research toward it: a model of large-language-model agents in the theory's terms; a reading of the evidence in hand under it; a first run that separates stateful from stateless agents and tests the partition harness; the map of skills and boundaries; the workflow and its first run on the reserve.

## How the pieces fit
The plan (tests/) fixes the order, what each phase produces, and the predictions the programme can fail on. Each phase gets its own frozen plan when its turn comes, on the owner's word. The HV Skill (file 33) is the judge every phase calls; the Planning skill (file 2) is the loop every phase runs; both are frozen and placed by this work, not changed by it. The other projects are read only.

## Log
W1. The project made, on the owner's word (decision W1), after HV log H69 and Planning log P3 froze the two skills and named the research plan as next and neither project's. A fifth folder, letter W; the root README's navigation table and the GLOSSARY's count of projects brought up to five (navigation only). Drafted: "W1 Research plan - a model of LLM agents in the semantics' terms, then the skills and their boundaries, then the workflow (draft)": the question frozen with the owner's six unknowns quoted and numbered Q1 to Q6; what is fixed; five phases with what each produces and waits on, and why the model comes before the skill map though the owner called the map the first step; a first sketch of the model in ten parts M1 to M10, each on a quoted sentence of file 10 with what would count against it (a call as an execution; the weights as a selected transport; nothing read off an emission; a skill file as a carrier and representation as a fidelity fact; stateful and stateless as retention, tracking what is represented at each call; the context as where the represented targets and the change list live; external memory as occurrences brought in by a transport with a provenance; a harness that sets what the model's components assign deletes them, one that sets what they read does not; an attention policy as a scrutinizable aspect; the repository's conventions as Parts III, VIII, IX and XI by hand); predictions P1.1 to P4.2 as counts that could fail; a first sketch of the skill map from Part XIV's dependence order with the operations no skill holds (finding a question, building, bearing, receipts, retention, scrutiny of practice); what counts as each outcome; what follows either way; failure of the programme itself; known weaknesses; not tested; the asks of the other projects; traps. Every quotation checked against file 10 by program before the draft was committed (27 phrases, all found once). Output: the draft (tests/), this record, the front-door files. Not tested: nothing run; the draft is one writer's, and the judges are next.

W2. Five Opus 5 judges at effort xhigh, armed with the hard-to-vary skill and made to falsify their own findings, read draft W1: against file 10 sentence by sentence; as an explanation under the skill's full procedure; against the record and every rule; against the best rival programme; against the owner's note H6 sentence by sentence and the rig that would run the first test. Returns kept as they came in `rigs/W2 judges/` with the tool audit; 1.29 million subagent tokens, 66 minutes. What they forced is read in "W2 Results - five Opus 5 judges on the draft research plan; what they forced into W3": M8 fused two losses and its own falsifier fired on its own arm; M3 read Derivation 9 as a licence for the inference it forbids; the two load-bearing predictions counted a field H64 struck; nine passages of file 10 read wider than written or cited to the wrong Part; four misreadings of the record; three constructions unmarked; nine operations of Part XIV missing from the skill map; two attack points rested on as settled. The rival judge found the two programmes the same at the level of the end goal, separated by one count on the skill map. Three of five judges ran one shell command each, a listing or an echo, against the rule, and said so; none wrote; git shows nothing changed. No prediction file was frozen before the round (Lesson W2). Output: the results file, the five returns, the audit. Not tested: nothing run.

W3. The frozen research plan, "W3 Research plan - a model of LLM agents in the semantics' terms, the instrument, the arrangements, the skill map, the workflow (frozen)": draft W1 with every edit W2's evidence forced, and four arms (an agent-written carry-over note; a port-setting harness; the context with the change list withheld; the router criticised) applied on the plan's own added job that Q1 to Q6 each get a runnable falsifier. Six phases now: the model; the reading; the instrument (the marking certified on the 96 reports in hand before any arm is measured with it); the arrangements; the skill map; the workflow. "Embodies" given a term from Part XIV's classes; the two indices declared; M8 split; M1 marked loose with its neighbours; the skill map with a row for every operation of the dependence order; predictions as counts that are falsifiers and never verdicts; the asks of the other projects and of the owner in section 12. Every quotation of file 10 checked by program for presence and for the Part it sits under, the program and output kept in `rigs/W3 quote check/` (45 quotations found, 31 others are the record's or the plan's own). Output: the plan, this record, the front-door files brought up to date. Not tested: nothing run; the plan waits on the owner's word for phase 1.

W4. The owner uploaded Semantics file 11, revision 1 of the theory, with the words "Here's the updated first." (decision W2). The file is byte-identical to the one on `main` at `Semantics/authority/11 ... revision 1.md`, made under Semantics decision S8 and log S76 and under test there in round S76 (not yet confirmed as that project's authority). Done: `main` merged into this branch (twenty-three commits, Semantics S65 to S76 and Language L66 to L74; nothing of this project or the root touched by the merge; nothing in Semantics edited from here), so file 11 sits where the Semantics record placed it. W3's forty-five quotations of file 10 checked against file 11 by the same program: nine are no longer verbatim, four of them the Derivation 3 change (the claim now qualified by the population: a differing survivor at an unseen pair only where the population admits one) and five rewordings. Drafted: "W4 Addendum to W3 - the theory is now file 11; what changes in the plan, and what it gives up (draft)": the authority sentence replaced (file 11 followed beside file 10, every row of phase 1's word list marked "10 and 11" or "11 only", the Semantics project's own S64 device); M2 restated under the qualified Derivation 3 with the population named (the parameter family the architecture admits) and the blindness paragraph cut; nine sentences of file 11 that hold parts W3 had marked constructions or held if (the boundary and continuity declared before attribution; ownership grounded in the boundary; an instruction about what to read as an outside contribution; a record derived from a carrier not an independent witness; a record reconstructed from its claim not a receipt; the obligations as declared inputs; a mentioned dimension not a port; the finite monotone theorem applying only where its assumptions hold; the well-founded order); five rewordings; what is unchanged; what is given up. Quotations checked by program against file 11 with Part (`rigs/W4 quote check/`, 29 found). Also seen in the merge: the Semantics record's S73 says the owner passed this project's W2 reports to that session, which read the file-10 judge's Derivation 3 finding as converging with its own O48 finding; and its decision S6, said to that session: "always push to main and never edit anything outside your own folder". Output: the addendum (draft), the quote check, this record. Not tested: nothing run; the addendum waits on two Opus 5 judges (log W5).

W5. Two Opus 5 judges at effort xhigh, armed with the hard-to-vary skill and self-falsifying, read draft addendum W4 with W3, file 11 and file 10: one against file 11 sentence by sentence, one Part by Part through the differences between the two files that bear on the plan, with the rival device built. Returns kept in `rigs/W5 judges/` with the tool audit (no shell command, no write); 0.49 million subagent tokens, 18 minutes. What they forced: the quote checker rewritten (Lesson W3) and W3 re-checked against both files (49 found in 10, 40 in 11, the ten that differ named); W3's sections 7, 9, 11, 13 and P1.1 brought to the two-file rule; two of W4's nine holders struck as read wider than written (Part X's ownership passage holds M9's boundary claim, not the skill-map premise; Part IV's second-witness sentence holds the receipts, not M5 ii); M9's "outside the declared boundary" struck against W3's own declaration, and the skill file's side of the boundary declared; M2's population as the set of transports the physics and the architecture admit, its independence limb restored, M6's clause restated; five more holders in file 11 named (scope as a criticizable part of the claim; the coarse account; Build's first representation; the carrier section; criticality and redundant routes; the measurement sentences; the general normative paragraph); M10 given a mark; the HV ask extended by a third item (the skill's word list states the unqualified Derivation 3). Output: "W5 Addendum to W3 - the theory is now file 11; what changes in the plan, and what it gives up (frozen)", "W5 Results - two Opus 5 judges on the addendum W4; what they forced into W5", the returns, the program and outputs. Not tested: nothing run; the addendum's corrections were checked by program for presence and Part and by no judge for width.

W6. Phase 1 of W3, on the owner's word (decisions W3: "11 is the new authority. Also, if the research is done and corpus compiled, time to describe LLMs within the language of 11. Use 5 Opus 5 agents with extra effort armed with current hard to vary to help."; W4: "Also give them access to internet for research if it's possible"). Plan W6 frozen first with nine predictions as counts that could fail, and an addendum for the internet access with the rule that anything read outside the repository is claimed. Then five Opus 5 agents at xhigh armed with file 33: three builders, one slice each (the call; the arrangement; the practice and the map), building under `building.md` and self-falsifying, with web research (11 searches, 16 fetched addresses, every one listed in the audit; zero shell commands, zero writes); the draft model W7 assembled by Claude from their returns with the seams and disagreements recorded; two judges on W7 (the file-11 judge, the procedure judge; zero shell commands, zero writes, no web). What the judges forced: twenty-nine edits, written into the assembly program that regenerates the model; one contradicting row, two wrong file marks (every mark now set by program from files 10 and 11), the word list merged by the judge's rule, the table's second column relabelled and one copied cell removed, C11's first rider held if with the eight rows it carries marked so, three holders of report counts moved to the notes, two constructions marked, the record column's gauge restored, two predictions added to the frozen list, and six parts narrowed to what the sentences carry. Predictions: W6.1 fails (one contradiction, reworded; two marks, corrected) and W6.9 fails (four parts marked differently by the two judges: B1, B3, C9, C11, each marked unknown in W8 with both judges' words until a third reading); W6.2 to W6.8 hold, W6.7 on the placement column with both counts recorded. Output: "W6 Plan ...", "W6 Addendum ...", `rigs/W6 builders/` (three returns, tool audit, `assemble_w7.py`, `assemble_w8.py`), `authority/W7 Model - ... (draft).md`, `rigs/W6 judges/` (two returns, tool audit, W8's checks against both files), "W6 Results - phase 1, the model built by three Opus 5 builders and judged by two; W8 frozen", and **`authority/W8 Model - an LLM agent in the language of the semantics, file 11 (frozen).md`, this project's first authority**. Lesson W4 (the assembly program's faults). Not tested: nothing run against a reader; the model is a reading of the theory and the record, and phase 4 is its first test.

W7. The owner asked for the model as its own theory, to download ("Can you turn that into its own theory and give it to me to download inline."). Written: "W9 Theory - a language-model agent in the semantics of file 11, standalone": model W8 restated in file 11's shape (what it claims and does not claim; primitives, indices, the adopted physics; Parts II to XII from the call to the placement of the operations; Part XIII what defeats it; Part XIV the standing of every claim as W8 marks it, the four W6.9 unknowns included). It adds no claim W8 does not carry and has not been judged as a theory; W8 stays the authority under test, and W9 is a reading of it for the owner. Quotations of file 11 checked by program (18 found; the rest are the record's or the theory's own phrases). Output: the file in `authority/`, sent to the owner. Not tested: nothing run.

W8. The go for getting the LLM theory right (decision W6), with the round's rules: Opus 5 at extra effort for data collection, review and coding; Sonnet 5 at high for running the harness and as a reader; DeepSeek V4.1 Flash as a second reader and as the independent cross-examiner, five at a time, token spend not limited; every agent armed with the hard-to-vary skill; at most five Opus and Sonnet subagents at a time; the workflow saved. Then two keys (decision W7; not in any file): DeepSeek, and Atria Asi with the rule that it is used for complex analysis, one request at a time, with token ceilings set significantly larger. Done: plan W10 frozen before any agent started (the stages A to F; the arms as W8 amends them plus the re-identification arm (x); predictions as counts with n = 8 documents, three repeats, k = baseline plus three; what the plan gives up against W3's order); the addendum for the keys and the Atria rule; the stage A workflow saved as `.claude/workflows/llm-theory-stage-a.js` with a copy in `rigs/W10 harness/workflows/` (the registry did not list it by name; it was run by path, which is the same file); stage A launched: five Opus 5 builders at xhigh (the harness coder; the corpus collector; the fourth-clause collector over open trainers' records; the instrument; the API clients for DeepSeek and Atria) then one Opus 5 reviewer with a dry run. The DeepSeek key checked with one twenty-token request before the run. Output: plan W10 and its addendum, the saved workflow, this entry; stage A's returns when they come. Not tested: nothing run against a reader yet.

W9. Stage A of plan W10 returned (five Opus 5 builders at xhigh in parallel, then one Opus 5 reviewer at xhigh with a dry run; the saved workflow, run by path). Returns kept unchanged in `rigs/W10 harness/stage-A returns/` with the tool audit (no write outside the harness folder; A3 fetched 49 addresses, A6 13, A2 2, A1 2; nothing sent to any API; no key in the environment during the run), with one exception recorded in the audit: the reviewer quoted both key values verbatim as the pattern of its key-safety sweep, and each was replaced by program with a redaction mark before the file was committed (Lesson W5). Built: the harness (`code/`, twenty modules: the DeepSeek driver with five threads and prefix completion, the Sonnet prompt driver and wrapper, the summariser, carry-over, partition, name-exchange, first and blind second marker, agreement by field; `code/clients/` for DeepSeek and Atria Asi); the corpus (`corpus/`: 17 documents with fetcher entries, 9 exchangeable, split frozen at eight arms, four reserve, five held out; texts not kept); the fourth clause against open trainers' records (`fourth-clause/`); the instrument (`instrument/criteria.json`, 32 fields, 18 within-step and 14 cross-step, with the marking plan). The reviewer returned 24 faults to the makers: 6 that block stage B (no stage-B adapter for the 96 record files; agreement read over uncertified fields; the first marker asking for record-sourced fields; a hole in the test_pull criterion; part-label keying; the rivals naming rule), 14 that block stage C (the harness refusing every real corpus document over the digest's shape and crashing on the split; the re-identification arm's question quoting the document unexchanged; the partition arm's assembler handed the whole document; the leak gauge too narrow; a substring grep standing in for a marked field; four predictions computed by no program; the Sonnet run record missing five fields; the token ceiling below the service's maximum; PA.1's arm (a) clause unreachable on either transport; the edited-skill arm leaving the graph that routes; the qualification of Derivation 3 in no reader's context and the choice unrecorded; the cross-step fields not intersected with what the arms documents afford; the fallback split folding the held-out documents into reserve), 4 of wording (one fixed by the reviewer). Predictions: W10.1 holds on the rig's own fixture only, not yet on a corpus document; W10.2 holds (17 against 12; 9 against 6); W10.3 holds at the reviewer's levels A and B (a sentence quantifying over a named source's contents; a named subset) and fails at level C (a named document in a published corpus stating the loss of the model trained on it), so the clause stays unknown from here; W10.4 holds. The reviewer also dropped twelve of its own findings after checking (listed in its return). The regenerable `dry/` folder is ignored by git (`rigs/W10 harness/.gitignore`). Output: the returns and audit, this entry. Not tested: nothing run against a reader; every fault is returned to its maker before stage B, as stage A5 says (log W10).

W10. The fix round under addendum W11, and the MiMo client under addendum W12. Fix round: three rounds of the saved workflow (ten Opus 5 agents at xhigh; 2.65 million subagent tokens; 134 minutes; returns unchanged in `rigs/W10 harness/fix-round returns/round-1..3/` with the tool audit: 835 shell commands, no web tool, the only network use A2's fetcher writing three corpus texts to the scratch folder outside the repository; nothing sent; no key in the environment; no write outside the harness folder). Round 1: the three fixers fixed all 24 faults and declined none; the reviewer verified 23 by running and returned four new or reopened (the part-label key still not unique, fault 4; the widened leak gate stopping an arm (d) run on ordinary English, 24; the rivals naming rule undecidable, 25; a wrong fault number in a comment, 26, fixed by the reviewer). Round 2: 4 and 25 fixed and verified; 24 fixed at the site named and reopened at three more (27); the affordance table excluding a field the corpus does not record either way (28). Round 3: 27 and 28 fixed and verified; the narrowing of the pull rules left them inert on the label shapes readers actually write ("P2", a heading), which is fault 29, one clause of one function in `partition.py` and the reviewer's own wording in round 2; two wording faults (30 fixed by the reviewer; 31, a stand-in table in a fixture, left for A1). New in the harness: `record_adapter.py` (the 96 reports of the record in the rig's shape, a count gate at 96), the stage-B path run end to end on stand-in marks and then deleted, the certified-only reading of the predictions with the read-outs printed beside and never inside a count, the four missing prediction counts, the Sonnet run record's five fields, MAX_TOKENS at 393,216, D1 to D4 of W11 applied (`derivation3_qualification_sent: false` on every run record; the assembler without the document; PA.1's arm (a) half reported not reached; `instrument/affordance.json` with the "never varies" flag). Predictions: W10.9 holds (eleven arms on both transports on corpus document W1, hash-checked, 168 request bodies saved, nothing sent) and W10.1 is re-ticked on a corpus document; W10.10 holds (0 of 24 declined); **W10.8 falsified, narrowly**: after round three one fault blocking stage C remains (29), zero blocking stage B, so on W11's own terms stage B does not start until the owner says. Also read from the round: **W3 P3.4's count has gone past its allowance before any report was marked** (A4's marking plan section 14.4: `marks_per_part`, `pairs_that_pull` and `rivals_built` each reworded a second time under the reviewer's faults, on reports that are among the 96; A4 wrote both readings and left which governs to the plan owner, and added the stricter rule that any change to the instrument after stage B begins stops the phase). MiMo client: one Opus 5 agent (A7; return and audit with the stage-A returns) researched the Xiaomi MiMo platform (everything claimed with address and date) and wrote `code/clients/mimo_client.py` with the 30-a-minute pacer (decision W9; the Atria client got the same pacer, its dry run 35 checks); live check by Claude with the key in the command environment only: the owner's Token Plan key is refused by the cn cluster and accepted by the sgp cluster, whose reply names `mimo-v2.6-pro`; the default region set to sgp with that evidence in the file; W10.11 holds; the two key-free records kept. Decisions W8 (MiMo as a stage E examiner family), W9 (Atria and MiMo at 30 requests a minute), W10 (the live handover, `records/HANDOVER.md`). Lesson W6. Output: the fix-round returns and audit, the harness as the round-3 reviewer checked it, addendum W12, the MiMo client and its live-check records, the handover, this entry. Not tested: nothing run against a reader; fault 29 is open; the two consequences above wait on the owner.

## Next step
Two consequences of frozen rules put to the owner: W10.8's (a fourth pass at fault 29, or stage B on the record that the arm (d) gate misses the commonest form of a pulling pair) and W3 P3.4's (which reading governs the pre-marking rewordings of three fields). Then stage B (the 96 reports, two blind Sonnet 5 markers), then C to F.

## Traps
- Letting this file drift from Status. The log is the record.
