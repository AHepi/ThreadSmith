# HANDOVER - the live state of the Workflow project's round W10

*Kept on the owner's word (decision W10) so that a session whose context was compacted, or a fresh session, can continue without the conversation. Updated at every step; the last update is dated at the foot. Read this first, then READ ME FIRST, then the project story's last entries. Nothing here replaces the record: the story, Decisions, Lessons, Status and INDEX are the record; this is the pointer into it.*

## 1. Where we are (one paragraph)
Plan W10 ("getting the LLM theory right") is running. Stage A (build and collect) returned and was reviewed: 24 faults (log W9). The fix round under addendum W11 is running now as a background workflow; when it returns, its returns are saved, logged (log W10), committed, and stage B starts. A MiMo 2.6 Pro client is being built in parallel under addendum W12 (decision W8). Nothing has been run against a reader yet. No results file for W10 exists yet; it comes at stage F.

## 2. Standing rules (owner's, in force)
- Branch: `claude/hv-skill-scope-kl0oyr`; push there only. Commit and push after every step (a stop hook checks for uncommitted changes; the fixers' in-progress edits are the one thing left uncommitted while a round runs).
- Never Fable 5.1 as a subagent or reader (HV decisions H4, H7). Opus 5 at xhigh for data collection, review, coding; Sonnet 5 at high for running the harness and as the reader under test; DeepSeek V4.1 Flash as second reader and cross-examiner, five at a time, token spend not limited (decision W6). Atria Asi for complex analysis, one request at a time, large ceilings (W7). MiMo 2.6 Pro as a stage E examiner family (W8). Atria and MiMo at most 30 requests a minute (W9).
- At most five Opus and Sonnet subagents at a time, counted together. DeepSeek's five and MiMo's are on top.
- Every agent armed with the hard-to-vary skill: `HV Skill/authority/33/hard-to-vary/` (SKILL.md and six references). Agents not under test are told the qualified Derivation 3 in file 11's words: "A correspondence produced by selection is faithful where it was tested and, wherever its population admits an alternative, unconstrained where it was not." Readers under test get file 33 byte-identical, no qualification (W11 decision D3).
- Keys: DeepSeek, Atria, MiMo. In no file, ever; used only in a command's environment (`DEEPSEEK_API_KEY=... python3 ...`). They live in the conversation only. **If the conversation is lost, the keys are lost: ask the owner to paste them again** (and to rotate or delete them at the providers when the round is done). Scan every agent return for key values before committing (Lesson W5). No key is in the environment during rounds that send nothing.
- Other projects (Semantics, Language, HV Skill, Planning) read only. Semantics file 11 is the authority; W8 is the model under test; W9 its restatement.
- Frozen plan with predictions before any run; raw returns kept unchanged; every result names "not tested"; numbering per project (files W1, W2, ... by order of writing; log entries W1, W2, ... separately; predictions W10.n belong to plan W10 and its addenda). Next file number: W13. Next log entry: W10.
- Every stage's workflow saved under `.claude/workflows/llm-theory-*.js` with a copy in `Workflow/rigs/W10 harness/workflows/`; run by `scriptPath` (the name registry did not list them).

## 3. Running now (background workflows)
- **Fix round** (`llm-theory-stage-a-fixes.js`, run id `wf_bcfabafb-195`, transcripts under `/root/.claude/projects/-home-user-ThreadSmith/00bda300-7321-5714-a655-4358c87aab9b/subagents/workflows/wf_bcfabafb-195/`, journal.jsonl there): three Opus 5 fixers in the makers' roles (A1 harness: faults 0, 1, 2, 6-17, 19, 20 and W11's D1-D4; A4 instrument: 3, 4, 5, 18; A2 corpus: 21, 22), then the A5 reviewer again with a dry run on a corpus document fetched to the scratch folder `/tmp/claude-0/-home-user-ThreadSmith/00bda300-7321-5714-a655-4358c87aab9b/scratchpad/w10texts` (outside the repository); loops on blocking faults, at most three rounds. Returns `{rounds: [{round, fixes: {role: FIX}, review}]}`. When it returns: save each fixer's and reviewer's JSON unchanged to `Workflow/rigs/W10 harness/fix-round returns/round-<n>/`, write a tool audit from the agent-*.jsonl transcripts (count shell commands, fetched addresses, any write outside the harness folder), scan for key values, tick W10.8 to W10.10 (addendum W11 section 4), log W10, commit everything under the harness folder (its `dry/` is gitignored), push.
- **MiMo client** (`llm-theory-mimo-client.js`, run id `wf_3387d700-782`): one Opus 5 agent writing `code/clients/mimo_client.py` and a README section. When it returns: save the return to `fix-round returns/` (or `stage-A returns/A7-mimo-client.json`), add the 30 rpm pacer as in `atria_client.py` (functions `_pace`, `_pace_check`, `_PACE_SLEEP`, `RPM_CAP = 30`), run its `--dry`, then one live check with `MIMO_API_KEY` in the command environment only (a few tokens; the key from the owner's message), tick W10.11's first half, commit.
- If a workflow is found dead or its result empty: read its journal.jsonl before assuming anything; resume with `Workflow({scriptPath, resumeFromRunId})`.

## 4. What comes next, in order (plan W10 section 3)
1. **Stage B**, the instrument certified: the 96 reports in hand re-marked by two blind Sonnet 5 markers (effort high, five at a time) under `instrument/criteria.json`; agreement by field; P3.1-P3.5 ticked (thresholds in `instrument/marking-plan.md` section 6). The 96 = the 48 hidden close-marking reports of HV plan 49 (`HV Skill/rigs/plan 49 rig - DeepSeek on outside papers/marking/to_mark.md`, from `runs/`) plus the 48 repeat reports (`runs_repeat`, `runs_repeat_31`, `runs_sonnet5`, `runs_sonnet5_31`, twelve each). The harness's stage-B adapter (fault 0) maps them; `first_marker.py prompts|collect|validate` and `second_marker.py prep|collect|compare` (shuffle, closed mapping) make the prompts; markers must be blind to reader, version, mode and repeat. Save the workflow as `llm-theory-stage-b.js`; keep five Sonnet agents at a time with a worker pool in the script; each marker agent writes its marks JSON under `rigs/W10 harness/stage-B/` and returns it; then `agreement.py` by program. Freeze nothing new: the predictions are W3's P3.1-P3.5 as W10 section 5 restates them.
2. **Stage C**, the arms on eight documents, three repeats: Sonnet 5 through subagents (workflow script carrying outputs between steps; `sonnet_prompts.py plan`, `wrap_sonnet.py`), DeepSeek through `run_deepseek.py` with the key in the environment (MAX_TOKENS at the documented maximum after fault 14's fix). Arms a, b, c, cctl, cprime, d, e, f, r, x; k only on the owner's word.
3. **Stage D**, marking by two Sonnet markers; agreement; the counts of section 5.
4. **Stage E**, five DeepSeek examiners plus Atria (one at a time) plus MiMo (W12), all at 30 rpm for Atria and MiMo; W10.5 read per family, never summed.
5. **Stage F**, up to five Opus 5 reviewers: the results file for W10; W8 re-marked; the theory reissued as a new numbered file.

## 5. Owner asks still open (from earlier; not blocking)
"5 tops" sentence into HV Decisions; root navigation edits; harness material for Q1; the 33 held-out sources of HV file 50 not to be spent; whether to push to main or open a PR (asked, unanswered; push to the branch until then); rotate or delete the keys when the round is done.

## 6. Files to know
- Plan: `tests/W10 Plan - ...md` and addenda W11 (fix round, four decisions D1-D4), W12 (MiMo). Model: `authority/W8 ...(frozen).md`. Theory for the owner: `authority/W9 ...`.
- Harness: `rigs/W10 harness/` (`code/` with `README.md` and `CRITERIA-SCHEMA.md`; `code/clients/` DeepSeek, Atria, MiMo; `corpus/` sources.json, fetch.py, split.json, coverage.md; `fourth-clause/`; `instrument/` criteria.json, marking-plan.md, affordance.json; `skill/` the byte-identical copy of file 33; `workflows/`; `stage-A returns/`; `.gitignore` for `dry/`).
- Old rig the harness builds on: `HV Skill/rigs/plan 49 rig - DeepSeek on outside papers/`.

*Last updated 22 September 2026, after decision W10, with the fix round and the MiMo client build running.*
