# HANDOVER - the live state of the Workflow project's round W10

*Kept on the owner's word (decision W10) so that a session whose context was compacted, or a fresh session, can continue without the conversation. Updated at every step; the last update is dated at the foot. Read this first, then READ ME FIRST, then the project story's last entries. Nothing here replaces the record: the story, Decisions, Lessons, Status and INDEX are the record; this is the pointer into it.*

## 1. Where we are (one paragraph)
Plan W10 ("getting the LLM theory right") is running. Stage A returned and was reviewed (log W9); the fix loop (addenda W11, W13) closed at round 4 with a zero-fault review (logs W10, W11; every round a pushed commit, hashes in `fix-round returns/VERSIONS.md`). Plan W14 (stage B) is frozen and **stage B's 192 marks are all in and saved**: 96 first-marker marks (M001-M096, `stage-B/w10/marking1/marks/`) and 96 hidden second-marker marks (R001-R096, `stage-B/w10/marking2/marks/`), M085 re-run alone after a timeout; returns, the M085 re-run's return and the tool audit are in `rigs/W10 harness/stage-B/`. The log runs through entry W11; the last commit on the old branch is f1bb829. The owner lifted W13's pause (decision W14), and **this session is running stage B's collection by program**: the harness's collect, validate, compare and `stage_b.py` steps, an independent recompute of the same counts from the raw marks, and a field-by-field comparison of the two (workflow `llm-theory-stage-b-collect`). After it come the results file for stage B (W15), a questioning of that file by Atria and MiMo, log W12, then stage C. No results file for W10 exists yet; it comes at stage F.

## 2. Standing rules (owner's, in force)
- **Branch: `claude/hv-skill-scope-kl0oyr-oz6b4c`** (decision W14), made from the head of `claude/hv-skill-scope-kl0oyr` at f1bb829 and pushed; push there only. The old branch `claude/hv-skill-scope-kl0oyr` is no longer pushed to (replaced by W14). Commit and push after every step (a stop hook checks for uncommitted changes; work in progress is the one thing left uncommitted while a step runs). The branch is shared with other folders: add only the files a step wrote, by name, never `git add -A`.
- **Folders (W14):** this session writes only in `Workflow/`, `HV Skill/` and `Planning/` (plus the virtual environment and scratch space outside the repository); never in `Semantics/`, `Language/`, `tutorials/`, `.claude/` or any file at the repository root. The Workflow project's own work still edits only `Workflow/` (READ ME FIRST); Semantics and Language are read only. Semantics file 11 is the authority; W8 is the model under test; W9 its restatement.
- **The orchestrator (W14):** Claude, the session's main loop, plans, interprets and orchestrates; every piece of work, running, writing, recording, committing, is done by subagents.
- **Python (W14):** every program runs with the virtual environment's interpreter, `/home/user/.venvs/threadsmith/bin/python`, never the system `python3`. The harness needs only `requests` beyond the standard library. Where older notes say `python3 ...`, read that interpreter.
- **Subagents (W15):** every subagent is Opus 5.5 (`claude-opus-5-5` in the Workflow tool's `model` option; this runtime, Claude Code 2.1.280, accepts it) at effort high or xhigh, never max, at most five at a time. This replaces W6's Opus 5 roles and settles W12's runtime condition (W12's fallback to Opus 5 no longer applies). Never Fable 5.1 as a subagent or reader (HV decisions H4, H7), standing. The frozen plan's Sonnet 5 roles (reader under test in stage C; the two markers in stage D) wait on the owner's answer (section 5); nothing in stage B's collection runs in those roles.
- **Atria Asi and MiMo 2.6 Pro (W16):** used only to question results, cross-examine and test this project's investigations, never to build or to run an arm; up to three requests of each family in flight at once (replaces W7's one Atria request at a time); at most thirty requests a minute each, retries included, held in the client (W9, standing); every request gets a large output ceiling, and an empty return is read as a ceiling fault first and re-sent with a larger ceiling, never counted as an answer. MiMo's place as a stage E examiner family (W8, addendum W12) stands within this.
- **DeepSeek V4.1 Flash** as second reader and cross-examiner, five at a time, token spend not limited (W6), standing; but **no DeepSeek request runs until the owner supplies the key again** (W17).
- **Every worker armed with the hard-to-vary skill (W18):** every subagent and every Atria and MiMo request gets HV file 33, all eight files: `HV Skill/authority/33/hard-to-vary/SKILL.md` and the seven files of its `references/` (building, by-domain, question-bank, reporting, testing-against-cases, the-idea-in-depth, word-list). The orchestrator is not armed. Workers not under test are told the qualified Derivation 3 in file 11's words: "A correspondence produced by selection is faithful where it was tested and, wherever its population admits an alternative, unconstrained where it was not." Readers under test get file 33 byte-identical, no qualification (W11 decision D3).
- **Keys (W7, W8, W17):** Atria Asi and MiMo 2.6 Pro were supplied again this session; DeepSeek was not. Keys live in the conversation only, in no file, ever; used only in a command's environment (`ATRIA_API_KEY=... /home/user/.venvs/threadsmith/bin/python ...`). **If the conversation is lost, the keys are lost: ask the owner to paste them again** (and to rotate or delete them at the providers when the round is done). No key is in the environment during steps that send nothing.
- **Key scan before every commit**, and of every agent return (Lesson W5), by pattern and never by writing a key value: `atr_[A-Za-z0-9_-]{20,}`, `tp-[a-z0-9]{30,}`, `sk-[A-Za-z0-9]{20,}` over every file to be added. A hit stops the commit.
- Frozen plan with predictions before any run; raw returns kept unchanged; every result names "not tested"; numbering per project (files W1, W2, ... by order of writing; log entries W1, W2, ... separately; decisions W1, W2, ...; predictions W10.n belong to plan W10 and its addenda). **Next file number: W15. Next log entry: W12. Next decision: W19.**
- **Workflows (W14):** every stage's workflow is saved in `Workflow/rigs/W10 harness/workflows/llm-theory-*.js` and run by path (`scriptPath`). `.claude/workflows/` is left as it stands and is not written (replaces the old rule of saving there with a copy in the harness).

## 3. Running now
**Stage B's collection** (workflow `llm-theory-stage-b-collect`, saved in `rigs/W10 harness/workflows/`, run by path). Stage B's step list, where it stands:
1. **Done.** Markers' returns saved unchanged to `rigs/W10 harness/stage-B/returns.json` (run id `wf_540f616b-2fb`) and `returns_M085_rerun.json`; every marker's transcript audited (`stage-B/tool_audit.json`: Read paths other than the skill files and its own prompt strike that mark, W14 section 1 and W10.18; any Bash or Edit named); key scan; all 192 marks in; commit f1bb829 (old branch).
2. **Running.** From `rigs/W10 harness/code/`, with `/home/user/.venvs/threadsmith/bin/python`: `first_marker.py collect --scratch "../stage-B" --reader record96 --tag first`; `second_marker.py collect --scratch "../stage-B" --reader record96`; `first_marker.py validate --marks ../marking/marks_first_record96.json` and the same for `marks_second_record96.json`; `second_marker.py compare --scratch "../stage-B" --reader record96`; `stage_b.py --reader record96` (thresholds per set of 48, pooled, the split, the certified list -> `marking/stage_b_record96.json`). Beside them, an independent recompute of the same counts from the raw marks, and a field-by-field comparison of the two; a disagreement is reported and explained before any tick.
3. **Next.** Tick P3.1, P3.2, P3.3, P3.5 (per set), P3.4 (failed on the count, recorded), W10.16, W10.17, W10.18 (plan W14 section 4). Write the results file **"W15 Results - stage B ..."** in `results/`; have it questioned by Atria and MiMo (up to three of each, armed with the skill, large ceilings, W16); log W12; commit; push.
4. **Missing or unreadable marks.** None missing: all 192 are in. If the collection finds a mark it cannot read, re-run only that prompt with `llm-theory-stage-b.js` (`args.prompts` = the list of prompt paths) before ticking; a marker re-run is a Sonnet 5 role, so it waits on the owner's answer in section 5.
Then stage C (see section 4), with `agreement.py --certified ../marking/stage_b_record96.json`'s certified list (the file's `certified` array; agreement.py's --certified expects a file: check its reader and adapt by program if the shape differs).

## 4. What comes next, in order (plan W10 section 3)
1. **Stage B**, the instrument certified: the 96 reports in hand re-marked by two blind Sonnet 5 markers (effort high, five at a time) under `instrument/criteria.json`; agreement by field; P3.1-P3.5 ticked (thresholds in `instrument/marking-plan.md` section 6). The 96 = the 48 hidden close-marking reports of HV plan 49 (`HV Skill/rigs/plan 49 rig - DeepSeek on outside papers/marking/to_mark.md`, from `runs/`) plus the 48 repeat reports (`runs_repeat`, `runs_repeat_31`, `runs_sonnet5`, `runs_sonnet5_31`, twelve each). The harness's stage-B adapter (fault 0) maps them; `first_marker.py prompts|collect|validate` and `second_marker.py prep|collect|compare` (shuffle, closed mapping) make the prompts; markers must be blind to reader, version, mode and repeat. Save the workflow as `llm-theory-stage-b.js`; keep five Sonnet agents at a time with a worker pool in the script; each marker agent writes its marks JSON under `rigs/W10 harness/stage-B/` and returns it; then `agreement.py` by program. Freeze nothing new: the predictions are W3's P3.1-P3.5 as W10 section 5 restates them. (The markers are done; section 3.)
2. **Stage C**, the arms on eight documents, three repeats: Sonnet 5 through subagents (workflow script carrying outputs between steps; `sonnet_prompts.py plan`, `wrap_sonnet.py`), DeepSeek through `run_deepseek.py` with the key in the environment (MAX_TOKENS at the documented maximum after fault 14's fix). Arms a, b, c, cctl, cprime, d, e, f, r, x; k only on the owner's word. **W15:** whether the reader under test stays Sonnet 5 or becomes Opus 5.5 is put to the owner before stage C starts.
3. **Stage D**, marking by two Sonnet markers; agreement; the counts of section 5. **W15:** whether the two markers stay Sonnet 5 (the instrument was certified in stage B on Sonnet 5 markers) is put to the owner with the stage C question.
4. **Stage E**, five DeepSeek examiners plus Atria plus MiMo (W12), all at 30 rpm for Atria and MiMo; W10.5 read per family, never summed. (Atria and MiMo now three at a time each, W16; DeepSeek waits on its key, W17; section 2.)
5. **Stage F**, up to five reviewers (Opus 5.5 at high or xhigh, W15): the results file for W10; W8 re-marked; the theory reissued as a new numbered file.

## 5. Owner asks still open (from earlier; not blocking unless marked)
- **The Sonnet 5 roles in stages C and D (W15):** do the reader under test (stage C) and the two markers (stage D) stay Sonnet 5, as the frozen plan says and as the instrument was certified, or become Opus 5.5? Blocks stage C.
- **The DeepSeek key (W17):** not supplied this session; nothing that sends to DeepSeek runs until it is. Blocks stage C's DeepSeek arms and stage E's DeepSeek examiners.
- From earlier: "5 tops" sentence into HV Decisions; root navigation edits (not possible in this session: W14 keeps the root closed); harness material for Q1; the 33 held-out sources of HV file 50 not to be spent; whether to push to main or open a PR (asked, unanswered; push to the branch until then); rotate or delete the keys when the round is done.

## 6. Files to know
- Plan: `tests/W10 Plan - ...md` and addenda W11 (fix round, four decisions D1-D4), W12 (MiMo), W13 (fix loop); plan W14 (stage B). Model: `authority/W8 ...(frozen).md`. Theory for the owner: `authority/W9 ...`.
- Harness: `rigs/W10 harness/` (`code/` with `README.md` and `CRITERIA-SCHEMA.md`; `code/clients/` DeepSeek, Atria, MiMo; `corpus/` sources.json, fetch.py, split.json, coverage.md; `fourth-clause/`; `instrument/` criteria.json, marking-plan.md, affordance.json; `skill/` the byte-identical copy of file 33; `workflows/`, the saved workflows' only place from W14, run by path (the older copies in `.claude/workflows/` are left as they stand); `stage-A returns/`; `stage-B/`; `marking/`; `.gitignore` for `dry/`).
- Old rig the harness builds on: `HV Skill/rigs/plan 49 rig - DeepSeek on outside papers/`.

*Last updated 23 September 2026, after decisions W14 to W18; stage B's collection running on branch `claude/hv-skill-scope-kl0oyr-oz6b4c`.*
