# W16-atria-L1-s1 - the content of the reply, as extracted by program

Extracted by send_w16.py from `W16-atria-L1-s1.record.json` (the record's `message.content`), unchanged below the rule. Family atria, lens L1, send 1. Verdict under plan W16 section 4.3: answer (HTTP 200, content not empty, finish reason 'stop'). The reply's `reasoning_content`, if any, is kept in the record and is not reproduced here.

---

1.
**Target:** W15, "The short answer", section 4's table row for `test_remove` ("same. Certifiable only under the independent's reading A3 of "the other eight tests"; the texts forbid the other reading (comparison section 4)") and section 5's row "The other fields at 39" ("test_remove is included only under the reading that gives it 39. The texts forbid the other reading, which would leave test_remove unable to be certified (comparison section 4).")
**Claim:** The instrument's own words do not forbid the other reading — they state it. The section 6 table puts "the other eight tests" at 39, and nine of the eleven tests remain once swap and poke have their own row, so "eight" can only be read as excluding one test; the exclusion that makes the count come out at eight is test_remove, the third within-step test, leaving the eight cross-step tests. The reading that gives test_remove 39 comes from W14 section 4's paraphrase ("The other fields at 39"), not from the instrument's table, and under the table's literal reading test_remove has no threshold and cannot be certified — which would move the headline from sixteen certified fields to fifteen.
**Evidence:** (seen) Marking plan section 6's table, document 5: "the other eight tests, remove_in_groups, shape_elements, modules_self_reported, attributions | 39 of 48 | **added here**". (seen) criteria.json's `threshold_added_here.which`, document 7: "the eight other tests, remove_in_groups, shape_elements, modules_self_reported, attributions". (seen) The eleven tests in criteria.json are remove, swap, poke, flip, reverse, hunt, addjob, rival, pull, patches, inside; removing swap and poke leaves nine, and test_remove is within-step while the other eight of those nine are cross-step. (seen) W14 section 4, document 2: "The other fields at 39 (the instrument's own addition)." (seen) The independent's A3, document 10: "The eight could be the eight cross-step tests (section 3 says 'Eight of the eleven tests are cross-step'), which would leave test_remove with no threshold. Chosen: 39, following W14 section 4's 'The other fields at 39'." (seen) `stage_b_record96.json`, document 8: test_remove `certified: true`, close 46/2/0, repeat 48/0/0, threshold 39.
**What would settle it:** Read marking plan section 3 (not among the documents provided) to confirm it names the eight cross-step tests and places remove with swap and poke; then ask the plan owner which text fixes the threshold list — W14 section 4's "the other fields" or the instrument's "the other eight tests". If W14 governs, 39 stands and the sixteen hold. If the instrument's table governs literally, test_remove has no threshold, the certified list is fifteen, and P4.4's within-step set is left with no certified test at all.

2.
**Target:** W15, "The short answer": "The two differ only where the frozen texts can be read two ways, or where a program applies a rule no frozen text states. There are four such places (the comparison's D1 to D4). No certification and no prediction verdict moves under any of the readings."
**Claim:** The last sentence is too wide. The readings it covers are the four D-readings of section 3; the A3 reading of "the other eight tests" (point 1) is a reading the frozen texts admit, it is not among the four, and under it a certification does move. The sentence is also too wide internally: W15's own D1 row records that a per-set result moves, even though no certification does.
**Evidence:** (seen) W15 section 3, D1 row: "The repeat set's per-set result does move: it misses by document and reaches by report." (seen) W15 section 3's table is bounded by the comparison's D1 to D4 and by the ruling quoted above it. (seen) The A3 evidence of point 1, including the independent's statement that under the other reading "test_remove cannot be certified".
**What would settle it:** Restate the claim as "no certification or prediction verdict moves under any of the four D-readings", and say separately whether the A3 reading is counted as settled by W14 or left open. If it is left open, "sixteen of twenty-two" is a reading-dependent number rather than a reading-independent one.

3.
**Target:** W15 section 3: "This ruling is **the orchestrator's, made on 23 September 2026 after the counts had been seen**. It is not a frozen rule, and W14 does not contain it:" and section 2, item 5: "The choice of which numbers govern was made after seeing them (section 3)."
**Claim:** This lens's second question — was the reading chosen after the counts, and does W15 say so — passes here for both the four readings and the choice of the in-place numbers; W15 says so outright, and twice. The one count the ruling makes govern, `marks_per_part`'s gate, is computed by a rule no frozen text states, and W15 says that too, in the D1 row and in HF1. The check to record is what rests on it: the "no" for `marks_per_part`, which is also the outcome under the report reading, so no verdict turns on the unwritten rule.
**Evidence:** (seen) W15 section 3, D1 row: "The ceil scaling is stated in no frozen text. `stage_b.py`'s docstring lists it among 'Rules fixed in W14', and W14 contains no 'ceil', 'scaled' or 'proportion' (c3)." (seen) W14, document 2, read in full, contains none of those three words. (seen) comparison D1: "That part is a fault of the harness: it records as W14's a rule W14 does not contain." (seen) `stage_b_record96.json`, document 8: `marks_per_part` close documents 16, `agree_documents` 6, `threshold` 14; repeat documents 2, `agree_documents` 0, `threshold` 2; `agree_reports` 37 and 44; `certified` false. (worked out) ceil(40/48 × 16) = 14 and ceil(40/48 × 2) = 2, matching the file's thresholds; and 37 < 40 in the close set under the report reading, so the field is struck under both units.
**What would settle it:** Nothing to settle on disclosure. If the owner prefers "the texts' best reading governs" (the rival W15 names), the report reading gives the same certification and the same P3.2 verdict; only the repeat set's per-set result changes, from "misses" to "reaches".

4.
**Target:** W15, "The short answer": "The counts can be trusted as counts. The in-place run of the harness's programs equals the scratch-copy run apart from timestamps, paths and log layout. The independent recompute gives the same agree/disagree/unreadable counts on every field in each set, and names the same reports."
**Claim:** The third sentence is too wide. The independent recompute does not count `modules_served` at all, so on that field there is no second count to match; and `modules_served` is the one field whose harness count (0 / 0 / 96) holds by construction, because `second_marker.py collect` fills no run-record field. It is also the field P4.6 is read on, so its unreadable-by-construction count is a single-route count carrying a stage-D read-out.
**Evidence:** (seen) comparison's table, `modules_served` row: "p52 / repeat / pooled | 0/0/48, 0/0/48, 0/0/96 | not counted | 39 printed / none | | not compared". (seen) comparison D4: "After collection, the first marks carry all four on 96 reports, and the second marks carry none: the second validate log names each one missing 96 times." (seen) comparison section 6: "**modules_served** ... which only the harness counts." (seen) `stage_b_record96.json`, document 8: `modules_served` close 0/0/48, repeat 0/0/48, pooled 0/0/96, `certified_candidate` false. (seen) W15 HF8 names the same fault.
**What would settle it:** Restate the sentence as "on every field both programs count". A third route that fills the four run-record fields on both sides from the run records and compares them is what would give `modules_served` a check; until then its 0/0/96 is one program's construction, not an agreed count.

5.
**Target:** W15, "The short answer": "**question_identity was not tested; it did not fail between markers.**"
**Claim:** The second clause is too wide as written. The two markers did disagree on one report: the repeat set reads 3 agree, 1 disagree, 44 unreadable, and the pooled disagree count is 1. W15's own section 6.4 states the finding correctly ("The failure is 91 unreadable reports and 1 disagreement"), so the short answer reads more strongly than the finding it summarises.
**Evidence:** (seen) `stage_b_record96.json`, document 8, `question_identity`: repeat `agree` 3, `disagree` 1, `unreadable` 44; pooled 4 / 1 / 91. (seen) W15 section 6.4: "The failure is 91 unreadable reports and 1 disagreement, so the rival is ruled out as the cause of this failure." (seen) W15 section 5, P3.3 row: "1 / 0 / 47 and 3 / 1 / 44."
**What would settle it:** Restate as "the failure is not a failure of agreement between the markers" or "the cause is the missing input, not the markers". The finding itself — that the cause sits in the stage-B inputs — rests on the prompt check (all 192 prompts carry no question; `prompt_for` takes no question argument, comparison section 4 and c5) and is unaffected.

6.
**Target:** W15 section 6.5: "**Governing reading (read set A, over readable reports): the field is marks_per_part.** On the unread reports only: … By document: 6 of 16 (needs 14) and 0 of 2 (needs 2)."
**Claim:** The "By document" numbers are not counts of the unread reports; they are the full-set gate from P3.2 over all 48 reports of each set. Setting them inside the "On the unread reports only" list folds the P3.2 gate into the W10.16 consequence. The conclusion is the same either way — struck — so no verdict moves.
**Evidence:** (seen) `stage_b_record96.json`, document 8, `marks_per_part` per_set: close `documents` 16, `agree_documents` 6, `threshold` 14; repeat `documents` 2, `agree_documents` 0, `threshold` 2; `certified` false — the same numbers W15 quotes, and they are computed over each set's 48 reports, not over its unread subset. (worked out) The unread-only counts W15 does give are report counts and check out: close 33 + 10 + 1 = 44, and 44 = 48 − 4 read in the close set; repeat 28 of 32, and 32 = 48 − 16 read.
**What would settle it:** Either compute the unread-only document count (documents all of whose *unread* reports agree) if the consequence is meant to apply by document, or move the "By document" line out of the unread-only list and label it as the P3.2 gate. Neither changes the outcome.

7.
**Target:** W15 section 1, "What the in-place equality shows, and what it does not.": "- **What it forbids:** any output difference other than a timestamp or a path. The canaries show the check could have caught one."
**Claim:** The check's own four classes are timestamps (100), a path computed from the rig's location (96), absolute paths in log lines (4) and log format (11); "other than a timestamp or a path" omits the log-format class, which is neither.
**Evidence:** (seen) W15 section 1's table row "In place against the scratch copy": "Every difference falls in one of four classes: timestamps 100, a path computed from the rig's location 96 (`from_the_record` in the ignored run records), absolute paths in log lines 4, log format 11. FINDINGS 0." (worked out) 100 + 96 + 4 + 11 = 211 differences, all inside those four classes.
**What would settle it:** Restate as "any output difference other than a timestamp, a path or a log-format line". Nothing turns on it; the sentence's work is to say the check could have caught a changed mark or count, and the canaries are what show that.

8.
**Target:** W15 section 5, W10.16 row, "Other readings beside": "C: both over all (marks_per_part +10.5, rivals_built +12.4); rivals_built alone over readable (+11.3)."
**Claim:** The +11.3 figure for `rivals_built` under reading C over readable reports cannot be checked from the documents provided, and it duplicates reading B's gap over all reports, so it may be taken from the wrong row. The independent's own table gives reading C as `rivals_built` +12.4 over all with no readable-base figure, and says only "rivals_built alone over readable".
**Evidence:** (seen) Document 10, section 5, reading C row: "C | +10.5 (+9.2 of readable) | −4.3 | +12.4 | both over n; rivals_built alone over readable". (seen) Document 10, reading B row: "+11.3 (+12.8 of readable)". (claimed) The sources W15 names for these figures — `stage-B/results-w15/output.txt` and the independent's `output.txt` — are not among the documents provided.
**What would settle it:** Read `stage-B/independent-check/output.txt` and `stage-B/results-w15/output.txt` and check reading C's `rivals_built` gap over readable reports. The verdict does not wait on it: reading C already fails on both fields over all reports, so "fails under all ten reading-and-base pairs" stands whatever the figure is.

9.
**Target:** W15, "The short answer" ("**W10.18 holds.**") and section 5's W10.18 row ("**holds** | 0 markers read another file. 0 marks struck. No re-run. | The audit is **claimed**: the transcripts behind it are not in this container (comparison section 6). No harness program applies W10.18 (fault HF3).")
**Claim:** This verdict does not follow from the frozen texts applied to the marks; it follows from `tool_audit.json`, which is a claim rather than a seen thing, and W15 says so in the row itself and again in section 9. The disclosure is in place; what is missing is any independent read of the transcripts and any program that applies the strike rule, so the audit is the only route to the verdict.
**Evidence:** (seen) comparison section 6: "The transcripts behind the audit are not in this container, so the audit itself is claimed, not seen." (seen) comparison section 5, item 1: "No harness program reads `tool_audit.json` or strikes anything (c6)." (seen) W15 HF3: "No program applies W10.18: none reads `tool_audit.json` or strikes a mark." (seen) W15 section 9 lists the transcripts as not tested.
**What would settle it:** Read the 192 transcripts in whichever container holds them and recount Read calls outside the skill and the marker's own prompt. Zero outside reads confirms "holds"; any outside read changes the strike count, and more than three triggers W14's re-run clause.