# W16-atria-L2-s1 - the content of the reply, as extracted by program

Extracted by send_w16.py from `W16-atria-L2-s1.record.json` (the record's `message.content`), unchanged below the rule. Family atria, lens L2, send 1. Verdict under plan W16 section 4.3: answer (HTTP 200, content not empty, finish reason 'stop'). The reply's `reasoning_content`, if any, is kept in the record and is not reproduced here.

---

## Lens L2: the explanations W15 gives

**1. Target:** Short answer, "**The two differ only where the frozen texts can be read two ways, or where a program applies a rule no frozen text states.** There are four such places (the comparison's D1 to D4). No certification and no prediction verdict moves under any of the readings." (also section 1's "There are four such places (the comparison's D1 to D4)").
**Claim:** The two-cause classification covers D1, D2 and D3 but not D4. D4 is not a difference in any computed number at all, and not a two-way reading or an unstated program rule; it is an error in one program's *stated reason*. Calling it one of "four such places" where "the two differ" is too wide.
**Evidence:** comparison.md, "### D4. The independent's stated reason for not counting the four run-record fields. **No number differs.**" and "**Classification: a fault of the independent program**, in its stated reason. Its brief kept it from reading the harness's code, so it could not have seen this. The exclusion still stands on its other ground: none of the four is a certified candidate." (seen). D1 and D2 are genuine count differences with causes in readings; D3 is the harness printing a threshold for fields the texts give none — a program applying an unwritten rule, which fits the second cause (seen). D4 fits neither.
**What would settle it:** Read `stage-B/independent-check/output.json`'s stated reason for the four run-record fields and `comparison.md` §3 D4 together (both here). If D4 changes no number, W15's short answer should say three differences plus one mis-stated reason, and should not list D4 as a place "the two differ".

**2. Target:** Short answer, "**The counts can be trusted as counts.** The in-place run of the harness's programs equals the scratch-copy run apart from timestamps, paths and log layout. The independent recompute gives the same agree/disagree/unreadable counts on every field in each set, and names the same reports." (section 1).
**Claim:** The corroboration is real (same counts *and* same named reports, 96 of 96 lists, seen), but one thing it rests on is not recorded anywhere in these documents: whether the writer of `recompute.py` had seen the harness's counts before finishing it. Both sets of numbers were committed together, so nothing in the record establishes the independence in time.
**Evidence:** W15 §2 item 5: "The scratch-copy counts and the independent counts were committed at f708cc4 before the in-place run at 0d25964." (seen — one commit, both outputs). comparison.md §1: "The independent numbers are `output.json` as the independent worker left it. `recompute.py` was not run again, so its outputs are compared exactly as returned." (seen). README §1: "Another worker is running the harness's own collection programs on the same marks." (seen — parallel work, but no statement of who saw what when). W15 §9 itself names the surviving gap: "A third route to the counts that shares neither the per-kind rule nor the pairing. This file's program uses the harness's own `marks.same`, so it is not that route either." (seen).
**What would settle it:** The session or commit order for `recompute.py` and `checks/harness-programs-on-a-scratch-copy/`. If the independent's output.json was written before the scratch-copy counts existed in the worker's view, the corroboration is clean as W15 says. If not, the agreement is weaker evidence and W15 should say so.

**3. Target:** Section 1 table, row "In place against the scratch copy": "Every difference falls in one of four classes: timestamps 100, a path computed from the rig's location 96 (`from_the_record` in the ignored run records), absolute paths in log lines 4, log format 11. FINDINGS 0. The canaries (a changed mark, a changed count) were caught." and section 1's "**What it forbids:** any output difference other than a timestamp or a path. The canaries show the check could have caught one."
**Claim:** The canary claim for the `07` check is not backed by any number in the documents here. The only canary with figures belongs to a different check: the comparison's re-run, where "A canary with one count lowered gave 416/34". So "a changed mark ... were caught" may stand, but it cannot be checked from what W15 quotes, and the summary "any output difference other than a timestamp or a path" also silently includes the 96 `from_the_record` field values in the run records and 11 log-format items.
**Evidence:** comparison.md §1 and W15 §1 both give the 416/34 canary for the comparison re-run (`08-checks-c1-c7-inplace.txt`), not for `07` (seen). Neither document quotes `07-equal-to-the-scratch-copy.py` or `.txt`, which are named as holding the check (seen, as file names only). The four classes are listed with counts 100/96/4/11 (seen), which is more than "a timestamp or a path".
**What would settle it:** Read `stage-B/collection/07-equal-to-the-scratch-copy.py` and `.txt`. If the canary there changed a count only, a mark change that preserved its report's agree/disagree status would not have been tested; if both a mark and a count canary were run with the results recorded, the claim stands as written.

**4. Target:** Section 7, fault HF10's last column: "Any re-run of the stage-B workflow, and stage D's marking workflow. **The audit's list is taken from the skill folder, not from the instruction.**"
**Claim:** Read as a statement in the present tense, this asserts the opposite of the fault it sits in: if the audit's expected list came from the skill folder it would name eight files and would *catch* the omission. The row's own fault text says the list is the same seven. The sentence is presumably the fix to apply, but placed in the "Fix before" column it is ambiguous, and as written it is not checkable.
**Evidence:** W15 §7 HF10, fault column: "the audit's expected list is built from the same seven, so the audit cannot see the omission." (seen, same row). W15 §2 item 2: "Why the audit could not catch it. The audit's expected list of skill files is the same seven, so it passes by construction." (seen). Independent README E6: "The audit's own key is `read_all_seven_skill_files`." (seen). The Lessons line states the intended rule in the fix form: "an audit's expected list comes from the source (the skill folder), never from the instruction it audits." (seen).
**What would settle it:** Read `stage-B/tool_audit.json` and the audit's builder. If its expected list is seven file names, the sentence in HF10's last column is misworded and should read as an instruction ("the list must be taken from the skill folder"), not a description.

**5. Target:** Section 2, item 3: "**Explanation: the version string in W14 is stale; W14 meant the criteria as they stood.** It forbids any sign in W14 that the markers were to mark under A4-2. Rival: W14 meant A4-2, and the markers marked under the wrong instrument. Check: W14 section 3 names the fields "reworded twice or more" as marks_per_part, pairs_that_pull and rivals_built. Those three were reworded twice only in section 14, so W14's own text presupposes A4-3 at least. The rival is ruled out."
**Claim:** The named check supports the reading but is not what rules the rival out. Section 3's list is consistent with A4-3+; it does not forbid a W14 whose heading deliberately pinned A4-2 while its body was written later. What actually forbids that is the marking plan's own addendum rule ("the later section wins") plus the heading's own words "with the marking plan's addenda" — which W15 quotes only in the Effect line, not as part of the check. W14's own git history, which would settle the stale-versus-deliberate question directly, is not said to have been examined.
**Evidence:** W14's heading (document 2): "The instrument is `instrument/criteria.json` version W10-A4-2 with the marking plan's addenda; from the moment the first marker starts, any change to any field stops the phase" (seen — the string "W10-A4-2" *is* a sign in W14, so the explanation's "forbids any sign" is not literally met). marking-plan.md head: "Where section 13 and an earlier section differ, section 13 is the later text and is the instrument" and "**Section 14** is round 2's addendum ... under the same rule" (seen). W15's own "What happened": criteria.json was W10-A4-4 from dd7da4e (15:33), before W14's freeze at 87fd44b (16:47) (seen).
**What would settle it:** `git log -p` on W14's own file: was the "reworded twice or more" list written with or before the version string, and was the string ever W10-A4-3 or A4-4? If the string was never updated while the body was, "stale" is the right word and the check W15 names becomes corroborating rather than decisive.

**6. Target:** Section 2's heading: "**Departures from the frozen plan** (section 2). None of them moves a number." together with item 2's "**Effect.** Unknown. by-domain.md is the module for "what counts as a part, a job or a change in this field". It bears most on marks_per_part and the per-test fields. What would settle it: re-marking a sample with all eight files read and comparing. That was not run."
**Claim:** "None of them moves a number" is true of the counts as computed and false as a claim about the marks. For item 2 the effect on the marks is explicitly unknown, and the marks are the numbers that feed every count. A reader can take the heading as stronger than the item it heads.
**Evidence:** W15 §2 item 2, "Effect. Unknown." and "Bearing on W10.18. None." (seen). W15 §8: the certification "holds for these 96 reports' kinds" and the struck fields' disagreements concentrate in p52 mode 0 (seen) — by-domain.md is the module on what counts as a part, job or change, and marks_per_part and shape_elements are among the fields that fail there. W15 §9: "Whether reading `by-domain.md` would have changed any mark" is listed as not tested (seen).
**What would settle it:** Re-mark a sample of the nine p52 reports that disagree on attributions and the nine on test_addjob (both lists are in §6.7) with all eight files in the instruction. If the marks move, a number did move and the heading needs qualifying ("no computed count changed; the effect of the seven-file instruction on the marks is unknown").

**7. Target:** Section 3's ruling: "The governing numbers are those of the programs W14 section 1 names as written before the run (the harness's collection and `stage_b.py`), run in place. Wherever those programs apply a rule no frozen text states, or the texts admit two readings, W15 names it and gives every reading's numbers beside" — with the D1 row's governing reading "Documents all of whose reports agree, against ceil(40/48 × documents): p52 6 of 16 (needs 14); repeat 0 of 2 (needs 2)" — and §6.5: "No frozen text says what threshold applies to fewer than 48 reports. Two gates were computed: the threshold scaled to the unread reports, ceil(threshold/48 × n), and the absolute threshold."
**Claim:** The justification "W14 section 1 names the program" is a provenance claim, not a rule, and W15's own evidence shows the program attributes to W14 a scaling W14 does not contain. The ruling then adopts that scaling as governing anyway. Nothing fails because p52 misses under both units, but the reported governing numbers for marks_per_part ("needs 14", "needs 2", and in §6.5 "scaled 37", "scaled 27", "scaled 36") are numbers no frozen text produces. The same device is used a second time in §6.5 for the unread-reports gate, where it is again unstated.
**Evidence:** W15 §3 D1: "The ceil scaling is stated in no frozen text. `stage_b.py`'s docstring lists it among "Rules fixed in W14", and W14 contains no "ceil", "scaled" or "proportion" (c3)." (seen). W14 §2 (document 2): "`marks_per_part` is read by document as the plan says. The consequences in section 6 ... are applied as written." (seen — the consequences are to be applied *as written*, and the scaling is not written). HF1: "`stage_b.py` gates marks_per_part by document against ceil(40/48 × documents), a scaling no frozen text states" and "The reading is ratified in an addendum first" (seen — an addendum that has not been written). W15 §3 "What the ruling gives up": "On D1 it lets a rule the harness wrote, and attributed to W14, govern a count against the marking plan's own gloss." (seen — W15 states the cost).
**What would settle it:** An addendum ratifying the document-unit gate and the ceil scaling, or the owner's ruling that the report unit governs. The check that separates them already exists and is reported (c3: the independent's per-report outcomes grouped by document give 6 of 16 and 0 of 2), so either ruling can be ticked — but until one is ratified, the "needs 14" and "needs 2" figures are held only by the orchestrator's ruling, and should be marked as such rather than presented as thresholds.

**8. Target:** Section 5, "**W10.16's own reading of its failure, with its rival.**" — "**The checks** (the independent's E2, all run): Placebo: under read set B, four fields never reworded twice also show gaps over 10 points ... Permutation: random read sets of the same per-set sizes reach the observed gap in 6.3% of draws for marks_per_part under A, in 30% for rivals_built under B, and in 1.2% for rivals_built under D." and "**Result:** ... It is separated for rivals_built only under W13's read set (D)."
**Claim:** "Separated only under D" holds if the rival is chance; it does not hold for the rival the placebo was built for. The placebo is the check that asks whether the gap is *specific to fitted fields*, and it was run only under B, where it found four never-reworded fields over 10 points — i.e. it undercut "fitted". Under D no placebo is reported, so the separation under D rests on the permutation alone, and the permutation cannot say whether the gap is specific.
**Evidence:** W15 §5 gives the placebo "under read set B" only, and the permutation under A, B and D (seen). Independent README E2, Check 1: "Under B, 4 of the 21 fields never reworded twice also show gaps over 10 points: test_swap +11.4, test_poke +11.3, test_reverse +13.3 and shape_elements +18.0." and Check 2: "Under B, 30% of random read sets give a rivals_built gap of 11.3 points or more." (seen — so under B both checks point away from "fitted"). E2 under D gives the permutation and the per-set split but no placebo (seen).
**What would settle it:** Run the placebo under read set D: compute the read-minus-unread gap for the fields never reworded twice (test_swap, test_poke, test_reverse, shape_elements and the rest) with D's 20 read reports. If four or more of them exceed 10 points under D, the gap is not specific to fitted fields there either, and W15's "separated only under D" should read "not separated under any read set; chance ruled out under D".

**9. Target:** Section 5's result line: "There it reads as the qualified Derivation 3 says: faithful where it was tested, and unconstrained where it was not."
**Claim:** "Unconstrained where it was not" is applied to quantities W15 has just measured. On the unread reports the agreement is not unknown — it is 61 of 75 readable (81.3%) for marks_per_part under A, and under D the independent measures rivals_built at 57 of 76 (75.0%) on the unread reports. A measurement is a constraint; the language softens a measured drop.
**Evidence:** W15 §5: "marks_per_part read 20 of 20 (100.0%) against unread 61 of the 75 readable of 76 (81.3%), a gap of **+18.7**" (seen). Independent README E2, Check 3: "rivals_built's gap sits wholly in p52: read 5/5 against unread 31/43" and under D "rivals_built is read 20/20 against unread 57/76, +25 points" (seen — 57/76 = 75.0%). Note 81.3% is above the instrument's own added line of 80 percent of 48 (39 of 48), so on the reports its makers never read, marks_per_part sits at the instrument's own bar.
**What would settle it:** The wording, not a check: "measured, and lower" is what the numbers show. If a further check were wanted, compare the unread-report agreement of the twice-reworded fields with that of fields never reworded (the placebo already does this under B), which is the nearest thing to a prediction the qualified Derivation 3 offers here.

**10. Target:** Section 6.3, "**Where the P3.5 failure sits**": "Two explanations of the failure: the criterion reads two ways on any report, or it reads two ways on some kinds of report. The first forbids disagreements that gather in one kind. The check is where they sit: test_swap's 8 disagreements are all in p52, and 5 of them are p52's mode-0 reports ... So swap's failure is held by the close set's report kinds, and poke's is not."
**Claim:** The conclusion is right but the check as stated is weaker than the one that establishes it. "5 of 8" is weak on its own (against a 1/3 mode-0 share, 5 or more of 8 arises about 9% of the time by chance). What holds the conclusion is the rate — 5 of the 16 mode-0 reports disagree against 3 of the 32 other p52 reports — together with 0 of 48 in the repeat set. And "the close set's report kinds" names a difference that is not one difference: the two sets differ at once in documents (16 vs 2), in whether mode 0 is present, in readers (DeepSeek only vs DeepSeek and Sonnet 5) and in skill version, so the check cannot say which of those does the work.
**Evidence:** stage_b_record96.json: test_swap close 40/8/0, repeat 48/0/0 (seen). W15 §8 gives the mode-0 counts (seen), which match independent README E3: "test_swap, test_patches and rivals_built 11/16 each" in mode 0 (seen — 11 of 16 agreeing means 5 of 16 disagreeing). p52 is 16 documents × 3 modes = 48, so mode 0 holds 16 reports (seen, from W14 §1's "sixteen documents, run ids `<set>_<stem>`" and the three modes named in W15 §8). W15 §6.3's own poke breakdown — "p52 modes 0, 1 and 2 have 5, 2 and 2; rep 2, rep31 2, son 1, son31 5" (seen) — shows poke spread, and the sum is 5+2+2+2+2+1+5 = 19 ✓.
**What would settle it:** Compare p52 modes 1 and 2 (32 reports, 16 documents, DeepSeek, no mode 0) against the repeat set (48 reports, 2 documents, modes 1 and 2, DeepSeek and Sonnet 5). The documents here already carry the counts to do it: test_swap's p52 non-mode-0 disagreements are 3 of 32 against 0 of 48 in repeat, so the two sets differ even with mode held fixed. That would show mode 0 is not the whole of "report kind", and W15's phrase should be "the close set, and within it mode 0" rather than "the close set's report kinds".

**11. Target:** Section 6.3, "**What that leaves P4.4 able to show (worked out).**": "After stage B no certified field records a part's mark: marks_per_part is struck. So P4.4, as this rule leaves it, reaches the marks only through test_remove. The other three certified fields are the report's shape, the modules the reader says it opened, and the attributions."
**Claim:** The step "reaches the marks only through test_remove" does not follow. test_remove's value is one of RAN / NAME ONLY / ABSENT / CANNOT — a test outcome, not one of the eight marks a part carries. So P4.4 as left reaches no per-part mark at all; the honest statement is that its subject matter has no certified field. The chain also rests on a quotation from marking plan section 10 ("P4.4 'is about the within-step *marks*, not about the vocabulary'"), which is not among the documents here.
**Evidence:** criteria.json, test_remove: `"kind": "enum", "values": ["RAN", "NAME ONLY", "ABSENT", "CANNOT"]` (seen). marks_per_part is the only field whose kind is `map_enum` over the eight marks, and it is struck (seen, W15 §4 and §6.2). The four certified within-step fields after P3.5 are test_remove, shape, modules_self_reported, attributions (seen, W15 §6.3 and marking plan §6); of these, none carries a part's mark of the eight. Marking plan §10 is not excerpted in the documents above, so the quotation is claimed.
**What would settle it:** Read marking plan §10 and W3's P4.4, and ask of each certified within-step field what a disagreement on it would be a disagreement *about*. If none is a per-part mark, W15 should say "P4.4 reaches no part's mark" rather than "reaches the marks only through test_remove"; the question it puts to the owner survives either way.

**12. Target:** Section 6.4, "**The explanation.** The failure sits in the stage-B inputs, the rig layer: the markers were never handed the question the criterion compares against, so most of them left the field null." with "**The check.** All 192 prompts have only the sections "THE FIELDS", "THE REPORT" and "END OF THE REPORT". None carries a question handed in (independent output.txt). `first_marker.prompt_for(num, doc, report, crit, out_path)` has no place for one."
**Claim:** The mechanism is plausible but not separated from a rival the section-heading check cannot reach. criteria.json records, inside the question_identity field itself, a stand-in target — "the explanation this document gives of why at least one of the three propositions must be true" — described as what the comparison "was made against", and `first_marker.py builds its prompt from every field` (criteria.json's own ask). Whether that stand-in reached the markers is not settled by the section headings, which only show that no *handed-in question* was present. If the stand-in was in the prompt, the nulls have a second sufficient cause (the stand-in is P3-specific and the criterion still says "the one handed in"), and the field was in fact marked on 5 of 96 reports, 4 of them agreeing — so "not tested" is right for the handed-in comparison, not for the field as a whole.
**Evidence:** criteria.json, question_identity's `how_the_record_was_used`: "So the comparison was made against a stand-in target, written here before the two reports were marked: 'the explanation this document gives of why at least one of the three propositions must be true'. The stand-in is what the W10 harness's question.py would build for that document. What this does not test: whether a marker handed two literal questions reads them the same way." (seen). criteria.json, `asks_of_the_harness`: "first_marker.py builds its prompt from every field" (seen). Independent README §1 freeze check: "Each field's criterion, heading, kind, per, layer and closed list appears word for word as `criteria.json` has it." (seen — `how_the_record_was_used` is not in that list, so its presence in the prompt is unknown). stage_b_record96.json: question_identity close 1/0/47, repeat 3/1/44 (seen), so 5 reports carry values from both markers and 4 agree. W15 §6.4's own arithmetic — "both markers null on 45 and 32; one null on 2 and 12; both valued on 1 and 4, of which 1 and 3 equal" — sums to 48 in each set (seen, consistent with the counts).
**What would settle it:** Read one P3 marking prompt (e.g. the prompt for a `rep_P3-*` report) and ask whether the stand-in string appears in it; and list the five reports on which both markers gave a value, to see whether they are P3 reports. If the stand-in is present, W15's mechanism should read "the criterion's comparison target was present only as a P3 stand-in, and the criterion still required 'the one handed in', which no prompt carried". If it is absent, the mechanism stands as written.

**13. Target:** Short answer, "**question_identity was not tested; it did not fail between markers.**" (and §6.4's "the failure is 91 unreadable reports and 1 disagreement, so the rival is ruled out as the cause of this failure").
**Claim:** The second clause is too wide as a headline. There is one between-marker disagreement (the repeat set's 3 agree / 1 disagree), so the markers did disagree on one of the five reports both valued. What the claim means — that the threshold failure is driven by the 91 unreadable, not by marker disagreement — is right, and the rival ("the markers cannot agree") is properly ruled out *as the cause*; but the phrasing invites the stronger, false reading.
**Evidence:** stage_b_record96.json, question_identity: close 1/0/47, repeat 3/1/44, pooled 4/1/91 (seen — one disagreement). W15 §6.4: "Both markers gave a value on only 5 of 96 reports, and they agree on 4 of those." (seen — 4 agree + 1 disagree).
**What would settle it:** Already settled by the numbers W15 quotes; the fix is wording — "the failure is 91 unreadable reports and 1 disagreement" (as §6.4 has it) rather than "it did not fail between markers".

**14. Target:** Section 4's table row: "| test_remove | within-step | yes | 39, section 6 (added) |" with the note "test_remove is included only under the independent's reading A3 of "the other eight tests"; the texts forbid the other reading (comparison section 4)".
**Claim:** The source cited for test_remove's threshold is imprecise. Marking plan §6's row names "the other eight tests, remove_in_groups, shape_elements, modules_self_reported, attributions" — and the "other eight" are the cross-step tests, so test_remove is not in that row. The threshold comes instead from W14 §4's "The other fields at 39" read together with W3 P3.5's "remove carries it alone", which is the independent's A3 and the right reading.
**Evidence:** marking plan §6 (document 5): "the other eight tests, remove_in_groups, shape_elements, modules_self_reported, attributions | 39 of 48 | **added here**" (seen — test_remove absent). criteria.json, `threshold_added_here.which`: "the eight other tests, remove_in_groups, modules_self_reported, attributions, shape_elements" (seen — again without test_remove). Independent README A3: "Chosen: 39, following W14 section 4's 'The other fields at 39'. Under the other reading the counts (46 and 48) are the same, but test_remove cannot be certified." (seen). W14 §4 (document 2): "The other fields at 39 (the instrument's own addition)." (seen). criteria.json, `sorting.the_line_drawn`: "W3 P3.5 confirms them ('they are dropped from the within-step set for phase 4 and remove carries it alone')" (seen — the anchor that forbids the other reading).
**What would settle it:** No check needed beyond the texts quoted; the table's source cell should read "39, W14 §4 read with W3 P3.5 ('remove carries it alone'); section 6's row names the eight cross-step tests". The sixteen-certified headline depends on this reading, and it is well held — but not by section 6.

**15. Target:** Section 1, "**What the in-place equality shows, and what it does not.**": "**The rival:** equality by construction, because the code, instrument, marks and mapping were byte-identical in the two runs. That rival holds, and it is the point. The in-place run adds only that the numbers no longer depend on where the rig sits."
**Claim:** This is the tightest explanation in the file, and the check does real work: it forbids any change to the marks, the mapping or the programs between the two runs, and W15 says so. The one thing it does not forbid, which the wording does not quite carry, is a change made *before* the scratch run — the marks are "kept as it came" (claimed), and the audit behind W10.18 is claimed because the transcripts are not in the container. So "the numbers no longer depend on where the rig sits" is the exact scope, and no wider.
**Evidence:** W15 §1: "It does not re-test any rule the programs apply. Those rules are tested only by the independent recompute, which shares with the harness the per-kind rule of marking plan section 5 and the pairing through `index.json` and the mapping. A fault shared there would pass both (comparison section 7)." (seen — W15 states the limit). comparison.md §6: "The transcripts behind the audit are not in this container, so the audit itself is claimed, not seen." (seen). W15 §9: "The transcripts behind `tool_audit.json` ... are not in this container, so the audit is claimed, and W10.18's verdict rests on it." (seen).
**What would settle it:** Nothing further, for this explanation. The check that would widen it from location-independence to mark-integrity is a comparison of the saved marks against the markers' own written returns at the moment of writing, which is what the audit's transcripts would hold; W15 already names them as absent.

If a single thread runs through these, it is that W15's *numbers* are faithful to the files and its *limitations* are honestly catalogued in sections 7, 9 and 10 — the looseness sits in the explanatory bridges: the short answer's neat two-cause story for D1 to D4, the canary and independence claims that carry no figures, two checks (the placebo under read set D, the stand-in's presence in the prompt) that were run somewhere else than where the conclusion needs them, and a few wordings ("reaches the marks only through test_remove", "unconstrained where it was not", "it did not fail between markers") that state more than the cited checks show.