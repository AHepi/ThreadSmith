You are marking one report against criteria that were frozen before any report was read. You are not judging the report and you are not judging the document. You are recording, field by field, what the report does, by the criterion given for that field, in the words a stranger could apply.

Read the report below. Then write one JSON object with one key per field, and nothing else, to the output path at the end of this file, using Write once. Where a field's values are a closed list, use one of them and nothing else. Where you cannot tell, use null: a guess is worse than a gap, and a gap is reported as unreadable, not as agreement.


Report number: M007    Document: D1

=== THE FIELDS ===

### test_remove - Remove
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's general rule for the eleven tests, kept: RAN (applied with a concrete change or a named neighbour), NAME ONLY (mentioned with no content behind it), ABSENT, CANNOT. For remove: RAN when the report takes a named part away (removes it, cuts it, drops it, asks what happens without it) and says what happens to a named job or to the conclusion. NAME ONLY when removal is mentioned and no part is taken away. ABSENT when no sentence takes anything away. CANNOT only in file 33's own form: the test named, the sentence of the document that leaves it nothing to reach quoted, and a part left unknown with that test named; anything less is NAME ONLY.
the example that fixes the boundary: 'RAN' because Remove it and every "simulated mind" term dies; the trilemma has nothing left to talk about.

### test_swap - Swap
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Reworded, because H64 put this test's inter-marker agreement at 36 of 48. RAN when the report puts a named near neighbour in the place of a named part and says what happens to a named job or to the conclusion. The word 'swap' need not appear: 'a fever chart or a share price works as well' is a swap. A class of replacements with no particular neighbour named ('use a different observer-selection rule') is NAME ONLY. ABSENT when no replacement is named anywhere. CANNOT in file 33's form only.
the example that fixes the boundary: 'RAN' because Swap it for "the difference is that space is reversible and time is not" and the rest of the speech (balloons, drift) reads almost the same.

### test_poke - Poke
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Reworded, because H64 put this test's inter-marker agreement at 35 of 48, the lowest of the eleven. RAN only when the report names, for one named part, both halves: a change to the world it says would alter the outcome, and one it says would not, the second sitting next to the disputed line. One half alone is NAME ONLY. A report that only says whether the document offers a pair is NAME ONLY, however well it says it: the field records whether the reader made a change, and a reader who names no change has made none. A reader who says the test has no purchase on a document of this kind, quotes the sentence that leaves it nothing to reach, and leaves a part unknown with the test named, is CANNOT. ABSENT when neither half nor the test is mentioned.
the example that fixes the boundary: 'RAN' because This job survives a large swap: the paper notes its estimate could be off by several orders of magnitude without changing the conclusion, because the margin between required and available compute is so wide. Both halves of that poke check out as claimed.

### test_flip - Flip the outcome
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept: H64 put this test's agreement at 46 of 48, the highest. RAN when the report asks what it would have said had the opposite happened and answers; or, where the report says the conclusion is derived, when it takes file 33's redirection and says the conclusion covers every outcome because it is derived, or names a condition whose removal breaks it. An answer of 'no, it could not have covered the opposite' is RAN. NAME ONLY when the flip is named and not answered.
the example that fixes the boundary: 'RAN' because Could the same machinery have "explained" the opposite? If interest or survival rates are near zero, the equation forces f_sim near zero instead - which is exactly disjunct (1) or (2).

### test_reverse - Reverse (poke the cause, then poke the effect)
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept: H64 put this test's agreement at 40 of 48. RAN when the report pokes the supposed cause and the supposed effect separately and says which moved, or says which of the two questions the explanation answers on the ground of that pair. NAME ONLY when direction is named and no separate poke of either end is given.
the example that fixes the boundary: 'RAN' because Poke the lever and only the report moves; poke the outcome and nothing moves in the lever.

### test_hunt - Hunt the answer in the starting points
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept: H64 put this test's agreement at 43 of 48. RAN when the report names a starting point of the explanation and says whether the conclusion is already sitting in it under another name, and answers. An answer of 'no' is RAN. NAME ONLY when circularity is named and no starting point is examined.
the example that fixes the boundary: 'RAN' because P2 and P3 put motion along time into the premise. "Time is only a kind of Space" is the conclusion under another name; P6 repeats it.

### test_addjob - Add a job
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Reworded, because H64 put this test's agreement at 35 of 48 and named the cause: 'Added means two things: Step 2's provenance tag for a job the reader supplied, and Step 5's test Add a job; readers wrote the tag and never ran the test.' So: RAN only when the report names something else that must also be so if the explanation is right, which is not already in the report's own job list, and says whether the explanation covers it as it stands or which rival version it rules out. A job carrying the tag 'added' in the job list is not this test and does not make it RAN. NAME ONLY when the test is named and what follows is a part of the document restated.
the example that fixes the boundary: 'RAN' because If we are simulated, why do we observe a coherent micro-world at all? P6 answers it — by absorbing anything. My job is added; a job held only by my addition is not held.

### test_rival - Build the best rival
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept: H64 put this test's agreement at 44 of 48. RAN when the report states a rival explanation in a sentence of its own and either names a change that would tell it from the explanation under test, or says plainly that no change on the change list can. NAME ONLY when a rival is named and neither is given.
the example that fixes the boundary: 'RAN' because The Medical Man names the obvious rival explanation aloud: "some sleight-of-hand trick." Nothing in the passage supplies a change that would tell the two apart — a check of the table's underside *before* the attempt, an independent operator, or a second unannounced trial.

### test_pull - Pull
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept, with one case added in the fix round on 22 September 2026 (the reviewer's fault 3; the addendum is marking-plan.md section 13.1). H64 put this test's agreement at 45 of 48. RAN in either of two cases. (i) The report says of two things in the explanation that making one stronger makes the other weaker, and says which gives way, or where the line is, or what would show the line drawn in the wrong place. (ii) The report names both sides of a tension between two things in the explanation - two things it presents as pulling against each other, competing, or in tension over one job, and not merely as different - and says which gives way, or where the line is, or what would show the line drawn in the wrong place; the stronger-weaker wording need not appear. A report that says the line is not drawn ('Nothing marks where the vapour regime ends and the solid regime begins') has said where the line is, in the same way that 'no gauge is offered' is the patches test run. NAME ONLY when a tension is asserted with neither side named, or when both sides are named and nothing is said about which gives way, where the line is, or what would show the line drawn in the wrong place. ABSENT when the report names no tension between two things in the explanation. CANNOT takes the general three-part form. The two things need not be items of the report's own part list; when they are not, this field is still RAN and pairs_that_pull is empty. What case (ii) gives up: a report that only contrasts two parts and says which the text favours is now RAN, so RAN no longer tells a report that asked the stronger-weaker question from one that reported a contrast with a winner; pairs_that_pull records the pair and quotes keeps the sentence, which is where that difference stays visible.
the example that fixes the boundary: 'RAN' because (a) P3 against P8: the more the patch covers, the less the vanishing shows.

### test_patches - Check the patches
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Plan 52's rule kept: H64 put this test's agreement at 41 of 48. RAN when the report names a patch, a narrowed range or a catch-all in the document and either asks what would show it swallowing a job (a gauge) or says the gauge is there and names it. Both answers, gauge and no gauge, are RAN. NAME ONLY when a catch-all is named and nothing is said about a gauge.
the example that fixes the boundary: 'RAN' because P10 DOOM / not-special | Held, catch-all with a gauge | The gauge is named: a giant meteor would show we had been exceptionally unlucky.

### test_inside - Look inside
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, NAME ONLY, ABSENT, CANNOT
criterion: Reworded, because H64 put this test's agreement at 38 of 48 and its second marker traced the cause to the report template: SKILL.md's whole-explanation-checks line 'names no slot for look inside or add a job'. So: RAN when the report names a change that reaches past what comes out, into how the thing works, and says what that change would show; or names two differently-built candidates that give the same outputs and the change that would tell them apart. NAME ONLY when a change that would reach inside is named and nothing is said about what it would show, including when the report says the change cannot be made. CANNOT when the report says the test has no purchase on a document of this kind, quotes the sentence of the document that leaves it nothing to reach, and leaves a part unknown with the test named.
the example that fixes the boundary: 'NAME ONLY' because The one change that would reach inside, opening the machine, is impossible by the claim's own terms: it is gone.

### shape_elements - The seven elements of a full report
kind: map_enum; per: report; layer: the thing under test
one of, and nothing else: Y, N
criterion: H64 struck shape's PART boundary ('34 of 48 agree, and every disagreement is the second marker saying PART where the first said NONE ... or FULL'). The written rule that replaces it: do not judge the shape; fill these seven, each Y or N, and the shape follows by the rule in the shape field. E1 the question frozen: the report says in one place what is being explained and what is asked. E2 the parts listed: a numbered or bulleted list of the parts of the explanation. E3 the jobs listed: a list of the things the explanation has to account for, set out apart from the parts; naming where the jobs came from is not a jobs list. E4 the change list: the report gathers, in one place, the changes it ranges over, or says what the change list leaves out. E5 tests run: at least one of the eleven applied with a concrete change or a named neighbour. E6 a mark on every part: every part in E2's list carries one of the eight marks. E7 the report's sections: the method's seven report sections are present and in its order, each doing its work, whatever they are called.
the example that fixes the boundary: {'E1': 'Y', 'E2': 'Y', 'E3': 'Y', 'E4': 'Y', 'E5': 'Y', 'E6': 'Y', 'E7': 'Y'} because J1 why Time is thought different from Space (given — the Medical Man); J2 why we cannot move in Time (given); J3 why the model vanished (given — the demonstration)

### shape - The report's shape
kind: enum; per: report; layer: the thing under test
one of, and nothing else: FULL, PART, NONE
criterion: Derived from shape_elements and never judged directly: FULL when all seven elements are Y; PART when one to six are Y; NONE when none is. A marker whose shape does not follow from its own seven elements has made a mistake, and the marks file is rejected by the check.
the example that fixes the boundary: 'FULL' because seven of seven elements Y; see shape_elements on the same report

### turned_own_test - Turned one of the document's own tests back on it
kind: enum; per: report; layer: the reader
one of, and nothing else: Y, N
criterion: H64 struck this field (8 of 48; 'the criterion's words are read two ways'). The written rule picks the stricter reading, because the looser one was Y in 41 of 48 and so separated nothing. Y only when all three hold: (i) the document states a test, check, gauge or standard and aims it at something other than the explanation under test; (ii) the report applies that same test to the explanation under test; (iii) the report says what the result was. N otherwise. In particular, a report that only notes that the document's own author ran a test on their own explanation is N: that is the document testing itself, not the report turning a test back on it.
the example that fixes the boundary: 'Y' because The Psychologist offers a gauge — a spinning spoke, a flying bullet — and it fails: both are blurred, not invisible.

### same_explanation - The same-explanation verdict
kind: enum; per: report; layer: the thing under test
one of, and nothing else: PRESENT, ABSENT
criterion: H64 struck this field as a count ('38 of 48, every disagreement the second marker reading "nothing tells them apart" as the verdict') and said it is kept as quotes only. It returns here as presence on one document, never as a count across documents. PRESENT when the report says of two named candidates that no change on the change list tells them apart, in those words or in words that carry them ('the same explanation at this level', 'they are one explanation', 'nothing on the list separates them'), and the sentence is copied into same_explanation_quote. ABSENT otherwise. A report that says a rival 'is not ruled out', or that a rival is 'not separated yet by the evidence', is ABSENT: not-ruled-out is not nothing-separates-them.
the example that fixes the boundary: 'PRESENT' because At this point in the story the two are, by the method's own rule, **the same explanation at this level**: no change on the list (only "watch the table") tells them apart.

### same_explanation_quote - The sentence that carries the verdict
kind: text; per: report; layer: the thing under test
criterion: The report's own sentence, copied exactly, when same_explanation is PRESENT; empty when ABSENT. Never compared for agreement; kept so a disagreement can be traced and so the verdict is read as a quotation and not as a number.
the example that fixes the boundary: 'At this point in the story the two are, by the method\'s own rule, **the same explanation at this level**: no change on the list (only "watch the table") tells them apart.' because as above

### marks_per_part - The eight marks, part by part
kind: map_enum; per: part; layer: the thing under test
one of, and nothing else: held, held if, two routes, loose, idle, borrowed, fixed, unknown
criterion: New field (W3 P3.2). One entry per part. THE KEY, as the fix round fixed it on 22 September 2026 (the reviewer's fault 4; marking-plan.md section 13.2): take the part's label from the report's part-by-part section - the table or list that gives each part its mark - where there is one, and otherwise from the report's parts list; then normalise it. To normalise: take the characters up to the first space, '(' or '['; strip from each end of that token every character that is not a letter or a digit; use what is left as the key when it is label-shaped, that is at most two letters followed by at most two digits ('A', 'H', 'P3', 'P10', 'iv'); otherwise use the whole label as the report writes it, trimmed, with the same stripping at each end ('Time is a fourth dimension'). The rule is written so that the two lists one report commonly carries - '**A.** Time is a fourth dimension' in the parts list and 'A (time = 4th dimension)' in the marks table - give one key and not two. A row that covers two parts under one label ('P3, P4 decomposition and ratio') is one entry under that label's normalised key. TWO CLAUSES ADDED IN ROUND 2 OF THE FIX ROUND (22 September 2026, the reviewer's fault 4, held open after round 1; marking-plan.md section 14.1), because the rule as it stood gave one key to two different parts in seven of the record's ninety-six reports. (1) A TOKEN IS LABEL-SHAPED ONLY WHERE IT STANDS AS AN ENUMERATOR. The label must not begin with a quotation mark of any kind (" “ ” ' ‘ ’ «), and what follows the token's last letter or digit inside the label must be at least one character that is not a letter or a digit — the separator — and then at least one more letter or digit — the rest of the label. So 'P3 the sum', '**A.** Time is a fourth dimension', 'A (time = 4th dimension)', 'H, exact images' and 'iv) the third premise' all key to their enumerator, and '"a mathematical line... has no real existence"', '"I don't, for certain, know which"' and '"To capture these advances… necessitated an update"' do not key to 'a', 'I' and 'To' but keep the whole label. A label that is a bare token with nothing after it ('P1', 'A') is not an enumerator either, and the whole-label rule gives back the same string, so nothing moves. (2) THE KEY IS UNIQUE WITHIN ONE REPORT. Where two or more rows of the part-by-part section normalise to the same key, each of those rows — and only those rows — keeps its whole label as the report writes it, trimmed and end-stripped by the same stripping; a row that did not collide keeps its normalised key. So 'A, specific 4-D content' and 'A, "jargon + named pushback"' are two entries and not one, and 'B.' and 'B′.' likewise. The criterion says both clauses out loud because a marker must apply them, and pairs_that_pull inherits them. THE VALUE is the one of the eight marks the report gives that part. Match the longest mark first, so 'held if' is read before 'held'. Extra words around a mark do not change it: 'Held, but coarser than stated' is held; 'Held, catch-all with a gauge' is held; 'Loose, ungauged catch-all' is loose. A part whose mark cell contains two or more different marks of the eight is left out of the map and its cell is copied into quotes. A part with no mark is left out. Read by document, never summed.
the example that fixes the boundary: {'P1': 'borrowed', 'P7': 'idle', 'P10': 'held'} because P10 DOOM / not-special | Held, catch-all with a gauge | The gauge is named: a giant meteor would show we had been exceptionally unlucky.

### pairs_that_pull - Pairs that pull, named
kind: set; per: report; layer: the thing under test
criterion: New field (W3 P3.2). One member per pair the report names as pulling against each other, written 'A|B', where A and B are two different part keys of the report, normalised exactly as marks_per_part's key rule normalises them, in the order the report writes them. THE KEYS ARE marks_per_part's KEYS AND THIS FIELD INHERITS BOTH CLAUSES ADDED TO THEM IN ROUND 2 OF THE FIX ROUND (22 September 2026, the reviewer's fault 4; marking-plan.md section 14.1): a token is label-shaped only where it stands as an enumerator, and a key that would collide with another row's inside one report is replaced, for the colliding rows only, by the whole label trimmed and end-stripped. Where a colliding row's key is a whole label, the member carries that whole label on that side, so a pull named on a report whose keys collide is written with the long keys and not with the short ones. A PULL NAMED BETWEEN TWO GROUPS of the report's own parts (the fix round, 22 September 2026, the reviewer's fault 4; marking-plan.md section 13.2) gives ONE MEMBER PER CROSS PAIR: for 'A-C ... D and G' the members are A|D, A|G, B|D, B|G, C|D, C|G, the left of each member taken from the side the report names first. A side written as a range ('A-C', 'P1-P3', with any dash) means every part of the report's own list from the first named to the last named inclusive, in the order that list runs; a side written as a list ('D and G', 'P2, P4') means the parts it names. A side that is not written as an explicit range or list of the report's own part labels - a description such as 'the argument parts' - cannot be expanded: the pull is not a member, the sentence goes in quotes, and test_pull still records that the test ran. A tension named between things that are not in the report's part list is not a member and goes in quotes; the test_pull field still records that the test ran. A part said to pull against itself is not a member. The empty set is a value and means the report names no pair; use null only when you cannot tell whether a sentence names a pair.
the example that fixes the boundary: ['P3|P8'] because (a) P3 against P8: the more the patch covers, the less the vanishing shows. (b) P9 against itself: pass through matter at speed, jam into it at zero.

### rivals_built - Rivals built, named
kind: set; per: report; layer: the thing under test
criterion: New field (W3 P3.2). One member per rival the report builds. A rival is built when the report states it in a sentence of its own and either names a change that would tell it from the explanation under test or says plainly that no change on the change list can. A rival only named is not a member and goes in quotes. HOW A MEMBER IS WRITTEN (the fix round, 22 September 2026, the reviewer's fault 5; marking-plan.md section 13.3): the report's own short name for the rival where it gives one, lowercased and trimmed, with any quotation marks round it removed and any punctuation at either end of what is left dropped (so a name quoted with the sentence's full stop inside the quotation marks, "some sleight-of-hand trick.", is written 'some sleight-of-hand trick'); otherwise the first five words of the sentence that states the rival, lowercased and trimmed, each word as the report writes it, with punctuation at a word's edge dropped. The report gives a short name of its own when the sentence that states the rival carries a phrase that names the rival as a thing - a noun phrase, with no verb of its own inside it - in quotation marks or otherwise set off as the rival's name ('some sleight-of-hand trick'). A quoted phrase with a verb in it ('he built a machine, don't ask how') states the rival and does not name it, so the five-word rule applies. The sentence that states the rival is the first sentence in which the rival is put, and its words are its whitespace-separated tokens counted from that sentence's first word. THREE CLAUSES ADDED IN ROUND 2 OF THE FIX ROUND (22 September 2026, the reviewer's fault 25; marking-plan.md section 14.2), because the rule as round 1 left it was not decidable on reports neither A4 nor the reviewer had named. (1) WHERE THE SENTENCE STARTS. A lead-in that names the test is not part of the sentence: any list marker ('-', '*', '1.'), and then any run set in bold, italics, brackets or parentheses whose text carries the word 'rival' or 'rivals' ('**Rival.**', '- **Best rival.**', '**Rivals:**', '**Rival explanation.**'), together with the punctuation and the space that close it. The sentence's first word is the first word after that run, so the five words are counted from there and never from the lead-in: '**Rival explanation.** Drop the Fourth-Dimension argument for a bare …' gives 'drop the fourth-dimension argument for' and never 'rival explanation drop the fourth-dimension'. (2) WHICH SENTENCE. 'The first sentence in which the rival is put' is the first sentence of the report's own whole-explanation section — the section that runs file 33's whole-explanation checks (flip, reverse, hunt the answer in the starting points, add a job, build the best rival, pull, check the patches, look inside) — that states that rival. A mention in a part-by-part cell is not it: the cell goes in quotes, and a rival the report states only in a part-by-part cell and nowhere in the whole-explanation section is not a member. Where the report has no whole-explanation section, it is the first sentence of the report outside the part-by-part section that states the rival; that last clause is the instrument's and not the reviewer's, and is here so that the rule reaches a report of any shape. (3) TWO RIVALS IN ONE SENTENCE. Where one sentence states two or more rivals, each takes the report's own noun phrase for it as its name, written by the naming rule above, whether or not that phrase is quoted or otherwise set off: "Benioff's machine and Feynman's simulator are the near neighbours" gives 'benioff's machine' and 'feynman's simulator', and 'The dissent, and *Phelps*.' gives 'the dissent' and 'phelps'. Where the sentence gives no noun phrase for them, THE FIELD IS NULL FOR THAT REPORT and the sentence goes in quotes, rather than two rivals collapsing into one five-word member. The empty set is a value.
the example that fixes the boundary: ['some sleight-of-hand trick'] because The Medical Man names the obvious rival explanation aloud: "some sleight-of-hand trick." Nothing in the passage supplies a change that would tell the two apart — a check of the table's underside *before* the attempt, an independent operator, or a second unannounced trial.

### remove_in_groups - Remove in groups
kind: enum; per: report; layer: the thing under test
one of, and nothing else: RAN, ABSENT
criterion: W8 part B4 names remove-in-groups among the fields whose object is the set of parts, and the eleven-test remove field is within-step, so this is carried apart. RAN when the report takes two or more named parts away together and says what happens; ABSENT otherwise. Removing parts one at a time, however many times, is ABSENT.
the example that fixes the boundary: 'ABSENT' because Remove it and every "simulated mind" term dies; the trilemma has nothing left to talk about.

### question_identity - The question the run froze
kind: enum; per: report; layer: the reader
one of, and nothing else: SAME, MOVED
criterion: W10 section 4: the same frozen question is handed to every arm and this field is recorded on every run. SAME when the question the report freezes has the same target as the one handed in (it is about the same explanation of the same thing) and asks the same thing of it. MOVED otherwise. Compare the target and what is asked, and nothing else. Do not compare the range: arm (f) is handed the question with the change list removed by design, so a field that read MOVED on every arm (f) run for that reason would be measuring the arm and not the drift. Copy both questions into question_frozen_quote.
the example that fixes the boundary: 'SAME' because I froze the one its abstract makes central: **at least one of three propositions is true — (1) almost no civilizations reach a posthuman stage; (2) almost no posthumans want to run ancestor-simulations; (3) almost all observers like us are simulated.**

### question_frozen_quote - The two questions, quoted
kind: text; per: report; layer: the reader
criterion: The report's own sentence stating the question it froze, copied exactly, then the target line of the frozen question as it was handed in. Never compared for agreement. It is here because SAME and MOVED agree between two runs that moved to two different questions, and without the quotations that agreement would be empty.
the example that fixes the boundary: "froze: 'I take the title's question as central, reached by that disjunction.' | handed in: 'the explanation this document gives of why at least one of the three propositions must be true'" because I take the title's question as central, reached by that disjunction.

### modules_self_reported - The modules the reader says it opened
kind: set; per: report; layer: the reader
criterion: W8 part C4 keeps this apart from the transport's record, and P4.9 compares the two. One member per reference file the report itself says it opened, written as one of the seven ids: the-idea-in-depth, question-bank, by-domain, building, testing-against-cases, reporting, word-list. A file named in prose counts ('the reporting file' is reporting; 'the question bank' is question-bank; 'the depth file' is the-idea-in-depth). A file the report says it did not open does not count. An ordinary use of a word that is also a file name does not count: 'building a case' is not the building module. The empty set is a value. Do not fill this from the run record; that is modules_served.
the example that fixes the boundary: ['by-domain', 'question-bank', 'reporting', 'the-idea-in-depth'] because The method's own map sent me to four modules: the depth file (first full run on something that matters), the domain file (this is an argument, plus story choices), the reporting file (the marks and report shape), and the question bank (wording of each test).

### marks_outside_closed_list - Marks outside the closed list
kind: int; per: report; layer: the thing under test
criterion: The number of parts in the report's part list whose mark cell contains none of the eight marks, or two or more different ones. It is the gauge that shows whether arm (e)'s skeleton set the mark vocabulary it claims to set: the skeleton prints the eight marks into the emission before the reader writes anything, so arm (e) should read zero. It is not certified and carries no arm difference, because a count going to zero under arm (e) is the port being set and not evidence about the within-step marks; reading P4.4 on it would pass P4.4 by construction.
the example that fixes the boundary: 4 because Computing-power estimate | **Loose** on the exact numbers, **held** on the qualitative point

### attributions - What the report attributes to a name (arm x)
kind: set; per: report; layer: the document
criterion: The field the re-identification arm is read on (W8 part A10, PA.3). One member per numbered item of the report's own part list or job list that the report attributes to a named speaker or agent of the document, written 'name|label': the name lowercased exactly as the report writes it, then the report's own label for the item. An item attributed to no name, or to the reader ('mine'), or to a thing rather than a person ('the demonstration'), is not a member. Attributions made outside the numbered lists are not members and go in quotes. The empty set is a value. Whether an attribution is right is not marked here: arm (x) is read against its own unexchanged arm (a) control on the same document, which is why W8 A10 requires that control, 'since the record already holds attribution errors without any exchange'.
the example that fixes the boundary: ['the narrator|P11', 'the psychologist|P8'] because P8 the Psychologist's "presentation below the threshold… diluted presentation"; P9 "slipping like a vapour through the interstices"; P10 his dust, cut chin, socks; P11 the narrator's "no kind of trick… could have been played upo

### layer_at_fault - The layer, named on every report
kind: enum; per: report; layer: the reader
one of, and nothing else: the document, the thing under test, the reader, none
criterion: Plan 52's three-layer rule, on every report: 'A finding contradicted by the document is the reader's fault unless the skill's wording pushed it there; only then is it the skill's. The marker must name which, in one line, every time.' For this round layer 2 is the thing under test: the skill's words together with the arm's arrangement. 'none' when the report contains nothing the marker can fault. It is not certified and carries no arm difference, because it is the marker's own attribution and a difference on it would be a difference between markers.
the example that fixes the boundary: 'the thing under test' because | P7 | fixed | the owner set it.

### layer_at_fault_line - The one line that names the layer
kind: text; per: report; layer: the reader
criterion: One line: what the fault is, and why it sits at the layer named. Where the layer is the thing under test, the line quotes the sentence of file 33 that pushed the reader there; plan 52's trap, kept: 'Recording a break without quoting the line of the skill that caused it.' Never compared.
the example that fixes the boundary: "fixed used for a part the document's own claimant set; file 33's line 'Fixed - the owner put this part outside the test, and said so' reads as the document's owner to a reader marking a document." because | P7 | fixed | the owner set it.

### quotes - The sentences the marker leaned on
kind: text; per: report; layer: the reader
criterion: Free text: every sentence a value above rests on that is not already quoted in another field, and every cell a rule sent here (a mark cell with two marks; a pull whose sides are not part labels; a rival named and not built). Never compared for agreement, kept so a disagreement can be traced.
the example that fixes the boundary: "pull sent here because neither side is a part label: 'Two later-stated claims about the mechanism pull against each other'" because Two later-stated claims about the mechanism pull against each other

=== THE REPORT ===

RFC 1958 is best judged as a rational reconstruction of Internet design culture, not a formal or falsifiable theory. It holds up where it names constraints that repeatedly shaped protocol choices. It is loose where it states maxims, dated assumptions, or ideals as facts—and it admits this (“snapshot,” “not a formal or invariant reference model”).

**What does the work**

The strongest explanatory parts are:

- **The end-to-end argument and fate-sharing (2.3).** This is the document’s real load-bearing claim. It explains why IP is a narrow waist, why reliability/retransmission/security tend to live in endpoints, and why state inside the network is treated as suspect. The Saltzer quotation supplies a mechanism: some functions require endpoint knowledge, so network-provided versions can only be performance enhancements. Fate-sharing—keep state at endpoints so it dies only when they die—then explains the preference for datagrams over classical virtual circuits.
- **IP as an inter-networking layer independent of hardware (2.2).** This explains the Internet’s ability to absorb new link technologies and to offer one platform over heterogeneous media. It is the architectural counterpart to the end-to-end argument.
- **Rough consensus, running code, and constant change (1, 2.4, 3.14).** These explain the Internet’s evolutionary character better than any static blueprint. They also predict why formal OSI-style designs fared poorly relative to deployed, iterated protocols.
- **Operational rules that name real failure modes.** Avoiding circular dependencies (3.11), self-describing objects (3.12), unambiguous addresses (4.4–4.5), and minimizing manual state (2.3) are concrete and testable. They explain many actual pathologies—DNS/routing loops, NAT and middlebox breakage, misconfiguration.

**What is loose**

The document’s weaknesses are also clear:

- **Many “principles” are maxims, not explanations.** “Keep it simple” (3.5), “modularity is good” (3.6), and “avoid options” (3.8) are design preferences. They do not explain why the Internet succeeded; OSI was modular and layered yet failed for deployment, economic, and political reasons. RFC 1958 often post-hoc rationalizes success rather than deriving it.
- **“Constant change” is self-undermining.** If every principle can be deprecated tomorrow, the list has no invariant explanatory core. It survives only as a meta-principle about provisionality.
- **The “one protocol at the Internet layer” ideal is not the reality.** IPv4 and IPv6 coexist; MPLS, overlays, tunnels, NATs, and middleboxes add multiple network-layer behaviors. The document acknowledges transition but understates how much the architecture is now plural and middlebox-mediated.
- **“Nobody owns the Internet” is historically contingent and now overstated.** Governments, carriers, CDNs, cloud providers, registries, and standards bodies exercise substantial control. The claim explains 1990s culture more than current architecture.
- **End-to-end is overextended.** Networks do maintain state: routes, QoS reservations, compression histories, NAT bindings, firewalls, caches. Fate-sharing is a preference, not an iron law. CDNs, load balancers, and proxies place function inside the network precisely because it improves performance or security.
- **The security section is dated.** 6.2 says carrier confidentiality is secondary to endpoint responsibility. That was plausible in 1996; today transport security is pervasive and carriers often provide DDoS, DNS, and filtering services. 6.5’s mandated algorithm also sits awkwardly with 6.3’s algorithm agility and with decades of cryptographic ossification problems.
- **Several rules conflict or have costs.** “Be strict when sending, tolerant when receiving” (3.9) can hide faults and create security issues. “Avoid options” (3.8) conflicts with extensibility and heterogeneity. These are useful heuristics, not laws.

**What would test it**

To test RFC 1958 as an explanation, one would need comparative, empirical, and formal work:

- **Comparative architecture histories.** Compare the Internet with OSI, X.25, ATM, SS7, IMS, and newer proposals like Named Data Networking or SCION. If following these principles predicts success and violating them predicts failure across cases, the explanation gains force. If deployment and economics dominate, the principles are less causal.
- **Measurement of function placement.** Track what fraction of traffic passes through NATs, firewalls, CDNs, proxies, and SDN controllers, and whether state inside the network is increasing or decreasing. The end-to-end/fate-sharing thesis predicts a bias toward endpoint state; reality can confirm or falsify that.
- **Protocol deployment experiments.** Study IPv6, DNSSEC, QUIC/HTTP/3, multipath TCP, LISP, and mobility. Do designs that respect end-to-end and fate-sharing deploy better? Do those that violate them fail for the predicted reasons?
- **Security and algorithm tests.** Observe whether carrier-provided security ever becomes primary, and whether mandated algorithms produce agility or ossification. Test whether endpoints really can “not depend on the confidentiality or integrity of the carriers.”
- **Formal and economic modeling.** Derive conditions under which end-to-end is optimal versus middlebox-mediated architectures. Use game theory to model standards adoption and “rough consensus.” Then compare predictions with observed outcomes.
- **Counterfactual or scaled testbeds.** Build networks with different principles—centralized control, per-flow state, strong network security—and see whether they match RFC 1958’s expectations at scale.
- **Designer-intent studies.** Ask whether RFC 1958 actually guided decisions or merely described them afterward. If it was ignored, its explanatory role is retrospective, not causal.

**Verdict**

RFC 1958 holds up as a useful, honest snapshot of Internet design culture. Its strongest explanatory claims are the end-to-end argument, fate-sharing, the narrow waist, and rough consensus/running code. Its weakest are the universal maxims, the “nobody owns it” claim, and the security/state assumptions. It explains better why the Internet is hard to change than why it took exactly this shape. Testing it requires historical comparison, deployment data, and formal modeling—not exegesis.

=== END OF THE REPORT ===

Write one JSON object with these keys: test_remove, test_swap, test_poke, test_flip, test_reverse, test_hunt, test_addjob, test_rival, test_pull, test_patches, test_inside, shape_elements, shape, turned_own_test, same_explanation, same_explanation_quote, marks_per_part, pairs_that_pull, rivals_built, remove_in_groups, question_identity, question_frozen_quote, modules_self_reported, marks_outside_closed_list, attributions, layer_at_fault, layer_at_fault_line, quotes
Output path (use Write, once, the JSON only): /home/user/ThreadSmith/Workflow/rigs/W10 harness/stage-B/w10/marking1/marks/M007.json
