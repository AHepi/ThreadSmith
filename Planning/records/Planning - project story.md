# Planning - project story

*The Planning project's own record, kept here from 21 September 2026. Numbering is this project's own, P1 first. See READ ME FIRST in this folder.*

Authority: the skill "planning", file 1, in `authority/1/planning/`, a draft.

## The goal
A skill that chooses what to do next in research on the skills themselves and, later, in the workflow the semantics theory calls for: which claim to test, which tests, in what order, how many times, when to stop, what to freeze first, what a result forces, what is held out, how readers and markers are arranged and audited, and when to wait for the owner. Harden it, freeze it, then the research.

## How the pieces fit
The skill is the authority. Its first test asks a second model to plan with it and compares the plan with one written without it. Changes are new numbered files, made only when a test forces them. The HV Skill is the judge this skill calls; the boundary is HV RELATIONS R6.

## Log
P1. The project made, on the owner's words (HV decisions H5 and H6). Drafted: file 1, the skill "planning": one main file (what the skill is for; the loop it runs, from a frozen question through a frozen plan with predictions to a results file that names what it did not test; the rules for changing a thing under test; the owner's word) and six references: freezing (plans and predictions), splits (fitting, held-out, reserve), readers-and-markers (a reader under test, blind marking, the tool audit, agreement read by field), changes (when a change is forced, one change for every open fault, freeze), owner (the word protocol and what may never run), and testing-against-cases (the HV Skill's module, copied on loan). Every rule in it is one the HV Skill's record used between H55 and H67, or one the root conventions state; none is new. Output: file 1 (draft), the project's front-door files, this record. Not tested: nothing run.

P2. Plan for the first test, frozen: "P2 Plan - the planning skill read by a second model, against the plans written without it.md". Not run: the owner chose to harden from the results in hand (HV decision H7, this project's P3).

P3. Hardened from the results in hand and frozen (decisions P3, P4 here; HV log H69). One Opus 5 judge at effort xhigh, armed with the hard-to-vary skill and made to falsify its own verdicts, read file 1 against the record it claims to come from: 66 parts, 38 held, 12 loose, 1 idle, 1 unknown, 7 borrowed, 2 fixed, and thirteen edits the record forces, every one a place where the draft's words and the record's disagreed: "five is the least a judgment rests on" has no case (three per box is what the record used, and calls small); "counts, never rates" against H63's own rate; "where the faults share a cause" against H67's five causes; what a repeat's outcome forces is written into the plan before it runs (H56 did); the marker keeps three layers apart (plan 52); a plan carries its failure conditions and its known weaknesses (plan 52); Fable 5.1 never, with or without permission, Opus 5 permitted by H7; the coverage map from plan 49; the reserve's true provenance and that no keyed test has ever run, with the Semantics project's twenty-four keyed near-cases named (read only); the router row for the loaned module widened to its own first line; "in a sentence" dropped; the key deleted at the provider, as plan 49 says. File 2 is file 1 with those edits (30 changed lines) and is frozen: no change until the research plan from Semantics file 10 asks for one. What the judge left loose and the record does not settle: "a third reading settles them", which no plan has run. Output: file 2 (`authority/2/planning/`), this entry. Not tested: nothing run; the judge is the same kind of agent that wrote the draft, and said so.

## Next step
None for the skill: file 2 is frozen. The research plan from Semantics file 10, which is neither project's, comes next.

## Traps
- Letting this file drift from Status. The log is the record.
